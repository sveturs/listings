# Лучшие фильтры карты в мире

## 🎯 Цель
Сделать фильтры карты, которые превзойдут Airbnb, OLX и Google Maps по удобству и функциональности.

## 💡 Главная идея
**Умные фильтры, которые меняются в зависимости от категории**

- Выбрал "Автомобили" → появились фильтры: марка, модель, год, пробег
- Выбрал "Недвижимость" → появились фильтры: комнаты, площадь, этаж
- Выбрал "Электроника" → появились фильтры: бренд, состояние, гарантия

## 🔧 Техническая реализация

### Backend (1 неделя)
```go
// Добавить параметр lang к существующему API
GET /api/v1/marketplace/categories/{id}/attributes?lang=sr

// Обновить ответ с переводами
{
  "data": [
    {
      "id": 1,
      "name": "rooms",
      "display_name": "Број соба", // Переведено
      "attribute_type": "select",
      "translated_options": [
        {"value": "1", "label": "1 соба"},
        {"value": "2", "label": "2 собе"}
      ]
    }
  ]
}
```

### Frontend (1 неделя)
```typescript
// Hook для загрузки фильтров категории
const { attributes } = useCategoryFilters(categoryId);

// Компонент умных фильтров
<SmartFilters 
  categoryId={selectedCategory}
  onChange={handleFiltersChange}
/>
```

### Поиск (1 неделя)
```sql
-- Добавить фильтрацию по атрибутам к существующему поиску
WHERE EXISTS (
  SELECT 1 FROM listing_attribute_values lav
  WHERE lav.listing_id = ml.id 
    AND lav.attribute_id = $attr_id
    AND lav.text_value = $attr_value
)
```

## 🎨 UX магия

### 1. Мгновенная адаптация
```
Категория не выбрана:
┌─────────────────┐
│ Выберите        │
│ категорию       │
└─────────────────┘

Выбрали "Автомобили":
┌─────────────────┐
│ 🚗 Марка        │
│ [BMW ▼]         │
│ 🏭 Модель       │
│ [X5 ▼]          │
│ 📅 Год          │
│ [2015] - [2023] │
└─────────────────┘
```

### 2. Умные зависимости
- Выбрал BMW → показать только модели BMW
- Выбрал год 2020+ → предложить малый пробег
- Выбрал студию → предложить площадь 25-35м²

### 3. Быстрые фильтры
```typescript
// Популярные комбинации для каждой категории
const quickFilters = {
  cars: ["Новые", "Малый пробег", "Без ДТП"],
  realestate: ["С фото", "Новостройка", "С балконом"],
  electronics: ["С гарантией", "Как новый", "В коробке"]
};
```

### 4. Мобильная магия
```typescript
// Bottom sheet с фильтрами
<BottomSheet>
  <FilterTabs>
    <Tab>Основные</Tab>
    <Tab>Дополнительно</Tab>
  </FilterTabs>
  <SwipeableFilters />
  <ApplyButton>Показать {count} объявлений</ApplyButton>
</BottomSheet>
```

## 🚀 Что получим

### Пользователь увидит:
1. **Мгновенную адаптацию** - фильтры меняются под категорию
2. **Умные предложения** - система подсказывает логичные комбинации
3. **Быстрый доступ** - популярные фильтры одним кликом
4. **Идеальный мобайл** - удобно на любом устройстве

### Технически получим:
1. **Масштабируемость** - легко добавлять новые категории и атрибуты
2. **Производительность** - только нужные фильтры загружаются
3. **Мультиязычность** - всё переведено из БД
4. **Совместимость** - никаких breaking changes

## 📋 План (4 недели)

### Неделя 1: Backend
- Добавить `?lang=sr` к API атрибутов
- Загружать переводы из БД
- Тестировать на 3 категориях

### Неделя 2: Frontend
- Hook `useCategoryFilters`
- Компонент `SmartFilters`
- Интеграция с картой

### Неделя 3: Поиск
- Обновить поиск для фильтрации по атрибутам
- Добавить debounce
- Оптимизировать SQL

### Неделя 4: UX полировка
- Быстрые фильтры
- Мобильная версия
- Анимации и микроинтеракции

## 🎯 Результат

**Фильтры, которые будут лучше чем у Airbnb, потому что:**
- Умнее адаптируются под контент
- Быстрее работают
- Удобнее на мобильных
- Поддерживают любые языки

**Пользователи скажут: "Вау, как удобно!"** 🔥

---

**Простое ТЗ. Четкий план. Лучший результат.**
