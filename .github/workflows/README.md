# GitHub Actions Workflows

## 🌙 Svetu Integration Tests

Полные интеграционные тесты для проверки работоспособности всего стека приложения.

### 📅 Расписание и запуск

**Автоматически:**
- Каждую ночь в 2:00 UTC (5:00 MSK, 3:00 CET)
- Регулярная проверка стабильности системы

**Ручной запуск:**
1. GitHub → Actions → "Svetu Integration Tests" 
2. "Run workflow" → выбрать ветку
3. Опция "Run with debug logging" для подробных логов

### 🧪 Что тестируется

**Инфраструктура:**
- PostgreSQL с PostGIS расширением
- Redis для кеширования
- OpenSearch для поиска
- MinIO для хранения файлов

**Приложение:**
- Go Backend API (порт 3000)
- Next.js Frontend (порт 3001)
- База данных с миграциями и фикстурами
- Playwright E2E тесты главной страницы

### 📊 Результаты и мониторинг

**Отчеты:**
- Автоматический summary в GitHub Actions
- Артефакты сохраняются на 7 дней
- Playwright HTML отчеты
- Логи backend и frontend

**Статусы:**
- ✅ Все тесты прошли - приложение работает
- ❌ Тесты упали - требует внимания разработчиков

### 🚀 Преимущества

1. **Раннее обнаружение проблем** - находим баги до пользователей
2. **Отсутствие блокировки разработки** - не замедляют обычные PR
3. **Комплексное тестирование** - проверка всего стека
4. **Регулярность** - ежедневная проверка стабильности

## Локальный запуск

Для отладки workflows локально можно использовать [act](https://github.com/nektos/act):

```bash
# Установка act
brew install act  # macOS
# или
curl https://raw.githubusercontent.com/nektos/act/master/install.sh | sudo bash  # Linux

# Запуск workflow локально
act workflow_dispatch -W .github/workflows/nightly-integration-tests.yml

# С конкретным событием
act schedule -W .github/workflows/nightly-integration-tests.yml
```

## Мониторинг

### Где смотреть результаты
1. GitHub Actions: https://github.com/sveturs/svetu/actions
2. Фильтр по workflow: "Nightly Integration Tests"
3. Артефакты с логами и отчетами Playwright

### Метрики успешности
- **Uptime**: Процент успешных ночных тестов за месяц
- **MTTR**: Среднее время исправления падающих тестов
- **Flakiness**: Процент нестабильных тестов

## Troubleshooting

### Если тесты падают
1. Проверить логи в артефактах
2. Запустить workflow вручную для воспроизведения
3. Проверить изменения за последние 24 часа
4. Локально воспроизвести с теми же переменными окружения

### Частые проблемы
- **Timeout**: Увеличить `timeout-minutes` в workflow
- **Сервисы не запускаются**: Проверить health checks
- **Frontend не собирается**: Проверить переменные окружения