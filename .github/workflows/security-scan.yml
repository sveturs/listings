name: Security Scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 6 * * *'

env:
  GO_VERSION: '1.25'
  GOPRIVATE: github.com/vondi-global/*

jobs:
  govulncheck:
    name: Go Vulnerability Check
    runs-on: self-hosted
    steps:
      - name: Cleanup old caches
        run: |
          chmod -R +w ${{ github.workspace }}/.gomodcache || true
          rm -rf ${{ github.workspace }}/.gomodcache
          rm -rf ${{ github.workspace }}/.gotmp

      - uses: actions/checkout@v4
        with:
          clean: false

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Configure Git for private modules
        run: git config --global url."https://${{ github.token }}@github.com/".insteadOf "https://github.com/"

      - name: Run govulncheck
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./... || echo "::warning::Vulnerabilities found, review the output above"
        continue-on-error: true

  gosec:
    name: GoSec Static Analysis
    runs-on: self-hosted
    steps:
      - name: Cleanup old caches
        run: |
          chmod -R +w ${{ github.workspace }}/.gomodcache || true
          rm -rf ${{ github.workspace }}/.gomodcache
          rm -rf ${{ github.workspace }}/.gotmp

      - uses: actions/checkout@v4
        with:
          clean: false

      - name: Run Gosec Security Scanner
        uses: securego/gosec@master
        with:
          args: '-no-fail -fmt text ./...'

  trivy:
    name: Trivy Filesystem Scan
    runs-on: self-hosted
    steps:
      - name: Cleanup old caches
        run: |
          chmod -R +w ${{ github.workspace }}/.gomodcache || true
          rm -rf ${{ github.workspace }}/.gomodcache
          rm -rf ${{ github.workspace }}/.gotmp

      - uses: actions/checkout@v4
        with:
          clean: false

      - name: Run Trivy vulnerability scanner (filesystem)
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'table'
          severity: 'CRITICAL,HIGH'
          exit-code: '0'
