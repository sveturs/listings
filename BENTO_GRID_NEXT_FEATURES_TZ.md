# Техническое задание: Доработка главной страницы

## Краткое описание
Добавление новых функций на главную страницу: топ-5 популярных поисковых запросов под полем поиска и новая плитка с картографическим сервисом в BentoGrid.

## 1. Топ-5 популярных поисковых запросов

### Описание
Под полем поиска в Hero-плитке BentoGrid нужно отобразить блок с 5 самыми популярными поисковыми запросами для быстрого доступа.

### Визуальная часть
- **Заголовок**: "Популярные запросы:" с иконкой TrendingUp из lucide-react
- **Отображение**: 5 запросов в виде кликабельных тегов/чипов
- **Стиль**: badge или chip компоненты из DaisyUI
- **Цвета**: нейтральная схема (base-200) с hover эффектом
- **Мобильная версия**: горизонтальный скролл если не помещаются
- **Анимация**: плавное появление снизу вверх

### Функциональность
- **При клике на тег**:
  - Заполнить поле поиска выбранным запросом
  - Автоматически выполнить поиск
  - Трекать событие через useBehaviorTracking
- **Источник данных**: UnifiedSearchService.getTrendingSearches()
- **Обновление**: каждые 5 минут (если пользователь на странице)
- **Загрузка**: показывать скелетон

### Пример кода
```typescript
// В BentoGrid Hero Card после SearchBar
<div className="mt-4">
  <div className="flex items-center gap-2 mb-3">
    <TrendingUp className="w-4 h-4 text-base-content/60" />
    <span className="text-sm font-medium text-base-content/70">
      Популярные запросы:
    </span>
  </div>
  <div className="flex flex-wrap gap-2">
    {trendingQueries.map((query, index) => (
      <button
        key={index}
        className="badge badge-lg badge-outline hover:badge-primary transition-colors cursor-pointer"
        onClick={() => handleTrendingClick(query)}
      >
        {query}
      </button>
    ))}
  </div>
</div>
```

### API интеграция
- Использовать существующий метод UnifiedSearchService.getTrendingSearches()
- Кешировать результаты в localStorage с TTL 5 минут
- При ошибке загрузки не показывать блок

## 2. Плитка с картографическим сервисом

### Описание
Новая большая плитка в BentoGrid для привлечения внимания к картографическому функционалу.

### Размер и позиция
- **Размер**: col-span-2, row-span-2 (как Hero-плитка)
- **Позиция**: вторая большая плитка после поисковой
- **Мобильная версия**: полная ширина под поисковой

### Визуальное оформление
- **Градиент фона**: from-secondary/20 via-secondary/10 to-transparent
- **Бордер**: border-secondary/20
- **Фон**: интерактивная мини-карта (полупрозрачная)
- **Иконка**: MapPin с badge "Карта"

### Структура компонента
```typescript
interface MapTileProps {
  nearbyListings: number;
  activeDistricts: number;
  userLocation?: { lat: number; lon: number };
}

<div className="col-span-1 md:col-span-2 lg:col-span-2 row-span-2 group">
  <div className="card h-full bg-gradient-to-br from-secondary/20 via-secondary/10 to-transparent border border-secondary/20 shadow-lg hover:shadow-2xl hover:scale-[1.02] transition-all duration-500 overflow-hidden relative">
    
    {/* Фоновая мини-карта */}
    <div className="absolute inset-0 opacity-20">
      <MiniMapPreview />
    </div>
    
    {/* Контент */}
    <div className="card-body p-6 lg:p-8 flex flex-col justify-between h-full relative z-10">
      <div>
        <div className="flex items-center gap-2 mb-4">
          <MapPin className="w-6 h-6 text-secondary" />
          <span className="badge badge-secondary">Карта</span>
        </div>
        
        <h2 className="card-title text-2xl lg:text-3xl font-bold mb-4">
          Исследуйте товары на карте
        </h2>
        
        <p className="text-base-content/70 mb-6">
          Находите объявления рядом с вами и в интересующих районах города
        </p>
        
        {/* Статистика */}
        <div className="grid grid-cols-2 gap-4 mb-6">
          <div className="bg-base-100/50 backdrop-blur rounded-lg p-3">
            <p className="text-2xl font-bold text-secondary">{nearbyListings}</p>
            <p className="text-sm text-base-content/60">товаров рядом</p>
          </div>
          <div className="bg-base-100/50 backdrop-blur rounded-lg p-3">
            <p className="text-2xl font-bold text-secondary">{activeDistricts}</p>
            <p className="text-sm text-base-content/60">активных районов</p>
          </div>
        </div>
      </div>
      
      <div className="flex justify-between items-center">
        <Link href="/map" className="btn btn-secondary group-hover:scale-105 transition-transform">
          Открыть карту
          <ArrowRight className="w-4 h-4 ml-2" />
        </Link>
        
        {userLocation && (
          <div className="text-sm text-base-content/60">
            📍 Ваше местоположение определено
          </div>
        )}
      </div>
    </div>
  </div>
</div>
```

### Мини-карта превью
- Упрощенная версия карты (Canvas или SVG)
- Точки объявлений как светящиеся точки
- Анимация плавного мерцания
- При наведении: усиление свечения

### Интерактивность
- **Hover эффект**: параллакс для мини-карты
- **Клик по кнопке**: переход на /map
- **Геолокация**: автоопределение с разрешения пользователя

### Данные
- **API**: GET /api/v1/marketplace/map/stats
- **Кеширование**: 10 минут
- **Обновление**: при фокусе окна

## 3. Обновление BentoGrid

### Новые пропсы
```typescript
interface BentoGridProps {
  // Существующие пропсы...
  
  // Новые пропсы
  trendingSearches?: string[];
  mapStats?: {
    nearbyListings: number;
    activeDistricts: number;
    userLocation?: { lat: number; lon: number };
  };
  onTrendingSearchClick?: (query: string) => void;
}
```

### Новый порядок плиток
1. Hero Card с поиском и популярными запросами (2x2)
2. Карта (2x2) - новая
3. Популярные категории (1x1)
4. Безопасные сделки (1x1)
5. Избранное объявление (1x1)
6. Статистика пользователей (1x1)
7. Рядом с вами (1x1)
8. Последние обновления (1x1)

## 4. Технические требования

### Анимации
- Framer-motion для плавных анимаций
- Stagger эффект для последовательного появления
- Skeleton loading для динамических данных

### Производительность
- Lazy loading для мини-карты
- Debounce для обновления данных
- React.memo для оптимизации рендеров

### Доступность
- ARIA метки для интерактивных элементов
- Поддержка клавиатурной навигации
- Семантическая HTML разметка

## 5. Файлы для изменения

1. `/src/components/ui/BentoGrid.tsx` - основные изменения
2. `/src/components/ui/MiniMapPreview.tsx` - новый компонент
3. `/src/app/[locale]/HomePageClient.tsx` - передача пропсов
4. `/src/app/[locale]/actions.ts` - получение данных карты
5. `/src/services/marketplaceService.ts` - метод getMapStats

## 6. Приоритет реализации

1. **Высокий**: Популярные поисковые запросы
2. **Средний**: Плитка с картой
3. **Низкий**: Анимации и оптимизации

## 7. Критерии приемки

### Популярные запросы
- [ ] Отображаются под полем поиска
- [ ] Кликабельны и выполняют поиск
- [ ] Обновляются каждые 5 минут
- [ ] Работает трекинг событий
- [ ] Адаптивны на мобильных

### Плитка с картой
- [ ] Правильный размер и позиция
- [ ] Мини-карта в фоне
- [ ] Анимации при hover
- [ ] Переход на /map работает
- [ ] Статистика загружается из API
- [ ] Адаптивный дизайн

### Общие требования
- [ ] TypeScript типизация
- [ ] Нет ошибок ESLint
- [ ] Успешная сборка проекта
- [ ] Покрытие тестами
- [ ] Документация обновлена