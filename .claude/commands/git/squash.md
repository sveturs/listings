---
allowed-tools: Bash(git reset:*), Bash(git commit:*), Bash(git diff:*), Bash(git status:*), Bash(git log:*)
description: Commit all changes
---
# squash <commit_hash>

## Usage

```
/git:squash
```

You need to squash all commits up to $ARGUMENTS
To describe a commit, you need to collect descriptions of all commits that will be squashed and prepare a list with changes
