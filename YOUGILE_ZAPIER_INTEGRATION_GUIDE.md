# Инструкция по импорту задач в YouGile через Zapier

## Шаг 1: Подготовка Google Sheets

1. **Создайте новую таблицу в Google Sheets**
2. **Импортируйте CSV файл** `yougile_tasks.csv`:
   - Файл → Импортировать → Загрузить файл
   - Выберите `yougile_tasks.csv`
   - Настройки импорта: "Заменить таблицу"

3. **Структура таблицы должна содержать колонки:**
   - Название
   - Описание
   - Колонка
   - Приоритет
   - Оценка (часы)
   - Эпик
   - Метки
   - Чеклист

## Шаг 2: Настройка YouGile

1. **Создайте проект в YouGile**
2. **Настройте колонки:**
   - 📋 Бэклог
   - 📌 К выполнению
   - 🚀 В работе
   - 🔍 На проверке
   - ✅ Готово

3. **Получите API токен YouGile:**
   - Перейдите в Настройки → API
   - Скопируйте ваш API ключ

## Шаг 3: Настройка Zapier

### Создание Zap для импорта задач:

1. **Войдите в Zapier** и создайте новый Zap

2. **Trigger (Триггер):**
   - Выберите: **Google Sheets**
   - Event: **New Spreadsheet Row**
   - Подключите ваш Google аккаунт
   - Выберите созданную таблицу
   - Worksheet: Sheet1

3. **Action (Действие):**
   - Выберите: **YouGile**
   - Event: **Create Task** (Создать стикер)
   - Подключите YouGile (используйте API ключ)

4. **Настройка маппинга полей:**
   ```
   YouGile поле → Google Sheets колонка
   --------------------------------
   Title → Название
   Description → Описание
   Column → Колонка
   Priority → Приоритет
   Estimate → Оценка (часы)
   Labels → Метки
   ```

5. **Для чеклиста:**
   - В YouGile API чеклисты добавляются отдельным запросом
   - Создайте второй Zap для добавления чеклистов после создания задачи

## Шаг 4: Альтернативный метод через Make (Integromat)

Make предоставляет больше возможностей для сложных сценариев:

1. **Создайте сценарий в Make:**
   - Google Sheets (Watch Rows) → YouGile (Create Task)

2. **Преимущества Make:**
   - Можно обрабатывать чеклисты в одном сценарии
   - Поддержка условной логики
   - Batch операции для массового импорта

## Шаг 5: Полуавтоматический метод

Если Zapier не подходит, используйте **расширение для Chrome**:

1. **Установите Kanban Tool для Chrome**
2. **Откройте Google Sheets с задачами**
3. **Копируйте задачи построчно в YouGile**

## Полезные советы:

1. **Тестирование:**
   - Сначала импортируйте 1-2 задачи для проверки
   - Убедитесь, что колонки правильно мапятся

2. **Приоритеты в YouGile:**
   - Критический → 🔴 Красный
   - Высокий → 🟡 Желтый
   - Средний → 🟢 Зеленый
   - Низкий → ⚪ Серый

3. **Метки (теги):**
   - Разделяйте через точку с запятой в CSV
   - YouGile автоматически создаст теги

4. **Чеклисты:**
   - В CSV используйте символ ✓ для разделения пунктов
   - Можно добавить вручную после импорта

## Проблемы и решения:

**Проблема:** YouGile не появляется в Zapier
**Решение:** Используйте Webhooks by Zapier с YouGile API

**Проблема:** Не импортируются русские символы
**Решение:** Убедитесь, что CSV в UTF-8 кодировке

**Проблема:** Ошибка при создании задачи
**Решение:** Проверьте, что названия колонок точно совпадают

## API YouGile для разработчиков:

Если хотите написать свой скрипт импорта:

```bash
# Создание задачи через API
curl -X POST https://api.yougile.com/api-v2/tasks \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Название задачи",
    "description": "Описание",
    "columnId": "ID_КОЛОНКИ",
    "projectId": "ID_ПРОЕКТА"
  }'
```

## Контакты поддержки:

- **YouGile Support:** support@yougile.com
- **Zapier Help:** https://help.zapier.com
- **Make (Integromat):** https://www.make.com/en/help