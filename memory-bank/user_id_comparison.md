# Сравнение полей user_id между слоями системы

## 1. Backend User Model (models.User)
- **Поле ID пользователя**: `id`
- **Тип**: `int`
- **JSON тег**: `"id"`
- **Расположение**: backend/internal/domain/models/models.go

## 2. Backend Listing Model (models.MarketplaceListing)
- **Поле user_id**: `UserID`
- **Тип**: `int`
- **JSON тег**: `"user_id"`
- **Расположение**: backend/internal/domain/models/models.go, строки 39-79
- **Примечание**: Использует CamelCase в Go и snake_case в JSON

## 3. Frontend User Type (generated)
- **Поле ID пользователя**: `id?`
- **Тип**: `number` (опциональный)
- **Расположение**: frontend/svetu/src/types/generated/api.ts
- **Путь в типах**: `components['schemas']['models.User']`

## 4. Frontend Listing Type (generated)
- **Поле user_id**: `user_id?`
- **Тип**: `number` (опциональный)
- **Расположение**: frontend/svetu/src/types/generated/api.ts, строка 18745
- **Путь в типах**: `components['schemas']['models.MarketplaceListing']`

## 5. OpenSearch Listings Mapping
- **Поле user_id**: `user_id`
- **Тип**: `integer`
- **Расположение**: backend/internal/storage/opensearch/mappings.go, строки 239-241
- **Примечание**: Не является keyword, а числовой тип integer

## Выявленные различия и соответствия

### ✅ Соответствия:
1. **Типы данных согласованы**:
   - Backend: `int` (Go)
   - Frontend: `number` (TypeScript)
   - OpenSearch: `integer`
   - Все представляют числовой тип

2. **Именование в JSON согласовано**:
   - Backend Listing экспортирует как `"user_id"`
   - Frontend получает как `user_id`
   - OpenSearch индексирует как `user_id`

3. **ID пользователя в модели User**:
   - Backend: поле `id` с JSON тегом `"id"`
   - Frontend: поле `id` типа `number`

### ⚠️ Особенности:
1. **Frontend типы опциональные**: Все поля во frontend типах помечены как опциональные (`?`), что соответствует практике TypeScript для API типов

2. **Соглашение об именовании**:
   - Go код использует CamelCase (`UserID`)
   - JSON/API использует snake_case (`user_id`)
   - Это стандартная практика для Go приложений

3. **Вложенные объекты**:
   - Frontend Listing может включать полный объект `user?: components['schemas']['models.User']`
   - OpenSearch также индексирует вложенный объект user с полем `user.id` типа `integer`

### 📊 Итоговая таблица соответствий:

| Слой | Поле | Тип | JSON имя | Примечания |
|------|------|-----|----------|------------|
| Backend User | `id` | `int` | `"id"` | ID пользователя |
| Backend Listing | `UserID` | `int` | `"user_id"` | Ссылка на пользователя |
| Frontend User | `id?` | `number` | `id` | Опциональное |
| Frontend Listing | `user_id?` | `number` | `user_id` | Опциональное |
| OpenSearch | `user_id` | `integer` | `user_id` | Индексированное поле |

### ✅ Заключение:
Поля user_id полностью согласованы между всеми слоями системы. Различия в именовании (CamelCase vs snake_case) и опциональности полей соответствуют стандартным практикам для соответствующих языков и технологий.