# Задача 008: Исправление кэширования в админке и бага с "телефон"

## 🎯 Описание проблем

### 1. Проблема кэширования в админке
**Проблема:** Админка показывает кэшированные данные, из-за чего новые категории не отображаются сразу после создания.

**Почему это критично:** 
- Администраторы должны видеть актуальные данные немедленно
- Это создает путаницу и ощущение, что система работает неправильно
- Приходится вручную очищать Redis кэш

**Решение:**
- Добавить заголовки отключения кэша для всех админских API endpoints
- Или использовать отдельную логику кэширования для админки (более короткое TTL)
- Добавить автоматическую инвалидацию кэша при создании/изменении категорий

### 2. Баг с ключевым словом "телефон"
**Проблема:** Слово "телефон" появляется во всех категориях как ключевое слово.

**Возможные причины:**
- Ошибка в миграции с тестовыми данными
- Баг в коде, который добавляет дефолтное значение
- Проблема с фикстурами

**Где проверить:**
1. `backend/migrations/` - поиск по слову "телефон"
2. `backend/fixtures/` - поиск тестовых данных
3. Код создания категорий - дефолтные значения

## 🔧 План исправления

### Этап 1: Исправление кэширования админки
```go
// В админских handlers добавить заголовки:
c.Set("Cache-Control", "no-cache, no-store, must-revalidate")
c.Set("Pragma", "no-cache") 
c.Set("Expires", "0")
```

Или создать middleware для всех `/api/v1/admin/*` endpoints:
```go
func NoCache() fiber.Handler {
    return func(c *fiber.Ctx) error {
        if strings.HasPrefix(c.Path(), "/api/v1/admin/") {
            c.Set("Cache-Control", "no-cache, no-store, must-revalidate")
            c.Set("Pragma", "no-cache")
            c.Set("Expires", "0")
        }
        return c.Next()
    }
}
```

### Этап 2: Поиск и удаление бага с "телефон"

1. **Поиск в миграциях:**
```bash
cd backend/migrations/
grep -r "телефон" .
grep -r "phone" .
```

2. **Поиск в фикстурах:**
```bash
cd backend/fixtures/
grep -r "телефон" .
```

3. **Поиск в коде:**
```bash
find backend -name "*.go" -exec grep -l "телефон\|phone" {} \;
```

4. **Проверка базы данных:**
```sql
SELECT category_id, keyword, COUNT(*) 
FROM category_keywords 
WHERE keyword = 'телефон' OR keyword = 'phone'
GROUP BY category_id, keyword;
```

5. **Очистка базы:**
```sql
DELETE FROM category_keywords WHERE keyword = 'телефон' OR keyword = 'phone';
```

### Этап 3: Автоматическая инвалидация кэша

Добавить в сервис категорий автоматическую очистку кэша при изменениях:
```go
func (s *CategoryService) CreateCategory(category *Category) error {
    // ... создание категории
    
    // Инвалидация кэша
    s.cache.Delete("categories:*")
    s.cache.Delete("admin:categories:*")
    
    return nil
}
```

## 🚦 Критичность

**Высокая** - обе проблемы критично влияют на работу администраторов:
- Кэширование создает путаницу и замедляет работу
- Баг с "телефон" засоряет данные и ухудшает качество поиска

## ✅ Критерии готовности

- [ ] Админка показывает новые категории сразу после создания
- [ ] Слово "телефон" удалено из всех категорий где не должно быть
- [ ] Добавлена автоматическая инвалидация кэша при изменениях
- [ ] Проведено тестирование на создание/редактирование категорий

## 📋 Тестирование

1. Создать новую категорию в админке
2. Сразу проверить, что она отображается без очистки кэша
3. Проверить, что "телефон" больше не появляется автоматически
4. Проверить работу поиска по категориям после очистки