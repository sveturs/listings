# Phase 3: Геопоиск и фильтры - Детальный план реализации

**Дата**: 2025-07-12  
**Статус**: Планирование  
**Приоритет**: Высокий  

## 📋 Обзор

### 🎯 Цель Phase 3
Создать полноценную систему геопоиска с продвинутыми фильтрами, интеграцией OpenSearch и интеллектуальными алгоритмами поиска.

### 📊 Текущее состояние (анализ)

#### ✅ Что уже реализовано в Phase 1-2:
1. **Базовая карта**:
   - Интерактивная карта MapBox GL JS
   - Нативная GPU кластеризация
   - Маркеры с popup информацией
   - Геолокация пользователя

2. **Геометрические расчеты**:
   - Интеграция @turf/turf для точных расчетов
   - Реальные изохроны через Mapbox Isochrone API
   - Фильтрация маркеров по зонам доступности
   - NativeSliderControl для радиуса/времени ходьбы

3. **Backend инфраструктура**:
   - PostGIS миграции (098-103)
   - Таблица `listings_geo` с полной структурой
   - API endpoints для геокодирования и поиска
   - Кэширование геокодирования

4. **Frontend компоненты**:
   - InteractiveMap с полным функционалом
   - MapmarkerData и GeoJSON поддержка
   - Hooks для геопоиска и геолокации
   - Мобильная адаптация

#### 🔄 Что требует доработки для Phase 3:
1. **OpenSearch интеграция** - отсутствует
2. **Продвинутые фильтры** - базовые фильтры есть, нужна доработка
3. **Интеллектуальный поиск** - логика требует улучшения
4. **Геоаналитика** - полностью отсутствует
5. **Зоны доставки** - не реализованы

### 🎯 Задачи Phase 3

#### 3.1 Продвинутый геопоиск
- Интеграция с OpenSearch для полнотекстового геопоиска
- Фасетные фильтры с географическими ограничениями
- Поиск по сложным геометриям (полигоны, маршруты)
- Гибридный поиск (текст + география + ML ранжирование)

#### 3.2 Умные фильтры
- Динамические фильтры на основе текущего view карты
- Категорийные фильтры с поддержкой иерархии
- Ценовые диапазоны с географической корреляцией
- Временные фильтры (часы работы, доступность)
- Пользовательские сохраненные фильтры

#### 3.3 Системы доставки
- Зоны доставки для витрин (рисование полигонов)
- Интеграция с D Express и Pošta Srbije API
- Расчет стоимости доставки в реальном времени
- Пункты выдачи на карте

#### 3.4 Аналитика и отчетность
- Тепловые карты поисковых запросов
- Статистика по районам для продавцов
- A/B тестирование алгоритмов поиска
- Метрики производительности геопоиска

---

## 🗓️ План реализации (4 недели)

### **Неделя 1: OpenSearch интеграция и базовый геопоиск**

#### День 1-2: Настройка OpenSearch
**Задачи:**
- [ ] Добавить OpenSearch в docker-compose.yml
- [ ] Создать mapping для геоданных в OpenSearch
- [ ] Настроить синхронизацию PostgreSQL → OpenSearch
- [ ] Реализовать базовые CRUD операции через OpenSearch

**Технические детали:**
```yaml
# docker-compose.yml добавление
opensearch:
  image: opensearchproject/opensearch:2.11.0
  environment:
    - discovery.type=single-node
    - plugins.security.disabled=true
    - OPENSEARCH_JAVA_OPTS=-Xms1g -Xmx1g
  volumes:
    - opensearch_data:/usr/share/opensearch/data
  ports:
    - "9200:9200"
    - "9600:9600"
```

**Mapping для listings:**
```json
{
  "mappings": {
    "properties": {
      "id": {"type": "long"},
      "title": {"type": "text", "analyzer": "serbian_analyzer"},
      "description": {"type": "text", "analyzer": "serbian_analyzer"},
      "price": {"type": "double"},
      "category_id": {"type": "long"},
      "category_name": {"type": "keyword"},
      "location": {"type": "geo_point"},
      "address_components": {
        "type": "object",
        "properties": {
          "city": {"type": "keyword"},
          "district": {"type": "keyword"},
          "street": {"type": "text"},
          "postal_code": {"type": "keyword"}
        }
      },
      "delivery_zones": {"type": "geo_shape"},
      "created_at": {"type": "date"},
      "seller_rating": {"type": "float"},
      "view_count": {"type": "long"},
      "favorite_count": {"type": "long"}
    }
  }
}
```

#### День 3-4: Backend геопоиск API
**Задачи:**
- [ ] Создать OpenSearch клиент в Go
- [ ] Реализовать эндпоинты для геопоиска
- [ ] Добавить поддержку сложных фильтров
- [ ] Оптимизировать производительность запросов

**Новые API endpoints:**
```go
// /api/v1/gis/search/advanced
POST /api/v1/gis/search/advanced
{
    "query": "детские товары",
    "location": {"lat": 44.8176, "lng": 20.4649},
    "radius": "5km",
    "filters": {
        "categories": [1, 2, 3],
        "price_range": {"min": 1000, "max": 50000},
        "delivery_available": true,
        "seller_rating": {"min": 4.0}
    },
    "sort": [
        {"_geo_distance": {"location": {"lat": 44.8176, "lng": 20.4649}}},
        {"price": "asc"}
    ],
    "size": 50,
    "from": 0
}

// /api/v1/gis/search/suggest
GET /api/v1/gis/search/suggest?q=детск&location=44.8176,20.4649
```

#### День 5: Frontend компоненты базового поиска
**Задачи:**
- [ ] Создать компонент AdvancedSearchFilter
- [ ] Интегрировать с новыми API endpoints
- [ ] Добавить автодополнение поиска
- [ ] Реализовать фильтры в реальном времени

### **Неделя 2: Умные фильтры и UX улучшения**

#### День 6-7: Фасетные фильтры
**Задачи:**
- [ ] Реализовать динамические фильтры на основе результатов
- [ ] Добавить счетчики результатов для каждого фильтра
- [ ] Создать компонент многоуровневых категорий
- [ ] Реализовать ценовые гистограммы

**Компоненты:**
```typescript
// FacetedFilters.tsx
interface FacetFilter {
  key: string;
  label: string;
  type: 'checkbox' | 'range' | 'radio' | 'geo';
  options: FacetOption[];
  value: any;
}

interface FacetOption {
  label: string;
  value: any;
  count: number; // количество результатов
  selected: boolean;
}

// CategoryHierarchyFilter.tsx
interface CategoryNode {
  id: number;
  name: string;
  children: CategoryNode[];
  count: number;
  level: number;
}
```

#### День 8-9: Географические фильтры
**Задачи:**
- [ ] Поиск по произвольным полигонам (нарисованным пользователем)
- [ ] Фильтр по административным районам
- [ ] Поиск вдоль маршрута
- [ ] Исключение определенных зон

**Компоненты:**
```typescript
// GeographicFilters.tsx
- DrawPolygonFilter - рисование зон поиска
- DistrictSelector - выбор районов
- RouteSearchFilter - поиск вдоль маршрута
- ExclusionZones - исключение зон
```

#### День 10: Сохраненные поиски и персонализация
**Задачи:**
- [ ] Возможность сохранения поисковых запросов
- [ ] Персональные рекомендации на основе истории
- [ ] Уведомления о новых товарах в сохраненных поисках
- [ ] Аналитика предпочтений пользователя

### **Неделя 3: Системы доставки и зоны**

#### День 11-12: Зоны доставки для витрин
**Задачи:**
- [ ] Компонент редактора зон доставки (polygon drawing)
- [ ] API для управления зонами доставки
- [ ] Проверка принадлежности точки к зоне
- [ ] Расчет стоимости доставки по зонам

**База данных:**
```sql
-- Таблица зон доставки
CREATE TABLE delivery_zones (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    storefront_id UUID NOT NULL REFERENCES storefronts(id),
    zone_name VARCHAR(100),
    zone_polygon geography(POLYGON, 4326) NOT NULL,
    delivery_fee DECIMAL(10,2) DEFAULT 0,
    min_order_amount DECIMAL(10,2) DEFAULT 0,
    max_delivery_time_minutes INT,
    color VARCHAR(7), -- HEX цвет
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Расписание доставки
CREATE TABLE delivery_schedules (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    zone_id UUID NOT NULL REFERENCES delivery_zones(id),
    day_of_week INT NOT NULL, -- 0-6 (Пн-Вс)
    start_time TIME NOT NULL,
    end_time TIME NOT NULL,
    is_available BOOLEAN DEFAULT true
);
```

#### День 13-14: Интеграция с службами доставки
**Задачи:**
- [ ] Интеграция D Express API
- [ ] Интеграция Pošta Srbije API
- [ ] Карта пунктов выдачи
- [ ] Расчет стоимости доставки в реальном времени

**API клиенты:**
```go
// D Express integration
type DExpressClient struct {
    apiURL string
    apiKey string
    client *http.Client
}

func (c *DExpressClient) CalculateDeliveryPrice(params DeliveryParams) (*DeliveryPrice, error)
func (c *DExpressClient) GetPickupPoints(city string) ([]PickupPoint, error)
func (c *DExpressClient) CreateShipment(order ShipmentOrder) (*Shipment, error)

// Pošta Srbije integration
type PostaClient struct {
    apiURL string
    username string
    password string
}
```

#### День 15: Выбор доставки в UI
**Задачи:**
- [ ] Компонент выбора службы доставки
- [ ] Карта с пунктами выдачи
- [ ] Калькулятор стоимости доставки
- [ ] Интеграция в процесс оформления заказа

### **Неделя 4: Аналитика, оптимизация и запуск**

#### День 16-17: Геоаналитика для продавцов
**Задачи:**
- [ ] Тепловые карты поисковых запросов
- [ ] Статистика по районам (продажи, просмотры)
- [ ] Анализ конкурентов в радиусе
- [ ] Рекомендации по расширению зон доставки

**Дашборд аналитики:**
```typescript
// Analytics components
- GeoHeatmap - тепловая карта активности
- DistrictStats - статистика по районам
- CompetitorAnalysis - анализ конкурентов
- DeliveryOptimization - оптимизация доставки
```

#### День 18-19: Производительность и кэширование
**Задачи:**
- [ ] Оптимизация OpenSearch запросов
- [ ] Кэширование частых поисковых запросов
- [ ] CDN для тайлов карт
- [ ] Мониторинг производительности

**Кэширование:**
```go
// Redis кэширование
type GeoCacheService struct {
    redis *redis.Client
    ttl   time.Duration
}

// Кэширование результатов поиска
func (s *GeoCacheService) CacheSearchResults(key string, results SearchResults) error
func (s *GeoCacheService) GetCachedResults(key string) (*SearchResults, error)

// Кэширование зон доставки
func (s *GeoCacheService) CacheDeliveryZones(storefrontID string, zones []DeliveryZone) error
```

#### День 20: A/B тестирование и запуск
**Задачи:**
- [ ] A/B тестирование алгоритмов ранжирования
- [ ] Мониторинг метрик поиска
- [ ] Настройка алертов
- [ ] Production deployment

---

## 🏗️ Техническая архитектура Phase 3

### Backend компоненты

#### 1. OpenSearch интеграция
```go
// internal/proj/opensearch/
├── client/
│   ├── opensearch_client.go      // Клиент OpenSearch
│   ├── index_manager.go          // Управление индексами
│   └── query_builder.go          // Построитель запросов
├── service/
│   ├── search_service.go         // Сервис поиска
│   ├── indexing_service.go       // Сервис индексирования
│   └── analytics_service.go      // Аналитика поиска
└── types/
    ├── search_types.go           // Типы для поиска
    └── analytics_types.go        // Типы для аналитики
```

#### 2. Геопоиск расширения
```go
// internal/proj/gis/
├── advanced/
│   ├── faceted_search.go         // Фасетный поиск
│   ├── route_search.go           // Поиск по маршруту
│   └── polygon_search.go         // Поиск по полигонам
├── delivery/
│   ├── zone_manager.go           // Управление зонами
│   ├── dexpress_client.go        // D Express API
│   └── posta_client.go           // Pošta API
└── analytics/
    ├── geo_analytics.go          // Геоаналитика
    ├── heatmap_generator.go      // Генератор тепловых карт
    └── competitor_analysis.go    // Анализ конкурентов
```

#### 3. Новые API endpoints
```yaml
# Продвинутый поиск
POST /api/v1/gis/search/advanced
POST /api/v1/gis/search/faceted
POST /api/v1/gis/search/route
POST /api/v1/gis/search/polygon

# Зоны доставки
GET    /api/v1/gis/delivery/zones/:storefront_id
POST   /api/v1/gis/delivery/zones
PUT    /api/v1/gis/delivery/zones/:zone_id
DELETE /api/v1/gis/delivery/zones/:zone_id
POST   /api/v1/gis/delivery/check-availability

# Службы доставки
GET  /api/v1/gis/delivery/providers
POST /api/v1/gis/delivery/calculate-price
GET  /api/v1/gis/delivery/pickup-points

# Аналитика
GET /api/v1/gis/analytics/heatmap
GET /api/v1/gis/analytics/districts
GET /api/v1/gis/analytics/competitors
```

### Frontend компоненты

#### 1. Продвинутый поиск
```typescript
// components/GIS/Search/
├── AdvancedSearchDialog.tsx      // Основной диалог поиска
├── FacetedFilters.tsx           // Фасетные фильтры
├── CategoryHierarchy.tsx        // Иерархия категорий
├── PriceHistogram.tsx           // Ценовая гистограмма
├── GeographicFilters.tsx        // Географические фильтры
└── SavedSearches.tsx            // Сохраненные поиски
```

#### 2. Зоны доставки
```typescript
// components/GIS/Delivery/
├── ZoneEditor.tsx               // Редактор зон (MapBox Draw)
├── ZoneList.tsx                // Список зон доставки
├── DeliveryCalculator.tsx      // Калькулятор доставки
├── PickupPointsMap.tsx         // Карта пунктов выдачи
└── DeliveryServiceSelector.tsx // Выбор службы доставки
```

#### 3. Аналитика
```typescript
// components/GIS/Analytics/
├── GeoHeatmap.tsx              // Тепловая карта
├── DistrictStatistics.tsx      // Статистика районов
├── CompetitorMap.tsx           // Карта конкурентов
├── PerformanceMetrics.tsx      // Метрики производительности
└── RecommendationsPanel.tsx    // Панель рекомендаций
```

### Базы данных

#### PostgreSQL схема (дополнения)
```sql
-- Сохраненные поиски пользователей
CREATE TABLE saved_searches (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id),
    name VARCHAR(100) NOT NULL,
    search_query JSONB NOT NULL,
    location geography(POINT, 4326),
    radius INTEGER,
    filters JSONB,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    last_used TIMESTAMPTZ DEFAULT NOW(),
    notification_enabled BOOLEAN DEFAULT false
);

-- Статистика поисковых запросов
CREATE TABLE search_analytics (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id),
    query_text TEXT,
    location geography(POINT, 4326),
    filters JSONB,
    results_count INTEGER,
    clicked_listing_id BIGINT,
    search_duration_ms INTEGER,
    created_at TIMESTAMPTZ DEFAULT NOW()
);
```

#### OpenSearch индексы
```json
// listings_geo_index
{
  "settings": {
    "number_of_shards": 3,
    "number_of_replicas": 1,
    "analysis": {
      "analyzer": {
        "serbian_analyzer": {
          "type": "custom",
          "tokenizer": "standard",
          "char_filter": ["cyrillic_latin_mapping"],
          "filter": ["lowercase", "serbian_stemmer"]
        }
      }
    }
  }
}

// search_analytics_index
{
  "mappings": {
    "properties": {
      "timestamp": {"type": "date"},
      "user_id": {"type": "keyword"},
      "query": {"type": "text"},
      "location": {"type": "geo_point"},
      "results_count": {"type": "integer"},
      "response_time": {"type": "integer"}
    }
  }
}
```

---

## 📊 Метрики успеха Phase 3

### Технические метрики
| Метрика | Текущее | Цель Phase 3 | Измерение |
|---------|---------|--------------|-----------|
| Время отклика поиска | ~500ms | <200ms | OpenSearch |
| Точность геопоиска | 85% | 95% | A/B тесты |
| Покрытие индексом | 90% | 99% | Мониторинг |
| Uptime поиска | 99.0% | 99.9% | Prometheus |

### Продуктовые метрики
| Метрика | Базовое | Цель (3 мес) | Метод |
|---------|---------|--------------|-------|
| Использование продвинутого поиска | 0% | 30% | Google Analytics |
| Конверсия из геопоиска | 5% | 12% | Tracking |
| Среднее время на карте | 2 мин | 5 мин | Analytics |
| Настроенных зон доставки | 0 | 200 | DB метрики |

### Бизнес метрики
| Метрика | Ожидаемое улучшение |
|---------|-------------------|
| Локальные продажи | +40% |
| Средний чек | +25% |
| Удержание пользователей | +30% |
| NPS продавцов | +20 пунктов |

---

## ⚠️ Риски и митигация

### Технические риски
1. **OpenSearch производительность**
   - Риск: Медленные запросы при больших объемах
   - Митигация: Тестирование нагрузки, оптимизация индексов

2. **API лимиты внешних сервисов**
   - Риск: Превышение лимитов D Express/Pošta
   - Митигация: Кэширование, rate limiting, fallback

3. **Сложность UI**
   - Риск: Перегруженный интерфейс
   - Митигация: Поэтапное добавление функций, A/B тесты

### Бизнес риски
1. **Медленное принятие пользователями**
   - Митигация: Onboarding, tutorial, постепенный rollout

2. **Высокие операционные расходы**
   - Митигация: Мониторинг затрат, автомасштабирование

---

## 🚀 План развертывания

### Поэтапный rollout
1. **Alpha** (10% пользователей) - базовый геопоиск
2. **Beta** (30% пользователей) - полный функционал
3. **Production** (100%) - после сбора метрик

### Мониторинг и алерты
```yaml
# Prometheus метрики
- opensearch_query_duration_seconds
- geo_search_requests_total
- delivery_zone_checks_total
- search_conversion_rate

# Grafana дашборды
- GeoSearch Performance
- User Behavior Analytics
- Business Metrics
```

### Резервное копирование
- Ежедневный backup OpenSearch индексов
- Snapshot PostgreSQL с геоданными
- Мониторинг целостности данных

---

## 🎯 Критерии готовности

### Pre-launch checklist
- [ ] OpenSearch кластер стабилен >99.9% uptime
- [ ] Все API endpoints документированы в Swagger
- [ ] Load тестирование пройдено (1000+ конкурентных пользователей)
- [ ] Security audit завершен
- [ ] Мониторинг и алерты настроены
- [ ] Backup процедуры протестированы
- [ ] User acceptance testing пройден
- [ ] Performance benchmarks достигнуты

### Success criteria (1 месяц после запуска)
- [ ] 25%+ пользователей используют геопоиск
- [ ] Среднее время на карте увеличилось до 4+ минут
- [ ] 100+ настроенных зон доставки
- [ ] 0 critical производственных инцидентов
- [ ] 95%+ точность геопоиска в user testing

---

## 🔄 Следующие этапы (Post Phase 3)

### Phase 4: ML и персонализация
- Машинное обучение для ранжирования результатов
- Персональные рекомендации на основе геолокации
- Предсказание спроса по районам
- Автоматическая оптимизация зон доставки

### Phase 5: Расширенная интеграция
- Google My Business интеграция
- Социальные сети (геотеги)
- IoT датчики для пунктов выдачи
- AR/VR функции для навигации

---

**План готов к реализации!** 🚀

Документ содержит полную техническую спецификацию для реализации Phase 3 с детальным breakdown задач, техническими решениями и метриками успеха.