# 📋 Паспорт OpenSearch Marketplace Index

## 🏷️ Метаданные
- **Назначение:** Полнотекстовый поиск и фильтрация объявлений маркетплейса
- **Тип компонента:** Инфраструктура / NoSQL Search Engine  
- **Статус:** Активный, используется в production
- **Версия OpenSearch:** 2.x
- **Индекс:** `marketplace`

## 🎯 Назначение
OpenSearch marketplace index - центральный компонент поисковой системы, обеспечивающий быстрый и релевантный поиск по объявлениям маркетплейса с поддержкой мультиязычности, геопоиска и сложных фильтров.

## 🔧 Конфигурация подключения

### Переменные окружения
```bash
OPENSEARCH_URL=http://localhost:9200      # URL сервера OpenSearch
OPENSEARCH_USERNAME=admin                 # Имя пользователя
OPENSEARCH_PASSWORD=admin                 # Пароль
OPENSEARCH_MARKETPLACE_INDEX=marketplace  # Название индекса
```

### Структура Config
```go
type OpenSearchConfig struct {
    URL              string // Адрес OpenSearch сервера
    Username         string // Имя пользователя
    Password         string // Пароль
    MarketplaceIndex string // Название индекса маркетплейса
}
```

## 🏗️ Архитектура системы

### Основные компоненты
1. **OpenSearchClient** - базовый клиент для работы с OpenSearch
2. **Repository** - репозиторий для marketplace операций
3. **Storage Interface** - интерфейс для синхронизации
4. **Service Layer** - бизнес-логика

### Файловая структура
```
backend/internal/
├── storage/opensearch/
│   ├── client.go          # Базовый клиент OpenSearch
│   └── mappings.go        # Схемы индексов
├── proj/marketplace/storage/opensearch/
│   ├── interface.go       # Интерфейсы
│   ├── repository.go      # Реализация поиска
│   └── types.go          # Типы данных
├── domain/search/
│   └── types.go          # Структуры поисковых параметров
└── storage/postgres/
    └── db.go             # Интеграция с PostgreSQL
```

## 📊 Структура индекса "marketplace"

### Настройки индекса
```json
{
  "settings": {
    "number_of_shards": 1,
    "number_of_replicas": 1,
    "analysis": {
      "analyzer": {
        "serbian_analyzer": "custom с stemmer для сербского",
        "russian_analyzer": "custom с stemmer для русского", 
        "english_analyzer": "custom с stemmer для английского",
        "default_analyzer": "standard + lowercase + html_strip",
        "autocomplete": "edge_ngram для автодополнения",
        "shingle_analyzer": "для фразового поиска"
      }
    }
  }
}
```

### Основные поля индекса

| Поле | Тип | Анализатор | Описание |
|------|-----|------------|----------|
| `id` | integer | - | ID объявления |
| `title` | text | multi-field с языковыми анализаторами | Заголовок |
| `description` | text | multi-field с языковыми анализаторами | Описание |
| `price` | double | - | Цена |
| `old_price` | double | - | Старая цена |
| `has_discount` | boolean | - | Есть ли скидка |
| `condition` | text + keyword | - | Состояние товара |
| `status` | keyword | - | Статус объявления |
| `location` | text + keyword | - | Местоположение |
| `coordinates` | geo_point | - | Координаты |
| `city` | text + keyword | - | Город |
| `country` | text + keyword | - | Страна |
| `category_id` | integer | - | ID категории |
| `user_id` | integer | - | ID пользователя |
| `storefront_id` | integer | - | ID витрины |
| `created_at` | date | - | Дата создания |
| `updated_at` | date | - | Дата обновления |
| `views_count` | integer | - | Количество просмотров |
| `average_rating` | double | - | Средний рейтинг |
| `review_count` | integer | - | Количество отзывов |

### Структура переводов
```json
{
  "translations": {
    "sr": {"title": "...", "description": "..."},
    "ru": {"title": "...", "description": "..."},
    "en": {"title": "...", "description": "..."}
  }
}
```

### Структура атрибутов (nested)
```json
{
  "attributes": [
    {
      "attribute_id": 123,
      "attribute_name": "make",
      "display_name": "Марка",
      "attribute_type": "select",
      "text_value": "BMW",
      "text_value_lowercase": "bmw",
      "numeric_value": null,
      "boolean_value": null,
      "display_value": "BMW",
      "translations": {
        "en": "BMW",
        "sr": "BMW", 
        "ru": "БМВ"
      }
    }
  ]
}
```

### Важные атрибуты в корне документа
- `make`, `model` - марка и модель (для автомобилей)
- `car_keywords` - ключевые слова для автомобилей
- Numeric/boolean атрибуты выносятся как отдельные поля

## 🔄 Процесс синхронизации данных

### Создание/обновление объявления
```
Service.CreateListing() → Storage.CreateListing() → Storage.IndexListing() → OpenSearch.IndexDocument()
```

### Удаление объявления
```
Service.DeleteListing() → Storage.DeleteListing() → Storage.DeleteListingIndex() → OpenSearch.DeleteDocument()
```

### Полная переиндексация
```bash
# Команда
./reindex --type=listings --batch-size=500

# Процесс
1. Удаление старого индекса
2. Создание нового индекса с mapping
3. Пакетная загрузка из PostgreSQL (по 100 записей)
4. Индексация через BulkIndex
```

### Автоматическая синхронизация
- При создании объявления → автоматическая индексация
- При обновлении → переиндексация документа
- При удалении → удаление из индекса
- При изменении атрибутов → обновление nested полей

## 🔍 Поисковые запросы и фильтры

### Основной поисковый интерфейс
```go
type SearchParams struct {
    Query              string                 // Текстовый запрос
    CategoryID         *int                   // Фильтр по категории
    PriceMin           *float64               // Минимальная цена
    PriceMax           *float64               // Максимальная цена
    Condition          string                 // Состояние товара
    City               string                 // Город
    Country            string                 // Страна
    StorefrontID       *int                   // ID витрины
    Location           *GeoLocation           // Геопоиск
    Distance           string                 // Радиус поиска
    AttributeFilters   map[string]string      // Фильтры по атрибутам
    CustomQuery        map[string]interface{} // Прямой Elasticsearch DSL
    Language           string                 // Язык поиска
    Sort               string                 // Поле сортировки
    SortDirection      string                 // Направление сортировки
}
```

### Типы поисковых запросов

#### 1. Полнотекстовый поиск
- Multi-match по title, description
- Поиск с учетом языка (анализаторы)
- Нечеткий поиск (fuzziness)

#### 2. Геопоиск
```json
{
  "geo_distance": {
    "distance": "10km",
    "coordinates": {"lat": 44.7866, "lon": 20.4489}
  }
}
```

#### 3. Фильтры по атрибутам
```json
{
  "nested": {
    "path": "attributes",
    "query": {
      "bool": {
        "must": [
          {"term": {"attributes.attribute_name": "make"}},
          {"term": {"attributes.text_value_lowercase": "bmw"}}
        ]
      }
    }
  }
}
```

#### 4. Автодополнение
```json
{
  "suggest": {
    "completion": {
      "prefix": "bmw",
      "completion": {"field": "title_suggest", "size": 10}
    }
  }
}
```

## 🏷️ Агрегации и фасеты

### Поддерживаемые агрегации
- `categories` - группировка по категориям
- `cities` - группировка по городам
- `conditions` - группировка по состоянию
- `price_ranges` - ценовые диапазоны
- `attributes` - агрегация по nested атрибутам

### Структура результата
```go
type SearchResult struct {
    Listings     []*models.MarketplaceListing // Найденные объявления
    Total        int                          // Общее количество
    Took         int64                        // Время выполнения (мс)
    Aggregations map[string][]Bucket          // Фасеты
    Suggestions  []string                     // Подсказки
}
```

## ⚡ Производительность и оптимизация

### Размеры пакетов
- Индексация: по 100 документов (BulkIndex)
- Переиндексация: пакеты по 500 записей
- Поиск: по умолчанию 20 результатов на страницу

### Особенности индексации
- Поддержка HTML strip в анализаторах
- Edge n-gram для автодополнения (1-20 символов)
- Shingle анализатор для фразового поиска
- Множественные анализаторы для мультиязычности

### Логирование
```go
logger.Info().Msgf("Индексация объявления %d с данными: %s", listing.ID, docJSON)
logger.Info().Msgf("OpenSearch ответил успешно. Время: %d мс", took)
```

## 🔧 Управление и команды

### Команды для управления
```bash
# Полная переиндексация
./reindex --type=listings --batch-size=500

# Переиндексация конкретного типа  
./reindex --type=marketplace

# Проверка индекса через API
curl -X GET "localhost:9200/marketplace/_mapping"
```

### Проверка состояния
```bash
# Количество документов
curl "localhost:9200/marketplace/_count"

# Статистика индекса
curl "localhost:9200/marketplace/_stats"

# Настройки индекса
curl "localhost:9200/marketplace/_settings"
```

## 🚨 Особенности и ограничения

### Важные моменты
1. **Данные из OpenSearch на главной странице** - изменения в PostgreSQL НЕ отображаются до переиндексации
2. **Nested атрибуты** - требуют специальных запросов для фильтрации
3. **Мультиязычность** - поддержка сербского, русского, английского языков
4. **Автомобильные атрибуты** - make/model выносятся в корень для быстрого поиска
5. **Геопоиск** - поддержка координат и радиуса поиска

### Обработка ошибок
- Graceful degradation если OpenSearch недоступен
- Fallback на PostgreSQL поиск
- Подробное логирование всех операций

## 🔗 Связи с другими компонентами

### Прямые зависимости
- **PostgreSQL** - источник данных для синхронизации
- **Backend API** - получение поисковых запросов
- **Frontend Search** - отображение результатов поиска

### Обратные зависимости
- **Главная страница маркетплейса** - отображение найденных объявлений
- **Страница поиска** - фильтрация и сортировка
- **API автодополнения** - подсказки при вводе

### Интеграции
- **MinIO** - ссылки на изображения объявлений
- **Redis** - кеширование поисковых запросов (потенциально)

## 🎯 Бизнес-логика

### Основные функции
1. **Полнотекстовый поиск** - поиск по заголовку и описанию
2. **Фильтрация** - по цене, категории, местоположению, атрибутам  
3. **Геопоиск** - поиск в радиусе от точки
4. **Сортировка** - по релевантности, цене, дате, рейтингу
5. **Агрегация** - фасеты для фильтрации
6. **Автодополнение** - подсказки при вводе

### Бизнес-правила
- Поиск только по активным объявлениям
- Учет языка пользователя при поиске
- Приоритет объявлений с хорошими рейтингами
- Скрытие объявлений заблокированных пользователей

## 📊 Мониторинг и метрики

### Ключевые метрики
- Время выполнения поисковых запросов
- Количество индексированных документов
- Частота переиндексации
- Процент успешных поисковых запросов

### Алерты
- Превышение времени ответа (>500ms)
- Недоступность OpenSearch сервиса
- Ошибки индексации документов
- Рассинхронизация с PostgreSQL

---
**Паспорт создан:** 2025-06-29  
**Компонент:** OpenSearch Marketplace Index  
**Статус:** Активный в production