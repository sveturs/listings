# Паспорт компонента: FileUploadProgress

## 📋 Метаданные
- **Путь**: `/frontend/svetu/src/components/Chat/FileUploadProgress.tsx`
- **Роль**: Компонент отображения прогресса загрузки файлов
- **Тип**: Client Component
- **Размер**: 95 строк

## 🎯 Назначение
Визуальное отображение процесса загрузки файлов в чат:
- Показывает список загружаемых файлов
- Отображает прогресс загрузки с процентами
- Индикация статусов: ожидание, загрузка, успех, ошибка
- Возможность отмены/удаления файлов

## 🔧 Структура Props
```typescript
interface FileUploadProgressProps {
  uploadingFiles: UploadingFile[];      // Массив загружаемых файлов
  onRemove: (fileId: string) => void;   // Обработчик удаления файла
}
```

### Тип UploadingFile:
```typescript
interface UploadingFile {
  id: string;
  name: string;
  size: number;
  progress: number;
  status: 'pending' | 'uploading' | 'success' | 'error';
  error?: string;
}
```

## 🔗 Зависимости и связи

### Внутренние зависимости:
- `@/types/chat` - тип UploadingFile

### Внешние зависимости:
- `react-icons/fa` - иконки (FaFile, FaTimes)

## 🧠 Управление состоянием

Компонент не имеет локального состояния, работает как чистый презентационный компонент.

## 💼 Бизнес-логика

### 1. Форматирование размера файла:
- Автоматический выбор единицы измерения (Bytes, KB, MB, GB)
- Точность до 2 знаков после запятой
- Идентичная логика как в ChatAttachments

### 2. Управление статусами:
- **pending**: серый цвет, текст "Ожидание..."
- **uploading**: primary цвет, прогресс-бар
- **success**: зелёный цвет, текст "Загружено"
- **error**: красный цвет, отображение ошибки

### 3. Возможность удаления:
- Кнопка удаления показывается только для статусов 'error' и 'pending'
- Загружаемые и успешно загруженные файлы удалить нельзя

## 🎨 UI структура

### Контейнер:
- Фон: bg-base-200
- Скругление: rounded-lg
- Отступы: p-3
- Заголовок "Загрузка файлов:"

### Карточка файла:
- Фон: bg-base-100
- Скругление: rounded
- Отступы: p-2
- Структура:
  ```
  [Иконка] [Название и размер файла] [Кнопка удаления]
           [Прогресс-бар или статус]
  ```

### Прогресс-бар:
- Высота: h-2
- Фон: bg-base-200
- Заполнение: bg-primary
- Плавная анимация: transition-all duration-300
- Ширина через inline style

### Цветовая схема:
- Ожидание: text-base-content/60 (полупрозрачный)
- Загрузка: text-primary (основной цвет)
- Успех: text-success (зелёный)
- Ошибка: text-error (красный)

## 🔄 Примеры использования

```tsx
// В MessageInput
<FileUploadProgress
  uploadingFiles={Object.values(uploadingFiles)}
  onRemove={removeUploadingFile}
/>
```

## 🐛 Известные особенности

1. **Hardcoded текст**: Статусы не локализованы, используется русский язык

2. **Условный рендеринг**: Возвращает null если нет файлов

3. **Единообразие**: Использует ту же функцию formatFileSize что и ChatAttachments

4. **Простота**: Нет анимаций появления/исчезновения элементов

5. **Производительность**: 
   - Чистый компонент без side effects
   - Перерендеривается при каждом обновлении прогресса