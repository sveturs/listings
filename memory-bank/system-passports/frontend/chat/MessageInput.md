# Паспорт компонента: MessageInput

## 📋 Метаданные
- **Путь**: `/frontend/svetu/src/components/Chat/MessageInput.tsx`
- **Роль**: Компонент ввода и отправки сообщений в чате
- **Тип**: Client Component
- **Размер**: 453 строки

## 🎯 Назначение
Универсальное поле для создания и отправки сообщений с поддержкой:
- Многострочного ввода текста с автоматической высотой
- Выбора и отправки файлов (изображения, документы)
- Выбора эмодзи через EmojiPicker
- Индикации набора текста (typing indicator)
- Предпросмотра выбранных файлов
- Отображения прогресса загрузки файлов

## 🔧 Структура Props
```typescript
interface MessageInputProps {
  chat?: MarketplaceChat;      // Существующий чат
  initialListingId?: number;    // ID товара для нового чата
  initialSellerId?: number;     // ID продавца/контакта
}
```

## 🔗 Зависимости и связи

### Внутренние зависимости:
- `@/hooks/useChat` - управление отправкой сообщений и файлов
- `@/contexts/AuthContext` - контекст аутентификации
- `@/types/chat` - типы для чатов и сообщений
- `@/utils/toast` - уведомления пользователю
- `@/utils/fileValidation` - валидация и форматирование файлов
- `./EmojiPicker` - компонент выбора эмодзи
- `./FileUploadProgress` - компонент прогресса загрузки

### Внешние зависимости:
- `next-intl` - интернационализация
- `next/image` - оптимизированные изображения
- React hooks: `useState`, `useRef`, `useEffect`

### Используемые API/Hooks:
```typescript
const {
  sendMessage,         // Функция отправки сообщения
  setUserTyping,      // Установка статуса печатания
  uploadFiles,        // Загрузка файлов к сообщению
  uploadingFiles,     // Объект с загружаемыми файлами
  removeUploadingFile // Удаление файла из загрузки
} = useChat();
```

## 🧠 Управление состоянием

### Локальное состояние:
```typescript
const [message, setMessage] = useState('');              // Текст сообщения
const [showEmojiPicker, setShowEmojiPicker] = useState(false);  // Показ EmojiPicker
const [isTyping, setIsTyping] = useState(false);        // Флаг печатания
const [selectedFiles, setSelectedFiles] = useState<File[]>([]);  // Выбранные файлы
```

### Refs:
```typescript
const inputRef = useRef<HTMLTextAreaElement>(null);      // Поле ввода
const fileInputRef = useRef<HTMLInputElement>(null);     // Скрытый input для файлов
const typingTimeoutRef = useRef<NodeJS.Timeout>(null);  // Таймер для typing indicator
```

## 💼 Бизнес-логика

### 1. Отправка сообщения:
- Проверка наличия текста или файлов
- Автоматическая генерация текста для файлов без подписи
- Формирование payload в зависимости от типа чата:
  - Существующий чат
  - Новый чат с товаром
  - Прямое сообщение контакту
- Последовательная отправка сообщения и файлов
- Обработка rate limit (429) как предупреждения

### 2. Управление typing indicator:
- Запуск при начале ввода текста
- Автоматическая остановка через 3 секунды
- Очистка при отправке сообщения

### 3. Валидация файлов:
- Проверка типа, размера, количества файлов
- Максимум 10 файлов на одно сообщение
- Отображение ошибок через toast
- Форматирование размера файла

### 4. Автоматическая высота textarea:
- Минимальная высота: 44px
- Максимальная высота: 120px
- Динамическая подстройка по содержимому

### 5. Горячие клавиши:
- Enter - отправить сообщение
- Shift+Enter - новая строка

## 🎨 UI структура

### Основные секции:

1. **Прогресс загрузки файлов** (если есть):
   - Карточка с FileUploadProgress компонентом
   - Отступ: p-4 pb-0

2. **Предпросмотр выбранных файлов**:
   - Изображения: эскизы 80x80 (sm:96x96)
   - Другие файлы: карточки с названием
   - Кнопки удаления на каждом файле

3. **Панель ввода** (p-2 sm:p-4):
   - Кнопка прикрепления файлов (слева)
   - Поле ввода с кнопкой эмодзи внутри
   - Кнопка отправки (справа)

### Стилизация:
- Фон: градиент from-gray-50 to-white
- Поле ввода: rounded-2xl с hover shadow
- Кнопки: btn-ghost с цветными hover состояниями
- Эмодзи picker: абсолютное позиционирование z-[9999]

### Поддерживаемые форматы файлов:
```
accept="image/*,video/*,.pdf,.doc,.docx,.txt"
```

## 🔄 Примеры использования

```tsx
// В существующем чате
<MessageInput 
  chat={currentChat}
/>

// Для нового чата с товаром
<MessageInput 
  initialListingId={123}
  initialSellerId={456}
/>

// Для прямого сообщения контакту
<MessageInput 
  initialSellerId={789}
/>
```

## 🐛 Известные особенности

1. **URL History**: После создания нового чата очищает параметры URL

2. **Rate Limiting**: Обрабатывает 429 ошибки как предупреждения

3. **Память**: Создает URL.createObjectURL для превью, освобождает через onLoad

4. **Валидация**: Использует внешний модуль fileValidation

5. **Toast уведомления**: Использует кастомную реализацию toast

6. **Производительность**: 
   - Нет мемоизации обработчиков
   - Пересоздание функций при каждом рендере
   - Может быть оптимизировано через useCallback