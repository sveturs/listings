# Паспорт компонента: MessageItem

## 📋 Метаданные
- **Путь**: `/frontend/svetu/src/components/Chat/MessageItem.tsx`
- **Роль**: Компонент отображения отдельного сообщения в чате
- **Тип**: Client Component
- **Размер**: 255 строк

## 🎯 Назначение
Отображает одно сообщение в чате с поддержкой:
- Текстовых сообщений с форматированием
- Эмодзи с анимацией для популярных эмодзи
- Вложений (изображения, файлы)
- Комбинированных сообщений (изображение + подпись)
- Индикации статуса доставки/прочтения
- Аватаров отправителей

## 🔧 Структура Props
```typescript
interface MessageItemProps {
  message: MarketplaceMessage;  // Объект сообщения
  isOwn: boolean;               // Это сообщение текущего пользователя
}
```

## 🔗 Зависимости и связи

### Внутренние зависимости:
- `@/types/chat` - типы для сообщений (MarketplaceMessage)
- `@/hooks/useChat` - хук для удаления вложений
- `./AnimatedEmoji` - анимированные эмодзи (динамический импорт)
- `./ChatAttachments` - компонент отображения вложений

### Внешние зависимости:
- `date-fns` - форматирование времени с локализацией
- `next-intl` - получение текущей локали
- `next/dynamic` - динамический импорт для AnimatedEmoji
- `isomorphic-dompurify` - санитизация HTML контента

### Используемые API/Hooks:
```typescript
const { deleteAttachment } = useChat();  // Функция удаления вложения
```

## 🧠 Управление состоянием

### Локальное состояние:
Компонент не использует локальное состояние

### Вычисляемые значения:
- `isEmojiOnly` - проверка, содержит ли сообщение только эмодзи (1-3 шт)
- Форматированное время через date-fns

## 💼 Бизнес-логика

### 1. Определение типа сообщения:
- Только текст
- Только вложения
- Комбинированное (вложения + подпись)
- Только эмодзи (специальный стиль)

### 2. Обработка эмодзи:
- Регулярное выражение для проверки: `/^(\p{Emoji_Presentation}|\p{Emoji}\uFE0F){1,3}$/u`
- Список из 31 анимированного эмодзи
- Увеличенный размер для одиночных эмодзи

### 3. Форматирование времени:
- Формат: "HH:mm" с локализацией (ru/en)
- Индикация прочтения: ✓ (отправлено), ✓✓ (прочитано)

### 4. Санитизация контента:
- Использует DOMPurify для безопасности
- Запрещены все HTML теги (`ALLOWED_TAGS: []`)
- Сохраняется текстовое содержимое

### 5. Генерация инициалов:
- Берёт первые буквы слов имени
- Максимум 2 символа в верхнем регистре

### 6. Удаление вложений:
- Доступно только для собственных сообщений
- Обработка ошибок через console.error

## 🎨 UI структура

### Основные элементы:

1. **Контейнер сообщения** (chat):
   - Выравнивание: chat-start (чужие), chat-end (свои)
   - Отступы для desktop: lg:pr-[5cm] или lg:pl-[5cm]

2. **Аватар** (только для чужих сообщений):
   - Размер: w-10 rounded-full
   - Изображение или инициалы на фоне base-300

3. **Bubble сообщения**:
   - Свои: bg-blue-500/80 text-white
   - Чужие: bg-green-100/70 с рамкой
   - Эмодзи: прозрачный фон без тени
   - Максимальная ширина: 95% (mobile), 85% (sm), 70% (md)

4. **Комбинированные сообщения**:
   - Вложения сверху (через ChatAttachments)
   - Текст снизу с отрицательным margin-top (-8px)
   - Ширина подписи: calc(100% - 4px)

### Специальные стили:
- `.photo-caption-own` - подпись к фото для своих сообщений
- `.photo-caption-with-border` - подпись с рамкой для чужих
- `.message-content-wrapper` - контейнер для текста и времени
- `.message-time-status` - стиль для времени и статуса

## 🔄 Примеры использования

```tsx
// В компоненте ChatWindow
{chatMessages.map((message, index) => (
  <MessageItem
    key={`${message.id}-${index}`}
    message={message}
    isOwn={message.sender_id === user?.id}
  />
))}
```

## 🐛 Известные особенности

1. **Динамический импорт**: AnimatedEmoji загружается асинхронно для избежания проблем с SSR

2. **Проверка подписей**: Специальная логика для определения, является ли текст подписью к файлу

3. **Стили DaisyUI**: Использует классы chat, chat-bubble из DaisyUI

4. **Безопасность**: Весь пользовательский контент проходит через DOMPurify

5. **Responsive**: Адаптивные размеры текста (text-sm sm:text-base)

6. **Производительность**: 
   - Мемоизация не используется (может быть оптимизировано)
   - Каждое сообщение рендерится независимо