# Паспорт компонента: ProductWizard

## Метаданные
- **Путь**: `frontend/svetu/src/components/products/ProductWizard.tsx`
- **Роль**: Пошаговый мастер создания товара
- **Тип**: Container Component
- **Размер**: 154 строки

## Назначение
Компонент-оркестратор для пошагового создания товара в витрине. Управляет навигацией между шагами, отображает прогресс и координирует работу дочерних компонентов-шагов.

## Props структура
```typescript
interface ProductWizardProps {
  storefrontSlug: string;  // Слаг витрины для которой создается товар
}
```

## Зависимости
- **Библиотеки**: React, next-intl
- **Контекст**: `CreateProductContext` - глобальное состояние создания товара
- **Компоненты-шаги**:
  - `CategoryStep` - выбор категории
  - `BasicInfoStep` - основная информация
  - `AttributesStep` - атрибуты категории
  - `PhotosStep` - загрузка фотографий
  - `PreviewStep` - предпросмотр и публикация

## Управление состоянием

### Из контекста
```typescript
const { 
  state,      // Текущее состояние (currentStep, completedSteps)
  goToStep,   // Переход на конкретный шаг
  nextStep,   // Следующий шаг
  prevStep    // Предыдущий шаг
} = useCreateProduct();
```

### Конфигурация шагов
```typescript
const STEPS = [
  { id: 'category', icon: '🏷️' },    // Выбор категории
  { id: 'basic', icon: '📝' },       // Основная информация
  { id: 'attributes', icon: '⚙️' },  // Атрибуты
  { id: 'photos', icon: '📸' },      // Фотографии
  { id: 'preview', icon: '👁️' },    // Предпросмотр
];
```

## Бизнес-логика

### Рендеринг шагов
```typescript
switch (state.currentStep) {
  case 0: return <CategoryStep onNext={nextStep} />;
  case 1: return <BasicInfoStep onNext={nextStep} onBack={prevStep} />;
  case 2: return <AttributesStep onNext={nextStep} onBack={prevStep} />;
  case 3: return <PhotosStep onNext={nextStep} onBack={prevStep} />;
  case 4: return <PreviewStep onBack={prevStep} storefrontSlug={storefrontSlug} />;
}
```

### Навигация
- Кликабельные шаги (только завершенные и текущий)
- Автоматическая блокировка недоступных шагов
- Визуальная индикация завершенных шагов

## UI структура

### Мобильная версия
```
┌─────────────────────────────────┐
│ [🏷️] Выбор категории           │
│ ▓▓▓▓░░░░░░░░░░░░ 1/5          │
└─────────────────────────────────┘
```

### Десктопная версия
```
Выбор категории                    1/5
▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░░░░░░░░░░░

[🏷️]    [📝]    [⚙️]    [📸]    [👁️]
Категория Инфо  Атрибуты Фото Просмотр
```

### Визуальные состояния шагов
- **Завершенный**: зеленый с галочкой
- **Текущий**: primary цвет, увеличенный размер
- **Доступный**: обычный вид, кликабельный
- **Недоступный**: серый, некликабельный

## Примеры использования

### В странице создания товара
```tsx
// app/[locale]/storefronts/[slug]/products/new/page.tsx
export default function NewProductPage({ params }) {
  return (
    <CreateProductProvider>
      <div className="container mx-auto p-4">
        <h1>Создание нового товара</h1>
        <ProductWizard storefrontSlug={params.slug} />
      </div>
    </CreateProductProvider>
  );
}
```

### С предзагруженными данными
```tsx
// Загрузка черновика
const draft = await loadDraft();

<CreateProductProvider initialState={draft}>
  <ProductWizard storefrontSlug={slug} />
</CreateProductProvider>
```

## Известные особенности

### Позитивные
- ✅ Адаптивный дизайн (мобильная/десктоп версии)
- ✅ Визуальный прогресс с процентами
- ✅ Гибкая навигация между завершенными шагами
- ✅ Эмодзи для визуальной идентификации шагов
- ✅ Плавные анимации переходов

### Технический долг
- ⚠️ Жестко закодированное количество шагов
- ⚠️ Нет сохранения состояния при уходе со страницы
- ⚠️ Отсутствует валидация при переходе между шагами

### Возможные улучшения
- 💡 Динамическая конфигурация шагов
- 💡 Автосохранение черновика
- 💡 Keyboard navigation (стрелки)
- 💡 Breadcrumbs для дополнительной навигации
- 💡 Индикатор несохраненных изменений