# Паспорт компонента: BulkActions

## Метаданные
- **Путь**: `frontend/svetu/src/components/products/BulkActions.tsx`
- **Роль**: Панель массовых действий над выбранными товарами
- **Тип**: UI Component
- **Размер**: 167 строк

## Назначение
Компонент отображает sticky панель с действиями для массовых операций над выбранными товарами:
- Активация/деактивация товаров
- Массовое удаление с подтверждением
- Экспорт выбранных товаров
- Массовое редактирование (опционально)

## Props структура
```typescript
interface BulkActionsProps {
  selectedCount: number;               // Количество выбранных товаров
  onBulkDelete: () => void;           // Callback удаления
  onBulkStatusChange: (active: boolean) => void;  // Callback изменения статуса
  onBulkExport: () => void;           // Callback экспорта
  onBulkEdit?: () => void;            // Опциональный callback редактирования
  onClearSelection: () => void;       // Callback очистки выбора
  isProcessing?: boolean;             // Индикатор обработки операции
}
```

## Зависимости
- **Библиотеки**: React, next-intl
- **Иконки**: FiTrash2, FiToggleLeft, FiToggleRight, FiDownload, FiEdit3, FiPackage, FiX, FiAlertTriangle

## Управление состоянием

### Локальное состояние
- `showDeleteConfirm` - флаг подтверждения удаления (автосброс через 5 сек)

### Защита от случайного удаления
```typescript
const handleDelete = () => {
  if (!showDeleteConfirm) {
    setShowDeleteConfirm(true);
    setTimeout(() => setShowDeleteConfirm(false), 5000); // Автосброс
  } else {
    onBulkDelete();
    setShowDeleteConfirm(false);
  }
};
```

## Бизнес-логика

### Массовые операции
1. **Изменение статуса**
   - Активация выбранных товаров
   - Деактивация выбранных товаров
   - Dropdown меню с опциями

2. **Удаление**
   - Двухэтапное подтверждение
   - Визуальное изменение кнопки
   - Автоматический сброс подтверждения

3. **Экспорт**
   - Экспорт выбранных товаров
   - Поддержка разных форматов (CSV/XML)

## UI структура

### Sticky панель
```
[Иконка] N товаров выбрано | [Очистить] ... [Статус ▼] [Редактировать] [Экспорт] | [Удалить]
[================== Прогресс-бар при обработке ==================]
```

### Компоненты панели
1. **Информация о выборе**
   - Иконка FiPackage
   - Количество выбранных
   - Кнопка очистки выбора

2. **Действия**
   - Dropdown статуса (активировать/деактивировать)
   - Кнопка редактирования (опционально)
   - Кнопка экспорта
   - Разделитель
   - Кнопка удаления с подтверждением

3. **Прогресс операции**
   - Progress bar
   - Текст "Обработка..."

## Примеры использования

### Базовое использование
```tsx
<BulkActions
  selectedCount={5}
  onBulkDelete={handleBulkDelete}
  onBulkStatusChange={handleStatusChange}
  onBulkExport={handleExport}
  onClearSelection={handleClearSelection}
/>
```

### С обработкой и редактированием
```tsx
<BulkActions
  selectedCount={selectedIds.length}
  onBulkDelete={async () => {
    await dispatch(bulkDeleteProducts({ 
      storefrontSlug, 
      productIds: selectedIds 
    }));
  }}
  onBulkStatusChange={async (isActive) => {
    await dispatch(bulkUpdateStatus({ 
      storefrontSlug, 
      productIds: selectedIds, 
      isActive 
    }));
  }}
  onBulkExport={() => dispatch(exportProducts({ storefrontSlug }))}
  onBulkEdit={() => openBulkEditModal()}
  onClearSelection={() => dispatch(clearSelection())}
  isProcessing={bulkOperation.isProcessing}
/>
```

## Известные особенности

### Позитивные
- ✅ Sticky позиционирование для удобства
- ✅ Защита от случайного удаления
- ✅ Визуальная обратная связь при обработке
- ✅ Dropdown для группировки связанных действий
- ✅ Автоматический сброс подтверждения

### Технический долг
- ⚠️ Нет возможности отмены операции
- ⚠️ Отсутствует детальный прогресс операций
- ⚠️ Нет истории массовых операций

### Возможные улучшения
- 💡 Добавить undo функциональность
- 💡 Детализация прогресса (N из M обработано)
- 💡 Выбор формата экспорта в UI
- 💡 Предпросмотр изменений перед применением
- 💡 Batch операции с очередью