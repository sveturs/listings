# Паспорт компонента: ProductList

## Метаданные
- **Путь**: `frontend/svetu/src/components/products/ProductList.tsx`
- **Роль**: Основной список товаров витрины с фильтрами и массовыми операциями
- **Тип**: Container Component
- **Размер**: 421 строк

## Назначение
Отображает список товаров витрины с возможностями:
- Поиск и фильтрация товаров
- Переключение режимов отображения (сетка/список/таблица)
- Массовый выбор и операции над товарами
- Бесконечная прокрутка
- Сортировка и группировка

## Props структура
```typescript
interface ProductListProps {
  storefrontSlug: string;     // Слаг витрины
  loading?: boolean;          // Индикатор загрузки
  hasMore?: boolean;          // Есть ли еще товары
  onLoadMore?: () => void;    // Callback загрузки следующей страницы
  totalCount?: number;        // Общее количество товаров
}
```

## Зависимости
- **Библиотеки**: React, Redux, next-intl, react-icons
- **Компоненты**: 
  - `ProductCard` - карточка товара
  - `BulkActions` - панель массовых действий
  - `InfiniteScrollTrigger` - триггер бесконечной прокрутки
- **Store**: `productSlice` - управление состоянием товаров
- **Иконки**: FiGrid, FiList, FiMenu, FiFilter, FiSearch, FiX, FiCheck

## Управление состоянием

### Redux состояние
```typescript
const {
  products,        // Массив товаров
  selectedIds,     // ID выбранных товаров
  ui: { isSelectMode, viewMode },  // UI режимы
  bulkOperation: { isProcessing },  // Статус массовых операций
  filters          // Активные фильтры
} = useSelector((state: RootState) => state.products);
```

### Локальное состояние
- `showFilters` - видимость расширенных фильтров
- `localSearch` - локальный поиск с debounce

### Диспатчи
- `toggleProductSelection` - выбор/отмена выбора товара
- `selectAll/clearSelection` - выбрать все/сбросить
- `toggleSelectMode` - режим массового выбора
- `setViewMode` - режим отображения
- `setFilters` - установка фильтров
- `bulkDeleteProducts` - массовое удаление
- `bulkUpdateStatus` - массовое изменение статуса
- `exportProducts` - экспорт товаров

## Бизнес-логика

### Поиск с debounce
```typescript
useEffect(() => {
  const timer = setTimeout(() => {
    dispatch(setFilters({ search: localSearch }));
  }, 300);
  return () => clearTimeout(timer);
}, [localSearch, dispatch]);
```

### Фильтрация
- **По статусу**: активные/неактивные
- **По складу**: в наличии/мало/нет в наличии  
- **По цене**: диапазон min-max
- **По поиску**: текстовый поиск с задержкой

### Массовые операции
- Удаление выбранных товаров
- Изменение статуса (активация/деактивация)
- Экспорт в CSV/XML

## UI структура

### Панель управления
1. **Поиск**
   - Input с иконкой поиска
   - Кнопка очистки
   - Debounced обновление

2. **Фильтры**
   - Кнопка-toggle показа фильтров
   - Индикатор активных фильтров
   - Расширенная панель с селектами

3. **Управление отображением**
   - Счетчик товаров
   - Режим массового выбора
   - Переключатель вида (grid/list/table)

### Отображение товаров
- **Grid**: карточки в сетке (адаптивная)
- **List**: карточки списком
- **Table**: табличное представление

### Состояния
- Пустой список с сообщением
- Индикатор загрузки
- Бесконечная прокрутка

## Примеры использования

### Базовое использование
```tsx
<ProductList 
  storefrontSlug="my-store"
  loading={loading}
  hasMore={hasMore}
  onLoadMore={handleLoadMore}
  totalCount={100}
/>
```

### С массовыми операциями
```tsx
// В родительском компоненте
const handleLoadMore = () => {
  dispatch(loadMoreProducts({ storefrontSlug }));
};

// ProductList автоматически управляет:
// - Выбором товаров
// - Массовыми действиями
// - Фильтрацией
```

## Известные особенности

### Позитивные
- ✅ Полный функционал управления товарами
- ✅ Три режима отображения
- ✅ Debounced поиск для производительности
- ✅ Адаптивный дизайн
- ✅ Интеграция с Redux для состояния

### Технический долг
- ⚠️ Нет оптимизации рендеринга для больших списков
- ⚠️ Отсутствует виртуализация для table view
- ⚠️ Фильтры сбрасывают пагинацию

### Возможные улучшения
- 💡 Добавить виртуальный скроллинг для больших списков
- 💡 Сохранение фильтров в URL для deep linking
- 💡 Drag & drop для изменения порядка
- 💡 Bulk edit для редактирования нескольких товаров
- 💡 Экспорт с выбором полей