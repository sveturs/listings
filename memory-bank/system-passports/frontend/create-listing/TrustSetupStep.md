# TrustSetupStep.tsx

## Метаданные
- **Путь**: `frontend/svetu/src/components/create-listing/steps/TrustSetupStep.tsx`
- **Роль**: UI компонент настройки доверия
- **Тип**: Step компонент мастера создания объявления
- **Размер**: 300 строк

## Назначение
Седьмой шаг мастера создания объявления. Настраивает систему доверия с верификацией телефона, предпочтениями встреч и доступностью для региональной безопасности торговли.

## Props структура
```typescript
interface TrustSetupStepProps {
  onNext: () => void;   // Переход к следующему шагу
  onBack: () => void;   // Возврат к предыдущему шагу
}
```

## Зависимости
### Внешние зависимости
- `react` - управление состоянием
- `next-intl` - интернационализация

### Внутренние зависимости
- Пока не интегрирован с CreateListingContext (TODO)

## Управление состоянием
### Локальное состояние
```typescript
const [formData, setFormData] = useState({
  phoneVerified: boolean,              // Статус верификации телефона
  preferredMeetingType: string,        // personal/pickup/delivery
  meetingLocations: string[],          // Безопасные места встреч
  availableHours: string,              // Часы доступности
  trustBadges: string[],              // Бейджи доверия (не используется)
});

const [isVerifyingPhone, setIsVerifyingPhone] = useState(false);
```

### Типы встреч
```typescript
const meetingTypes = [
  { id: 'personal', icon: '🤝', label: 'trust.meeting.personal' },
  { id: 'pickup', icon: '🏪', label: 'trust.meeting.pickup' },
  { id: 'delivery', icon: '🚚', label: 'trust.meeting.delivery' },
];
```

## Бизнес-логика
### Верификация телефона
```typescript
// TODO: Интегрировать реальную верификацию телефона через SMS/звонок
// ВРЕМЕННОЕ РЕШЕНИЕ: Фейковая верификация для демонстрации
const handlePhoneVerification = async () => {
  setTimeout(() => {
    setFormData(prev => ({ ...prev, phoneVerified: true }));
  }, 2000);
};
```

### Валидация
```typescript
const canProceed = formData.phoneVerified && formData.preferredMeetingType;
```

### Безопасные места встреч
```typescript
// TODO: Загружать из API безопасные места встреч по городам
// ВРЕМЕННОЕ РЕШЕНИЕ: Хардкодные места для демонстрации
const commonMeetingPlaces = [
  'Тржни центар', 'Кафе', 'Паркинг', 'Аутобуска станица', 'Трг'
];
```

## UI структура
### Верификация телефона
- **Required field**: обязательна для продолжения
- **Mock verification**: кнопка с loading состоянием
- **Success state**: alert с подтверждением
- **Regional info**: информация о местных операторах

### Типы встреч
- **Radio cards**: визуальные карточки с иконками
- **Conditional content**: места встреч показываются только для personal
- **Required selection**: один вариант должен быть выбран

### Места встреч
- **Multi-select buttons**: toggle selection для мест
- **Conditional rendering**: только для personal meeting type
- **Regional places**: локальные безопасные места

### Часы доступности
- **Free text field**: произвольный ввод расписания
- **Placeholder examples**: региональные примеры формата

## Примеры использования
```tsx
// В мастере создания объявления
<TrustSetupStep
  onNext={() => setCurrentStep(7)}
  onBack={() => setCurrentStep(5)}
/>

// Получение настроек доверия
const trustSettings = {
  phoneVerified: formData.phoneVerified,
  meetingType: formData.preferredMeetingType,
  locations: formData.meetingLocations,
  hours: formData.availableHours
};
```

## Известные особенности
### Позитивные
- ✅ Обязательная верификация телефона для безопасности
- ✅ Региональные безопасные места встреч
- ✅ Гибкие настройки типов встреч
- ✅ Условное отображение контента
- ✅ Понятные иконки и описания

### Технический долг
- ⚠️ Mock верификация телефона вместо реальной SMS/call API
- ⚠️ Не интегрирован с CreateListingContext
- ⚠️ Hardcoded список безопасных мест
- ⚠️ Отсутствует валидация format расписания
- ⚠️ trustBadges поле не используется

### Потенциальные улучшения
- Интегрировать реальную SMS верификацию (Twilio, local providers)
- Добавить интеграцию с глобальным контекстом
- Создать API для загрузки безопасных мест по городам
- Добавить calendar picker для hours availability
- Реализовать trust score system на основе верификаций
- Добавить геолокационные предложения безопасных мест
- Улучшить UX для phone verification flow