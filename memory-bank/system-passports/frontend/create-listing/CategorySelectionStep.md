# CategorySelectionStep.tsx

## Метаданные
- **Путь**: `frontend/svetu/src/components/create-listing/steps/CategorySelectionStep.tsx`
- **Роль**: UI компонент выбора категории
- **Тип**: Step компонент мастера создания объявления
- **Размер**: 300 строк

## Назначение
Второй шаг мастера создания объявления. Предоставляет интерфейс выбора категории товара с загрузкой данных из API и группировкой по популярности.

## Props структура
```typescript
interface CategorySelectionStepProps {
  onNext: () => void;   // Переход к следующему шагу
}
```

## Зависимости
### Внешние зависимости
- `react` - состояние и эффекты
- `next-intl` - интернационализация

### Внутренние зависимости
- `@/contexts/CreateListingContext` - контекст создания объявления
- `@/services/marketplace` - API сервис для загрузки категорий
- `@/utils/toast` - уведомления пользователю

## Управление состоянием
### Локальное состояние
```typescript
const [categories, setCategories] = useState<Category[]>([]);      // Загруженные категории
const [loading, setLoading] = useState(true);                     // Индикатор загрузки
const [selectedCategory, setSelectedCategory] = useState<Category | null>(); // Выбранная категория
const [error, setError] = useState<string | null>(null);          // Ошибки загрузки
```

### Типы данных
```typescript
interface Category {
  id: number;                              // ID категории
  name: string;                           // Название категории
  icon?: string;                          // Эмодзи иконка (по умолчанию 📦)
  slug?: string;                          // URL slug
  parent_id?: number | null;              // ID родительской категории
  translations?: Record<string, string>;   // Переводы названий
  level?: number;                         // Уровень вложенности
  count?: number;                         // Количество объявлений
}
```

## Бизнес-логика
### Загрузка категорий
1. **API запрос** через MarketplaceService.getCategories()
2. **Преобразование данных** с установкой дефолтных значений
3. **Фильтрация** только корневых категорий (parent_id === null)
4. **Обработка ошибок** с показом toast уведомлений

### Группировка категорий
- **Популярные**: первые 6 категорий в крупных карточках
- **Остальные**: оставшиеся категории в компактном виде
- **Адаптивная сетка**: 1-2-3 колонки для популярных, 1-2-4 для остальных

### Выбор категории
- **Single selection** с radio группой
- **Визуальная обратная связь** при выборе (border, background, check icon)
- **Автосохранение** в глобальный контекст при выборе

## UI структура
### Секции интерфейса
1. **Заголовок и описание** с эмодзи
2. **Популярные категории** - сетка 3 колонки
3. **Все категории** - сетка 4 колонки
4. **Обработка ошибок** - alert компонент
5. **Региональная подсказка** - info alert
6. **Кнопка продолжения** - disabled если ничего не выбрано

### Карточки категорий
- **Hover эффекты**: scale(1.05) для популярных
- **Индикатор выбора**: галочка в кружке
- **Счетчик объявлений**: отображается если > 0
- **Адаптивность**: скрытие счетчика на мобильных

### Состояния загрузки
- **Loading**: Spinner по центру экрана
- **Error**: Alert с иконкой и описанием ошибки
- **Success**: Отображение категорий в grid layout

## Примеры использования
```tsx
// В мастере создания объявления
<CategorySelectionStep
  onNext={() => setCurrentStep(2)}
/>

// Получение выбранной категории
const { state } = useCreateListing();
console.log(state.category); // { id, name, slug }
```

## Известные особенности
### Позитивные
- ✅ Автоматическая загрузка категорий из API
- ✅ Группировка по популярности для лучшего UX
- ✅ Полная адаптивность под разные экраны
- ✅ Обработка ошибок с fallback UI
- ✅ Интеграция с глобальным контекстом
- ✅ Кэширование выбора при навигации по шагам

### Технический долг
- ⚠️ Console.log в production коде (строки 81, 88)
- ⚠️ Hardcoded разделение на популярные/остальные (slice(0,6))
- ⚠️ Нет поддержки подкategorий (только корневые)
- ⚠️ Иконки категорий как эмодзи вместо SVG
- ⚠️ Отсутствует поиск по категориям

### Потенциальные улучшения
- Добавить поиск/фильтрацию категорий
- Реализовать поддержку подкатегорий (breadcrumb)
- Вынести логику популярности в API/конфигурацию
- Добавить drag & drop для сортировки
- Использовать SVG иконки вместо эмодзи
- Добавить infinite scroll для большого количества категорий
- Реализовать сохранение недавно выбранных категорий