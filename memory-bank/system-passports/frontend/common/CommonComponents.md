# Паспорт модуля: Common Components

## 📋 Метаданные
- **Название**: Common Components  
- **Путь**: `frontend/svetu/src/components/common/` и корневые компоненты
- **Роль**: Переиспользуемые UI и системные компоненты
- **Уровень**: Модуль переиспользуемых компонентов

## 🎯 Назначение
Модуль содержит переиспользуемые компоненты, которые используются во всех частях приложения: базовые UI элементы, системные провайдеры, утилитарные компоненты и компоненты состояния.

## 📊 Состав модуля: 15 компонентов

### 🖼️ UI Компоненты (8 компонентов)

#### 1. InfiniteScrollTrigger
**Путь**: `components/common/InfiniteScrollTrigger.tsx`

```typescript
interface InfiniteScrollTriggerProps {
  onTrigger: () => void;
  loading?: boolean;
  hasMore?: boolean;
  threshold?: number;
  rootMargin?: string;
  className?: string;
}
```

**Назначение**: Триггер для бесконечной прокрутки списков

**Ключевые особенности**:
- Использует `IntersectionObserver` API для отслеживания видимости
- `forwardRef` для прямого доступа к DOM элементу
- Автоматическая отмена наблюдения при unmount
- Настраиваемые пороги срабатывания
- Предотвращение двойных вызовов

#### 2. ViewToggle
**Путь**: `components/common/ViewToggle.tsx`

```typescript
interface ViewToggleProps {
  currentView: 'grid' | 'list' | 'table';
  onViewChange: (view: 'grid' | 'list' | 'table') => void;
  className?: string;
  availableViews?: Array<'grid' | 'list' | 'table'>;
}
```

**Назначение**: Переключатель режимов отображения данных

**Ключевые особенности**:
- Поддержка 3 режимов: сетка, список, таблица
- SVG иконки для каждого режима
- Настраиваемый набор доступных режимов
- Состояния active/inactive с DaisyUI стилями

#### 3. FormField
**Путь**: `components/FormField.tsx`

```typescript
interface FormFieldProps {
  label: string;
  name: string;
  type?: 'text' | 'email' | 'password' | 'number' | 'tel' | 'url' | 'search';
  value: string;
  onChange: (value: string) => void;
  error?: string;
  placeholder?: string;
  required?: boolean;
  disabled?: boolean;
  maxLength?: number;
  pattern?: string;
  autoComplete?: string;
  className?: string;
}
```

**Назначение**: Универсальное поле формы с валидацией

**Ключевые особенности**:
- Полная поддержка HTML5 типов input
- Встроенная обработка ошибок
- Индикатор обязательности поля
- Счетчик символов для длинных полей
- Доступность с ARIA атрибутами

#### 4. OptimizedImage
**Путь**: `components/OptimizedImage.tsx`

```typescript
interface OptimizedImageProps {
  src: string;
  alt: string;
  width?: number;
  height?: number;
  className?: string;
  priority?: boolean;
  loading?: 'lazy' | 'eager';
  placeholder?: string;
  onError?: () => void;
  onClick?: () => void;
}
```

**Назначение**: Оптимизированное отображение изображений

**Ключевые особенности**:
- Next.js Image с оптимизацией
- Lazy loading по умолчанию
- Fallback на placeholder при ошибке
- Поддержка priority loading
- Обработчики событий

#### 5. SafeImage
**Путь**: `components/SafeImage.tsx`

```typescript
interface SafeImageProps {
  src: string;
  alt: string;
  width?: number;
  height?: number;
  className?: string;
  fallback?: string;
  onError?: () => void;
}
```

**Назначение**: Безопасное отображение изображений с белым списком доменов

**Ключевые особенности**:
- Валидация доменов перед загрузкой
- Белый список: localhost, svetu.rs, cdn.svetu.rs
- Автоматический fallback на placeholder
- Специальная отладка для объявления #177
- Error boundary для изображений

#### 6. LanguageSwitcher
**Путь**: `components/LanguageSwitcher.tsx`

```typescript
interface LanguageSwitcherProps {
  className?: string;
  variant?: 'dropdown' | 'buttons';
}
```

**Назначение**: Переключатель языков интерфейса

**Ключевые особенности**:
- Поддержка en/ru локалей
- Два варианта UI: dropdown и кнопки
- Использует next-intl для переключения
- Сохранение текущего роута при смене языка
- Флаги стран в качестве иконок

#### 7. IconPicker
**Путь**: `components/IconPicker.tsx`

```typescript
interface IconPickerProps {
  value?: string;
  onChange: (icon: string) => void;
  className?: string;
  placeholder?: string;
}
```

**Назначение**: Выбор иконки из предустановленного набора

**Ключевые особенности**:
- 240+ эмодзи-иконок в 12 категориях
- Поиск по названию иконки
- Группировка: лица, животные, еда, спорт, транспорт и др.
- Responsive grid layout
- Поддержка keyboard navigation

#### 8. GoogleIcon
**Путь**: `components/GoogleIcon.tsx`

```typescript
interface GoogleIconProps {
  size?: number;
  className?: string;
}
```

**Назначение**: SVG иконка Google для OAuth кнопок

**Ключевые особенности**:
- Официальная SVG иконка Google
- Настраиваемый размер
- Оптимизированный SVG код
- Соответствие Google Brand Guidelines

### 📊 Компоненты состояния (4 компонента)

#### 9. DraftStatus
**Путь**: `components/DraftStatus.tsx`

```typescript
interface DraftStatusProps {
  className?: string;
}
```

**Назначение**: Индикатор наличия черновиков

**Ключевые особенности**:
- Подсчет черновиков в localStorage
- Визуальный badge с количеством
- Автообновление при изменениях
- Анимация появления/исчезновения

#### 10. DraftsModal
**Путь**: `components/DraftsModal.tsx`

```typescript
interface DraftsModalProps {
  isOpen: boolean;
  onClose: () => void;
}
```

**Назначение**: Модальное окно управления черновиками

**Ключевые особенности**:
- Просмотр всех сохраненных черновиков
- Восстановление черновика в форму
- Удаление отдельных черновиков
- Экспорт черновиков в JSON
- Очистка всех черновиков

#### 11. ErrorBoundary
**Путь**: `components/ErrorBoundary.tsx`

```typescript
interface ErrorBoundaryProps {
  children: React.ReactNode;
  fallback?: React.ComponentType<{ error: Error; resetError: () => void }>;
  onError?: (error: Error, errorInfo: ErrorInfo) => void;
}
```

**Назначение**: Перехват и обработка ошибок React

**Ключевые особенности**:
- Перехват JavaScript ошибок в дереве компонентов
- Кастомный fallback UI
- Логирование ошибок в console/сервис
- Кнопка сброса для восстановления
- Предотвращение краха всего приложения

### ⚙️ Системные компоненты (3 компонента)

#### 12. WebSocketManager
**Путь**: `components/WebSocketManager.tsx`

```typescript
interface WebSocketManagerProps {
  url: string;
  onMessage?: (data: any) => void;
  onConnected?: () => void;
  onDisconnected?: () => void;
  reconnectInterval?: number;
}
```

**Назначение**: Менеджер WebSocket соединений

**Ключевые особенности**:
- Автоматическое переподключение
- Обработка состояний соединения
- Очередь сообщений при отключении
- Heartbeat для проверки соединения
- Graceful cleanup при unmount

#### 13. ReduxProvider
**Путь**: `components/ReduxProvider.tsx`

```typescript
interface ReduxProviderProps {
  children: React.ReactNode;
}
```

**Назначение**: Провайдер Redux store для приложения

**Ключевые особенности**:
- Инициализация Redux store
- Подключение middleware
- DevTools в development режиме
- Типизированные hooks
- SSR совместимость

#### 14. AdminGuard
**Путь**: `components/AdminGuard.tsx`

```typescript
interface AdminGuardProps {
  children: React.ReactNode;
  fallback?: React.ReactNode;
  requiredRole?: 'admin' | 'moderator';
}
```

**Назначение**: Защита административных маршрутов

**Ключевые особенности**:
- Проверка роли пользователя
- Настраиваемые уровни доступа
- Fallback UI для неавторизованных
- Интеграция с системой аутентификации
- Redirect на login при необходимости

#### 15. AuthStateManager
**Путь**: `components/AuthStateManager.tsx`

```typescript
interface AuthStateManagerProps {
  children?: React.ReactNode;
}
```

**Назначение**: Менеджер состояния аутентификации

**Ключевые особенности**:
- Инициализация состояния при загрузке
- Обновление токенов
- Синхронизация с сервером
- Обработка logout при истечении токена
- Не рендерит UI (return null)

## 🔗 Общие зависимости

### Внешние библиотеки
- `react`: Базовые хуки и типы
- `next/image`: Оптимизация изображений
- `next/navigation`: Роутинг
- `next-intl`: Интернационализация
- `clsx`: Управление CSS классами

### Внутренние зависимости
- `@/hooks/*`: Кастомные хуки
- `@/store/*`: Redux store и slices
- `@/utils/*`: Утилитарные функции
- `@/contexts/*`: React контексты

## 📊 Принципы разработки

### 1. Переиспользуемость
- Все компоненты разработаны для многократного использования
- Гибкая настройка через props
- Минимальные предположения о контексте использования

### 2. Типизация
- Строгая TypeScript типизация всех props
- Экспорт интерфейсов для переиспользования
- Генерические типы где необходимо

### 3. Доступность
- ARIA атрибуты для всех интерактивных элементов
- Поддержка keyboard navigation
- Семантическая HTML разметка

### 4. Производительность
- Мемоизация дорогих вычислений
- Lazy loading для изображений
- Optimistic updates где возможно

### 5. Обработка ошибок
- Graceful degradation при ошибках
- Fallback состояния
- Подробное логирование для отладки

## 🎯 Примеры использования

### Бесконечная прокрутка
```jsx
<InfiniteScrollTrigger
  onTrigger={loadMore}
  loading={isLoading}
  hasMore={hasNextPage}
  threshold={0.8}
/>
```

### Переключатель видов
```jsx
<ViewToggle
  currentView={view}
  onViewChange={setView}
  availableViews={['grid', 'list']}
/>
```

### Поле формы
```jsx
<FormField
  label="Email"
  name="email"
  type="email"
  value={email}
  onChange={setEmail}
  error={emailError}
  required
/>
```

### Защищенный роут
```jsx
<AdminGuard requiredRole="admin">
  <AdminPanel />
</AdminGuard>
```

## 🐛 Известные особенности

1. **SafeImage**: Специальная отладка для объявления #177
2. **WebSocketManager**: Не рендерит UI компонент
3. **InfiniteScrollTrigger**: Требует forwardRef для интеграции
4. **DraftsModal**: Экспорт в JSON может быть большим для множества черновиков
5. **IconPicker**: 240+ иконок могут замедлить рендеринг на слабых устройствах

## 🔒 Безопасность

- **SafeImage**: Белый список доменов для изображений  
- **AdminGuard**: Проверка ролей на клиенте и сервере
- **FormField**: Экранирование пользовательского ввода
- **WebSocketManager**: Проверка origin для WebSocket

## 🌐 Локализация

Все компоненты с текстом поддерживают:
- Переводы через next-intl
- RTL языки (будущая функциональность)
- Числовые форматы по локали
- Форматы дат и времени