# Паспорт компонента: PaymentProcessing

## 📋 Метаданные
- **Путь**: `/frontend/svetu/src/components/payment/PaymentProcessing.tsx`
- **Роль**: UI Component - Payment Processing Status Display
- **Тип**: React Component
- **Размер**: 154 строки

## 🎯 Назначение
Компонент для отображения процесса обработки платежа в реальном времени. Показывает текущий статус, прогресс проверки, этапы обработки и анимированные индикаторы загрузки.

## 🔧 Props структура

```typescript
interface PaymentProcessingProps {
  status: string;           // Текущий статус платежа
  attempts: number;         // Количество попыток проверки
  maxAttempts: number;      // Максимальное количество попыток
}
```

## 🔗 Зависимости

### Внешние библиотеки
- `next-intl` - интернационализация (закомментировано)

### Внутренние зависимости
- Нет прямых зависимостей от других компонентов

## 🔄 Управление состоянием
- Stateless компонент
- Все данные приходят через props
- Визуализация основана на текущем статусе

## 🧩 Бизнес-логика

### Статусы обработки платежа
1. **checking** - Проверка платежа
   - Иконка: 🔍
   - Цвет: info (голубой)
   - Описание: "Получаем информацию о статусе транзакции"

2. **pending** - Ожидание обработки
   - Иконка: ⏳
   - Цвет: warning (желтый)
   - Описание: "Платеж находится в очереди на обработку"

3. **processing** - Обработка платежа
   - Иконка: ⚡
   - Цвет: primary (основной)
   - Описание: "Банк обрабатывает ваш платеж"

4. **authorized** - Платеж авторизован
   - Иконка: ✅
   - Цвет: success (зеленый)
   - Описание: "Средства заблокированы, завершаем транзакцию"

5. **default** - Неизвестный статус
   - Иконка: ❓
   - Динамический заголовок из статуса

### Расчет прогресса
```typescript
const progress = Math.min((attempts / maxAttempts) * 100, 100);
```

## 🎨 UI структура

### Основная карточка
- Card компонент с тенью
- Центрированный контент

### Элементы интерфейса

1. **Анимированная иконка**
   - Размер text-6xl
   - Пульсирующая анимация
   - Динамический цвет по статусу

2. **Заголовок и описание**
   - Динамический текст по статусу
   - Описание с opacity-70

3. **Прогресс-бар**
   - Показывает attempts/maxAttempts
   - DaisyUI progress компонент
   - Primary цвет

4. **Анимация загрузки**
   - 3 точки с bounce анимацией
   - Последовательная задержка (0ms, 150ms, 300ms)

5. **Информационное сообщение**
   - Alert компонент
   - Предупреждение не закрывать окно

6. **Этапы обработки**
   - Steps компонент (вертикальный/горизонтальный)
   - 4 этапа: Проверка → Обработка → Авторизация → Завершение
   - Динамическая подсветка активных этапов

## 💡 Примеры использования

```typescript
// Начальная проверка
<PaymentProcessing
  status="checking"
  attempts={1}
  maxAttempts={10}
/>

// В процессе обработки
<PaymentProcessing
  status="processing"
  attempts={5}
  maxAttempts={10}
/>

// Успешная авторизация
<PaymentProcessing
  status="authorized"
  attempts={7}
  maxAttempts={10}
/>

// Интеграция с платежной логикой
function PaymentFlow() {
  const [status, setStatus] = useState('checking');
  const [attempts, setAttempts] = useState(0);
  const maxAttempts = 10;
  
  useEffect(() => {
    const interval = setInterval(() => {
      // Check payment status
      checkPaymentStatus().then(newStatus => {
        setStatus(newStatus);
        setAttempts(prev => prev + 1);
        
        if (newStatus === 'authorized' || attempts >= maxAttempts) {
          clearInterval(interval);
        }
      });
    }, 2000);
    
    return () => clearInterval(interval);
  }, []);
  
  return (
    <PaymentProcessing
      status={status}
      attempts={attempts}
      maxAttempts={maxAttempts}
    />
  );
}
```

## ⚠️ Известные особенности

### Локализация
- ❌ **НЕ использует i18n** - все тексты hardcoded на русском языке
- Закомментирован импорт useTranslations

### Hardcoded значения
- Тексты статусов на русском языке
- Эмодзи для иконок статусов
- Фиксированные задержки анимации

### Стилизация
- Использует DaisyUI компоненты (card, progress, steps, alert)
- Tailwind анимации (animate-pulse, animate-bounce)
- Inline стили для animation-delay

### Потенциальные улучшения
1. Добавить поддержку i18n для всех текстов
2. Вынести конфигурацию статусов в отдельный объект
3. Добавить поддержку кастомных иконок вместо эмодзи
4. Сделать этапы конфигурируемыми
5. Добавить callback для отмены операции