# Паспорт компонента: PaymentMethodSelector

## 📋 Метаданные
- **Путь**: `/frontend/svetu/src/components/payment/PaymentMethodSelector.tsx`
- **Роль**: UI Component - Payment Method Selection
- **Тип**: React Client Component
- **Размер**: 140 строк

## 🎯 Назначение
Компонент для выбора способа оплаты. Отображает доступные методы платежа с их описаниями, иконками и дополнительными комиссиями. Позволяет пользователю выбрать предпочтительный способ оплаты.

## 🔧 Props структура

```typescript
interface PaymentMethodSelectorProps {
  selectedMethod: string | null;           // ID выбранного метода
  onMethodSelect: (methodId: string) => void;  // Обработчик выбора
  amount: number;                          // Сумма платежа (для расчета комиссий)
  currency: string;                        // Код валюты
}
```

## 🔗 Зависимости

### Внешние библиотеки
- `next-intl` - интернационализация
- `@heroicons/react/24/outline` - иконки

### Внутренние зависимости
- Нет прямых зависимостей от других компонентов

## 🔄 Управление состоянием
- Stateless компонент
- Выбранный метод управляется родителем через props
- Обратная связь через callback `onMethodSelect`

## 🧩 Бизнес-логика

### Структура метода платежа
```typescript
interface PaymentMethod {
  id: string;           // Уникальный идентификатор
  name: string;         // Локализованное название
  description: string;  // Локализованное описание
  icon: React.ReactNode;  // Иконка метода
  fee?: number;          // Дополнительная комиссия
  disabled?: boolean;    // Флаг недоступности
}
```

### Доступные методы оплаты
1. **card** - Банковская карта
   - Без дополнительной комиссии
   - Иконка: CreditCardIcon

2. **cash_on_delivery** - Наложенный платеж
   - Комиссия: 2% от суммы
   - Иконка: BanknotesIcon

3. **bank_transfer** - Банковский перевод
   - Без дополнительной комиссии
   - Иконка: WalletIcon

### Форматирование валюты
```typescript
const formatCurrency = (value: number) => {
  return new Intl.NumberFormat('sr-RS', {
    style: 'currency',
    currency: currency,
  }).format(value);
};
```

## 🎨 UI структура

### Основной контейнер
- Space-y-3 для вертикальных отступов
- Заголовок выбора метода

### Карточка метода оплаты
- Card с динамической границей
- Hover эффект для неактивных карточек
- Подсветка выбранной карточки (primary цвет)
- Disabled состояние с opacity-50

### Структура карточки
1. **Иконка метода**
   - Динамический цвет (primary для выбранного)
   - Размер w-6 h-6

2. **Информация о методе**
   - Название метода
   - Описание
   - Дополнительная комиссия (если есть)

3. **Radio button**
   - Синхронизирован с выбранным методом
   - Стиль radio-primary

### Информационный блок
- Alert компонент с info стилем
- Сообщение о безопасности платежа

## 💡 Примеры использования

```typescript
// Базовое использование
const [selectedMethod, setSelectedMethod] = useState<string | null>(null);

<PaymentMethodSelector
  selectedMethod={selectedMethod}
  onMethodSelect={setSelectedMethod}
  amount={5000}
  currency="RSD"
/>

// С предвыбранным методом
<PaymentMethodSelector
  selectedMethod="card"
  onMethodSelect={handleMethodChange}
  amount={10000}
  currency="EUR"
/>

// Интеграция с формой оплаты
function PaymentForm() {
  const [paymentMethod, setPaymentMethod] = useState<string | null>(null);
  const totalAmount = 7500;
  
  const handlePayment = () => {
    if (!paymentMethod) {
      alert('Please select payment method');
      return;
    }
    // Process payment
  };
  
  return (
    <>
      <PaymentMethodSelector
        selectedMethod={paymentMethod}
        onMethodSelect={setPaymentMethod}
        amount={totalAmount}
        currency="RSD"
      />
      <button 
        onClick={handlePayment}
        disabled={!paymentMethod}
      >
        Pay Now
      </button>
    </>
  );
}
```

## ⚠️ Известные особенности

### Локализация
- Использует sr-RS локаль для форматирования валют
- Все тексты через i18n переводы

### Hardcoded значения
- Комиссия за наложенный платеж: 2%
- Фиксированный список методов оплаты
- Дефолтная валюта 'RSD'

### Интерактивность
- Клик по всей карточке выбирает метод
- Radio button дублирует функционал выбора
- Disabled методы не кликабельны

### Ключи переводов
```
payment.selectMethod
payment.methods.card
payment.methods.cardDescription
payment.methods.cashOnDelivery
payment.methods.cashOnDeliveryDescription
payment.methods.bankTransfer
payment.methods.bankTransferDescription
payment.additionalFee
payment.securePaymentInfo
```

### Стилизация
- Использует DaisyUI компоненты (card, radio, alert)
- Динамические классы для состояний
- Tailwind классы для позиционирования