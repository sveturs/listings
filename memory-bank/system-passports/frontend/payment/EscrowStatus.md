# Паспорт компонента: EscrowStatus

## 📋 Метаданные
- **Путь**: `/frontend/svetu/src/components/payment/EscrowStatus.tsx`
- **Роль**: UI Component - Payment Escrow Status Display
- **Тип**: React Client Component
- **Размер**: 144 строк

## 🎯 Назначение
Компонент для отображения статуса эскроу-счета (удержания средств) в платежной системе. Показывает текущее состояние платежа, защиту покупателя и информацию о сроках выплаты в зависимости от уровня продавца.

## 🔧 Props структура

```typescript
interface EscrowStatusProps {
  status: 'held' | 'released' | 'refunded' | 'pending';
  amount: number;
  currency?: string;           // Валюта (по умолчанию 'RSD')
  releaseDate?: Date;          // Дата освобождения средств
  sellerTier?: 'new' | 'verified' | 'gold';  // Уровень продавца
}
```

## 🔗 Зависимости

### Внешние библиотеки
- `next-intl` - интернационализация
- `@heroicons/react/24/outline` - иконки

### Внутренние зависимости
- Нет прямых зависимостей от других компонентов

## 🔄 Управление состоянием
- Stateless компонент
- Все данные приходят через props
- Нет локального состояния

## 🧩 Бизнес-логика

### Форматирование валюты
```typescript
const formatCurrency = (value: number) => {
  return new Intl.NumberFormat('sr-RS', {
    style: 'currency',
    currency: currency,
  }).format(value);
};
```

### Определение периода удержания средств
```typescript
const getEscrowDays = () => {
  switch (sellerTier) {
    case 'gold':
      return 1;      // 1 день для золотых продавцов
    case 'verified':
      return 3;      // 3 дня для верифицированных
    default:
      return 7;      // 7 дней для новых продавцов
  }
};
```

### Конфигурация статусов
- **held**: Средства удержаны (синий цвет, иконка щита)
- **released**: Средства выплачены (зеленый цвет, галочка)
- **refunded**: Возврат средств (красный цвет, крестик)
- **pending**: Ожидание (желтый цвет, часы)

## 🎨 UI структура

### Основной контейнер
- Alert компонент с динамическим цветом
- Flex-колонка с gap-2

### Структура контента
1. **Заголовок с иконкой**
   - Иконка статуса
   - Название и описание статуса
   - Сумма в валюте

2. **Информация о защите покупателя** (только для статуса "held")
   - Список преимуществ защиты
   - Информация об автоматическом освобождении средств

3. **Уровень продавца** (опционально)
   - Badge с цветовой индикацией уровня

## 💡 Примеры использования

```typescript
// Базовое использование
<EscrowStatus 
  status="held"
  amount={5000}
/>

// С датой освобождения
<EscrowStatus 
  status="held"
  amount={10000}
  currency="EUR"
  releaseDate={new Date('2025-07-05')}
  sellerTier="verified"
/>

// Статус выплачено
<EscrowStatus 
  status="released"
  amount={7500}
/>

// Статус возврата
<EscrowStatus 
  status="refunded"
  amount={3000}
/>
```

## ⚠️ Известные особенности

### Локализация
- Использует sr-RS локаль для форматирования дат и валют
- Все тексты через i18n переводы

### Hardcoded значения
- Дефолтная валюта 'RSD' (сербский динар)
- Фиксированные периоды удержания (1, 3, 7 дней)

### Стилизация
- Использует DaisyUI alert компонент
- Динамические классы через шаблонные строки
- Badge компоненты для уровней продавца

### Ключи переводов
```
payment.escrow.held
payment.escrow.heldDescription
payment.escrow.released
payment.escrow.releasedDescription
payment.escrow.refunded
payment.escrow.refundedDescription
payment.escrow.pending
payment.escrow.pendingDescription
payment.escrow.releaseDate
payment.escrow.buyerProtection
payment.escrow.confirmReceipt
payment.escrow.disputeOption
payment.escrow.autoRelease
payment.escrow.sellerTier
payment.escrow.tier.new
payment.escrow.tier.verified
payment.escrow.tier.gold
```