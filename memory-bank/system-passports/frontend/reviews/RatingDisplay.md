# Паспорт компонента: RatingDisplay

## 📋 Метаданные
- **Путь**: `/frontend/svetu/src/components/reviews/RatingDisplay.tsx`
- **Роль**: UI Component - Star Rating Display
- **Тип**: React Client Component
- **Размер**: 103 строки

## 🎯 Назначение
Компонент для отображения рейтинга в виде звёзд. Поддерживает отображение целых, половинчатых и пустых звёзд, а также показ числового значения рейтинга и количества отзывов.

## 🔧 Props структура

```typescript
interface RatingDisplayProps {
  rating: number;              // Значение рейтинга (например, 4.5)
  maxRating?: number;          // Максимальный рейтинг (по умолчанию 5)
  size?: 'sm' | 'md' | 'lg';  // Размер звёзд
  showValue?: boolean;         // Показывать числовое значение
  totalReviews?: number;       // Количество отзывов
  className?: string;          // Дополнительные CSS классы
}
```

## 🔗 Зависимости

### Внешние библиотеки
- React

### Внутренние зависимости
- Нет

## 🔄 Управление состоянием
- Stateless компонент
- Все данные приходят через props
- Нет локального состояния

## 🧩 Бизнес-логика

### Расчёт звёзд
```typescript
const fullStars = Math.floor(rating);      // Количество полных звёзд
const hasHalfStar = rating % 1 !== 0;      // Есть ли половинчатая звезда
```

### Размеры компонента
```typescript
// Размеры звёзд
sizeClasses = {
  sm: 'w-4 h-4',    // 16x16px
  md: 'w-5 h-5',    // 20x20px
  lg: 'w-6 h-6',    // 24x24px
}

// Размеры текста
textSizeClasses = {
  sm: 'text-sm',    // 14px
  md: 'text-base',  // 16px
  lg: 'text-lg',    // 18px
}
```

### Алгоритм отрисовки
1. Вычисляет количество полных звёзд
2. Определяет наличие половинчатой звезды
3. Для каждой позиции от 1 до maxRating:
   - Если позиция <= fullStars: рисует полную звезду
   - Если позиция == fullStars + 1 и есть половинка: рисует половинчатую
   - Иначе: рисует пустую звезду

## 🎨 UI структура

### Основной контейнер
- Flex контейнер с выравниванием по центру
- Gap между элементами

### Структура элементов
```
[★★★★☆] 4.5 (123)
└──┬──┘  │   └─ Количество отзывов (опционально)
   │     └─ Числовое значение (опционально)
   └─ Звёзды
```

### Типы звёзд

1. **Полная звезда**
   - Цвет: text-yellow-400
   - Заливка: fill-current

2. **Половинчатая звезда**
   - Использует два SVG наложенных друг на друга
   - Нижний слой: серая звезда (text-gray-300)
   - Верхний слой: жёлтая звезда с clip-path (50%)

3. **Пустая звезда**
   - Цвет: text-gray-300
   - Заливка: fill-current

## 💡 Примеры использования

```typescript
// Базовое использование
<RatingDisplay rating={4.5} />

// Без числового значения
<RatingDisplay 
  rating={3.7} 
  showValue={false} 
/>

// С количеством отзывов
<RatingDisplay 
  rating={4.2} 
  totalReviews={156} 
/>

// Маленький размер без значения
<RatingDisplay 
  rating={4} 
  size="sm" 
  showValue={false} 
/>

// Большой размер с полной информацией
<RatingDisplay 
  rating={4.8} 
  size="lg" 
  totalReviews={1234} 
/>

// Кастомный максимальный рейтинг
<RatingDisplay 
  rating={7.5} 
  maxRating={10} 
/>

// С дополнительными стилями
<RatingDisplay 
  rating={4.3} 
  className="bg-white p-2 rounded-lg shadow" 
/>
```

## ⚠️ Известные особенности

### Стилизация
- Использует inline SVG для звёзд
- Фиксированные цвета (yellow-400 для заполненных, gray-300 для пустых)
- Использует Tailwind классы

### Половинчатые звёзды
- Реализованы через clip-path CSS
- Два SVG элемента в одном контейнере
- Может не поддерживаться старыми браузерами

### Форматирование
- Рейтинг всегда показывается с одним знаком после запятой (.toFixed(1))
- Количество отзывов в скобках

### Отсутствие интернационализации
- Нет поддержки i18n
- Формат чисел не учитывает локаль
- Нет альтернативных текстов для accessibility

### Производительность
- Рендерит SVG для каждой звезды отдельно
- Нет мемоизации для больших списков