# Паспорт компонента: RatingInput

## 📋 Метаданные
- **Путь**: `/frontend/svetu/src/components/reviews/RatingInput.tsx`
- **Роль**: UI Component - Interactive Star Rating Input
- **Тип**: React Client Component
- **Размер**: 123 строки

## 🎯 Назначение
Интерактивный компонент для выбора рейтинга в виде звёзд. Поддерживает hover-эффекты, анимации, текстовые подсказки для каждого уровня рейтинга и валидацию для обязательных полей.

## 🔧 Props структура

```typescript
interface RatingInputProps {
  value: number;                      // Текущее значение рейтинга
  onChange: (rating: number) => void; // Обработчик изменения
  maxRating?: number;                 // Максимальный рейтинг (по умолчанию 5)
  size?: 'sm' | 'md' | 'lg';        // Размер звёзд
  disabled?: boolean;                 // Заблокировать ввод
  className?: string;                 // Дополнительные CSS классы
  required?: boolean;                 // Обязательное поле
  error?: string;                     // Текст ошибки валидации
}
```

## 🔗 Зависимости

### Внешние библиотеки
- React

### Хуки
- `useState` - для управления hover состоянием

## 🔄 Управление состоянием

### Локальное состояние
```typescript
hoverRating: number  // Рейтинг при наведении (0 = нет наведения)
```

### Внешнее состояние
- `value` - текущий рейтинг (управляется родителем)
- `onChange` - обратная связь с родителем

## 🧩 Бизнес-логика

### Размеры звёзд
```typescript
sizeClasses = {
  sm: 'w-6 h-6',    // 24x24px
  md: 'w-8 h-8',    // 32x32px
  lg: 'w-12 h-12',  // 48x48px
}
```

### Текстовые описания рейтингов
```typescript
1: 'Ужасно' (text-error)
2: 'Плохо' (text-warning)
3: 'Нормально' (text-info)
4: 'Хорошо' (text-success)
5: 'Отлично' (text-success)
```

### Логика отображения
- `activeRating = hoverRating || value` - приоритет hover над value
- Звёзды <= activeRating отображаются жёлтыми
- Остальные - серыми

### Интерактивность
- При hover: звезда увеличивается и поворачивается
- При клике: устанавливается новый рейтинг
- При disabled: курсор not-allowed, нет hover эффектов

## 🎨 UI структура

### Основной контейнер
```
┌─────────────────────────────────┐
│ [★★★☆☆] Нормально              │ <- Звёзды + текст
│ Ошибка валидации                │ <- Ошибка (если есть)
└─────────────────────────────────┘
```

### Визуальные эффекты

1. **Звезда в обычном состоянии**
   - Серая (text-base-300) для неактивных
   - Жёлтая (text-warning) для активных
   - Drop shadow для активных

2. **Звезда при hover**
   - scale-125 (увеличение на 25%)
   - rotate-12 (поворот на 12 градусов)
   - Плавная анимация (duration-300)

3. **Текст рейтинга**
   - Появляется с анимацией slide-in-from-left
   - Цвет зависит от значения
   - Жирный шрифт (font-medium)

## 💡 Примеры использования

```typescript
// Базовое использование
const [rating, setRating] = useState(0);

<RatingInput 
  value={rating} 
  onChange={setRating} 
/>

// С валидацией обязательного поля
<RatingInput 
  value={rating} 
  onChange={setRating}
  required={true}
  error="Пожалуйста, оцените товар"
/>

// Большой размер
<RatingInput 
  value={rating} 
  onChange={setRating}
  size="lg"
/>

// Заблокированный ввод
<RatingInput 
  value={4} 
  onChange={() => {}}
  disabled={true}
/>

// В форме отзыва
function ReviewForm() {
  const [rating, setRating] = useState(0);
  const [error, setError] = useState('');
  
  const handleSubmit = () => {
    if (rating === 0) {
      setError('Оценка обязательна');
      return;
    }
    // Submit review
  };
  
  return (
    <form>
      <RatingInput
        value={rating}
        onChange={(r) => {
          setRating(r);
          setError(''); // Clear error on change
        }}
        required
        error={error}
      />
      <button onClick={handleSubmit}>Отправить</button>
    </form>
  );
}
```

## ⚠️ Известные особенности

### Локализация
- ❌ **Hardcoded тексты на русском языке**
- Нет поддержки i18n для текстовых описаний
- aria-label на английском

### Стилизация
- Использует DaisyUI цветовые классы (warning, error, success, info)
- Tailwind анимации (animate-in, fade-in, slide-in)
- Фиксированные размеры звёзд

### Доступность
- aria-label для каждой кнопки-звезды
- Поддержка клавиатурной навигации
- Focus стили (scale-110)

### Анимации
- Hover эффекты с transform и transition
- Fade-in анимация для текста и ошибок
- Slide-in анимация для текста рейтинга

### Производительность
- Рендерит все звёзды при каждом изменении
- Нет мемоизации обработчиков
- Частые перерендеры при движении мыши