# Паспорт модуля: Reviews Index

## 📋 Метаданные
- **Путь**: `/frontend/svetu/src/components/reviews/index.ts`
- **Роль**: Module Exports - Public API Aggregator
- **Тип**: TypeScript Export Module
- **Размер**: 8 строк

## 🎯 Назначение
Файл-бочка (barrel file) для агрегации и реэкспорта всех публичных компонентов модуля reviews. Предоставляет единую точку входа для импорта компонентов системы отзывов и упрощает структуру импортов в других частях приложения.

## 🔗 Экспортируемые компоненты

### Основные компоненты
```typescript
export { RatingDisplay } from './RatingDisplay';      // Отображение рейтинга звёздами
export { RatingInput } from './RatingInput';          // Ввод рейтинга пользователем
export { RatingStats } from './RatingStats';          // Статистика рейтингов
export { ReviewForm } from './ReviewForm';            // Форма создания отзыва
export { ReviewList } from './ReviewList';            // Список отзывов с фильтрами
export { ReviewsSection } from './ReviewsSection';    // Контейнер всей системы отзывов
```

### Вспомогательные компоненты
```typescript
export { ReviewFormExample } from './ReviewFormExample'; // Демонстрационный компонент
```

## 🏗️ Архитектура модуля

### Иерархия компонентов
```
ReviewsSection (Контейнер верхнего уровня)
├── RatingStats (Статистика)
├── ReviewForm (Форма создания)
│   ├── RatingInput (Ввод рейтинга)
│   └── ImageGallery (Просмотр фото) *
└── ReviewList (Список отзывов)
    ├── RatingDisplay (Показ рейтинга)
    └── ImageGallery (Просмотр фото) *

ReviewFormExample (Автономный пример)
└── ReviewForm (Демонстрация использования)

* ImageGallery не экспортируется через index, импортируется напрямую
```

### Модульная структура
```typescript
// Компоненты уровня UI (независимые)
RatingDisplay  // Только отображение, stateless
RatingInput    // Интерактивный ввод, локальное состояние

// Компоненты уровня функций (бизнес-логика)
RatingStats    // Работа с данными статистики
ReviewForm     // Создание отзывов, мутации
ReviewList     // Отображение списка, пагинация, фильтрация

// Компоненты уровня контейнеров (оркестрация)
ReviewsSection // Координация всех компонентов, управление состоянием

// Служебные компоненты
ReviewFormExample // Документация и примеры
```

## 💡 Использование

### Импорт отдельных компонентов
```typescript
import { RatingDisplay, RatingInput } from '@/components/reviews';

// Использование в другом компоненте
<RatingDisplay rating={4.5} size="lg" />
<RatingInput value={rating} onChange={setRating} />
```

### Импорт контейнера
```typescript
import { ReviewsSection } from '@/components/reviews';

// Полная интеграция системы отзывов
<ReviewsSection
  entityType="listing"
  entityId={listing.id}
  sellerId={listing.seller_id}
/>
```

### Импорт для создания кастомных решений
```typescript
import { 
  RatingStats, 
  ReviewList, 
  ReviewForm 
} from '@/components/reviews';

// Кастомная компоновка
<div className="custom-reviews">
  <RatingStats stats={stats} />
  <CustomHeader />
  <ReviewForm entityType="listing" entityId={123} />
  <ReviewList reviews={reviews} {...props} />
</div>
```

### Импорт для документации
```typescript
import { ReviewFormExample } from '@/components/reviews';

// В Storybook или документации
<ReviewFormExample />
```

## 🔧 Принципы организации

### Принцип единой ответственности
Каждый экспортируемый компонент имеет чётко определённую роль:
- **Display** - только отображение данных
- **Input** - только ввод данных пользователем
- **Form** - создание новых сущностей
- **List** - отображение коллекций с управлением
- **Stats** - агрегация и визуализация метрик
- **Section** - оркестрация и координация

### Принцип композиции
Компоненты спроектированы для переиспользования и композиции:
```typescript
// Можно использовать отдельно
<RatingDisplay rating={4.5} />

// Можно комбинировать
<div>
  <RatingDisplay rating={product.rating} />
  <span>из {product.reviews_count} отзывов</span>
</div>

// Можно встраивать в сложные структуры
<Card>
  <CardHeader>
    <RatingDisplay rating={rating} size="sm" />
  </CardHeader>
  <CardContent>
    <RatingStats stats={stats} />
  </CardContent>
</Card>
```

### Принцип минимальной связанности
Компоненты максимально независимы друг от друга:
- Нет циклических зависимостей
- Минимум shared state
- Передача данных через props
- События через callbacks

## 🎨 Паттерны экспорта

### Named Exports (рекомендуемый подход)
```typescript
// Экспорт с именами для tree-shaking
export { ComponentName } from './ComponentName';
```

### Преимущества текущего подхода
1. **Tree-shaking** - неиспользуемые компоненты исключаются из бандла
2. **Явность** - понятно какие компоненты доступны
3. **Рефакторинг** - легко переименовывать и перемещать
4. **IDE поддержка** - автодополнение и навигация

## ⚠️ Известные особенности

### Отсутствующие экспорты
- `ImageGallery` - намеренно не экспортируется, используется внутренне
- Типы и интерфейсы - экспортируются из `@/types/review`
- Хуки - экспортируются из `@/hooks/useReviews`

### Зависимости
- Все компоненты должны быть совместимы с barrel export
- Нет default exports в компонентах (только named)
- Компоненты не должны импортировать друг друга напрямую

### Производительность
- Tree-shaking работает только при использовании named imports
- Избегать `import * as Reviews` для оптимального размера бандла

### Будущее развитие
- Возможно добавление новых компонентов (ReviewAnalytics, ReviewModerator)
- Потенциальное разделение на подмодули (input/, display/, management/)
- Возможная интеграция с lazy loading для больших компонентов

### Альтернативные подходы импорта
```typescript
// ❌ Неоптимально - импортирует всё
import * as Reviews from '@/components/reviews';

// ✅ Оптимально - tree-shaking
import { RatingDisplay, ReviewForm } from '@/components/reviews';

// ✅ Альтернатива - прямой импорт
import { RatingDisplay } from '@/components/reviews/RatingDisplay';
```