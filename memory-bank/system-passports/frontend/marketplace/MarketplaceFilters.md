# Паспорт компонента: MarketplaceFilters

## 📋 Метаданные
- **Путь**: `/frontend/svetu/src/components/marketplace/MarketplaceFilters.tsx`
- **Роль**: Панель фильтров для маркетплейса
- **Тип**: Client Component
- **Размер**: 77 строк

## 🎯 Назначение
Компонент фильтрации товаров маркетплейса. В текущей версии поддерживает только фильтрацию по категориям с динамической загрузкой списка категорий из API.

## 🔧 Структура Props
```typescript
interface MarketplaceFiltersProps {
  onFilterChange: (filters: { category?: string }) => void;
}
```

## 🔗 Зависимости и связи

### Сервисы:
- `MarketplaceService` - загрузка категорий

### Типы:
- `components['schemas']['models.MarketplaceCategory']` - тип категории из OpenAPI

### Хуки:
- `useConfig` - доступ к конфигурации приложения

## 📊 Управление состоянием

### Локальное состояние:
```typescript
categories: Category[]  // Список категорий
loading: boolean       // Загрузка данных
error: boolean        // Флаг ошибки
```

## 🔄 Жизненный цикл

### useEffect загрузка категорий:
1. Устанавливает `loading = true`
2. Вызывает `MarketplaceService.getCategories()`
3. Сохраняет категории в state
4. Обрабатывает ошибки
5. Сбрасывает `loading = false`

## 🎨 UI структура

```
┌─────────────────────┐
│ Filters             │
├─────────────────────┤
│ [API URL в dev]     │
│                     │
│ ┌─────────────────┐ │
│ │ All Categories ▼│ │
│ └─────────────────┘ │
└─────────────────────┘
```

### Состояния UI:
1. **Loading**: skeleton элемент
2. **Error**: alert с сообщением
3. **Success**: select с категориями

## 🎯 Функциональность

### Фильтрация по категориям:
- Select dropdown со всеми категориями
- Опция "All Categories" для сброса
- onChange вызывает `onFilterChange({ category: value })`

### Debug информация:
- В development режиме показывает API URL
- Использует `config.env.isDevelopment`

## ⚡ Особенности реализации

### 1. Типизация категорий:
```typescript
type Category = components['schemas']['models.MarketplaceCategory'];
```
Использует сгенерированные типы из OpenAPI

### 2. Обработка ошибок:
- Простой boolean флаг
- Общее сообщение об ошибке
- Логирование в консоль

### 3. Пустое значение:
- `value=""` для "All Categories"
- Позволяет сбросить фильтр

## 🌍 Интернационализация
**Отсутствует** - все тексты захардкожены на английском:
- "Filters"
- "All Categories"
- "Failed to load categories"

## 🎨 Стилизация
- DaisyUI card для контейнера
- `select-bordered` для dropdown
- Skeleton для состояния загрузки
- Alert для ошибок

## ⚠️ Известные особенности

1. **Нет интернационализации**:
   - Тексты на английском
   - TODO: использовать useTranslations

2. **Простая обработка ошибок**:
   - Только boolean флаг
   - Нет retry механизма

3. **Ограниченная функциональность**:
   - Только фильтр по категориям
   - TODO: цена, локация, состояние

4. **Type assertion**:
   ```typescript
   setCategories(response.data as Category[]);
   ```
   - Приведение типа без проверки

## 📝 Примеры использования

```tsx
function MarketplacePage() {
  const [filters, setFilters] = useState({});
  
  return (
    <MarketplaceFilters 
      onFilterChange={(newFilters) => {
        setFilters({ ...filters, ...newFilters });
      }}
    />
  );
}
```

## 🔄 Связанные компоненты
- `MarketplaceList` - применяет фильтры
- `MarketplaceService` - источник категорий
- Будущие: PriceFilter, LocationFilter, etc.