# Паспорт компонента: ListingActions

## 📋 Метаданные
- **Путь**: `/frontend/svetu/src/components/marketplace/listing/ListingActions.tsx`
- **Роль**: Панель действий для страницы товара
- **Тип**: Client Component
- **Размер**: 319 строк

## 🎯 Назначение
Набор кнопок действий для детальной страницы товара:
- Добавление в избранное
- Добавление к сравнению
- Поделиться в соцсетях
- Пожаловаться на объявление

## 🔧 Структура Props
```typescript
interface ListingActionsProps {
  listing: {
    id: number;
    title: string;
    price: number;
    is_favorite?: boolean;
  };
}
```

## 🔗 Зависимости и связи

### Утилиты:
- `toast` - уведомления пользователю
- `useAuth` - проверка авторизации
- `useLocale` - текущая локаль

## 📊 Управление состоянием

### Локальное состояние:
```typescript
isFavorite: boolean      // В избранном
isInComparison: boolean  // В сравнении
shareMenuOpen: boolean   // Открыто меню шаринга
```

## 🔧 Основные функции

### handleFavoriteToggle:
- Проверка авторизации
- Оптимистичное обновление UI
- TODO: API вызов
- Откат при ошибке

### handleComparisonToggle:
- Переключение состояния
- TODO: интеграция со store
- Toast уведомление

### handleShare:
- Поддержка платформ:
  - WhatsApp
  - Telegram
  - Viber
  - Facebook
  - Twitter/X
  - Копировать ссылку
- Формирование URL с параметрами
- Fallback для старых браузеров

### fallbackCopyToClipboard:
- Создание скрытого textarea
- document.execCommand('copy')
- Обработка ошибок
- Очистка DOM

## 🎨 UI структура

```
[❤️] [📊] [🔗] [⚠️]
      └─ Dropdown меню:
         - WhatsApp
         - Telegram
         - Viber
         - Facebook
         - Twitter/X
         ─────────
         - Copy link
```

### Кнопки:
1. **Избранное** - сердце (красное если активно)
2. **Сравнение** - столбчатая диаграмма
3. **Поделиться** - иконка share с dropdown
4. **Пожаловаться** - треугольник с восклицательным знаком

## 🌍 Интернационализация

Inline переводы через тернарный оператор:
```typescript
locale === 'ru' ? 'текст на русском' : 'text in english'
```

Переводимые элементы:
- Toast сообщения
- aria-label атрибуты
- Текст "Copy link" в меню

## 🎯 Бизнес-логика

### Авторизация:
- Избранное требует авторизации
- Показывает toast с предложением войти

### Оптимистичные обновления:
- UI обновляется сразу
- Откат при ошибке API

### Формирование share текста:
```typescript
const text = `${listing.title} - ${listing.price}$`;
```

### URL encoding:
- encodeURIComponent для всех параметров
- Корректная работа с кириллицей

## ⚡ Особенности реализации

### 1. Clipboard API с fallback:
```typescript
if (navigator.clipboard && window.isSecureContext) {
  // Modern API
} else {
  // Fallback через execCommand
}
```

### 2. Share URLs:
- Прямые ссылки на share endpoints
- Открытие в новой вкладке
- Поддержка Viber через `viber://`

### 3. Dropdown управление:
- Ручное управление состоянием
- Закрытие после действия

## ⚠️ Известные особенности

1. **TODO: API интеграция**:
   - Нет реальных API вызовов
   - Только локальное состояние

2. **TODO: Comparison store**:
   - Нет интеграции с хранилищем
   - Состояние теряется при обновлении

3. **Report функциональность**:
   - Кнопка есть, но нет обработчика
   - Только UI без логики

4. **Hardcoded валюта**:
   - Всегда использует `$` в share тексте
   - Должна браться из настроек

5. **Inline переводы**:
   - Не использует систему i18n
   - Дублирование текстов

## 📝 Примеры использования

```tsx
<ListingActions
  listing={{
    id: 123,
    title: "iPhone 13 Pro",
    price: 999,
    is_favorite: false
  }}
/>
```

## 🔄 Связанные компоненты
- `ListingDetailPage` - использует действия
- `toast` - система уведомлений
- Будущие: FavoritesStore, ComparisonStore