# Паспорт компонента: SimilarListings

## 📋 Метаданные
- **Путь**: `/frontend/svetu/src/components/marketplace/listing/SimilarListings.tsx`
- **Роль**: Блок похожих товаров на странице товара
- **Тип**: Client Component
- **Размер**: 289 строк

## 🎯 Назначение
Отображает список похожих товаров с возможностью:
- Переключения между сеткой и списком
- Бесконечной прокрутки
- Адаптивного отображения
- Загрузки до 100 похожих товаров

## 🔧 Структура Props
```typescript
interface SimilarListingsProps {
  listingId: number;  // ID текущего товара
}
```

## 🔗 Зависимости и связи

### Сервисы:
- `apiClient` - HTTP клиент для API
- `config` - построение URL изображений

### Компоненты:
- `SafeImage` - безопасное отображение изображений
- `ViewToggle` - переключатель вида
- `InfiniteScrollTrigger` - триггер загрузки

### Хуки:
- `useViewPreference` - сохранение предпочтений
- `useInfiniteScroll` - бесконечная прокрутка
- `useLocale`, `useTranslations` - i18n

## 📊 Управление состоянием

### Локальное состояние:
```typescript
allListings: MarketplaceItem[]      // Все загруженные товары
displayedListings: MarketplaceItem[] // Отображаемые товары
loading: boolean                     // Загрузка данных
error: string | null                 // Текст ошибки
displayCount: number                 // Количество показанных
viewMode: 'grid' | 'list'           // Режим отображения
```

### Константы:
```typescript
ITEMS_PER_LOAD = 20  // Товаров за загрузку
```

## 🔄 Жизненный цикл

### 1. Загрузка похожих товаров:
```typescript
GET /api/v1/marketplace/listings/{id}/similar?limit=100
```
- Загружает до 100 похожих товаров
- Фильтрует текущий товар из списка
- Показывает первые 20

### 2. Управление отображением:
- При изменении `displayCount` обновляет slice
- Бесконечная прокрутка добавляет по 20

## 🎨 UI структура

### Grid View:
```
Похожие объявления                    [Grid|List]

┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐
│ Img│ │ Img│ │ Img│ │ Img│ │ Img│
│$100│ │$200│ │$150│ │$300│ │$250│
├────┤ ├────┤ ├────┤ ├────┤ ├────┤
│Title│ │Title│ │Title│ │Title│ │Title│
│📍Loc│ │📍Loc│ │📍Loc│ │📍Loc│ │📍Loc│
└────┘ └────┘ └────┘ └────┘ └────┘
```

### List View:
```
┌────────────────────────────┐
│ [Img] Title           $100 │
│       📍 Location          │
└────────────────────────────┘
```

### Responsive сетка:
- Mobile: 2 колонки
- Tablet: 3 колонки  
- Desktop: 4 колонки
- XL: 5 колонок

## ⚡ Особенности реализации

### 1. Оптимизация загрузки:
- Загружает 100 товаров сразу
- Показывает по 20 за раз
- Нет повторных API вызовов

### 2. Обработка ответа API:
```typescript
const items = Array.isArray(response.data)
  ? response.data
  : response.data?.data || [];
```
Поддержка разных форматов ответа

### 3. Фильтрация текущего товара:
```typescript
const filteredListings = items.filter(
  item => item.id !== listingId
);
```

### 4. Skeleton loading:
- 5 skeleton карточек
- Анимация пульсации
- Адаптивная сетка

## 🌍 Интернационализация

### Inline переводы:
```typescript
locale === 'ru' ? 'Похожие объявления' : 'Similar listings'
```

### Translations hook:
```typescript
tCommon('loadMore')  // Кнопка загрузки
```

## 🎨 Стилизация

### Grid режим:
- Квадратные карточки (aspect-square)
- Price badge внизу слева
- Hover эффекты (scale, shadow)

### List режим:
- Горизонтальные карточки
- Фиксированный размер изображения (24x24)
- Card компонент DaisyUI

## ⚠️ Известные особенности

1. **Hardcoded валюта**:
   - Всегда показывает `$`
   - TODO: использовать настройки

2. **Limit 100**:
   - Максимум 100 товаров
   - Может быть недостаточно

3. **Inline переводы**:
   - Не все тексты через i18n
   - Дублирование переводов

4. **Нет обработки пустого ответа**:
   - Компонент скрывается
   - Можно показать сообщение

5. **showButton={false}**:
   - Кнопка загрузки скрыта
   - Только автоматическая загрузка

## 📝 Примеры использования

```tsx
// На странице товара
<SimilarListings listingId={123} />
```

## 🔄 Связанные компоненты
- `ListingDetailPage` - использует компонент
- `MarketplaceCard` - похожая структура
- `ViewToggle` - переключатель видов
- `InfiniteScrollTrigger` - управление загрузкой