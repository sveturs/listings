# Паспорт компонента: PaymentDeliveryStep

## Метаданные
- **Путь**: `/frontend/svetu/src/components/storefronts/create/steps/PaymentDeliveryStep.tsx`
- **Роль**: Настройка способов оплаты и доставки
- **Тип**: Компонент формы с динамическими списками
- **Размер**: 268 строк

## Назначение
Пятый шаг мастера создания витрины. Позволяет настроить принимаемые способы оплаты и опции доставки с указанием служб доставки, времени и стоимости.

## Props
```typescript
interface PaymentDeliveryStepProps {
  onNext: () => void;  // Переход к следующему шагу
  onBack: () => void;  // Возврат к предыдущему шагу
}
```

## Зависимости
### Внешние
- `react` - хук состояния
- `next-intl` - локализация

### Внутренние
- `@/contexts/CreateStorefrontContext` - контекст формы создания

## Управление состоянием
### Локальное состояние
- `newDeliveryOption` - форма добавления новой опции доставки

### Контекст
- `formData.paymentMethods` - массив выбранных способов оплаты
- `formData.deliveryOptions` - массив настроенных опций доставки
- `updateFormData` - обновление данных

## Бизнес-логика
1. **Способы оплаты**:
   - cash (💵) - Наличные
   - card (💳) - Карта
   - bank_transfer (🏦) - Банковский перевод
   - cod (📦) - Наложенный платеж
   - postanska_stedionica (📮) - Поштанска штедионица

2. **Службы доставки**:
   - posta_srbije - Пошта Србије
   - aks - AKS
   - bex - BEX
   - city_express - City Express
   - d_express - D-Express
   - post_express - Post Express
   - daily_express - Daily Express
   - own_delivery - Собственная доставка

3. **Параметры доставки**:
   ```typescript
   interface DeliveryOption {
     providerName: string;              // Название службы
     deliveryTimeMinutes: number;       // Время доставки в минутах
     deliveryCostRSD: number;          // Стоимость в динарах
     freeDeliveryThresholdRSD?: number; // Порог бесплатной доставки
     maxDistanceKm?: number;           // Максимальное расстояние (не используется)
   }
   ```

4. **Управление опциями**:
   - Множественный выбор способов оплаты
   - Добавление/удаление опций доставки
   - Настройка параметров для каждой службы

## UI структура
```
<div className="card"> // Карточка формы
  └── <div className="card-body">
      ├── <h2> // "Оплата и доставка"
      ├── <p> // Подзаголовок
      │
      ├── <div> // Способы оплаты
      │   ├── <h3> // "Способы оплаты"
      │   └── <div className="grid"> // Чекбоксы 3 в ряд
      │       └── {paymentMethods.map()} // 5 способов
      │
      ├── <div> // Опции доставки
      │   ├── <h3> // "Опции доставки"
      │   ├── {deliveryOptions.map()} // Список добавленных
      │   │   └── <div> // Карточка опции
      │   │       ├── Название, время, стоимость
      │   │       └── Кнопка удаления
      │   │
      │   └── <div className="card"> // Форма добавления
      │       ├── Выбор службы (select)
      │       ├── Время доставки (number)
      │       ├── Стоимость (number)
      │       ├── Порог бесплатной (number, опционально)
      │       └── Кнопка "Добавить"
      │
      └── <div className="card-actions"> // Кнопки
          ├── Назад (ghost)
          └── Далее (primary)
```

## Примеры использования
```typescript
// В мастере создания витрины
<PaymentDeliveryStep
  onNext={() => setCurrentStep(6)}
  onBack={() => setCurrentStep(4)}
/>

// Данные в контексте после заполнения
{
  paymentMethods: ['cash', 'card', 'bank_transfer'],
  deliveryOptions: [
    {
      providerName: 'posta_srbije',
      deliveryTimeMinutes: 120,
      deliveryCostRSD: 250,
      freeDeliveryThresholdRSD: 3000
    },
    {
      providerName: 'own_delivery',
      deliveryTimeMinutes: 60,
      deliveryCostRSD: 200
    }
  ]
}
```

## Известные особенности
### Позитивные
- Региональные службы доставки Сербии
- Гибкая настройка параметров доставки
- Порог бесплатной доставки
- Визуальные иконки для способов оплаты

### Технический долг
- Жестко закодированная валюта RSD
- Поле maxDistanceKm определено но не используется
- Нет валидации минимальных/максимальных значений

### Возможные улучшения
- Добавить радиус доставки (maxDistanceKm)
- Поддержка разных валют
- Шаблоны стандартных настроек
- Интеграция с API служб доставки
- Расчет стоимости по весу/габаритам
- Настройка рабочих дней доставки