# 💰 Balance Handler - Система Управления Балансами

## 📋 Обзор модуля

**Назначение**: Модуль управления финансовыми балансами пользователей, включая пополнение счета, историю транзакций и управление методами оплаты.

**Местоположение**: `/backend/internal/proj/balance/`

**Версия API**: v1

**Основные возможности**:
- Просмотр текущего баланса пользователя
- Создание депозитов (пополнение баланса)
- История финансовых транзакций
- Управление методами оплаты
- Автоматический расчет комиссий
- Поддержка заморозки средств

## 🏗️ Архитектура и структура файлов

```
balance/
├── handler/
│   ├── balance.go         # Основной handler с endpoints
│   ├── handler.go         # Структура handler
│   └── routes.go          # Регистрация маршрутов
└── service/
    ├── balance.go         # Реализация бизнес-логики
    ├── interface.go       # Интерфейсы сервиса
    └── service.go         # Конструктор сервиса
```

## 🛠️ API Endpoints

### 🔐 Защищенные маршруты (JWT)

Все маршруты требуют аутентификации через JWT токен.

#### Управление балансом
- `GET /api/v1/balance` - Получить текущий баланс
- `POST /api/v1/balance/deposit` - Создать депозит (пополнение)

#### История и методы оплаты
- `GET /api/v1/balance/transactions` - История транзакций
- `GET /api/v1/balance/payment-methods` - Доступные методы оплаты

## 🗄️ Модели данных и структуры

### Основные модели

#### UserBalance (Баланс пользователя)
```go
type UserBalance struct {
    UserID        int       `json:"user_id"`
    Balance       float64   `json:"balance"`         // Доступный баланс
    FrozenBalance float64   `json:"frozen_balance"`  // Замороженные средства
    Currency      string    `json:"currency"`        // Валюта (по умолчанию RSD)
    UpdatedAt     time.Time `json:"updated_at"`
}
```

#### BalanceTransaction (Транзакция)
```go
type BalanceTransaction struct {
    ID             int             `json:"id"`
    UserID         int             `json:"user_id"`
    Type           string          `json:"type"`              // deposit, withdraw, payment, refund
    Amount         float64         `json:"amount"`            // Сумма с учетом комиссии
    Currency       string          `json:"currency"`
    Status         string          `json:"status"`            // pending, completed, failed
    PaymentMethod  string          `json:"payment_method"`    // card, bank_transfer и т.д.
    PaymentDetails json.RawMessage `json:"payment_details"`   // Детали платежа
    Description    string          `json:"description"`
    CreatedAt      time.Time       `json:"created_at"`
    CompletedAt    *time.Time      `json:"completed_at,omitempty"`
}
```

#### PaymentMethod (Метод оплаты)
```go
type PaymentMethod struct {
    ID            int             `json:"id"`
    Name          string          `json:"name"`              // Отображаемое имя
    Code          string          `json:"code"`              // Код метода (card, bank_transfer)
    Type          string          `json:"type"`              // Тип метода
    IsActive      bool            `json:"is_active"`
    MinimumAmount float64         `json:"minimum_amount"`    // Минимальная сумма
    MaximumAmount float64         `json:"maximum_amount"`    // Максимальная сумма (0 = без ограничений)
    FeePercentage float64         `json:"fee_percentage"`    // Процент комиссии
    FixedFee      float64         `json:"fixed_fee"`         // Фиксированная комиссия
    Credentials   json.RawMessage `json:"credentials,omitempty"`
    CreatedAt     time.Time       `json:"created_at"`
}
```

### Request/Response структуры

#### DepositRequest
```go
type DepositRequest struct {
    Amount        float64 `json:"amount" example:"1000.50"`
    PaymentMethod string  `json:"payment_method" example:"card"`
}
```

## 🔄 Бизнес-процессы и логика

### 1. Процесс пополнения баланса
```
1. Пользователь выбирает сумму и метод оплаты
2. Система валидирует:
   - Минимальную и максимальную сумму
   - Доступность метода оплаты
3. Рассчитывается комиссия:
   - Комиссия = FixedFee + (Amount * FeePercentage / 100)
   - Итоговая сумма = Amount + Комиссия
4. Создается платежная сессия через PaymentService
5. После успешной оплаты:
   - Обновляется статус транзакции
   - Увеличивается баланс пользователя
```

### 2. Автоматическое создание баланса
```
1. При первом обращении к балансу
2. Используется UPSERT операция
3. Создается запись с нулевым балансом
4. Валюта по умолчанию: RSD
```

### 3. История транзакций
```
1. Пагинированный список транзакций
2. Сортировка по дате (новые первые)
3. Фильтрация по статусу (опционально)
4. Детали каждой транзакции
```

## 🔒 Безопасность и валидация

### Контроль доступа
- Все операции требуют JWT аутентификацию
- Пользователь может видеть только свой баланс
- Транзакции доступны только владельцу

### Валидация операций
- Проверка положительной суммы депозита
- Валидация методов оплаты
- Проверка минимальной/максимальной суммы
- Контроль дублирования транзакций

### Безопасность транзакций
- Использование транзакций БД для атомарности
- Блокировка записей при обновлении (FOR UPDATE)
- Логирование всех финансовых операций

## 🗃️ База данных и связи

### Основные таблицы
- `user_balances` - Балансы пользователей
- `balance_transactions` - История транзакций
- `payment_methods` - Методы оплаты

### Связи
- UserBalance → User (one-to-one)
- BalanceTransaction → User (many-to-one)
- BalanceTransaction → PaymentMethod (many-to-one)

### Индексы
- `idx_user_balances_user_id` - UNIQUE индекс по пользователю
- `idx_balance_transactions_user_id` - Для истории транзакций
- `idx_balance_transactions_created_at` - Для сортировки по дате
- `idx_payment_methods_code` - По коду метода оплаты

## 🔗 Внешние интеграции

### Payment Service
- Создание платежных сессий
- Обработка webhooks от платежных систем
- Подтверждение успешных платежей

### Notification Service
- Уведомления о пополнении баланса
- Алерты при проблемах с транзакциями

## 🏭 Фабричные методы

### Handler Factory
```go
func NewBalanceHandler(
    balanceService balance.BalanceServiceInterface,
    paymentService paymentService.PaymentServiceInterface
) *BalanceHandler
```

### Service Factory
```go
func NewBalanceService(storage storage.Storage) *BalanceService
```

## ⚠️ Особенности реализации

### 1. Автоматическое создание баланса
- Используется `INSERT ... ON CONFLICT DO NOTHING`
- Гарантирует наличие записи баланса
- Избегает race conditions

### 2. Расчет комиссий
- Комиссия включена в итоговую сумму транзакции
- Поддержка процентной и фиксированной комиссии
- Прозрачность для пользователя

### 3. Транзакционная безопасность
- Все финансовые операции в транзакциях БД
- Использование `FOR UPDATE` для блокировки
- Откат при любых ошибках

### 4. Интеграция с платежами
- Депозит создает платежную сессию
- Фактическое зачисление после подтверждения
- Поддержка различных платежных шлюзов

## 🚀 TODO и улучшения

### Краткосрочные (1-2 месяца)
- [ ] Добавить вывод средств (withdraw)
- [ ] История транзакций с фильтрами
- [ ] Экспорт истории в CSV/PDF
- [ ] Push-уведомления о транзакциях

### Среднесрочные (3-6 месяцев)  
- [ ] Мультивалютность балансов
- [ ] Автоматическая конвертация валют
- [ ] Система лимитов на операции
- [ ] Регулярные платежи (подписки)

### Долгосрочные (6+ месяцев)
- [ ] Интеграция с банковскими API
- [ ] Криптовалютные платежи
- [ ] P2P переводы между пользователями
- [ ] Система кэшбэка и бонусов

## 📊 Метрики и мониторинг

### Ключевые метрики
- Общий объем депозитов
- Средний размер пополнения
- Популярные методы оплаты
- Процент успешных транзакций
- Время обработки платежей

### Мониторинг
- Неуспешные транзакции
- Превышение лимитов
- Задержки в обработке
- Ошибки интеграций

### Алерты
- Множественные неудачные попытки
- Аномально большие транзакции
- Сбои платежных систем
- Расхождения в балансах

---

**Статус**: ✅ Активная разработка  
**Последнее обновление**: 2024-06-29  
**Версия паспорта**: 1.0