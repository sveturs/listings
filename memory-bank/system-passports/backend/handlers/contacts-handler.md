# 👥 Contacts Handler - Система Управления Контактами

## 📋 Обзор модуля

**Назначение**: Модуль управления контактами между пользователями, включая добавление контактов, управление статусами связей и настройки приватности.

**Местоположение**: `/backend/internal/proj/contacts/`

**Версия API**: v1

**Основные возможности**:
- Добавление и удаление контактов
- Управление статусами контактов (ожидание, принят, заблокирован)
- Настройки приватности пользователей
- Проверка связей между пользователями
- Защита от спама и rate limiting
- Интеграция с чатом для добавления контактов

## 🏗️ Архитектура и структура файлов

```
contacts/
├── handler/
│   ├── handler.go     # Основной handler с endpoints
│   ├── routes.go      # Регистрация маршрутов
│   └── responses.go   # Структуры ответов API
```

## 🛠️ API Endpoints

### 🔐 Защищенные маршруты (JWT + CSRF + Rate Limit)

Все маршруты требуют:
- JWT аутентификацию
- CSRF защиту
- Rate limiting (300 запросов в минуту на пользователя)

#### Управление контактами
- `GET /api/v1/contacts` - Получить список контактов
- `POST /api/v1/contacts` - Добавить контакт
- `PUT /api/v1/contacts/{contact_user_id}/status` - Обновить статус контакта
- `DELETE /api/v1/contacts/{contact_user_id}` - Удалить контакт
- `GET /api/v1/contacts/{contact_user_id}/check` - Проверить статус контакта

#### Настройки приватности
- `GET /api/v1/contacts/privacy` - Получить настройки приватности
- `PUT /api/v1/contacts/privacy` - Обновить настройки приватности

## 🗄️ Модели данных и структуры

### Основные модели

#### UserContact (Контакт пользователя)
```go
type UserContact struct {
    ID              int       `json:"id"`
    UserID          int       `json:"user_id"`          // Кто добавил
    ContactUserID   int       `json:"contact_user_id"`  // Кого добавили
    Status          string    `json:"status"`           // pending, accepted, blocked
    CreatedAt       time.Time `json:"created_at"`
    UpdatedAt       time.Time `json:"updated_at"`
    
    // Дополнительные поля
    AddedFromChatID *int   `json:"added_from_chat_id,omitempty"` // Из какого чата добавлен
    Notes           string `json:"notes,omitempty"`               // Заметки о контакте
    
    // Связанные объекты
    User        *User `json:"user,omitempty"`         // Данные пользователя
    ContactUser *User `json:"contact_user,omitempty"` // Данные контакта
}
```

#### UserPrivacySettings (Настройки приватности)
```go
type UserPrivacySettings struct {
    UserID                        int       `json:"user_id"`
    AllowContactRequests          bool      `json:"allow_contact_requests"`           // Разрешить запросы контактов
    AllowMessagesFromContactsOnly bool      `json:"allow_messages_from_contacts_only"` // Сообщения только от контактов
    CreatedAt                     time.Time `json:"created_at"`
    UpdatedAt                     time.Time `json:"updated_at"`
}
```

### Статусы контактов
```go
const (
    ContactStatusPending  = "pending"   // Ожидает подтверждения
    ContactStatusAccepted = "accepted"  // Принят
    ContactStatusBlocked  = "blocked"   // Заблокирован
)
```

### Request/Response структуры

#### AddContactRequest
```go
type AddContactRequest struct {
    ContactUserID   int    `json:"contact_user_id" validate:"required"`
    Notes           string `json:"notes,omitempty"`
    AddedFromChatID *int   `json:"added_from_chat_id,omitempty"`
}
```

#### UpdateContactRequest
```go
type UpdateContactRequest struct {
    Status string `json:"status" validate:"required,oneof=accepted blocked"`
    Notes  string `json:"notes,omitempty"`
}
```

#### ContactsListResponse
```go
type ContactsListResponse struct {
    Contacts []UserContact `json:"contacts"`
    Total    int           `json:"total"`
    Page     int           `json:"page"`
    Limit    int           `json:"limit"`
}
```

#### UpdatePrivacySettingsRequest
```go
type UpdatePrivacySettingsRequest struct {
    AllowContactRequests          *bool `json:"allow_contact_requests,omitempty"`
    AllowMessagesFromContactsOnly *bool `json:"allow_messages_from_contacts_only,omitempty"`
}
```

## 🔄 Бизнес-процессы и логика

### 1. Добавление контакта
```
1. Валидация запроса (ID контакта)
2. Проверка, что не добавляет себя
3. Проверка дубликатов
4. Проверка настроек приватности получателя
5. Проверка блокировок
6. Создание записи со статусом "pending"
7. Отправка уведомления получателю
```

### 2. Управление статусами
```
1. Проверка существования контакта
2. Валидация нового статуса
3. Обновление статуса:
   - accepted: контакт принят
   - blocked: контакт заблокирован
4. Обновление timestamp
5. Отправка уведомления инициатору
```

### 3. Система приватности
```
1. AllowContactRequests:
   - true: любой может отправить запрос
   - false: запросы отклоняются
2. AllowMessagesFromContactsOnly:
   - true: сообщения только от контактов
   - false: сообщения от всех пользователей
```

### 4. Проверка связи
```
1. Поиск записи контакта между пользователями
2. Проверка статуса (accepted)
3. Возврат булевого результата
```

## 🔒 Безопасность и валидация

### Контроль доступа
- JWT токен для всех операций
- CSRF защита от подделки запросов
- Rate limiting против спама
- Проверка владения записью

### Валидация данных
- Обязательный contact_user_id
- Валидация статусов (accepted/blocked)
- Проверка на самого себя
- Ограничение длины заметок

### Защита приватности
- Скрытие заблокированных пользователей
- Уважение настроек приватности
- Невозможность обойти блокировку

## 🗃️ База данных и связи

### Основные таблицы
- `user_contacts` - Связи между пользователями
- `user_privacy_settings` - Настройки приватности

### Связи
- UserContact → User (many-to-one) - кто добавил
- UserContact → User (many-to-one) - кого добавили
- UserContact → Chat (many-to-one) - опционально
- UserPrivacySettings → User (one-to-one)

### Индексы
- `idx_user_contacts_user_id` - По пользователю
- `idx_user_contacts_contact_user_id` - По контакту
- `idx_user_contacts_status` - По статусу
- `UNIQUE(user_id, contact_user_id)` - Уникальность пары

## 🔗 Внешние интеграции

### Notification Service
- Уведомления о новых запросах контактов
- Уведомления о принятии/отклонении
- Уведомления о блокировке

### Chat Service
- Добавление контактов из чата
- Проверка прав на отправку сообщений
- Синхронизация статусов

### User Service
- Получение данных пользователей
- Проверка активности аккаунтов

## 🏭 Фабричные методы

### Handler Factory
```go
func NewHandler(services globalService.ServicesInterface) *Handler
```

### Service Interface
```go
type ContactsServiceInterface interface {
    AddContact(ctx context.Context, userID int, req *models.AddContactRequest) (*models.UserContact, error)
    UpdateContactStatus(ctx context.Context, userID int, contactUserID int, req *models.UpdateContactRequest) error
    GetContacts(ctx context.Context, userID int, status string, page, limit int) (*models.ContactsListResponse, error)
    RemoveContact(ctx context.Context, userID, contactUserID int) error
    GetPrivacySettings(ctx context.Context, userID int) (*models.UserPrivacySettings, error)
    UpdatePrivacySettings(ctx context.Context, userID int, req *models.UpdatePrivacySettingsRequest) (*models.UserPrivacySettings, error)
    AreContacts(ctx context.Context, userID1, userID2 int) (bool, error)
}
```

## ⚠️ Особенности реализации

### 1. Двусторонние контакты
- Контакт создается односторонне
- Статус "accepted" делает связь взаимной
- Блокировка также односторонняя

### 2. Rate Limiting
- 300 запросов в минуту на пользователя
- Защита от массового добавления
- Предотвращение спама

### 3. CSRF защита
- Обязательная для всех мутирующих операций
- Защита от межсайтовых атак

### 4. Обработка ошибок
- Специфичные коды ошибок для UI
- Логирование без раскрытия деталей
- Дружественные сообщения пользователю

## 🚀 TODO и улучшения

### Краткосрочные (1-2 месяца)
- [ ] Групповое добавление контактов
- [ ] Импорт контактов из файла
- [ ] Экспорт списка контактов
- [ ] Поиск по контактам

### Среднесрочные (3-6 месяцев)
- [ ] Категории контактов (друзья, коллеги)
- [ ] Взаимные рекомендации контактов
- [ ] История взаимодействий
- [ ] Временная блокировка

### Долгосрочные (6+ месяцев)
- [ ] Интеграция с социальными сетями
- [ ] Граф связей пользователей
- [ ] ML-рекомендации контактов
- [ ] Верификация контактов

## 📊 Метрики и мониторинг

### Ключевые метрики
- Количество активных контактов
- Процент принятых запросов
- Среднее время ответа на запрос
- Количество блокировок
- Использование приватности

### Мониторинг
- Rate limit превышения
- Неудачные попытки добавления
- Массовые операции
- Подозрительная активность

### Алерты
- Аномальное количество запросов
- Массовые блокировки
- Попытки обхода ограничений
- Сбои в интеграциях

---

**Статус**: ✅ Активная разработка  
**Последнее обновление**: 2024-06-29  
**Версия паспорта**: 1.0