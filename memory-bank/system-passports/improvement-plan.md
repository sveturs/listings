# План устранения несоответствий и улучшения системы

## Резюме анализа

✅ **Отличная новость**: Критических проблем с несоответствием наименований НЕ ОБНАРУЖЕНО!

Система демонстрирует высокое качество архитектуры и консистентность naming conventions. Найденные несоответствия являются минорными и связаны с естественной эволюцией системы.

## План действий

### Приоритет 1: Критические (выполнить немедленно)

**Статус**: ✅ **Отсутствуют критические проблемы**

### Приоритет 2: Важные (выполнить в течение 1-2 недель)

#### 2.1 Cleanup deprecated table `user_storefronts`

**Проблема**: Существуют две таблицы для storefronts
- `user_storefronts` (deprecated)  
- `storefronts` (current)

**План**:
1. **Исследование** (1 час):
   ```sql
   -- Проверить содержимое старой таблицы
   SELECT COUNT(*) FROM user_storefronts;
   
   -- Найти возможные зависимости в коде
   grep -r "user_storefronts" backend/
   ```

2. **Создание миграции** (30 минут):
   ```sql
   -- Создать миграцию для удаления deprecated table
   DROP TABLE IF EXISTS user_storefronts;
   ```

3. **Тестирование** (1 час):
   - Запустить все тесты
   - Проверить работу Storefront API
   - Убедиться в отсутствии ошибок

**Ожидаемый результат**: Чистая схема БД, упрощение поддержки

#### 2.2 Автоматизация проверок качества

**План**:
1. **Pre-commit hooks** (2 часа):
   ```bash
   # .pre-commit-config.yaml
   - repo: local
     hooks:
       - id: go-struct-tags-check
         name: Check Go struct JSON tags for snake_case
         entry: scripts/check-json-tags.sh
         language: script
   ```

2. **Скрипт проверки** (1 час):
   ```bash
   #!/bin/bash
   # scripts/check-json-tags.sh
   # Проверяет, что все JSON теги используют snake_case
   ```

3. **CI pipeline check** (30 минут):
   ```yaml
   # .github/workflows/consistency-check.yml
   - name: Naming Consistency Check
     run: |
       go vet ./...
       yarn lint --max-warnings 0
       scripts/check-naming-consistency.sh
   ```

**Ожидаемый результат**: Автоматическое предотвращение regression'ов

### Приоритет 3: Улучшения (выполнить в течение месяца)

#### 3.1 Стандартизация типов ID полей

**Проблема**: Непоследовательность между INT и BIGINT для ID полей

**План**:
1. **Аудит существующих таблиц** (1 час):
   ```sql
   SELECT table_name, column_name, data_type 
   FROM information_schema.columns 
   WHERE column_name LIKE '%_id' OR column_name = 'id';
   ```

2. **План миграции для новых таблиц** (30 минут):
   - Использовать `BIGSERIAL` для всех новых ID полей
   - Обновить шаблоны миграций

3. **Обновление документации** (30 минут):
   - Добавить в migration guidelines
   - Обновить паспорта БД

**Ожидаемый результат**: Консистентность для будущих таблиц

#### 3.2 Унификация TIMESTAMP полей

**Проблема**: Старые таблицы используют `TIMESTAMP`, новые `TIMESTAMP WITH TIME ZONE`

**План**:
1. **Инвентаризация** (30 минут):
   ```sql
   SELECT table_name, column_name, data_type 
   FROM information_schema.columns 
   WHERE data_type LIKE '%timestamp%';
   ```

2. **Стандарт для новых таблиц** (15 минут):
   - Всегда использовать `TIMESTAMP WITH TIME ZONE`
   - Обновить migration templates

3. **Оценка impact'а migration'а существующих** (1 час):
   - Оценить объем данных
   - Определить downtime требования
   - Создать план при необходимости

**Ожидаемый результат**: Consistent timezone handling

### Приоритет 4: Мониторинг и поддержка (ongoing)

#### 4.1 Регулярные аудиты

**План**:
1. **Ежемесячные проверки** (1 час в месяц):
   - Запуск consistency check скриптов
   - Обновление паспортов при изменениях
   - Проверка новых API endpoints

2. **Quarterly reviews** (4 часа в квартал):
   - Полный аудит naming conventions
   - Обновление improvement plan'а
   - Анализ новых паттернов

**Ожидаемый результат**: Поддержание высокого качества

#### 4.2 Обновление паспортов

**План**:
- При добавлении новых полей → обновить соответствующий паспорт
- При изменении API → обновить матрицу соответствий
- При рефакторинге → проверить влияние на все слои

## Конкретные задачи для реализации

### Неделя 1
- [ ] **Исследовать user_storefronts table**: проверить использование
- [ ] **Создать миграцию**: для удаления deprecated table
- [ ] **Настроить pre-commit hooks**: для автоматических проверок

### Неделя 2  
- [ ] **Протестировать миграцию**: на dev окружении
- [ ] **Развернуть в production**: удаление user_storefronts
- [ ] **Обновить CI pipeline**: добавить consistency checks

### Месяц 1
- [ ] **Аудит ID типов**: собрать полную картину
- [ ] **Обновить migration templates**: стандарты для новых таблиц
- [ ] **Создать monitoring dashboards**: для tracking качества

### Ongoing
- [ ] **Ежемесячные проверки**: 1-го числа каждого месяца
- [ ] **Обновление паспортов**: при каждом изменении схемы
- [ ] **Code review focus**: проверка naming conventions

## Метрики успеха

### Количественные
- **0** критических несоответствий в naming (цель: поддерживать)
- **100%** coverage новых полей в паспортах  
- **< 1 день** время обновления документации при изменениях
- **0** regression'ов в naming conventions

### Качественные
- Разработчики знают где найти паспорта слоев
- Новые разработчики быстро понимают naming conventions
- Code review процесс включает проверку consistency
- Документация всегда актуальна

## ROI (Return on Investment)

### Инвестиции
- **Первоначальные**: ~12 часов (cleanup + automation)
- **Ongoing**: ~2 часа в месяц (maintenance)

### Выгоды
- **Снижение bugs**: Меньше ошибок из-за неправильных field names
- **Faster onboarding**: Новые разработчики быстрее понимают систему  
- **Better maintainability**: Предсказуемая структура кода
- **Reduced debugging time**: Четкое понимание data flow между слоями

### Вывод
Система уже находится в отличном состоянии. Минимальные инвестиции в cleanup и automation обеспечат поддержание высокого качества architecture в долгосрочной перспективе.

## Следующие шаги

1. **Немедленно**: Начать с cleanup'а user_storefronts table
2. **На этой неделе**: Настроить automated checks
3. **В течение месяца**: Завершить все улучшения из Priority 3
4. **Регулярно**: Следовать monitoring и maintenance плану

Система готова к масштабированию и дальнейшему развитию! 🚀