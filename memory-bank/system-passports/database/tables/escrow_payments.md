# ПАСПОРТ ТАБЛИЦЫ: escrow_payments

## 📋 Основная информация

**Название**: escrow_payments  
**Назначение**: Эскроу платежи для marketplace (удержание средств)  
**Тип**: Транзакционная таблица  
**Критичность**: Критическая (финансовые операции)

## 🏗️ Структура таблицы

### Поля

| Поле | Тип | Ограничения | Описание |
|------|-----|-------------|----------|
| `id` | BIGSERIAL | PRIMARY KEY | Уникальный идентификатор эскроу |
| `payment_transaction_id` | BIGINT | FK, NOT NULL | Ссылка на платежную транзакцию |
| `seller_id` | INT | FK, NOT NULL | ID продавца |
| `buyer_id` | INT | FK, NOT NULL | ID покупателя |
| `listing_id` | INT | FK, NOT NULL | ID объявления |
| `amount` | DECIMAL(12,2) | NOT NULL, > 0 | Общая сумма эскроу |
| `marketplace_commission` | DECIMAL(12,2) | NOT NULL | Комиссия маркетплейса |
| `seller_amount` | DECIMAL(12,2) | NOT NULL | Сумма к выплате продавцу |
| `status` | VARCHAR(50) | DEFAULT 'held' | Статус эскроу |
| `release_date` | TIMESTAMP WITH TIME ZONE | NULL | Дата автоматического освобождения |
| `created_at` | TIMESTAMP WITH TIME ZONE | DEFAULT NOW() | Дата создания |
| `updated_at` | TIMESTAMP WITH TIME ZONE | DEFAULT NOW() | Дата обновления |

### Ограничения

- **escrow_payments_amount_positive**: `amount > 0`
- **escrow_payments_status_valid**: `status IN ('held', 'released', 'refunded')`
- **escrow_payments_amounts_sum**: `marketplace_commission + seller_amount = amount`

### Индексы

- PRIMARY KEY на `id`
- `idx_escrow_payments_seller_id` на `seller_id`
- `idx_escrow_payments_buyer_id` на `buyer_id`
- `idx_escrow_payments_status` на `status`
- `idx_escrow_payments_payment_transaction_id` на `payment_transaction_id`

## 🔗 Связи с другими таблицами

### Входящие связи (foreign keys)

- **payment_transaction_id** → payment_transactions.id (ON DELETE CASCADE)
- **seller_id** → users.id
- **buyer_id** → users.id
- **listing_id** → marketplace_listings.id

### Каскадные действия

- При удалении payment_transaction - удаляется эскроу (CASCADE)

## 💼 Бизнес-логика

### Назначение
Реализует механизм эскроу для защиты покупателей и продавцов на маркетплейсе. Средства удерживаются до подтверждения успешной сделки.

### Статусы эскроу
- **held** - средства удерживаются (по умолчанию)
- **released** - средства переданы продавцу
- **refunded** - средства возвращены покупателю

### Бизнес-правила
1. **Автоматическое освобождение**: После истечения `release_date` средства автоматически передаются продавцу
2. **Целостность сумм**: `marketplace_commission + seller_amount = amount`
3. **Положительные суммы**: Все денежные поля должны быть > 0
4. **Уникальность**: Одна транзакция = один эскроу

### Типовой жизненный цикл
1. **Создание**: При успешном платеже создается эскроу со статусом 'held'
2. **Удержание**: Средства удерживаются 7 дней (настраивается в payment_gateways.config)
3. **Освобождение**: 
   - Автоматически по `release_date`
   - Вручную при подтверждении получения товара
4. **Возврат**: При спорах или отмене сделки

## 📊 Go модель

**Файл**: `/backend/internal/domain/models/payment_gateway.go:51-71`

```go
type EscrowPayment struct {
    ID                    int64           `json:"id" db:"id"`
    PaymentTransactionID  int64           `json:"payment_transaction_id" db:"payment_transaction_id"`
    SellerID              int             `json:"seller_id" db:"seller_id"`
    BuyerID               int             `json:"buyer_id" db:"buyer_id"`
    ListingID             int             `json:"listing_id" db:"listing_id"`
    Amount                decimal.Decimal `json:"amount" db:"amount"`
    MarketplaceCommission decimal.Decimal `json:"marketplace_commission" db:"marketplace_commission"`
    SellerAmount          decimal.Decimal `json:"seller_amount" db:"seller_amount"`
    Status                string          `json:"status" db:"status"`
    ReleaseDate           *time.Time      `json:"release_date,omitempty" db:"release_date"`
    CreatedAt             time.Time       `json:"created_at" db:"created_at"`
    UpdatedAt             time.Time       `json:"updated_at" db:"updated_at"`

    // Relations
    PaymentTransaction *PaymentTransaction `json:"payment_transaction,omitempty"`
    Seller             *User               `json:"seller,omitempty"`
    Buyer              *User               `json:"buyer,omitempty"`
    Listing            *MarketplaceListing `json:"listing,omitempty"`
}
```

### Константы статусов
```go
const (
    EscrowStatusHeld     = "held"
    EscrowStatusReleased = "released"
    EscrowStatusRefunded = "refunded"
)
```

## 🔧 API интеграция

### Endpoints использующие таблицу
- **POST** `/payments/process` - создание эскроу при платеже
- **POST** `/escrow/{id}/release` - ручное освобождение средств
- **POST** `/escrow/{id}/refund` - возврат средств
- **GET** `/escrow/seller/{id}` - эскроу продавца
- **GET** `/escrow/buyer/{id}` - эскроу покупателя

## 📈 Характеристики производительности

### Объем данных
- **Ожидаемый размер**: Средний (все платежи через маркетплейс)
- **Тип нагрузки**: Запись при платежах, чтение для статусов
- **Рост данных**: Пропорционально количеству транзакций

### Запросы и производительность
- **Частые запросы**: Поиск по seller_id, buyer_id, status
- **Критичные операции**: Создание, обновление статуса
- **Индексы покрывают**: Основные фильтры и joins

## 🛡️ Безопасность и валидация

### Критичные данные
- **Финансовые суммы** - требуют точности и аудита
- **Статусы** - критичны для бизнес-логики

### Проверки целостности
1. **Положительные суммы**: CHECK constraints на amount
2. **Валидные статусы**: CHECK constraint на status
3. **Математическая целостность**: сумма комиссии + сумма продавца = общая сумма
4. **Каскадное удаление**: привязка к payment_transactions

### Аудит и логирование
- Все изменения статуса должны логироваться
- Финансовые операции требуют audit trail

## 🚀 Миграции

**Создана в миграции**: `000061_create_allsecure_payment_tables.up.sql:60-86`

### Особенности создания
- Каскадное удаление при удалении payment_transaction
- Строгие CHECK constraints для валидации данных
- Индексы для производительности

## 📝 Примечания и рекомендации

### Лучшие практики
1. **Атомарность операций**: Изменение статуса только в транзакциях
2. **Валидация сумм**: Проверка математической целостности
3. **Автоматизация**: Настройка cron для автоматического освобождения
4. **Уведомления**: Информирование сторон о статусах эскроу

### Потенциальные улучшения
- Добавить поле `dispute_reason` для спорных ситуаций
- Реализовать промежуточные статусы ('disputed', 'pending_release')
- Добавить audit_trail таблицу для истории изменений
- Интеграция с системой уведомлений

### Связанные компоненты
- **payment_transactions** - основа для создания эскроу
- **merchant_payouts** - финальная выплата после release
- **users** - участники сделки
- **marketplace_listings** - предмет сделки
- **Cron jobs** - автоматическое освобождение средств

### Мониторинг и метрики
- Среднее время удержания средств
- Процент автоматических vs ручных освобождений
- Количество возвратов и их причины
- Объем заблокированных средств

---
*Дата создания паспорта: 2025-06-29*  
*Версия системы: v1.0*