# Паспорт таблицы: price_history

## 🎯 Назначение
Система учёта истории изменения цен объявлений. Ведёт полную историю цен, обнаруживает манипуляции, вычисляет реальные скидки и поддерживает метаданные скидок в marketplace_listings.

## 📊 Структура таблицы

### Колонки
```sql
id                  SERIAL PRIMARY KEY          -- Уникальный ID записи
listing_id          INT NOT NULL                -- ID объявления (FK -> marketplace_listings.id)
price               DECIMAL(12,2) NOT NULL      -- Цена на данный период
effective_from      TIMESTAMP NOT NULL          -- Начало действия цены (по умолчанию CURRENT_TIMESTAMP)
effective_to        TIMESTAMP                   -- Конец действия цены (NULL = текущая)
change_source       VARCHAR(50) NOT NULL        -- Источник изменения: manual, import, system, create, update
change_percentage   DECIMAL(10,2)               -- Процент изменения от предыдущей цены
created_at          TIMESTAMP NOT NULL          -- Время создания записи (CURRENT_TIMESTAMP)
```

### Ограничения
- **FOREIGN KEY**: `listing_id` → `marketplace_listings(id)` CASCADE DELETE
- **NOT NULL**: `price`, `effective_from`, `change_source`, `created_at`

### Индексы
```sql
idx_price_history_listing_id    -- (listing_id) - быстрый поиск истории по объявлению
idx_price_history_effective     -- (listing_id, effective_to) - поиск активных цен
```

## 🔄 Связи с другими таблицами

### Входящие связи
- `marketplace_listings.id` ← `price_history.listing_id` (много к одному)

### Исходящие связи
- Автоматическое обновление `marketplace_listings.metadata` через триггеры

## 📈 Бизнес-логика

### Основные функции

1. **Автоматическое ведение истории**
   - При создании объявления: создаётся первая запись с `change_source = 'create'`
   - При изменении цены: закрывается старая запись (effective_to), создаётся новая

2. **Обнаружение манипуляций с ценами**
   - Паттерн: резкое повышение >30% с быстрым снижением <7 дней
   - Период реабилитации: 30 дней
   - Сохранение информации в `marketplace_listings.metadata.manipulation_detected_at`

3. **Автоматическое управление скидками**
   - Вычисление реальных скидок от максимальной цены
   - Минимальный порог скидки: 5%
   - Обновление метаданных в marketplace_listings.metadata.discount

4. **Защита от накрутки скидок**
   - Учёт только цен, действовавших минимум 3 дня
   - Исключение объявлений с обнаруженными манипуляциями

### Алгоритм работы триггеров

```sql
-- При создании/изменении объявления
TRIGGER trg_new_listing_price_history -> update_price_history('create')
TRIGGER trg_update_listing_price_history -> update_price_history('update')

-- При добавлении записи в price_history  
TRIGGER trig_update_metadata_after_price_change -> update_listing_metadata_after_price_change()
```

### Функции системы

1. **`check_price_manipulation(listing_id)`**
   - Возвращает: `BOOLEAN`
   - Алгоритм обнаружения манипуляций
   - Сохраняет результат в метаданные

2. **`update_price_history()`** 
   - Триггерная функция
   - Закрывает старые записи, создаёт новые
   - Вычисляет процент изменения

3. **`update_listing_metadata_after_price_change()`**
   - Триггерная функция  
   - Обновляет метаданные скидок
   - Проверяет манипуляции

4. **`refresh_discount_metadata()`**
   - Ручная функция массового обновления
   - Пересчитывает скидки для всех объявлений

## 💾 Реализация в коде

### Backend файлы
- `backend/internal/domain/models/price_history.go` - модели данных
- `backend/internal/storage/postgres/price_history.go` - репозиторий
- `backend/internal/proj/marketplace/service/price_history.go` - бизнес-логика

### Основные модели
```go
type PriceHistoryEntry struct {
    ID               int        `json:"id"`
    ListingID        int        `json:"listing_id"`
    Price            float64    `json:"price"`
    EffectiveFrom    time.Time  `json:"effective_from"`
    EffectiveTo      *time.Time `json:"effective_to,omitempty"`
    ChangeSource     string     `json:"change_source"`
    ChangePercentage float64    `json:"change_percentage,omitempty"`
    CreatedAt        time.Time  `json:"created_at"`
}

type DiscountData struct {
    DiscountPercent int       `json:"discount_percent"`
    PreviousPrice   float64   `json:"previous_price"`
    EffectiveFrom   time.Time `json:"effective_from"`
    HasPriceHistory bool      `json:"has_price_history"`
}
```

### Методы репозитория
```go
// Получение истории цен
func (db *Database) GetPriceHistory(ctx context.Context, listingID int) ([]models.PriceHistoryEntry, error)

// Добавление записи
func (db *Database) AddPriceHistoryEntry(ctx context.Context, entry *models.PriceHistoryEntry) error

// Закрытие активной записи
func (db *Database) ClosePriceHistoryEntry(ctx context.Context, listingID int) error

// Проверка манипуляций
func (db *Database) CheckPriceManipulation(ctx context.Context, listingID int) (bool, error)
```

## 🔍 Особенности работы

### Источники изменений (change_source)
- `'create'` - создание объявления
- `'update'` - ручное изменение пользователем
- `'import'` - импорт из внешних источников
- `'system'` - системные изменения

### Алгоритм обнаружения манипуляций
1. Поиск паттерна в последние 30 дней:
   - Повышение цены >30% 
   - Длительность высокой цены <7 дней
   - Быстрое снижение >10%
   - Финальная цена не слишком низкая (-10% от начальной)

2. При обнаружении:
   - Установка `metadata.manipulation_detected_at`
   - Удаление информации о скидках
   - Блокировка скидок на 30 дней

### Вычисление скидок
1. Поиск максимальной цены (действовавшей ≥3 дня)
2. Если текущая цена <максимальной и разница ≥5%
3. Создание объекта скидки в метаданных:
```json
{
  "discount": {
    "discount_percent": 25,
    "previous_price": 1000.00,
    "effective_from": "2024-01-01T00:00:00Z",
    "has_price_history": true
  }
}
```

## 📋 Примеры запросов

### Получение истории цен
```sql
SELECT id, listing_id, price, effective_from, effective_to, 
       change_source, COALESCE(change_percentage, 0), created_at
FROM price_history
WHERE listing_id = $1
ORDER BY effective_from DESC;
```

### Получение текущей цены
```sql
SELECT price, effective_from
FROM price_history
WHERE listing_id = $1 AND effective_to IS NULL
ORDER BY effective_from DESC
LIMIT 1;
```

### Проверка манипуляций
```sql
SELECT check_price_manipulation($1);
```

### Ручное обновление скидок
```sql
SELECT refresh_discount_metadata();
```

## 🎨 Диаграмма связей

```
marketplace_listings(id) -----> price_history(listing_id) [1:M]
                    |
                    ↓ (автоматическое обновление через триггеры)
marketplace_listings(metadata.discount) ← price_history (расчёт скидок)
```

## 📝 История изменений
- **000013_price_history.up.sql** - создание таблицы, индексов, функций и триггеров

## 🔗 Связанные компоненты
- [marketplace_listings.md](marketplace_listings.md) - основная таблица объявлений
- OpenSearch mappings - синхронизация has_price_history флага
- Frontend компоненты - отображение скидок и графиков цен

## ⚠️ Важные особенности

### Автоматизация
- Вся система работает на триггерах, ручное вмешательство не требуется
- При изменении цены автоматически пересчитываются скидки

### Производительность
- Индексы оптимизированы для частых запросов истории
- Минимальное воздействие на производительность операций с объявлениями

### Безопасность
- Защита от манипуляций с ценами
- Валидация корректности процентов скидок
- Логирование всех изменений с источником