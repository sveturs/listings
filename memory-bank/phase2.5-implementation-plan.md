# 📋 Детальный план реализации Фазы 2.5 - Система управления поиском

## 📍 Статус: ГОТОВ К РЕАЛИЗАЦИИ
**Приоритет**: КРИТИЧЕСКИЙ (блокирует Фазу 3)  
**Срок реализации**: 10 дней  
**Дата начала**: 2025-01-08  
**Дата завершения**: 2025-01-17  
**Ветка**: `feature/phase2.5-search-management-system`

## 🎯 Цели фазы

1. **Полная прозрачность** - видимость всех аспектов работы поиска
2. **Тотальный контроль** - возможность настройки любых параметров
3. **Data-driven решения** - все изменения на основе метрик
4. **Безопасность изменений** - история и быстрый откат
5. **Масштабируемость** - готовность к росту нагрузки

## ⚠️ КРИТИЧЕСКИЕ ЗАДАЧИ (День 0)

### 1. Транслитерация сербского языка
```go
// backend/pkg/utils/slug.go
var serbianCyrillicToLatin = map[string]string{
    "а": "a", "б": "b", "в": "v", "г": "g", "д": "d",
    "ђ": "đ", "е": "e", "ж": "ž", "з": "z", "и": "i",
    "ј": "j", "к": "k", "л": "l", "љ": "lj", "м": "m",
    "н": "n", "њ": "nj", "о": "o", "п": "p", "р": "r",
    "с": "s", "т": "t", "ћ": "ć", "у": "u", "ф": "f",
    "х": "h", "ц": "c", "ч": "č", "џ": "dž", "ш": "š",
    // Заглавные буквы
    "А": "A", "Б": "B", "В": "V", "Г": "G", "Д": "D",
    "Ђ": "Đ", "Е": "E", "Ж": "Ž", "З": "Z", "И": "I",
    "Ј": "J", "К": "K", "Л": "L", "Љ": "Lj", "М": "M",
    "Н": "N", "Њ": "Nj", "О": "O", "П": "P", "Р": "R",
    "С": "S", "Т": "T", "Ћ": "Ć", "У": "U", "Ф": "F",
    "Х": "H", "Ц": "C", "Ч": "Č", "Џ": "Dž", "Ш": "Š",
}

// Добавить в функцию NormalizeSearchQuery
func NormalizeSearchQuery(query string) string {
    // ... существующий код ...
    
    // Транслитерация сербской кириллицы
    for cyrillic, latin := range serbianCyrillicToLatin {
        query = strings.ReplaceAll(query, cyrillic, latin)
    }
    
    return query
}
```

### 2. Вынести веса в конфигурацию
```go
// backend/internal/domain/search_config.go
type CategorySearchConfig struct {
    CategoryID      int                    `json:"category_id"`
    AttributeWeights map[string]float32    `json:"attribute_weights"`
    TextWeight      float32                `json:"text_weight"`
    PriceWeight     float32                `json:"price_weight"`
    LocationWeight  float32                `json:"location_weight"`
}

// backend/internal/storage/postgres/search_config.go
func (r *SearchConfigRepository) GetCategoryWeights(categoryID int) (*CategorySearchConfig, error) {
    // Загрузка из таблицы category_search_weights
    // С fallback на дефолтные значения
}
```

### 3. Базовые синонимы для сербского
```sql
-- migrations/000XXX_serbian_base_synonyms.up.sql
INSERT INTO search_synonyms (term, synonym, language, weight) VALUES
-- Города
('Београд', 'Beograd', 'sr', 1.0),
('Нови Сад', 'Novi Sad', 'sr', 1.0),
('Крагујевац', 'Kragujevac', 'sr', 1.0),
('Ниш', 'Niš', 'sr', 1.0),
-- Недвижимость
('стан', 'stan', 'sr', 1.0),
('кућа', 'kuća', 'sr', 1.0),
('гарсоњера', 'garsonjera', 'sr', 1.0),
-- Транспорт
('аутомобил', 'automobil', 'sr', 1.0),
('ауто', 'auto', 'sr', 1.0),
('кола', 'kola', 'sr', 1.0),
-- Электроника
('телефон', 'telefon', 'sr', 1.0),
('рачунар', 'računar', 'sr', 1.0),
('компјутер', 'kompjuter', 'sr', 1.0);
```

## 📅 ПЛАН ПО ДНЯМ

### День 1-2: База данных и инфраструктура

#### Задачи:
1. **Создание миграций** (4ч)
   ```sql
   -- 000XXX_create_search_management_tables.up.sql
   -- Все таблицы из спецификации
   ```

2. **Настройка логирования** (4ч)
   - Middleware для перехвата всех поисковых запросов
   - Асинхронная запись в БД через канал
   - Ротация старых логов

3. **Базовая конфигурация** (4ч)
   ```env
   SEARCH_ADMIN_ENABLED=true
   SEARCH_LOGGING_ENABLED=true
   SEARCH_LOGGING_BUFFER_SIZE=1000
   SEARCH_ANALYTICS_RETENTION_DAYS=90
   ```

4. **Индексы и оптимизация** (4ч)
   - Партиционирование search_logs по месяцам
   - Индексы для быстрой аналитики
   - Материализованные представления для метрик

### День 3-4: Backend API

#### Структура модуля:
```
backend/internal/proj/search_admin/
├── handlers/
│   ├── dashboard.go      # Метрики и статистика
│   ├── analytics.go      # Аналитика запросов
│   ├── config.go         # Управление конфигурацией
│   ├── synonyms.go       # CRUD синонимов
│   ├── boost_rules.go    # Правила boost
│   ├── playground.go     # Тестирование
│   └── maintenance.go    # Обслуживание
├── services/
│   ├── logger.go         # Сервис логирования
│   ├── analyzer.go       # Анализ запросов
│   ├── scorer.go         # Объяснение scoring
│   └── experimenter.go   # A/B тестирование
├── repositories/
│   └── search_admin.go   # Работа с БД
└── middleware/
    └── admin_auth.go     # Проверка прав админа
```

#### Приоритетные endpoints:
```go
// День 3 - Базовые операции
POST   /api/admin/search/logs          # Запись логов
GET    /api/admin/search/dashboard/stats
GET    /api/admin/search/analytics/queries
GET    /api/admin/search/config/weights
PUT    /api/admin/search/config/weights/:categoryId

// День 4 - Расширенный функционал
GET    /api/admin/search/synonyms
POST   /api/admin/search/synonyms
POST   /api/admin/search/test
POST   /api/admin/search/explain
GET    /api/admin/search/performance/metrics
```

### День 5-6: Frontend - Dashboard и Analytics

#### Структура компонентов:
```
frontend/svetu/src/app/[locale]/admin/search/
├── layout.tsx                    # Общий layout админки
├── page.tsx                      # Dashboard
├── analytics/
│   ├── page.tsx                  # Страница аналитики
│   ├── components/
│   │   ├── QueryTable.tsx        # Таблица запросов
│   │   ├── ZeroResultsList.tsx   # Запросы без результатов
│   │   └── SearchTrends.tsx      # Графики трендов
├── config/
│   ├── page.tsx                  # Управление конфигурацией
│   ├── components/
│   │   ├── WeightsEditor.tsx     # Редактор весов
│   │   ├── SynonymsManager.tsx   # Управление синонимами
│   │   └── CategorySelector.tsx  # Выбор категории
└── shared/
    ├── MetricCard.tsx            # Карточка метрики
    ├── DateRangePicker.tsx       # Выбор периода
    └── AdminBreadcrumbs.tsx      # Хлебные крошки
```

#### UI компоненты (DaisyUI):
```tsx
// MetricCard.tsx
<div className="card bg-base-100 shadow-xl">
  <div className="card-body">
    <h2 className="card-title text-sm">{title}</h2>
    <div className="stat">
      <div className="stat-value">{value}</div>
      <div className="stat-desc">{trend}</div>
    </div>
  </div>
</div>

// WeightsEditor.tsx
<div className="form-control">
  <label className="label">
    <span className="label-text">{attribute}</span>
    <span className="label-text-alt">{currentWeight}%</span>
  </label>
  <input 
    type="range" 
    className="range range-primary" 
    min="0" 
    max="100"
  />
</div>
```

### День 7-8: Frontend - Configuration и Playground

#### Приоритетные страницы:

1. **Управление весами** (День 7)
   - Выбор категории
   - Слайдеры для каждого атрибута
   - Предпросмотр изменений
   - Кнопка "Применить"

2. **Управление синонимами** (День 7)
   - Таблица с фильтрацией
   - Добавление/редактирование
   - Импорт из CSV
   - Группировка по языкам

3. **Search Playground** (День 8)
   - Поле для тестового запроса
   - Выбор конфигурации
   - Результаты с explain
   - Визуализация scoring

### День 9: Интеграция и оптимизация

#### Задачи:
1. **Подключение логирования** (3ч)
   - Во все search endpoints
   - В suggestions API
   - В similar listings

2. **Оптимизация производительности** (3ч)
   - Кеширование конфигураций
   - Batch запись логов
   - Lazy loading в UI

3. **Feature flags** (2ч)
   ```go
   if config.GetBool("SEARCH_ADMIN_ENABLED") {
       // Регистрация роутов админки
   }
   ```

4. **Тестирование интеграции** (4ч)
   - E2E тесты основных сценариев
   - Нагрузочное тестирование логирования
   - Проверка прав доступа

### День 10: Документация и deploy

#### Документация:
1. **API документация** (2ч)
   - Swagger аннотации
   - Примеры запросов
   - Описание параметров

2. **Руководство администратора** (2ч)
   - Как настроить веса
   - Как добавить синонимы
   - Как читать метрики

3. **Техническая документация** (2ч)
   - Архитектура решения
   - Схема БД
   - Процесс деплоя

#### Pre-deployment checklist:
- [ ] Все тесты проходят
- [ ] Линтеры не показывают ошибок
- [ ] Документация обновлена
- [ ] Миграции протестированы
- [ ] Performance метрики в норме

## 📊 Метрики успеха

### Технические метрики:
- Время ответа админки < 200ms
- Логирование не влияет на производительность поиска (< 5ms overhead)
- Dashboard обновляется в реальном времени
- История изменений сохраняется полностью

### Бизнес метрики:
- 100% поисковых запросов логируется
- Время на изменение конфигурации < 1 минуты
- Возможность отката изменений < 30 секунд
- Полная видимость проблемных запросов

## 🚀 MVP vs Full версия

### MVP (обязательно к 17.01):
- ✅ Логирование всех запросов
- ✅ Dashboard с основными метриками
- ✅ Управление весами категорий
- ✅ Базовое управление синонимами
- ✅ Search playground для тестирования

### Full версия (можно после):
- ⏳ A/B тестирование
- ⏳ Boost правила
- ⏳ Расширенная аналитика
- ⏳ Автоматические рекомендации
- ⏳ Экспорт/импорт конфигураций

## 🔧 Технический стек

### Backend:
- Go 1.21+
- PostgreSQL 13+ (партиционирование, JSONB)
- Fiber v2 (web framework)
- OpenSearch 2.x (существующий)

### Frontend:
- React 19 + Next.js 15
- TypeScript 5.x
- TailwindCSS v4 + DaisyUI 4.x
- Redux Toolkit (state management)
- Recharts (графики)

## 📝 Риски и митигация

### Риск 1: Объем логов
**Проблема**: 10k запросов/день = 300k записей/месяц  
**Решение**: 
- Партиционирование по месяцам
- Автоматическая архивация старых данных
- Агрегированные метрики в матвью

### Риск 2: Производительность логирования
**Проблема**: Запись в БД может замедлить поиск  
**Решение**:
- Асинхронная запись через каналы
- Буферизация с batch insert
- Graceful degradation при перегрузке

### Риск 3: Сложность UI
**Проблема**: Много функций могут запутать админа  
**Решение**:
- Фокус на MVP функциях
- Прогрессивное раскрытие
- Подсказки и документация в UI

## ✅ Definition of Done

Фаза считается завершенной когда:
1. [ ] Логируется 100% поисковых запросов
2. [ ] Dashboard показывает метрики в реальном времени
3. [ ] Веса категорий можно изменить через UI
4. [ ] Синонимы можно добавить/удалить через UI
5. [ ] Search playground позволяет тестировать запросы
6. [ ] История всех изменений сохраняется
7. [ ] Документация написана и актуальна
8. [ ] Все тесты проходят успешно
9. [ ] Pre-check пройден (format + lint + build)
10. [ ] PR создан и готов к review

## 🎯 Следующие шаги

После завершения Фазы 2.5:
1. Собрать данные за 1-2 недели
2. Проанализировать паттерны поиска
3. Начать Фазу 3 с реальными метриками
4. Внедрить поведенческую оптимизацию на основе данных

---

**Готовы начать?** Создаем ветку и приступаем к реализации! 🚀