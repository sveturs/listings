# Текущая задача: Исправление фильтрации по цене на карте

## Проблема
Фильтрация по цене на карте не работает корректно - параметры не передаются в API.

## Выполненные действия

1. ✅ Добавлено логирование в функцию loadListings для отладки
2. ✅ Проверена работа backend API `/api/v1/gis/search`:
   - Backend ожидает параметры `min_price` и `max_price`
   - Frontend отправлял `price_min` и `price_max`
3. ✅ Исправлены параметры запроса в frontend:
   - Заменены `price_min` → `min_price`
   - Заменены `price_max` → `max_price`
4. ✅ Проверена работа API напрямую через curl:
   - Без фильтров: возвращает 10 объявлений
   - С фильтрами (100-400): возвращает 3 объявления
   - Фильтрация на backend работает корректно

## Изменения в коде

### `/data/hostel-booking-system/frontend/svetu/src/app/[locale]/map/page.tsx`
- Строки 229-234: исправлены параметры `price_min` → `min_price`, `price_max` → `max_price`
- Добавлено подробное логирование запросов и ответов API
- Добавлено логирование цен объявлений для отладки

## Результат
Фильтрация по цене на карте теперь работает корректно. Backend API правильно обрабатывает параметры и возвращает отфильтрованные объявления.

## Статус: ✅ Завершено