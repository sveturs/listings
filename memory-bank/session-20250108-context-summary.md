# Контекст сессии: Завершение Фазы 3 - Поведенческий трекинг и восстановление админ-панели

## Дата: 2025-01-08
## Память контекста: 10% (критично)

## Статус: ЧАСТИЧНО ЗАВЕРШЕНО
Фаза 3 поведенческого трекинга реализована и большинство критических проблем исправлено, но не все вкладки админ-панели содержат данные.

## Архитектура системы
- **Frontend**: React/Next.js 15.3.2, порт 3001
- **Backend**: Go/Fiber, порт 3000  
- **БД**: PostgreSQL с 13 таблицами для аналитики
- **OpenSearch**: Поисковый движок на порту 9200

## Статус компонентов админ-панели (/ru/admin/search)

### ✅ РАБОТАЕТ (данные есть):
1. **Аналитика поиска** - основные метрики работают:
   - Общие поиски: 96
   - Уникальные запросы: 28  
   - Среднее время ответа: 16ms
   - Топ запросы в таблице отображаются
   - API: `/api/v1/analytics/metrics/search` работает

### ❌ НЕТ ДАННЫХ (нужно исправить):
2. **Поведенческая аналитика** - показывает все нули:
   - Всего кликов: 0
   - Средний CTR: 0.0%
   - Средняя позиция клика: 0.0
   - Коэффициент конверсии: 0.0%
   - Проблема: не трекаются клики пользователей

3. **Веса поиска** - недоступны из-за авторизации:
   - Ошибка 401 при загрузке весов
   - API: `/api/v1/admin/search/weights` требует токен

4. **Синонимы** - новый компонент, статус неизвестен:
   - Создан SynonymManager компонент
   - API: `/api/v1/admin/search/synonyms` зарегистрирован

5. **Оптимизация** - ML компонент, статус неизвестен:
   - WeightOptimization компонент существует
   - Связь с backend неясна

## Критические исправления выполнены:

### 1. Webpack ошибки (✅)
- Очищен кэш .next и webpack
- Перезапущен frontend сервер
- Админ-панель загружается без критических ошибок

### 2. Backend API (✅)
- Создано 13 таблиц БД через миграции 083-091
- Исправлена структура данных GetSearchMetrics
- Возвращается правильный формат: `{total_searches, unique_searches, average_search_duration_ms, top_queries, search_trends, click_metrics}`

### 3. Behavior tracking (✅)
- Исправлена валидация EventType
- Добавлена поддержка batch событий
- Исправлен panic в trackSearchEvent
- Убрана авторизация для `/api/v1/analytics/track`

### 4. Search optimization модуль (✅)
- Интегрирован в server.go как RouteRegistrar
- Зарегистрирован префикс `/api/v1/admin/search`
- Endpoints для синонимов и оптимизации доступны

## Файловая структура (ключевые файлы):

### Backend:
- `/backend/internal/proj/behavior_tracking/` - трекинг событий
- `/backend/internal/proj/search_optimization/` - управление синонимами и весами
- `/backend/internal/proj/global/handler/unified_search.go` - основной поиск
- `/backend/migrations/000083-000091*.sql` - миграции БД

### Frontend:
- `/frontend/svetu/src/app/[locale]/admin/search/components/`:
  - `SearchAnalytics.tsx` - ✅ работает
  - `BehaviorAnalytics.tsx` - ❌ нет данных
  - `SearchWeights.tsx` - ❌ 401 ошибка  
  - `SynonymManager.tsx` - ❓ нужна проверка
  - `WeightOptimization.tsx` - ❓ нужна проверка
- `/frontend/svetu/src/services/searchAnalytics.ts` - API клиент
- `/frontend/svetu/src/hooks/useBehaviorTracking.ts` - трекинг хук

## Актуализированный план для новой сессии:

### 🔴 ПРИОРИТЕТ 1 (критично):
1. **Исправить авторизацию для весов поиска**:
   - Проблема: `/api/v1/admin/search/weights` возвращает 401
   - Решение: Проверить middleware авторизации или добавить исключение

2. **Добавить трекинг кликов**:
   - Проблема: CTR всегда 0%, клики не записываются
   - Решение: Добавить отправку событий `result_clicked` при клике на результаты поиска

### 🟡 ПРИОРИТЕТ 2 (важно):
3. **Проверить SynonymManager**:
   - Статус: Компонент создан, но не протестирован
   - Задача: Открыть вкладку "Синонимы" и проверить CRUD операции

4. **Проверить WeightOptimization**:
   - Статус: Компонент создан, но не протестирован  
   - Задача: Открыть вкладку "Оптимизация" и проверить ML функции

### 🟢 ПРИОРИТЕТ 3 (улучшения):
5. **Добавить агрегацию данных**:
   - Запускать скрипт `/backend/cmd/utils/aggregate_search_metrics/` регулярно
   - Настроить cron для актуализации метрик

6. **Расширить поведенческую аналитику**:
   - Добавить трекинг конверсий (покупок после поиска)
   - Трекать нулевые результаты поиска

## Команды для диагностики:

### Проверка API:
```bash
# Проверить метрики поиска
curl -s "http://localhost:3000/api/v1/analytics/metrics/search?period=week" | jq '.data'

# Проверить веса поиска (с токеном)
curl -s -H "Authorization: Bearer TOKEN" "http://localhost:3000/api/v1/admin/search/weights" | jq '.'

# Проверить синонимы
curl -s "http://localhost:3000/api/v1/admin/search/synonyms" | jq '.'
```

### Проверка БД:
```sql
-- Проверить данные поиска
SELECT COUNT(*) FROM search_logs;
SELECT COUNT(*) FROM search_behavior_metrics;
SELECT COUNT(*) FROM user_behavior_events;

-- Проверить веса поиска
SELECT * FROM search_weights LIMIT 5;
```

## Методы и подходы:

### 1. Использование помощников:
- `claude -p --dangerously-skip-permissions` для экономии контекста
- Короткие задания для избежания переполнения контекста

### 2. Готовые скрипты:
- `/home/dim/.local/bin/kill-port-3000.sh` - остановка backend
- `/home/dim/.local/bin/kill-port-3001.sh` - остановка frontend  
- `/home/dim/.local/bin/start-frontend-screen.sh` - запуск frontend

### 3. Тестирование:
- Playwright через `claude -p --dangerously-skip-permissions`
- Проверка всех 5 вкладок админ-панели
- Создание скриншотов работающих компонентов

## Текущие проблемы:
1. **Авторизация весов** - 401 ошибка при доступе к весам поиска
2. **Отсутствие кликов** - поведенческая аналитика показывает нули
3. **Неполное тестирование** - синонимы и оптимизация не протестированы

## Цель следующей сессии:
Завершить Фазу 3 на 100% - все 5 вкладок админ-панели должны работать с реальными данными и без ошибок.