# 🎯 Обновленный план улучшения поисковой системы

## ✅ Фаза 1: Базовые улучшения (ЗАВЕРШЕНА - 100%)

**Статус**: Полностью реализована и развернута  
**Pull Request**: [#62](https://github.com/sveturs/svetu/pull/62)

### Достигнутые результаты:
- ✅ Система весов атрибутов для категорий
- ✅ Многоуровневый алгоритм скоринга
- ✅ Поддержка storefront products
- ✅ Enhanced suggestions API
- ✅ Сохранение популярных запросов
- ✅ **НОВОЕ**: Исправлен алгоритм похожих объявлений (2025-01-07)
  - Убрана неправильная привязка к витринам
  - Реализован межкатегорийный поиск
  - Адаптивные веса для разных категорий

---

## ✅ Фаза 2: Интеллектуальный текстовый поиск (ЗАВЕРШЕНА - 100%)

**Статус**: Полностью реализована и протестирована  
**Дата завершения**: 2025-01-06  
**Бюджет**: $0 (используем существующую инфраструктуру)

### Достигнутые результаты:
- ✅ Нечеткий поиск с pg_trgm и unaccent
- ✅ Система синонимов для 3 языков (ru, sr, en)
- ✅ Индексы для быстрого поиска
- ✅ UI компонент для управления fuzzy search
- ✅ Интеграция с OpenSearch
- ✅ Исправлены ссылки для storefront products

### 📋 Реализованные задачи:

#### ✅ 2.1 Нечеткий поиск в PostgreSQL
```sql
-- Активация расширения для триграмм
CREATE EXTENSION IF NOT EXISTS pg_trgm;
CREATE EXTENSION IF NOT EXISTS unaccent;

-- Индексы для быстрого нечеткого поиска
CREATE INDEX idx_listings_title_trgm ON marketplace_listings USING gin (title gin_trgm_ops);
CREATE INDEX idx_listings_description_trgm ON marketplace_listings USING gin (description gin_trgm_ops);

-- Поиск с опечатками
SELECT * FROM marketplace_listings 
WHERE title % 'квортира'  -- найдет "квартира"
ORDER BY similarity(title, 'квортира') DESC;
```

**Что это дает:**
- Поиск с опечатками ("телифон" → "телефон")
- Частичные совпадения ("кварт" → "квартира")
- Ранжирование по похожести

#### ✅ 2.2 Система синонимов

**Таблица синонимов:**
```sql
CREATE TABLE search_synonyms (
    id SERIAL PRIMARY KEY,
    term VARCHAR(100) NOT NULL,
    synonym VARCHAR(100) NOT NULL,
    language VARCHAR(10) DEFAULT 'ru',
    weight FLOAT DEFAULT 1.0,
    category_id INT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Примеры синонимов
INSERT INTO search_synonyms (term, synonym) VALUES
    ('квартира', 'апартаменты'),
    ('квартира', 'жилье'),
    ('квартира', 'студия'),
    ('авто', 'машина'),
    ('авто', 'автомобиль'),
    ('телефон', 'смартфон'),
    ('телефон', 'мобильный');
```

**Расширение поискового запроса:**
```go
func expandQueryWithSynonyms(query string) []string {
    // Получаем синонимы из БД
    synonyms := getSynonymsForQuery(query)
    
    // Формируем расширенный запрос
    expandedTerms := append([]string{query}, synonyms...)
    
    return expandedTerms
}
```

#### ⏳ 2.3 Морфологический анализ (отложено)

**Использование стемминга для русского языка:**
```go
// Простой стеммер для русского
func russianStemmer(word string) string {
    // Убираем окончания
    suffixes := []string{"ами", "ями", "ах", "ях", "ов", "ев", "ий", "ый", "ая", "яя", "ое", "ее"}
    
    for _, suffix := range suffixes {
        if strings.HasSuffix(word, suffix) {
            return strings.TrimSuffix(word, suffix)
        }
    }
    return word
}

// Пример: "квартирами" → "квартир", "красивая" → "красив"
```

#### ⏳ 2.4 Транслитерация и нормализация (отложено)

```go
var translitMap = map[rune]string{
    'a': "а", 'b': "б", 'v': "в", 'g': "г", 'd': "д",
    'e': "е", 'z': "з", 'i': "и", 'k': "к", 'l': "л",
    'm': "м", 'n': "н", 'o': "о", 'p': "п", 'r': "р",
    's': "с", 't': "т", 'u': "у", 'f': "ф", 'h': "х",
}

// "kvartira" → "квартира"
// "iphone" → "айфон"
```

---

## 📊 Фаза 3: Поведенческая оптимизация (НОВАЯ)

**Цель**: Учитывать действия пользователей для улучшения результатов  
**Срок**: 1 неделя  
**Без внешних зависимостей**

### 📋 Задачи:

#### 3.1 Отслеживание кликов (2 дня)

```sql
CREATE TABLE search_clicks (
    id SERIAL PRIMARY KEY,
    search_query VARCHAR(255),
    listing_id INT,
    position INT,
    clicked_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    session_id VARCHAR(100),
    converted BOOLEAN DEFAULT FALSE
);

-- Индекс для аналитики
CREATE INDEX idx_search_clicks_query ON search_clicks(search_query, clicked_at);
```

#### 3.2 Автоматическая корректировка весов (2 дня)

```go
// Повышаем ранг часто кликаемых объявлений
func boostByClickRate(listings []Listing, query string) []Listing {
    clickStats := getClickStats(query)
    
    for i, listing := range listings {
        if ctr, exists := clickStats[listing.ID]; exists {
            // Бустим скор на основе CTR
            listings[i].Score *= (1 + ctr * 0.5)
        }
    }
    
    return listings
}
```

#### 3.3 Персонализация без ML (2 дня)

```javascript
// Frontend: сохраняем историю в localStorage
const viewHistory = {
    categories: [1100, 2000], // просмотренные категории
    priceRange: {min: 10000, max: 50000},
    attributes: {rooms: "3", brand: "Samsung"}
};

// При поиске учитываем предпочтения
if (userPreferences.categories.includes(listing.categoryId)) {
    listing.score *= 1.2; // boost для знакомых категорий
}
```

#### 3.4 A/B тестирование алгоритмов (1 день)

```go
func selectSearchAlgorithm(userID string) string {
    // 50/50 split для тестирования
    hash := crc32.ChecksumIEEE([]byte(userID))
    if hash % 2 == 0 {
        return "algorithm_v1"
    }
    return "algorithm_v2"
}
```

---

## 📈 Фаза 4: Мониторинг и аналитика

**Цель**: Измерять эффективность и находить проблемы  
**Срок**: 3-5 дней

### 📋 Задачи:

#### 4.1 Метрики поиска

```sql
CREATE MATERIALIZED VIEW search_metrics AS
SELECT 
    date_trunc('day', created_at) as day,
    COUNT(DISTINCT session_id) as unique_searches,
    COUNT(*) as total_searches,
    AVG(results_count) as avg_results,
    COUNT(*) FILTER (WHERE results_count = 0) as zero_results
FROM search_queries
GROUP BY 1;
```

#### 4.2 Дашборд администратора

- **Топ поисковых запросов**
- **Запросы без результатов**
- **CTR по категориям**
- **Эффективность синонимов**

#### 4.3 Автоматические алерты

```go
// Алерт при большом количестве пустых результатов
if zeroResultsRate > 0.3 {
    notifyAdmin("High zero-results rate: " + fmt.Sprintf("%.1f%%", zeroResultsRate*100))
}
```

---

## 💰 Сравнение с векторным поиском:

| Параметр | Векторный поиск (ML) | Улучшенный текстовый |
|----------|---------------------|---------------------|
| **Стоимость** | $200-500/месяц | $0 |
| **Время разработки** | 2-3 недели | 1-2 недели |
| **Зависимости** | OpenAI, Pinecone | Только PostgreSQL |
| **Сложность** | Высокая | Средняя |
| **Поддержка** | Требует ML-экспертизы | Обычные разработчики |
| **Качество** | 95% | 85% |

---

## 🎯 Приоритеты реализации:

### Завершено (Sprint 1-2):
1. ✅ Фаза 1 - Базовые улучшения
   - ✅ Система весов атрибутов
   - ✅ Многоуровневый алгоритм скоринга
   - ✅ Поддержка storefront products
   - ✅ **НОВОЕ**: Исправлен алгоритм похожих объявлений
2. ✅ Фаза 2 - Интеллектуальный текстовый поиск (частично)
   - ✅ Нечеткий поиск (pg_trgm)
   - ✅ Система синонимов
   - ✅ UI компонент для управления fuzzy search

### Следующий спринт (Sprint 3):
1. 🚀 Фаза 3 - Поведенческая оптимизация
   - ⏳ Отслеживание кликов
   - ⏳ Автоматическая корректировка весов
   - ⏳ Базовая персонализация
2. ⏳ Морфология и транслитерация (из Фазы 2)

### При наличии времени (Sprint 4):
1. 📈 Фаза 4 - Мониторинг и аналитика
   - ⏳ Метрики поиска
   - ⏳ Дашборд администратора
   - ⏳ Автоматические алерты
2. ⏳ A/B тестирование алгоритмов

---

## 🚀 Когда переходить к векторному поиску:

- **100K+ активных объявлений**
- **10+ языков поддержки**
- **$1000+/месяц бюджет на инфраструктуру**
- **Сложные семантические запросы** становятся критичными

До этого момента улучшенный текстовый поиск покроет 90% потребностей!

---

## 📝 История изменений:

### 2025-01-07: Исправлен алгоритм похожих объявлений
- Решена проблема с показом батареек вместо недвижимости
- Реализован межкатегорийный поиск с адаптивными весами
- Улучшен скоринг для категорий из одной группы

### 2025-01-06: Завершена Фаза 2
- Реализован нечеткий поиск с pg_trgm
- Добавлена система синонимов для 3 языков
- Создан UI компонент для управления fuzzy search