# Настройка автоматического форматирования кода

## 🚀 Быстрый старт

### 1. Установка pre-commit (один раз на машине)

```bash
# macOS
brew install pre-commit

# Ubuntu/Debian
sudo apt install pre-commit

# или через pip
pip install pre-commit
```

### 2. Активация pre-commit hooks (в проекте)

```bash
cd /path/to/svetu-project
pre-commit install
```

### 3. Первый запуск (отформатирует весь проект)

```bash
pre-commit run --all-files
```

## 📋 Команды для разработчиков

### Go Backend (в папке `backend/`)

```bash
# Форматирование кода
make format

# Проверка форматирования (не изменяет файлы)
make format-check

# Линтинг
make lint

# Все проверки перед коммитом
make pre-commit
```

### Frontend (в папке `frontend/svetu/`)

```bash
# Форматирование
yarn format

# Проверка форматирования
yarn format:check

# Линтинг
yarn lint

# Исправление линтинга
yarn lint:fix
```

## 🛠 Что делают форматтеры

### Go Backend:
- **`gofumpt`** - строгое форматирование Go кода (лучше чем `gofmt`)
- **`goimports`** - автоматическое управление импортами
- **`golangci-lint`** - комплексный линтер для Go

### Frontend:
- **`prettier`** - форматирование JS/TS/JSON/CSS/MD
- **`eslint`** - проверка качества кода

## 🔄 Workflow для разработчиков

1. **Делаете изменения** в коде
2. **Коммитите** - pre-commit hooks автоматически запустятся
3. **Если есть ошибки форматирования** - исправляются автоматически
4. **Если есть линт ошибки** - нужно исправить вручную
5. **Коммит проходит** только при успешных проверках

## 🤖 GitHub Actions

При создании PR автоматически проверяется:
- ✅ Форматирование Go кода
- ✅ Линтинг Go кода
- ✅ Форматирование Frontend кода

Если проверки не прошли - PR будет заблокирован.

## 📞 Поддержка

Если что-то не работает:
1. Убедитесь что установлен `pre-commit`
2. Запустите `pre-commit install` в корне проекта
3. Попробуйте `pre-commit run --all-files`

## 🎯 Результат

После настройки больше **НЕ БУДЕТ**:
- ❌ Миллионов изменений с trailing spaces
- ❌ Споров о форматировании кода
- ❌ Неконсистентного стиля кода
- ❌ Проблем с line endings

Будет:
- ✅ Автоматическое форматирование
- ✅ Единый стиль кода у всей команды
- ✅ Чистые PR без "шума"
- ✅ Фокус на логике, а не на форматировании