# Функционал редактирования витрин

## Обзор

Полностью реализованный функционал редактирования витрин с загрузкой изображений, доступный по адресу `/storefronts/[slug]/edit`.

## 🎯 Ключевые функции

### 1. Многовкладочный интерфейс
- **Основная информация** - название, описание, контакты
- **Местоположение** - адрес, страна, город, почтовый индекс  
- **Часы работы** - настройка расписания по дням недели
- **Оплата** - настройка принимаемых способов оплаты
- **Доставка** - настройка опций доставки
- **Медиа** - загрузка логотипа и баннера

### 2. Управление изображениями

#### Логотип витрины
- Рекомендуемый размер: 256x256px
- Максимальный размер файла: 5MB
- Поддерживаемые форматы: JPEG, PNG, WebP
- Предварительный просмотр
- Кнопка удаления

#### Баннер витрины  
- Рекомендуемый размер: 1200x400px
- Максимальный размер файла: 10MB
- Поддерживаемые форматы: JPEG, PNG, WebP
- Предварительный просмотр с ограничением высоты
- Кнопка удаления

### 3. UX особенности
- Валидация файлов перед загрузкой
- Индикаторы загрузки
- Toast уведомления о результатах операций
- Сброс input для повторного выбора файлов
- Автоматическое обновление данных

## 🔧 Техническая реализация

### Frontend компоненты

#### Основной компонент
```typescript
// /src/app/[locale]/storefronts/[slug]/edit/page.tsx
export default function EditStorefrontPage() {
  // Состояние формы, загрузки изображений
  // Обработчики для загрузки и удаления
  // Многовкладочный интерфейс
}
```

#### API интеграция
```typescript  
// /src/services/storefrontApi.ts
export const storefrontApi = {
  uploadLogo: async (storefrontId: number, file: File) => { /* ... */ },
  uploadBanner: async (storefrontId: number, file: File) => { /* ... */ },
}
```

### Backend endpoints

#### Загрузка изображений
- `POST /api/v1/storefronts/{id}/logo` - загрузка логотипа
- `POST /api/v1/storefronts/{id}/banner` - загрузка баннера

#### Обновление данных
- `PUT /api/v1/storefronts/{id}` - обновление информации витрины
- `GET /api/v1/storefronts/slug/{slug}` - получение данных витрины

### Хранение файлов
- **MinIO Storage** - объектное хранилище для изображений
- Путь логотипа: `/listings/storefronts/{id}/logo/{filename}`
- Путь баннера: `/listings/storefronts/{id}/banner/{filename}`
- Автоматическое определение content-type

## 📝 Структура данных

### StorefrontUpdateDTO
```go
type StorefrontUpdateDTO struct {
    Name        *string `json:"name,omitempty"`
    Description *string `json:"description,omitempty"`
    LogoURL     *string `json:"logo_url,omitempty"`
    BannerURL   *string `json:"banner_url,omitempty"`
    Phone       *string `json:"phone,omitempty"`
    Email       *string `json:"email,omitempty"`
    Website     *string `json:"website,omitempty"`
    Location    *Location `json:"location,omitempty"`
    Settings    JSONB   `json:"settings,omitempty"`
    // ...
}
```

### Состояние загрузки (Frontend)
```typescript
const [logoPreview, setLogoPreview] = useState<string | null>(null);
const [bannerPreview, setBannerPreview] = useState<string | null>(null);
const [uploadingLogo, setUploadingLogo] = useState(false);
const [uploadingBanner, setUploadingBanner] = useState(false);
```

## 🌍 Интернационализация

### Добавленные переводы (ru.json)
```json
{
  "storefronts": {
    "editStorefront": "Редактировать витрину",
    "media": "Медиа",
    "uploadLogo": "Загрузить логотип", 
    "uploadBanner": "Загрузить баннер",
    "logoUploadSuccess": "Логотип успешно загружен",
    "bannerUploadSuccess": "Баннер успешно загружен",
    "logoRemoved": "Логотип удален",
    "bannerRemoved": "Баннер удален",
    "fileTooLarge": "Файл слишком большой. Максимальный размер: {max}",
    // ...
  }
}
```

## 🔐 Безопасность

### Авторизация
- Проверка JWT токена или сессии
- Проверка прав владельца витрины
- Middleware `AuthRequiredJWT` на всех endpoints

### Валидация файлов
- Проверка размера файла
- Проверка MIME типа
- Ограничение по форматам изображений

## 🚀 Производительность

### Оптимизации загрузки
- Предварительное сжатие изображений на клиенте
- Прогрессивная загрузка
- Кэширование через MinIO

### UX оптимизации
- Мгновенный предварительный просмотр
- Асинхронная загрузка без блокировки UI
- Debounce для операций автосохранения

## 📊 Мониторинг

### Backend логирование
```
[11:33PM] INF Uploading file to MinIO contentType=image/jpeg size=448502
[11:33PM] INF File successfully uploaded to MinIO filePath=/listings/storefronts/1/logo/...
[11:33PM] INF Response duration=8.605993 method=POST path=/api/v1/storefronts/1/logo status=200
```

### Отслеживаемые события
- Загрузка изображений
- Ошибки валидации
- Время выполнения операций
- Объем загружаемых файлов

## 🔍 Отладка

### Общие проблемы
1. **413 Payload Too Large** - проверить размер файла
2. **415 Unsupported Media Type** - проверить MIME тип
3. **401 Unauthorized** - проверить авторизацию
4. **500 Server Error** - проверить логи backend

### Инструменты отладки
- Логи backend в `/tmp/backend.log`
- DevTools Network tab для HTTP запросов
- Console для JavaScript ошибок
- Toast уведомления для пользовательских ошибок

## 📅 История изменений

### v1.0 (22.07.2025)
- ✅ Полная реализация страницы редактирования
- ✅ Загрузка и удаление логотипа/баннера
- ✅ Интернационализация
- ✅ UX улучшения
- ✅ Исправление проблем с макетом