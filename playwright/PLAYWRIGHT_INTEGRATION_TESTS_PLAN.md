# План реализации интеграционных Playwright тестов

## Обзор

Создание интеграционных end-to-end тестов с использованием Playwright для полного стека приложения Svetu Platform, включающего:

- **Backend**: Go API на порту 3000
- **Frontend**: Next.js приложение на порту 3001
- **Database**: PostgreSQL с миграциями
- **Search**: OpenSearch для полнотекстового поиска
- **Тестирование**: Playwright в headless режиме

## Архитектура тестовой среды

```
playwright/
├── playwright.config.ts     # Конфигурация Playwright
├── tests/                   # Тестовые сценарии
│   ├── auth/               # Тесты аутентификации
│   ├── marketplace/        # Тесты маркетплейса
│   └── storefronts/        # Тесты витрин
├── fixtures/               # Тестовые данные
├── helpers/                # Вспомогательные функции
└── package.json            # Зависимости для тестов
```

## Этапы реализации

### 1. Настройка структуры тестов
- [ ] Создать папку `playwright/` в корне проекта
- [ ] Установить Playwright и зависимости
- [ ] Настроить `playwright.config.ts`
- [ ] Создать базовые helper'ы для тестов

### 2. Настройка тестовой среды
- [ ] Docker Compose конфигурация для тестовых сервисов
- [ ] Скрипты подготовки тестовой БД
- [ ] Конфигурация переменных окружения для тестов
- [ ] Настройка фикстур и тестовых данных

### 3. GitHub Actions workflow
- [ ] Service containers для PostgreSQL и OpenSearch
- [ ] Последовательность запуска сервисов
- [ ] Выполнение миграций БД
- [ ] Билд и запуск backend/frontend
- [ ] Запуск Playwright тестов

### 4. Базовые тестовые сценарии
- [ ] Тесты аутентификации (Google OAuth)
- [ ] Тесты создания объявлений
- [ ] Тесты поиска по маркетплейсу
- [ ] Тесты работы с витринами
- [ ] Тесты чатов и сообщений

## Детальный план выполнения

### Этап 1: Структура и конфигурация

**1.1 Создание структуры папок**
```bash
playwright/
├── playwright.config.ts
├── package.json
├── tests/
├── fixtures/
└── helpers/
```

**1.2 Установка зависимостей**
```json
{
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "dotenv": "^16.3.0"
  }
}
```

**1.3 Конфигурация Playwright**
- Headless режим для CI
- Множественные браузеры (Chrome, Firefox, Safari)
- Настройка базовых URL для frontend/backend
- Таймауты и retry логика
- Скриншоты при падении тестов

### Этап 2: Тестовая среда

**2.1 Docker Compose для тестов**
```yaml
services:
  postgres-test:
    image: postgres:15
    environment:
      POSTGRES_DB: svetu_test
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - "5433:5432"
      
  opensearch-test:
    image: opensearchproject/opensearch:2.11.0
    environment:
      - discovery.type=single-node
      - DISABLE_SECURITY_PLUGIN=true
    ports:
      - "9201:9200"
```

**2.2 Подготовка тестовых данных**
- SQL фикстуры для базовых данных
- Тестовые изображения для MinIO
- Mock данные для OpenSearch индексов

**2.3 Environment переменные**
```bash
# Тестовая конфигурация
TEST_DB_URL="postgres://test:test@localhost:5433/svetu_test"
TEST_OPENSEARCH_URL="http://localhost:9201"
TEST_FRONTEND_URL="http://localhost:3001"
TEST_BACKEND_URL="http://localhost:3000"
```

### Этап 3: GitHub Actions

**3.1 Workflow структура**
```yaml
name: Integration Tests
on: [push, pull_request]

jobs:
  integration-tests:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        # ... конфигурация
      opensearch:
        image: opensearchproject/opensearch:2.11.0
        # ... конфигурация

    steps:
      - name: Checkout
      - name: Setup Go + Node.js
      - name: Install dependencies
      - name: Run migrations
      - name: Start backend
      - name: Start frontend  
      - name: Run Playwright tests
      - name: Upload test results
```

**3.2 Последовательность запуска**
1. Подъем service containers (PostgreSQL, OpenSearch)
2. Установка Go и Node.js
3. Установка зависимостей (go mod, yarn)
4. Выполнение миграций БД
5. Билд и запуск backend в фоне
6. Билд и запуск frontend в фоне  
7. Ожидание готовности сервисов
8. Запуск Playwright тестов
9. Сбор артефактов (скриншоты, логи)

### Этап 4: Тестовые сценарии

**4.1 Категории тестов**

**Аутентификация**
- Переход на страницу логина
- Google OAuth flow (mock)
- Проверка сессии пользователя
- Logout функционал

**Маркетплейс**
- Просмотр главной страницы
- Поиск объявлений
- Фильтрация по категориям
- Просмотр детальной страницы объявления
- Создание нового объявления

**Витрины (Storefronts)**
- Создание новой витрины
- Добавление товаров в витрину
- Настройка витрины
- Просмотр публичной страницы витрины

**Чаты**
- Отправка сообщения продавцу
- Получение уведомлений
- История сообщений

**4.2 Пример тестового сценария**
```typescript
test('создание объявления в маркетплейсе', async ({ page }) => {
  // Авторизация
  await page.goto('/auth/login');
  await mockGoogleAuth(page);
  
  // Переход к созданию объявления
  await page.goto('/marketplace/create');
  
  // Заполнение формы
  await page.fill('[data-testid="title"]', 'Тестовое объявление');
  await page.fill('[data-testid="description"]', 'Описание товара');
  await page.selectOption('[data-testid="category"]', 'electronics');
  
  // Загрузка изображения
  await page.setInputFiles('[data-testid="images"]', 'fixtures/test-image.jpg');
  
  // Отправка формы
  await page.click('[data-testid="submit"]');
  
  // Проверка успешного создания
  await expect(page).toHaveURL(/\/marketplace\/listing\/\d+/);
  await expect(page.locator('[data-testid="title"]')).toHaveText('Тестовое объявление');
});
```

## Команды для разработки

### Локальный запуск тестов
```bash
# Запуск всех тестов
cd playwright && yarn test

# Запуск в UI режиме
cd playwright && yarn test:ui

# Запуск конкретного теста
cd playwright && yarn test tests/marketplace/create-listing.spec.ts

# Отладка тестов
cd playwright && yarn test:debug
```

### CI команды
```bash
# Запуск в headless режиме (как в CI)
cd playwright && yarn test:ci

# Генерация отчетов
cd playwright && yarn test:report
```

## Ожидаемые результаты

1. **Полная автоматизация**: Все тесты запускаются автоматически в PR и push
2. **Быстрая обратная связь**: Результаты тестов доступны в течение 10-15 минут
3. **Стабильность**: Тесты проходят стабильно благодаря изоляции данных
4. **Покрытие**: Основные пользовательские сценарии покрыты тестами
5. **Отчетность**: HTML отчеты с скриншотами падений
6. **Масштабируемость**: Легко добавлять новые тесты

## Преимущества подхода

- **Реальная среда**: Тестирование на настоящих сервисах
- **Изоляция**: Каждый тест запуск получает чистую БД
- **CI/CD интеграция**: Автоматическое выполнение в GitHub Actions
- **Кроссбраузерность**: Тестирование в разных браузерах
- **Отчетность**: Подробные отчеты с визуализацией падений

## Риски и митигация

**Риски:**
- Медленное выполнение тестов
- Нестабильность из-за зависимости от внешних сервисов
- Сложность отладки падающих тестов

**Митигация:**
- Параллельное выполнение тестов
- Использование фикстур и моков где возможно
- Детальное логирование и скриншоты
- Retry механизмы для нестабильных тестов

## Следующие шаги

1. ✅ Создать план (этот документ)
2. 🔄 Настроить базовую структуру playwright/
3. 🔄 Создать GitHub Actions workflow
4. 🔄 Написать первые базовые тесты
5. 🔄 Протестировать локально и в CI