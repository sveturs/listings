# Синхронизация справочных данных между монолитом и микросервисом listings

## Описание

Скрипт `sync_listings_data.py` синхронизирует справочные данные (категории, атрибуты, связи) из монолита (база `svetubd`) в микросервис listings (база `listings_dev_db`).

**Основная особенность:** Автоматическая трансформация VARCHAR полей в JSONB для поддержки многоязычности.

## Что синхронизируется

### 1. Categories (c2c_categories → categories)
Прямое копирование всех полей категорий.

### 2. Attributes (unified_attributes → attributes)
**Трансформация:**
- `name` (VARCHAR) → `name` (JSONB): `{"en": "value", "sr": "value", "ru": "value"}`
- `display_name` (VARCHAR) → `display_name` (JSONB): то же самое
- Остальные поля: прямое копирование

### 3. Category Attributes (unified_category_attributes → category_attributes)
Прямое копирование связей категория-атрибут.

## Использование

### Быстрый запуск:
```bash
python3 /p/github.com/sveturs/listings/scripts/sync_listings_data.py
```

### Запуск через временную копию:
```bash
cp /p/github.com/sveturs/listings/scripts/sync_listings_data.py /tmp/
python3 /tmp/sync_listings_data.py
```

## Что делает скрипт

1. ✅ Подключается к обеим БД
2. ✅ Собирает статистику ДО синхронизации
3. ✅ Очищает таблицы микросервиса (в правильном порядке с учетом FK)
4. ✅ Синхронизирует данные (с трансформацией для attributes)
5. ✅ Сбрасывает sequences
6. ✅ Проверяет целостность данных (FK, категория 1008)
7. ✅ Собирает статистику ПОСЛЕ синхронизации
8. ✅ Выводит итоговый отчёт

## Порядок операций

### Очистка (учитываются FK constraints):
1. `category_attributes` (зависит от categories и attributes)
2. `attributes` (независимая)
3. `categories` (независимая)

### Импорт (обратный порядок):
1. `categories` (сначала родительские таблицы)
2. `attributes` (независимая)
3. `category_attributes` (зависимая)

## Connection Strings

**Монолит (svetubd):**
```
postgres://postgres:mX3g1XGhMRUZEX3l@localhost:5433/svetubd
```

**Микросервис (listings_dev_db):**
```
postgres://listings_user:listings_secret@localhost:35434/listings_dev_db
```

## Проверка результатов

После синхронизации проверьте данные:

```bash
# Проверить категорию 1008
psql "postgres://listings_user:listings_secret@localhost:35434/listings_dev_db" \
  -c "SELECT id, name, slug FROM categories WHERE id = 1008;"

# Проверить трансформацию JSONB (первые 3 атрибута)
psql "postgres://listings_user:listings_secret@localhost:35434/listings_dev_db" \
  -c "SELECT id, code, name, display_name FROM attributes LIMIT 3;"

# Статистика
psql "postgres://listings_user:listings_secret@localhost:35434/listings_dev_db" \
  -c "SELECT
        (SELECT COUNT(*) FROM categories) as categories,
        (SELECT COUNT(*) FROM attributes) as attributes,
        (SELECT COUNT(*) FROM category_attributes) as cat_attrs;"
```

## Ожидаемые результаты

По состоянию на момент создания скрипта:
- **Categories:** 75
- **Attributes:** 203
- **Category Attributes:** 479

## Обработка ошибок

Скрипт автоматически:
- ✅ Делает rollback при ошибках
- ✅ Закрывает подключения к БД
- ✅ Выводит детальные логи с цветной подсветкой
- ✅ Возвращает exit code 1 при ошибке

## Логирование

Уровни логирования (с цветами):
- 🔵 **INFO** - информационные сообщения
- 🟢 **SUCCESS** - успешные операции
- 🟡 **WARNING** - предупреждения
- 🔴 **ERROR** - ошибки

## Когда запускать

Запускайте скрипт когда:
1. Добавили новые категории в монолите
2. Добавили новые атрибуты в монолите
3. Изменили связи категория-атрибут
4. Нужно полностью пересинхронизировать справочники

## Безопасность

⚠️ **ВНИМАНИЕ:** Скрипт выполняет `TRUNCATE` (полную очистку) таблиц микросервиса перед синхронизацией!

Убедитесь что:
- Нет активных операций с данными в микросервисе
- У вас есть backup БД (если требуется)

## Зависимости

```bash
pip3 install psycopg2-binary
```

## Технические детали

### Трансформация VARCHAR → JSONB

Функция `varchar_to_jsonb()` создаёт многоязычный JSON:

```python
Input:  "Brand"
Output: {"en": "Brand", "sr": "Brand", "ru": "Brand"}
```

Это позволяет микросервису использовать один формат для всех полей, а не хардкодить языки.

### Сброс sequences

После импорта сбрасываются sequences для `categories.id` и `attributes.id`, чтобы новые записи получали корректные ID.

### Проверка целостности

После синхронизации скрипт проверяет:
1. Все FK в `category_attributes` валидны
2. Категория 1008 (критичная для тестов) присутствует
3. JSONB поля имеют корректную структуру

## История изменений

- **2025-11-21:** Создан скрипт синхронизации с трансформацией VARCHAR → JSONB
- **Первый успешный запуск:** 75 категорий, 203 атрибута, 479 связей

## Автор

Создано для проекта vondi.rs (маркетплейс)
