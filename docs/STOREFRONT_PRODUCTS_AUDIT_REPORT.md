# 📋 АУДИТ ОТОБРАЖЕНИЯ ТОВАРОВ ВИТРИН НА ГЛАВНОЙ СТРАНИЦЕ

**Дата проведения:** 07.08.2025  
**Статус:** Выявлены критические проблемы

## 📊 Резюме

Проведен полный аудит системы отображения товаров витрин (storefront products) на главной странице маркетплейса. Выявлена **КРИТИЧЕСКАЯ ПРОБЛЕМА**: отсутствие синхронизации остатков товаров между PostgreSQL и OpenSearch после покупки.

## 🔍 Области анализа

### 1. База данных PostgreSQL
✅ **Статус:** Полнофункциональная структура

**Основные таблицы:**
- `storefronts` - информация о витринах (23 поля)
- `storefront_products` - товары витрин (26 полей, включая геолокацию)
- `storefront_product_variants` - варианты товаров (размеры, цвета)
- `storefront_orders` - заказы (33 поля с эскроу)
- `shopping_carts` и `shopping_cart_items` - корзины покупок

**Сильные стороны:**
- Полная система управления товарами и вариантами
- Гибкая система геолокации с уровнями приватности
- Продуманная система заказов с эскроу
- Интеграция с MinIO для хранения изображений

### 2. Backend API
✅ **Статус:** Хорошо структурированный сервисный слой

**Ключевые компоненты:**
- `ProductService` - управление товарами с интеграцией OpenSearch
- `OrderService` - создание заказов и управление остатками
- `VariantService` - управление вариантами товаров

**Процесс создания товара:**
1. Валидация прав владельца витрины
2. Создание товара в PostgreSQL (транзакция)
3. Создание вариантов (если есть)
4. Асинхронная индексация в OpenSearch

### 3. Индексация в OpenSearch
⚠️ **Статус:** Частично работает

**Что работает:**
- ✅ Индексация при создании товара
- ✅ Индексация при обновлении товара через API
- ✅ Индексация при удалении товара
- ✅ Поддержка частичных обновлений (метод `UpdateProductStock`)

**Структура документа в OpenSearch:**
```json
{
  "id": "sp_123",
  "product_type": "storefront",
  "stock_quantity": 10,
  "inventory": {
    "quantity": 10,
    "in_stock": true,
    "available": 10
  },
  "variants": [
    {
      "stock_quantity": 5,
      "available_quantity": 5
    }
  ]
}
```

### 4. Frontend отображение
✅ **Статус:** Полная интеграция

**Компоненты:**
- `BentoGridListings` - отображение товаров на главной
- `UnifiedProductCard` - универсальная карточка товара
- `UnifiedSearchService` - унифицированный поиск

**Особенности товаров витрин:**
- Специальная рамка с градиентом
- Бейдж с названием витрины
- Кнопка "Добавить в корзину"
- Модальное окно выбора вариантов

## 🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА

### Отсутствие реиндексации остатков после покупки

**Описание проблемы:**
При покупке товара остатки обновляются ТОЛЬКО в PostgreSQL, но НЕ в OpenSearch.

**Процесс покупки (текущий):**
```mermaid
graph LR
    A[Создание заказа] --> B[Блокировка товара]
    B --> C[Проверка остатков]
    C --> D[Резервирование]
    D --> E[Обновление в PostgreSQL]
    E --> F[Коммит транзакции]
    F --> X[❌ НЕТ обновления OpenSearch]
```

**Код проблемы:**
```go
// create_order_with_tx.go:108-111
if err := s.updateProductStockTx(ctx, tx, item.ProductID, item.VariantID, stockQuantity-item.Quantity); err != nil {
    return fmt.Errorf("failed to update stock: %w", err)
}
// ❌ Здесь НЕТ вызова обновления OpenSearch!
```

**Последствия:**
1. На главной странице отображаются устаревшие остатки
2. Товары с нулевыми остатками продолжают отображаться как доступные
3. Фильтрация по наличию работает некорректно
4. Возможны попытки покупки отсутствующих товаров

## 💡 РЕКОМЕНДАЦИИ

### 1. Срочное исправление (Priority: CRITICAL)

**Добавить реиндексацию после покупки:**

```go
// В OrderService добавить:
type OrderService struct {
    // ... существующие поля
    productSearchRepo opensearch.ProductSearchRepository
}

// В CreateOrderWithTx после коммита транзакции:
// Обновляем остатки в OpenSearch
for _, item := range items {
    stockData := map[string]interface{}{
        "stock_quantity": finalQuantity,
        "inventory": map[string]interface{}{
            "quantity": finalQuantity,
            "in_stock": finalQuantity > 0,
            "available": finalQuantity,
        },
    }
    
    if err := s.productSearchRepo.UpdateProductStock(ctx, int(item.ProductID), stockData); err != nil {
        logger.Error().Err(err).Msgf("Failed to update stock in OpenSearch for product %d", item.ProductID)
        // Не прерываем процесс, только логируем
    }
}
```

### 2. Дополнительные улучшения (Priority: HIGH)

1. **Добавить фоновую синхронизацию:**
   - Cron-задача для периодической синхронизации остатков
   - Проверка расхождений между PostgreSQL и OpenSearch

2. **Мониторинг:**
   - Метрики расхождений остатков
   - Алерты при критических расхождениях

3. **Оптимизация индексации:**
   - Батчевое обновление для массовых операций
   - Очередь обновлений с retry логикой

### 3. Архитектурные улучшения (Priority: MEDIUM)

1. **Event-driven архитектура:**
   - События изменения остатков
   - Асинхронная обработка через очередь сообщений

2. **Унификация сервисов:**
   - Все операции с товарами через ProductService
   - Централизованное управление синхронизацией

## 📈 Метрики для контроля

После внедрения исправлений отслеживать:
- Время синхронизации остатков (< 1 сек)
- Процент расхождений (< 0.1%)
- Количество ошибок индексации
- Задержка отображения изменений на главной

## 🎯 План действий

1. **Немедленно:** Реализовать реиндексацию после покупки
2. **В течение недели:** Добавить мониторинг и алерты
3. **В течение месяца:** Внедрить фоновую синхронизацию
4. **Долгосрочно:** Перейти на event-driven архитектуру

## 📊 Статистика проблемы

- **Таблиц в БД:** 17 связанных с витринами
- **Методов API:** 15+ для управления товарами
- **Компонентов Frontend:** 5 основных
- **Критических проблем:** 1 (отсутствие реиндексации)
- **Время на исправление:** ~2-4 часа

## ✅ Заключение

Система товаров витрин функционально полная и хорошо спроектирована. Единственная, но критическая проблема - отсутствие синхронизации остатков с поисковым индексом после покупки. После исправления этой проблемы система будет полностью готова к продакшену.