# Post Express TX 11 Language Bug - Final Report

**Date:** 2025-10-14
**Status:** ❌ **TX 11 не работает - баг Post Express API**
**Severity:** High - блокирует использование TX 11 для расчета стоимости

---

## 🐛 Проблема

TX 11 (PostarinaPosiljke - Calculate Postage) **не работает ни с одним языком** из-за ошибки в базе данных Post Express.

### Протестированные варианты:

| Язык | Результат | Ошибка |
|------|-----------|--------|
| `"sr"` (сербский) | ❌ FAIL | `Column 'PREVOD_SR' does not belong to table Prevodi.` |
| `"en"` (английский) | ❌ FAIL | `Column 'PREVOD_EN' does not belong to table Prevodi.` |
| `""` (пустая строка) | ❌ FAIL | `Column 'PREVOD_' does not belong to table Prevodi.` |

---

## 🔍 Root Cause Analysis

### Что происходит:

1. TX 11 пытается получить переводы сообщений из таблицы `Prevodi`
2. Код формирует имя колонки: `PREVOD_{Language}`
3. В базе данных Post Express **отсутствуют ВСЕ колонки переводов**
4. SQL запрос падает с ошибкой "Column does not belong to table"

### Доказательства:

**Backend логи (TX 11 с Language="sr"):**
```
DEBUG: WSP API Request - transaction_id: a2838e52-9fa7-460a-bdcb-10c903300778, type: 11
DEBUG: WSP API Response - status_code: 200, execution_time_ms: 49
DEBUG: WSP API Raw Response Body: {"Rezultat":3,"StrOut":null,"StrRezultat":"{\"Poruka\":\"Column 'PREVOD_SR' does not belong to table Prevodi.\",\"PorukaKorisnik\":\"Column 'PREVOD_SR' does not belong to table Prevodi.\",\"Info\":null}"}
ERROR: WSP transaction failed - Rezultat: 3, Poruka: Column 'PREVOD_SR' does not belong to table Prevodi.
```

**Frontend ошибка:**
```
Column 'PREVOD_SR' does not belong to table Prevodi.
```

---

## ✅ Работающие транзакции (для сравнения)

| Transaction | Описание | Статус | Использует Prevodi? |
|-------------|----------|--------|---------------------|
| TX 3 | GetNaselje (Search Settlements) | ✅ Работает | ❌ Нет |
| TX 4 | GetUlica (Search Streets) | ✅ Работает | ❌ Нет |
| TX 73 | B2B Manifest (Create Shipment) | ✅ Работает | ❌ Нет |
| TX 11 | PostarinaPosiljke (Calculate Postage) | ❌ **НЕ РАБОТАЕТ** | ✅ **Да** |

**Вывод:** Только TX 11 пытается использовать таблицу `Prevodi`, которая сломана в Post Express БД.

---

## 💡 Решения

### ❌ Что НЕ работает:

1. ~~Изменить язык на "en"~~ - та же ошибка
2. ~~Использовать пустую строку~~ - та же ошибка
3. ~~Исправить на нашей стороне~~ - это баг Post Express

### ✅ Что РАБОТАЕТ:

#### **Решение 1: Использовать TX 73 (B2B Manifest) для расчета стоимости** ⭐ РЕКОМЕНДУЕМ

TX 73 (CreateShipmentViaManifest) возвращает стоимость доставки в ответе:

**Преимущества:**
- ✅ Уже протестировано и работает
- ✅ Возвращает **реальную стоимость** от Post Express
- ✅ Можно использовать для калькулятора доставки на сайте

**Как использовать:**

```go
// 1. Создаем манифест с данными посылки
manifest, err := wspClient.CreateShipmentViaManifest(ctx, &shipmentRequest)

// 2. Получаем стоимость из ответа
if manifest.Rezultat == 0 && len(manifest.Porudzbine) > 0 {
    posiljka := manifest.Porudzbine[0].Posiljke[0]
    cost := posiljka.Iznos // Стоимость доставки в пара
    costRSD := float64(cost) / 100 // Конвертируем в RSD

    // 3. Опционально: можно отменить посылку если это был "dry run"
    // (но TX 73 не поддерживает dry run, поэтому посылка будет создана)
}
```

**Недостаток:** TX 73 создает реальную посылку, поэтому для расчета стоимости придется:
- Либо создавать посылку и потом отменять (если нужен только расчет)
- Либо показывать пользователю примерную стоимость из фиксированных тарифов

#### **Решение 2: Фиксированные тарифы**

Создать таблицу тарифов на основе:
- Веса посылки (диапазоны)
- Типа услуги (IdRukovanje)
- Зон доставки (почтовые коды)

**Пример структуры:**

```sql
CREATE TABLE postexpress_rates (
    id SERIAL PRIMARY KEY,
    service_id INT NOT NULL, -- IdRukovanje
    weight_from INT NOT NULL, -- граммы
    weight_to INT NOT NULL,
    zone VARCHAR(50) NOT NULL, -- "local", "regional", "national"
    base_rate INT NOT NULL, -- пара (1 RSD = 100 para)
    created_at TIMESTAMP DEFAULT NOW()
);
```

**Преимущества:**
- ✅ Быстрый расчет без API вызовов
- ✅ Стабильная цена для пользователей
- ✅ Можно обновлять вручную из прайс-листа Post Express

**Недостатки:**
- ❌ Нужно вручную поддерживать актуальность тарифов
- ❌ Может не учитывать все нюансы (доп. услуги, промо-акции)

#### **Решение 3: Сообщить в Post Express о баге** 📧

**Email:** support@postexpress.rs
**Тема:** TX 11 Database Error - Missing Translation Columns in Prevodi Table

**Шаблон сообщения:**

```
Dear Post Express Support,

We are integrating with your WSP API (version 0.2.4) and encountering a critical
database error when calling Transaction 11 (PostarinaPosiljke - Calculate Postage).

Error details:
- Transaction: TX 11 (PostarinaPosiljke)
- Error: "Column 'PREVOD_XX' does not belong to table Prevodi."
- Occurs with ALL languages: "sr", "en", "" (empty string)
- Other transactions (TX 3, 4, 73) work correctly

This appears to be a database schema issue in the Prevodi (Translations) table.

Test request we are sending:
{
  "IdRukovanje": 71,
  "PostanskiBrojOdlaska": "11000",
  "PostanskiBrojDolaska": "21000",
  "Masa": 500,
  "Otkupnina": 0,
  "Vrednost": 0,
  "PosebneUsluge": "PNA"
}

Could you please investigate and fix this issue?

Thank you for your assistance.

Best regards,
SVETU.rs Development Team
```

---

## 📊 Impact Assessment

### Что НЕ работает:
- ❌ TX 11 (Calculate Postage) - калькулятор стоимости доставки

### Что РАБОТАЕТ:
- ✅ TX 3 (GetNaselje) - поиск населенных пунктов
- ✅ TX 4 (GetUlica) - поиск улиц
- ✅ TX 6 (ProveraAdrese) - валидация адреса (не протестировано, но должно работать)
- ✅ TX 9 (ProveraDostupnostiUsluge) - проверка доступности услуги (не протестировано)
- ✅ TX 73 (B2B Manifest) - создание отправлений + **получение стоимости**

### Workaround для пользователей:

**Сейчас можем предложить:**
1. ✅ Автокомплит адресов (TX 3 + TX 4)
2. ✅ Создание отправлений с реальной стоимостью (TX 73)
3. ⚠️ Калькулятор стоимости - используем фиксированные тарифы ИЛИ TX 73

---

## 🎯 Рекомендации

### Краткосрочная перспектива (1-2 недели):
1. ✅ **Использовать TX 73 для расчета стоимости** при оформлении заказа
2. ✅ Показывать примерную стоимость из фиксированных тарифов на странице товара
3. ✅ Отправить bug report в Post Express

### Долгосрочная перспектива (1+ месяц):
1. ⏳ Дождаться исправления TX 11 от Post Express
2. ⏳ Обновить интеграцию после фикса
3. ✅ Периодически проверять работу TX 11 (раз в неделю)

---

## 📝 Связанные документы

- `/docs/POST_EXPRESS_ERROR_DISPLAY_FIX.md` - Исправление отображения ошибок
- `/docs/POST_EXPRESS_TX11_BUG_REPORT.md` - Первичный отчет о проблеме
- `/docs/POST_EXPRESS_INTEGRATION_FINAL_REPORT.md` - Общий статус интеграции
- `/docs/POST_EXPRESS_B2B_MANIFEST_STRUCTURE.md` - Документация TX 73

---

## 🔄 История изменений

### 2025-10-14 21:30
- ✅ Протестировано: Language="sr" → ошибка PREVOD_SR
- ✅ Frontend исправлен для отображения детальных ошибок

### 2025-10-14 21:44
- ✅ Протестировано: Language="en" → ошибка PREVOD_EN
- ✅ Протестировано: Language="" → ошибка PREVOD_

### 2025-10-14 21:50
- ✅ Подтверждено: TX 11 не работает ни с одним языком
- ✅ Создан финальный отчет с рекомендациями
- 📧 Готов шаблон письма для Post Express Support

---

**Status:** ❌ TX 11 blocked by Post Express database bug
**Workaround:** ✅ Use TX 73 (B2B Manifest) for postage calculation
**Next Steps:** 📧 Contact Post Express support

---

**Created:** 2025-10-14 21:50
**Author:** Claude Code
**Version:** 0.2.4
