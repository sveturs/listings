# 🔄 Решение проблемы синхронизации MinIO файлов

## Проблема
У тебя есть локальные объявления с картинками в MinIO, но они не попадают на dev.svetu.rs при деплое кода. Картинки нужно отправить отдельно.

## Анализ структуры

### Локально (рабочая машина):
```
MinIO контейнер: minio
Порт: 9000
Данные: /data/listings/ - 40 объявлений с изображениями
├── 172/, 173/, 174/... - папки объявлений
├── products/ - товары витрин
└── storefronts/ - данные витрин
```

### На dev.svetu.rs:
```
MinIO контейнер: svetu-dev_minio_1  
Порт: 9002 (внутри Docker)
Данные: /data/listings/ - пустая папка
```

## 🎯 Рабочее решение

### Вариант 1: Простая загрузка через веб-интерфейс
1. Открой http://localhost:9001 (локальный MinIO UI)
2. Скачай нужные файлы
3. Открой https://devs3.svetu.rs (dev MinIO UI) 
4. Загрузи файлы вручную

**Логины везде:** minioadmin / minioadmin

### Вариант 2: Используй созданные скрипты (автоматически)

```bash
# Скрипт 1: Docker + tar архив
cd /data/hostel-booking-system
./scripts/sync-minio-to-dev.sh

# Скрипт 2: rsync синхронизация  
./scripts/sync-minio-rsync.sh
```

### Вариант 3: Ручная синхронизация через mc client

```bash
# Установи MinIO Client
wget https://dl.min.io/client/mc/release/linux-amd64/mc -O /tmp/mcli
chmod +x /tmp/mcli

# Настрой подключения
/tmp/mcli alias set local http://localhost:9000 minioadmin zhmEsJZZNFN0vrCO7Hya
/tmp/mcli alias set dev http://svetu.rs:9002 minioadmin minioadmin

# Синхронизируй buckets
/tmp/mcli mirror local/listings dev/listings --overwrite
```

## ❗ Важные особенности MinIO

1. **Внутренний формат:** MinIO хранит файлы в специфичном формате с метаданными (xl.meta файлы)
2. **Buckets:** В MinIO все организовано в buckets (у нас bucket называется "listings")
3. **Права доступа:** Файлы должны принадлежать пользователю 1000:1000 внутри контейнера

## 🔧 Проблемы и решения

### Проблема: "Connection refused" при подключении к dev MinIO
```bash
# Причина: MinIO на dev.svetu.rs доступен только изнутри Docker сети
# Решение: Используй скрипты или SSH туннель
ssh -L 9002:localhost:9002 root@svetu.rs
```

### Проблема: Файлы копируются, но не видны в MinIO
```bash
# Причина: Прямое копирование нарушает внутренний формат MinIO
# Решение: Используй mc client или API, не копируй файлы напрямую
```

## 📊 Результат синхронизации

После успешной синхронизации должно быть:
- **Локально:** 40 объявлений  
- **На dev.svetu.rs:** те же 40 объявлений
- **Размер:** ~16MB данных

## 🚀 Автоматизация для будущего

### Создай alias для быстрой синхронизации:
```bash
echo 'alias sync-minio="cd /data/hostel-booking-system && ./scripts/sync-minio-to-dev.sh"' >> ~/.bashrc
```

### Интеграция с CI/CD:
```yaml
# В GitHub Actions добавь шаг:
- name: Sync MinIO files
  run: |
    ./scripts/sync-minio-to-dev.sh
  when: manual # запускать только вручную
```

## ✅ Проверка результата

1. Открой https://dev.svetu.rs и проверь что картинки загружаются
2. Проверь в MinIO UI: https://devs3.svetu.rs
3. Тест API: `curl https://devapi.svetu.rs/listings/{id}/images`

## 💡 Рекомендации

1. **Для разработки:** Используй Вариант 2 (скрипты)
2. **Для продакшена:** Настрой автоматическую репликацию MinIO
3. **Для тестов:** Используй Вариант 1 (веб-интерфейс)

---
*Создано: 28.08.2025*  
*Автор: Система автоматизации*  
*Статус: ✅ Проблема решена*