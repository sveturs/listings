# Статус интеграции Viber Bot с системой отслеживания

## ✅ Что реализовано

### Backend
1. **Webhook обработчик** (`/api/viber/webhook`)
   - Принимает сообщения от Viber
   - Распознает команды `track_TOKEN`
   - Обрабатывает различные типы событий (message, subscribed, etc.)

2. **Обработчик команд трекинга** (`HandleTrackDelivery`)
   - Парсит токен отслеживания из команды
   - Получает данные о доставке из БД
   - Генерирует ответ для пользователя

3. **Поддержка двух режимов**:
   - Direct Viber API (требует токен от partners.viber.com)
   - Infobip API (требует Infobip credentials)

4. **Модели данных**
   - ViberUser с поддержкой NULL полей
   - ViberSession для 24-часовых сессий
   - DeliveryInfo для информации о доставке

### Frontend
1. **Страница отслеживания** (`/track/[token]`)
   - Полноценная карта с Mapbox
   - Отображение курьера, точек pickup и delivery
   - WebSocket для real-time обновлений

2. **Режим встраивания для Viber**
   - Параметры `?viber=true&embedded=true`
   - Логика для скрытия header/footer (частично реализована)
   - Компактная панель информации

## ⚠️ Текущие проблемы

### 1. Webhook не получает реальные сообщения
**Причина**: `VIBER_AUTH_TOKEN=dev` в .env файле
**Решение**: Нужно зарегистрировать бота на https://partners.viber.com и получить реальный токен

### 2. Embedded режим показывает header/footer
**Проблема**: При открытии `/track/TOKEN?viber=true&embedded=true` все равно показывается полный интерфейс
**Причина**: Layout применяется на уровне выше TrackingClient компонента
**Решение**: Нужно условно рендерить layout в зависимости от searchParams

### 3. Отправка сообщений не работает
**Ошибка**: "invalidAuthToken" при попытке отправить ответ
**Причина**: Используется токен "dev" вместо реального

## 📋 Тестирование

### Локальное тестирование webhook
```bash
curl -X POST http://localhost:3000/api/viber/webhook \
  -H "Content-Type: application/json" \
  -H "X-Viber-Content-Signature: dev" \
  -d '{
    "event": "message",
    "sender": {"id": "test-viber-id", "name": "Test User"},
    "message": {"type": "text", "text": "track_TEST-TOKEN-123"}
  }'
```

### Просмотр страницы трекинга
- Обычная версия: http://localhost:3001/en/track/TEST-TOKEN-123
- Viber embedded: http://localhost:3001/en/track/TEST-TOKEN-123?viber=true&embedded=true

## 🔄 Процесс работы в production

1. Пользователь отправляет боту: `track_TEST-TOKEN-123`
2. Webhook получает сообщение и вызывает `HandleTrackDelivery`
3. Система запрашивает данные из `post_express_shipments`
4. Генерируется Rich Media сообщение с:
   - Статической картой маршрута (Mapbox Static API)
   - Текущим статусом и ETA
   - Кнопкой "Открыть карту" → ведет на embedded версию
   - Кнопкой "Обновить" → запрашивает свежие данные

## 📝 TODO для полного запуска

1. **Получить Viber Bot Token**
   - Зарегистрироваться на https://partners.viber.com
   - Создать бота
   - Получить auth token
   - Обновить VIBER_AUTH_TOKEN в .env

2. **Настроить webhook**
   - Установить webhook URL через Viber API
   - Проверить что https://svetu.rs/api/viber/webhook доступен

3. **Исправить embedded режим**
   - Модифицировать layout для условного рендеринга
   - Или создать отдельный layout для tracking страниц

4. **Настроить Mapbox**
   - Убедиться что MAPBOX_ACCESS_TOKEN настроен
   - Проверить лимиты Static API

5. **Интеграция с Post Express**
   - Получить реальные данные о доставках
   - Настроить GPS трекинг курьеров

## 🎯 Результат

После настройки пользователи смогут:
1. Отправить команду трекинга в Viber бот
2. Получить красивую карточку с картой маршрута
3. Открыть интерактивную карту прямо в Viber
4. Следить за курьером в реальном времени
5. Получать обновления статуса доставки