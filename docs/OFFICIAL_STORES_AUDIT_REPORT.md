# 📊 Аудит отображения официальных магазинов на главной странице

## 📅 Дата аудита: 16.09.2025

## 🔍 Текущая ситуация

### 1. Данные в базе данных
- **Всего витрин в БД**: 7
- **Активных витрин**: 4
  - AgroShop (8 товаров)
  - TechNova Electronics (7 товаров)
  - Fashion House Belgrade (7 товаров)
  - Home & Garden Center (6 товаров)
- **Неактивных витрин**: 3
  - 123455, sportmag, 3G Store

### 2. Backend API
✅ **API работает корректно**
- Эндпоинт: `/api/v1/storefronts`
- Поддерживает фильтрацию по `is_active=true`
- Правильно сортирует по количеству товаров (`sort_by=products_count`)
- Возвращает подробные данные о витринах

### 3. Frontend реализация
📍 **Файл**: `/frontend/svetu/src/app/[locale]/HomePageClient.tsx` (строки 421-535)

**Текущая логика:**
1. При загрузке компонента вызывается `loadStorefronts()` (строка 422)
2. Запрашиваются 4 активные витрины, отсортированные по количеству товаров
3. Если API возвращает данные - форматируются и отображаются
4. **ПРОБЛЕМА**: При ошибке или отсутствии данных показывается заглушка "Агентство недвижимости"

## ❌ Выявленные проблемы

### Проблема 1: Отсутствие изображений у витрин
- У 4 активных витрин нет `logo_url` и `banner_url`
- Frontend генерирует случайные аватары и фоновые изображения
- Это создает непрофессиональный вид

### Проблема 2: Заглушка вместо реальных данных
- При ошибке API показывается фейковое "Агентство недвижимости"
- Вместо этого нужно показать сообщение об ошибке или скрыть секцию

### Проблема 3: Отсутствие данных о подписчиках и рейтинге
- В БД нет полей `followers_count` и `rating` для витрин
- Frontend показывает случайные значения

### Проблема 4: Неверные ссылки на витрины
- Кнопка "Перейти в магазин" не имеет правильного обработчика
- Нужно направлять на `/storefront/{slug}` или `/storefront/{id}`

## ✅ Рекомендации по исправлению

### 1. Добавить реальные изображения для витрин
это я сам сделаю через редактирование витрин. смени владельца всех витрин и товаров витрин на voroshilovdo@gmail.com

### 2. Исправить обработку ошибок в frontend
```typescript
// HomePageClient.tsx, строка 508-529
} catch (error) {
  console.error('Failed to load storefronts:', error);
  // НЕ показываем заглушку, просто пустой массив
  setOfficialStores([]);
}
```

### 3. Добавить недостающие поля в БД
```sql
-- Миграция для добавления полей рейтинга и подписчиков
ALTER TABLE storefronts
ADD COLUMN IF NOT EXISTS followers_count INTEGER DEFAULT 0,
ADD COLUMN IF NOT EXISTS rating DECIMAL(3,2) DEFAULT 0.00;
```

### 4. Исправить навигацию на витрины
```typescript
// HomePageClient.tsx, строка 1390-1393
<Link href={`/storefront/${store.slug || store.id}`}>
  <button className="btn btn-primary btn-sm mt-4 w-full">
    {t('goToStore')}
  </button>
</Link>
```

### 5. Добавить реальные категории витринам
```sql
-- Добавить категории для витрин
UPDATE storefronts SET category_id =
  (SELECT id FROM marketplace_categories WHERE slug = 'industrial' LIMIT 1)
WHERE slug = 'agroshop-supplies';

UPDATE storefronts SET category_id =
  (SELECT id FROM marketplace_categories WHERE slug = 'electronics' LIMIT 1)
WHERE slug = 'technova-electronics';
```

## 📋 План действий

1. **Срочно (сегодня)**:
   - [ ] Убрать заглушку "Агентство недвижимости"
   - [ ] Исправить ссылки на витрины

2. **Важно (эта неделя)**:
   - [ ] Создать миграцию для добавления полей `followers_count` и `rating`
   - [ ] Загрузить реальные логотипы и баннеры для активных витрин
   - [ ] Связать витрины с категориями

3. **Желательно (позже)**:
   - [ ] Добавить индикатор загрузки вместо заглушки
   - [ ] Реализовать кэширование данных витрин
   - [ ] Добавить возможность фильтрации витрин по категориям

## 🎯 Итог

Система официальных магазинов **работает**, но требует доработок:
- Backend API функционирует корректно ✅
- В БД есть реальные витрины с товарами ✅
- Frontend правильно запрашивает и отображает данные ✅

**Главная проблема** - отсутствие визуального контента (логотипы, баннеры) и использование заглушек вместо реальных данных при ошибках.

После выполнения рекомендаций официальные магазины будут отображаться профессионально и привлекательно для пользователей.
