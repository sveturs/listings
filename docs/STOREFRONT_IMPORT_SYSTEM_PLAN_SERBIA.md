# 🏪 План реализации системы импорта товаров в витрины (Сербия)

**Дата создания:** 18.08.2025  
**Статус:** Планирование  
**Приоритет:** Высокий  

## 📋 Обзор

Данный документ содержит детальный план разработки настоящей системы импорта товаров для витрин на платформе SveTu, адаптированной под сербский рынок и местные особенности.

## 🎯 Цели и задачи

### Основные цели:
1. **Упростить процесс добавления товаров** для владельцев витрин
2. **Обеспечить интеграцию с популярными системами** в Сербии
3. **Поддержать локальные форматы данных** и стандарты
4. **Автоматизировать обновление каталогов** товаров
5. **Обеспечить соответствие сербскому законодательству**

### Целевая аудитория:
- Владельцы малого и среднего бизнеса в Сербии
- Сетевые магазины и франшизы
- Оптовые поставщики
- Интернет-магазины, мигрирующие на SveTu

## 🔍 Анализ текущего состояния

### ✅ Что уже реализовано:
1. **База данных витрин** - полная структура с поддержкой продуктов и вариантов
2. **API для витрин** - CRUD операции, поиск, аналитика
3. **Базовая система импорта** - XML и CSV парсеры
4. **Демо-страница** - UI/UX примеры всех методов импорта

### 🔨 Что нужно доработать:
1. **Реальные интеграции** с сербскими системами
2. **Полноценные парсеры** файлов
3. **Валидация и маппинг** категорий
4. **Система очередей** для обработки больших файлов
5. **Мониторинг и логирование** процессов импорта

## 🌍 Особенности сербского рынка

### 📊 Популярные торговые платформы:
1. **КупујемПродајем (KupujemProdajem)** - крупнейшая площадка
   - Нет публичного API
   - Требуется исследование возможностей парсинга
   
2. **Limundo** - аукционная площадка
   - API для продавцов (уточнить доступность)
   
3. **Halo oglasi** - объявления
   - Возможна интеграция через веб-скрапинг

### 🏢 Корпоративные системы:
1. **1С:Предприятие** - популярная ERP система
   - API интеграция через веб-сервисы
   - Экспорт в XML/CSV форматах
   
2. **SAP Business One** - используется крупными компаниями
   - REST API и ODBC подключения
   
3. **Microsoft Dynamics** - учетные системы
   - Экспорт данных через Power BI и Excel

### 💳 Специфика учета:
1. **ПДВ (PDV)** - НДС в Сербии (20%)
2. **Фискализация** - обязательные кассовые аппараты
3. **Валюта** - динары (RSD) как основная валюта
4. **Классификация товаров** - по европейским стандартам

## 🏗️ Архитектура системы

### 📦 Компоненты Backend

#### 1. Import Service (расширение существующего)
```
backend/internal/proj/storefronts/service/import_service.go
```
**Новая функциональность:**
- Поддержка асинхронной обработки
- Валидация сербских данных (ПДВ, валюта)
- Интеграция с системой очередей Redis

#### 2. Integration Service (новый)
```
backend/internal/proj/storefronts/service/integration_service.go
```
**Функции:**
- Подключение к внешним API
- Парсинг веб-страниц (для KupujemProdajem)
- Аутентификация и rate limiting

#### 3. Category Mapping Service (расширение)
```
backend/internal/proj/storefronts/service/category_mapping_service.go
```
**AI-функции:**
- Автоматическое определение категорий
- Обучение на сербских данных
- Предложение категорий по ключевым словам

#### 4. Validation Service (новый)
```
backend/internal/proj/storefronts/service/validation_service.go
```
**Проверки:**
- Соответствие сербскому законодательству
- Валидация цен и ПДВ
- Проверка обязательных полей

### 📁 Парсеры и интеграции

#### 1. Serbia-specific Parsers
```
backend/internal/proj/storefronts/parsers/serbia/
├── kupujemprodajem_parser.go
├── limundo_parser.go
├── oneс_parser.go
├── sap_parser.go
└── dynamics_parser.go
```

#### 2. Integration Adapters
```
backend/internal/proj/storefronts/integrations/
├── kupujemprodajem/
├── limundo/
├── one_c/
├── sap/
└── dynamics/
```

### 🎨 Frontend компоненты

#### 1. Расширение демо-страницы до реальной
```
frontend/svetu/src/app/[locale]/storefronts/[slug]/import/
├── page.tsx (основная страница)
├── components/
│   ├── FileUploader.tsx
│   ├── FieldMapper.tsx
│   ├── CategoryMapper.tsx
│   ├── ValidationResults.tsx
│   ├── ImportProgress.tsx
│   └── integrations/
│       ├── KupujemProdajemConnector.tsx
│       ├── OneCConnector.tsx
│       └── SAPConnector.tsx
```

#### 2. Новые хуки и сервисы
```
frontend/svetu/src/hooks/
├── useImport.ts
├── useFileUpload.ts
├── useFieldMapping.ts
└── useCategoryMapping.ts

frontend/svetu/src/services/
├── importApi.ts
├── validationApi.ts
└── integrationApi.ts
```

## 🛠️ Детальный план разработки

### 📅 Фаза 1: Инфраструктура (2 недели)

#### Неделя 1: Backend инфраструктура
- [ ] **Система очередей Redis**
  - Настройка Redis для обработки импорта
  - Создание Job структур для разных типов импорта
  - Worker pool для параллельной обработки

- [ ] **Расширение Import Service**
  - Асинхронная обработка файлов
  - Прогресс-трекинг импорта
  - Обработка ошибок и retry логика

- [ ] **Validation Service**
  - Валидация сербских форматов данных
  - Проверка ПДВ и цен в динарах
  - Валидация обязательных полей

#### Неделя 2: Database и миграции
- [ ] **Новые таблицы БД**
  ```sql
  -- Таблица заданий импорта
  CREATE TABLE import_jobs (
      id SERIAL PRIMARY KEY,
      storefront_id INTEGER REFERENCES storefronts(id),
      source_type VARCHAR(50) NOT NULL, -- 'file', 'api', 'scraping'
      source_name VARCHAR(100), -- '1C', 'KupujemProdajem'
      status VARCHAR(20) DEFAULT 'pending',
      progress INTEGER DEFAULT 0,
      total_items INTEGER DEFAULT 0,
      processed_items INTEGER DEFAULT 0,
      errors_count INTEGER DEFAULT 0,
      settings JSONB DEFAULT '{}',
      created_at TIMESTAMP DEFAULT NOW(),
      started_at TIMESTAMP,
      completed_at TIMESTAMP
  );

  -- Таблица ошибок импорта
  CREATE TABLE import_errors (
      id SERIAL PRIMARY KEY,
      job_id INTEGER REFERENCES import_jobs(id),
      line_number INTEGER,
      field_name VARCHAR(100),
      error_message TEXT,
      raw_data JSONB,
      created_at TIMESTAMP DEFAULT NOW()
  );

  -- Таблица маппинга категорий
  CREATE TABLE category_mappings (
      id SERIAL PRIMARY KEY,
      storefront_id INTEGER REFERENCES storefronts(id),
      external_category VARCHAR(255),
      internal_category_id INTEGER REFERENCES marketplace_categories(id),
      confidence_score DECIMAL(3,2),
      is_manual BOOLEAN DEFAULT FALSE,
      created_at TIMESTAMP DEFAULT NOW()
  );

  -- Таблица интеграций
  CREATE TABLE storefront_integrations (
      id SERIAL PRIMARY KEY,
      storefront_id INTEGER REFERENCES storefronts(id),
      integration_type VARCHAR(50), -- '1c', 'sap', 'kupujemprodajem'
      config JSONB NOT NULL,
      is_active BOOLEAN DEFAULT TRUE,
      last_sync TIMESTAMP,
      sync_frequency INTEGER, -- в минутах
      created_at TIMESTAMP DEFAULT NOW()
  );
  ```

### 📅 Фаза 2: Парсеры и валидация (2 недели)

#### Неделя 3: Парсеры файлов
- [ ] **Улучшенный CSV парсер**
  - Автоматическое определение разделителей
  - Поддержка различных кодировок (UTF-8, Windows-1251)
  - Предпросмотр данных с возможностью маппинга полей

- [ ] **Расширенный XML парсер**
  - Поддержка Digital Vision формата (уже есть)
  - YML формат (Яндекс.Маркет)
  - Generic XML с настраиваемыми XPath

- [ ] **Excel парсер**
  - Поддержка .xlsx и .xls файлов
  - Выбор листа для импорта
  - Обработка формул и форматирования

#### Неделя 4: Валидация и маппинг
- [ ] **Category Mapping Service**
  - AI-алгоритм для определения категорий
  - Обучение на существующих данных
  - Ручное переопределение маппинга

- [ ] **Серебская валидация**
  - Проверка форматов цен (динары)
  - Валидация ПДВ (20%)
  - Проверка обязательных полей по сербскому законодательству

### 📅 Фаза 3: Интеграции с внешними системами (3 недели)

#### Неделя 5: 1С:Предприятие интеграция
- [ ] **1C Web Service Client**
  - SOAP/REST клиент для подключения к 1С
  - Аутентификация (Basic Auth, токены)
  - Получение списка товаров и остатков

- [ ] **1C Data Transformer**
  - Преобразование данных из 1С в формат SveTu
  - Маппинг полей и категорий
  - Обработка изображений и документов

#### Неделя 6: КупујемПродајем интеграция
- [ ] **Web Scraping Service**
  - Парсинг публичных страниц KupujemProdajem
  - Respect robots.txt и rate limiting
  - Извлечение данных о товарах

- [ ] **Product Data Extractor**
  - Извлечение названий, цен, описаний
  - Скачивание и обработка изображений
  - Определение категорий

#### Неделя 7: Дополнительные интеграции
- [ ] **SAP Business One**
  - ODBC подключение к базе SAP
  - Экспорт через SAP DI API
  - Синхронизация остатков

- [ ] **WooCommerce/Shopify**
  - REST API интеграция
  - Импорт из популярных CMS
  - Webhook уведомления об изменениях

### 📅 Фаза 4: Frontend интерфейс (2 недели)

#### Неделя 8: Основной интерфейс
- [ ] **Страница импорта**
  - Превращение демо в рабочий интерфейс
  - Выбор метода импорта
  - Загрузка файлов с drag & drop

- [ ] **Маппинг полей**
  - Интерактивная таблица сопоставления
  - Предпросмотр результата
  - Сохранение шаблонов маппинга

#### Неделя 9: Мониторинг и управление
- [ ] **Дашборд импорта**
  - Список всех импортов
  - Статус и прогресс выполнения
  - Детали ошибок и логи

- [ ] **Настройки интеграций**
  - Подключение к внешним системам
  - Настройка расписания синхронизации
  - Тестирование подключений

### 📅 Фаза 5: Тестирование и доработка (1 неделя)

#### Неделя 10: Тестирование
- [ ] **Unit тесты**
  - Тестирование парсеров
  - Валидация данных
  - API эндпоинты

- [ ] **Интеграционные тесты**
  - Тестирование с реальными файлами
  - Проверка интеграций
  - Нагрузочное тестирование

- [ ] **UI/UX тестирование**
  - Удобство использования
  - Обработка ошибок
  - Мобильная адаптация

## 📋 API Endpoints (новые)

### Import Management
```
POST   /api/v1/storefronts/{id}/import/files          # Загрузка файла
POST   /api/v1/storefronts/{id}/import/validate       # Валидация файла
POST   /api/v1/storefronts/{id}/import/preview        # Предпросмотр
POST   /api/v1/storefronts/{id}/import/start          # Запуск импорта
GET    /api/v1/storefronts/{id}/import/jobs           # Список заданий
GET    /api/v1/storefronts/{id}/import/jobs/{job_id}  # Детали задания
DELETE /api/v1/storefronts/{id}/import/jobs/{job_id}  # Отмена задания
```

### Integrations
```
GET    /api/v1/storefronts/{id}/integrations          # Список интеграций
POST   /api/v1/storefronts/{id}/integrations          # Добавить интеграцию
PUT    /api/v1/storefronts/{id}/integrations/{int_id} # Обновить интеграцию
DELETE /api/v1/storefronts/{id}/integrations/{int_id} # Удалить интеграцию
POST   /api/v1/storefronts/{id}/integrations/{int_id}/test # Тест подключения
POST   /api/v1/storefronts/{id}/integrations/{int_id}/sync # Запуск синхронизации
```

### Category Mapping
```
GET    /api/v1/storefronts/{id}/categories/mapping    # Получить маппинг
POST   /api/v1/storefronts/{id}/categories/mapping    # Сохранить маппинг
POST   /api/v1/storefronts/{id}/categories/suggest    # AI предложения
```

## 🔐 Безопасность и соответствие

### 🛡️ Меры безопасности:
1. **Аутентификация API**
   - JWT токены для всех операций
   - Rate limiting по IP и пользователю
   - Логирование всех операций импорта

2. **Валидация данных**
   - Санитизация всех входящих данных
   - Проверка размеров файлов
   - Антивирусная проверка загружаемых файлов

3. **Приватность**
   - Шифрование чувствительных данных
   - Удаление временных файлов
   - Анонимизация логов

### 📜 Соответствие законодательству:
1. **GDPR/ЗЗЗПЛ** - защита личных данных
2. **ПДВ** - корректный расчет НДС
3. **Фискализация** - подготовка к кассовым аппаратам
4. **Маркировка** - поддержка кодов товаров

## 📊 Метрики и мониторинг

### 📈 Ключевые метрики:
1. **Производительность**
   - Время обработки файлов
   - Пропускная способность (товаров/минуту)
   - Использование ресурсов сервера

2. **Качество данных**
   - Процент успешных импортов
   - Количество ошибок валидации
   - Точность маппинга категорий

3. **Пользовательский опыт**
   - Время до первого импорта
   - Частота использования функций
   - Отзывы пользователей

### 🔍 Логирование:
1. **Structured Logging** (JSON)
2. **Уровни логов** (DEBUG, INFO, WARN, ERROR)
3. **Трейсинг операций** для отладки
4. **Алерты** при критических ошибках

## 💰 Бизнес-модель

### 💳 Ценообразование:
1. **Базовый план** - 50 импортов/месяц (бесплатно)
2. **Профессиональный** - неограниченно (€19/месяц)
3. **Энтерпрайз** - API интеграции (€99/месяц)

### 📈 Монетизация:
1. **Комиссия с продаж** - 2-5% с товаров
2. **Премиум функции** - AI категоризация
3. **Интеграционные услуги** - настройка под заказ

## 🚀 План развертывания

### 🌍 Инфраструктура:
1. **Redis** - система очередей
2. **MinIO** - хранение временных файлов
3. **PostgreSQL** - расширение схемы БД
4. **Nginx** - балансировка нагрузки

### 📦 Docker сервисы:
```yaml
# docker-compose.yml (дополнения)
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  import-worker:
    build: ./backend
    command: ["./import-worker"]
    depends_on:
      - redis
      - postgres
    environment:
      - REDIS_URL=redis://redis:6379
      - DATABASE_URL=postgres://...
```

## 🎉 Заключение

Данный план обеспечивает создание полноценной системы импорта товаров, адаптированной под сербский рынок. Система будет поддерживать все популярные форматы данных и интеграции с основными платформами, используемыми в Сербии.

**Ожидаемые результаты:**
- 📈 Увеличение количества товаров на платформе на 300%
- ⏱️ Сокращение времени добавления товаров с часов до минут
- 🤖 Автоматизация 80% рутинных операций по управлению каталогом
- 🎯 Привлечение крупных продавцов и B2B клиентов

**Срок реализации:** 10 недель  
**Команда:** 2-3 разработчика  
**Бюджет:** €15,000 - €25,000