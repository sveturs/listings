# 🤖 Параллельное выполнение задач с Claude

Руководство по использованию нескольких экземпляров Claude для параллельной работы.

---

## 🎯 Когда использовать

- Анализ нескольких больших модулей одновременно
- Генерация тестов для разных частей проекта
- Создание документации для разных компонентов
- Анализ логов на продакшн сервере пока работаешь локально

---

## 🚀 Быстрый старт

### Удалённое выполнение на сервере:
```bash
# Анализ системных ресурсов
ssh root@svetu.rs "claude --print 'Проанализируй состояние сервера: lscpu, free -h, df -h, docker ps. Дай рекомендации.'"

# Диагностика конкретного сервиса
ssh root@svetu.rs "claude --print 'Проверь работу PostgreSQL: подключение, размер БД, медленные запросы.'"

# Анализ логов
ssh root@svetu.rs "claude --print 'Изучи логи nginx за последний час: tail -1000 /var/log/nginx/error.log'"
```

### Локальное параллельное выполнение:
```bash
# Запуск нескольких задач в фоне
claude --print "Проанализируй Go backend в /data/hostel-booking-system/backend" > /tmp/analysis_backend.txt &
claude --print "Изучи React frontend в /data/hostel-booking-system/frontend" > /tmp/analysis_frontend.txt &
claude --print "Проверь БД в backend/migrations" > /tmp/analysis_database.txt &

# Мониторинг выполнения
jobs                    # показать активные задачи
tail -f /tmp/analysis_*.txt  # следить за прогрессом

# Ожидание завершения всех задач
wait

# Сбор результатов
cat /tmp/analysis_*.txt > /tmp/complete_analysis.txt
```

---

## 📋 Примеры использования

### 1. Генерация тестов для разных модулей:
```bash
claude --print "Создай unit тесты для Go handlers в backend/internal/handlers/" > /tmp/tests_handlers.txt &
claude --print "Создай тесты для React компонентов в frontend/src/components/" > /tmp/tests_components.txt &
claude --print "Создай интеграционные тесты для API endpoints" > /tmp/tests_integration.txt &

wait
cat /tmp/tests_*.txt > /tmp/all_tests.txt
```

### 2. Создание документации:
```bash
claude --print "Создай API документацию для всех endpoints в backend" > /tmp/docs_api.txt &
claude --print "Документируй архитектуру frontend приложения" > /tmp/docs_frontend.txt &
claude --print "Создай руководство по deployment" > /tmp/docs_deployment.txt &

wait
```

### 3. Анализ с использованием пайпов:
```bash
# Анализ логов через пайп
tail -100 /var/log/nginx/access.log | claude --print "Проанализируй эти логи nginx, найди паттерны трафика"

# Обработка результатов других команд
docker stats --no-stream | claude --print "Проанализируй использование ресурсов Docker"

# Работа с конфигурацией
cat /etc/nginx/nginx.conf | claude --print "Проверь конфигурацию nginx на оптимальность"
```

---

## 🔧 Управление процессами

### Проверка статуса:
```bash
# Показать активные фоновые задачи
jobs

# Детальная информация
jobs -l
```

### Остановка задач:
```bash
# Остановить конкретную задачу (по номеру из jobs)
kill %1

# Остановить все фоновые задачи
killall claude
```

---

## ⚙️ Автоматизация в скриптах

```bash
#!/bin/bash
# Скрипт полного анализа проекта

echo "Запуск комплексного анализа..."

# Локальный анализ кода
claude --print "Анализ backend кода" > /tmp/local_backend.out &
LOCAL_PID=$!

# Удалённый анализ сервера
ssh root@svetu.rs "claude --print 'Анализ серверных ресурсов'" > /tmp/remote_server.out &
REMOTE_PID=$!

# Анализ базы данных
claude --print "Анализ структуры БД" > /tmp/database.out &
DB_PID=$!

# Ожидание завершения
wait $LOCAL_PID
wait $REMOTE_PID
wait $DB_PID

echo "Анализ завершен, создание отчета..."

# Финальный отчет
claude --print "Создай сводный отчет на основе /tmp/local_backend.out, /tmp/remote_server.out, /tmp/database.out"
```

---

## ⚠️ Важные замечания

1. **Ограничение ресурсов** - не запускайте слишком много параллельных задач (рекомендуется max 3-5)
2. **Контекст** - каждый экземпляр Claude независим, не имеет доступа к контексту других
3. **Координация** - используйте финальный Claude для объединения результатов
4. **Логи** - всегда сохраняйте вывод в файлы для последующего анализа

---

## 📚 См. также

- [Основное руководство](CLAUDE.md)
- [Troubleshooting](CLAUDE_TROUBLESHOOTING.md)