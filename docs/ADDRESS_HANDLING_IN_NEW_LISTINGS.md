# 🗺️ Обработка адресов при создании новых объявлений

## Текущий процесс

### 1. Frontend - Ввод адреса

При создании объявления пользователь вводит адрес через компонент `LocationPicker`:

1. **Поиск по адресу** - используется Mapbox Geocoding API с параметром `language=${locale}`
2. **Выбор на карте** - обратное геокодирование координат
3. **Получение мультиязычных адресов** - система запрашивает адреса на трех языках (sr, en, ru)

Компонент сохраняет:
```typescript
{
  latitude: number,
  longitude: number,
  address: string,      // Адрес на текущем языке интерфейса
  city: string,         // Город на текущем языке
  country: string,      // Страна на текущем языке
  addressMultilingual: {
    sr: string,
    en: string,
    ru: string
  }
}
```

### 2. Frontend - Отправка на Backend

При отправке на сервер передаются только поля на текущем языке:
```javascript
{
  location: data.location?.address || '',
  city: data.location?.city || '',
  country: data.location?.country || 'Србија'
}
```

**⚠️ Проблема**: Мультиязычные адреса (`addressMultilingual`) не передаются на backend!

### 3. Backend - Сохранение и перевод

Backend автоматически переводит адреса на все языки через службу перевода:
1. Сохраняет оригинальные значения в `marketplace_listings`
2. Определяет язык оригинала
3. Переводит на другие языки через Google Translate/DeepL/Claude
4. Сохраняет переводы в таблице `translations`

## Выявленные проблемы

### 1. Язык сохранения адресов зависит от языка интерфейса

- Если пользователь создает объявление на **русском** интерфейсе → адреса сохраняются как "Белград, Сербия"
- Если на **английском** → "Belgrade, Serbia"
- Если на **сербском** → "Београд, Србија"

### 2. Автоматический перевод может быть неточным

Например, "Белград" может быть переведен на сербский как "Белград" (кириллица, но русское написание) вместо правильного "Београд".

### 3. Мультиязычные данные от Mapbox не используются

Frontend получает корректные адреса на всех языках от Mapbox, но не передает их на backend.

## Рекомендации по улучшению

### Вариант 1: Всегда сохранять на сербском (РЕКОМЕНДУЕТСЯ)

**Изменения в Frontend:**
```typescript
// services/listings.ts
static async createListing(data: CreateListingState): Promise<CreateListingResponse> {
  const request: CreateListingRequest = {
    // Всегда используем сербскую версию адреса
    location: data.location?.addressMultilingual?.sr || data.location?.address || '',
    city: getSerbianCity(data.location?.city) || data.location?.city || '',
    country: 'Србија',
    // ...
  };
}
```

**Преимущества:**
- Единообразие данных в БД
- Корректные сербские названия
- Соответствие местной специфике

### Вариант 2: Передавать мультиязычные адреса

**Изменения в Frontend:**
```typescript
const request: CreateListingRequest = {
  location: data.location?.address || '',
  city: data.location?.city || '',
  country: data.location?.country || 'Србија',
  // Новое поле
  address_translations: {
    sr: data.location?.addressMultilingual?.sr,
    en: data.location?.addressMultilingual?.en,
    ru: data.location?.addressMultilingual?.ru
  }
};
```

**Изменения в Backend:**
- Принимать `address_translations` в CreateListing
- Сохранять готовые переводы вместо автоматического перевода

**Преимущества:**
- Точные переводы от Mapbox
- Экономия на вызовах служб перевода
- Гарантированная корректность названий

### Вариант 3: Хардкод для сербских городов

Создать маппинг основных городов Сербии:
```go
var serbianCities = map[string]string{
  "Belgrade": "Београд",
  "Белград": "Београд",
  "Novi Sad": "Нови Сад",
  "Новый Сад": "Нови Сад",
  // ...
}
```

## Рекомендуемое решение

**Комбинация вариантов 1 и 2:**

1. Frontend всегда отправляет адреса на сербском языке (из `addressMultilingual.sr`)
2. Frontend также передает все мультиязычные версии
3. Backend сохраняет оригинал на сербском
4. Backend использует готовые переводы вместо автоматического перевода

Это обеспечит:
- ✅ Корректные сербские адреса в БД
- ✅ Точные переводы на все языки
- ✅ Единообразие данных
- ✅ Экономию ресурсов на переводах