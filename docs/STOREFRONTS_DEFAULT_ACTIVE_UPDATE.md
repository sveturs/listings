# 📊 Обновление дефолтного статуса витрин

## 📅 Дата: 16.09.2025

## ✅ Выполненные изменения

### 1. Изменено дефолтное значение поля `is_active`
- **Было**: `false` - витрины создавались неактивными по умолчанию
- **Стало**: `true` - витрины теперь активны сразу после создания
- **Миграция**: `000026_storefronts_active_by_default.sql`

### 2. Преимущества изменения
- ✅ Новые витрины сразу видны покупателям
- ✅ Не требуется дополнительная активация администратором
- ✅ Улучшен user experience для продавцов
- ✅ Ускорен процесс начала торговли

### 3. Текущее состояние
- Все 7 существующих витрин активны
- Новые витрины будут создаваться активными автоматически
- При необходимости витрину можно деактивировать вручную

## 📝 Техническая информация

### SQL изменение
```sql
ALTER TABLE storefronts
ALTER COLUMN is_active SET DEFAULT true;
```

### Откат (если потребуется)
```bash
cd /data/hostel-booking-system/backend
./migrator down 1
```

## 🎯 Результат

Теперь при создании новой витрины через API или интерфейс администратора, она автоматически будет активной и сразу доступной для покупателей на маркетплейсе.