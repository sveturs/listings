# 📋 Аудит ТЗ интеграции Post Express - Отчет о соответствии

## 📅 Дата аудита: 15.08.2025

## 🎯 Резюме

ТЗ на интеграцию Post Express **ТРЕБУЕТ СУЩЕСТВЕННОЙ АДАПТАЦИИ** под текущую архитектуру проекта Sve Tu. Документ хорошо проработан, но содержит несоответствия с реальной структурой проекта.

## ✅ Положительные аспекты ТЗ

1. **Детальная проработка API интеграции** - полное описание WSP Web API
2. **Комплексный подход** - охватывает C2C и B2C сценарии
3. **Готовые примеры кода** - можно использовать как основу
4. **Проработанные сценарии использования** - понятная бизнес-логика
5. **План миграций БД** - структурированный подход к изменениям

## ❌ Критические несоответствия

### 1. База данных

#### ❌ Отсутствующие таблицы в ТЗ:
- `post_express_settings` - не существует
- `post_express_shipments` - не существует  
- `post_express_settlements` - не существует
- `post_express_api_log` - не существует
- `storefront_post_express_settings` - не существует

#### ✅ Существующие таблицы:
- `marketplace_orders` - есть, структура соответствует
- `storefront_orders` - есть, структура соответствует
- `storefront_delivery_options` - есть, можно расширить для Post Express

#### 📌 Рекомендация:
Использовать существующую таблицу `storefront_delivery_options` с полем `provider='post-express'` и настройками в `provider_config` (JSONB).

### 2. Backend архитектура

#### ❌ Несоответствия структуры:
ТЗ предлагает:
```
backend/internal/proj/delivery/postexpress/
```

Реальная структура НЕ содержит модуля `delivery`. 

#### 📌 Рекомендация:
Создать модуль в соответствии с существующей архитектурой:
```
backend/internal/proj/postexpress/
├── handler/
│   ├── handler.go
│   ├── routes.go
│   └── responses.go
├── service/
│   ├── interface.go
│   ├── service.go
│   ├── client.go
│   └── tracking.go
├── storage/
│   ├── interface.go
│   └── postgres/
│       └── repository.go
└── module.go
```

### 3. Frontend архитектура

#### ✅ Соответствия:
- Next.js 15.3.2 - используется
- TypeScript - используется
- Tailwind CSS + DaisyUI - используется
- Redux Toolkit - используется (НЕ Zustand как упомянуто в ТЗ)
- next-intl - используется для локализации

#### ❌ Несоответствия:
ТЗ использует:
- `@heroicons/react` - проект использует `lucide-react`
- Inline стили Tailwind - проект использует DaisyUI классы

#### 📌 Рекомендация:
Адаптировать компоненты под:
- Использовать `lucide-react` вместо `@heroicons/react`
- Использовать DaisyUI классы: `btn`, `input`, `card`, `alert` и т.д.

### 4. API структура

#### ❌ Несоответствия путей:
ТЗ предлагает: `/api/v1/delivery/post-express/`

#### 📌 Рекомендация:
Использовать: `/api/v1/postexpress/` (соответствует паттерну проекта)

### 5. Система переводов

#### ✅ Соответствия:
- Модульная система переводов - используется
- Поддержка ru/en/sr локалей - есть (ТЗ упоминает только ru)

#### ❌ Несоответствия:
ТЗ не учитывает модульную систему переводов.

#### 📌 Рекомендация:
Создать модуль переводов:
```
frontend/svetu/src/messages/{ru,en,sr}/delivery.json
```

### 6. Swagger аннотации

#### ✅ Соответствия:
- Использование goswag - соответствует
- Структура ответов через utils.SuccessResponseSwag - правильно

#### 📌 Рекомендация:
После реализации обязательно запускать `make generate-types`

## 🔧 План адаптации ТЗ

### Фаза 1: Миграции БД (приоритет: ВЫСОКИЙ)
```sql
-- Использовать существующие таблицы с расширением
ALTER TABLE storefront_delivery_options 
ADD COLUMN IF NOT EXISTS post_express_config JSONB;

-- Создать только необходимые новые таблицы
CREATE TABLE post_express_shipments (...);
CREATE TABLE post_express_settlements (...);
```

### Фаза 2: Backend модуль (приоритет: ВЫСОКИЙ)
1. Создать модуль `/backend/internal/proj/postexpress/`
2. Адаптировать код из ТЗ под реальную структуру
3. Интегрировать с существующими сервисами (payments, orders, storefronts)

### Фаза 3: Frontend компоненты (приоритет: СРЕДНИЙ)
1. Адаптировать компоненты под DaisyUI
2. Заменить иконки на lucide-react
3. Создать модуль переводов delivery.json

### Фаза 4: Интеграция (приоритет: СРЕДНИЙ)
1. Подключить к существующему checkout процессу
2. Расширить storefront настройки
3. Интегрировать с системой уведомлений

## 📊 Оценка трудозатрат

| Компонент | План в ТЗ | С учетом адаптации | Дельта |
|-----------|-----------|-------------------|--------|
| Миграции БД | 1 день | 2 дня | +1 день |
| Backend | 10 дней | 12 дней | +2 дня |
| Frontend | 10 дней | 8 дней | -2 дня |
| Тестирование | 5 дней | 5 дней | 0 |
| **ИТОГО** | **26 дней** | **27 дней** | **+1 день** |

## ✨ Позитивные находки

1. **storefront_delivery_options** - готовая таблица для интеграции
2. **provider_config (JSONB)** - можно хранить все настройки Post Express
3. **Redux Toolkit** - упростит управление состоянием доставки
4. **Модульная система переводов** - легко добавить новый модуль

## ⚠️ Риски

1. **Отсутствие модуля delivery** - потребует создания с нуля
2. **Различие в UI библиотеках** - нужна переработка компонентов
3. **Отсутствие COD в текущей системе** - потребует доработки payments

## 📝 Рекомендации

### Немедленные действия:
1. ✅ Согласовать структуру модуля с командой
2. ✅ Определить использование существующих таблиц vs новых
3. ✅ Уточнить интеграцию с платежной системой для COD

### Корректировки ТЗ:
1. 📝 Переписать пути API под `/api/v1/postexpress/`
2. 📝 Адаптировать Frontend компоненты под DaisyUI
3. 📝 Добавить sr локаль в переводы
4. 📝 Использовать существующие таблицы где возможно

### Оптимизации:
1. 💡 Использовать `storefront_delivery_options` вместо новой таблицы
2. 💡 Интегрировать с существующим orders модулем
3. 💡 Переиспользовать компоненты из storefronts

## 🎯 Заключение

ТЗ является **ХОРОШЕЙ ОСНОВОЙ** для реализации, но требует адаптации под реальную архитектуру проекта. Основные изменения касаются:

1. Структуры БД - использование существующих таблиц
2. Backend архитектуры - создание модуля по паттерну проекта  
3. Frontend компонентов - адаптация под DaisyUI и lucide-react
4. Системы переводов - создание модульных файлов

**Рекомендую УТВЕРДИТЬ ТЗ с учетом предложенных корректировок.**

---
*Аудит выполнен: 15.08.2025*
*Статус: Требует адаптации*
*Готовность к реализации: 75%*