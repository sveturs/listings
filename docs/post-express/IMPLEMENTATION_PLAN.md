# 📋 ПЛАН ДОРАБОТОК POST EXPRESS ИНТЕГРАЦИИ
**Дата:** 2025-09-09  
**Приоритет:** 🔴 ВЫСОКИЙ  
**Оценка времени:** 2-3 дня

## 🎯 ЦЕЛЬ
Полностью запустить интеграцию с Post Express WSP API для автоматического создания и отслеживания посылок.

## 📊 ТЕКУЩЕЕ СОСТОЯНИЕ
- ✅ API доступен и отвечает
- ✅ Учетные данные b2b@svetu.rs активны
- ⚠️ Backend код содержит критические ошибки
- ⚠️ Отсутствуют обязательные поля для создания посылок
- ✅ Frontend готов к использованию

---

## 🔧 ФАЗА 1: ИСПРАВЛЕНИЕ BACKEND КОДА (День 1)

### 1.1 Исправить структуру TransakcijaIn
**Файл:** `/backend/internal/proj/postexpress/models/transaction.go`

```go
// Исправить опечатку в названии поля
type TransakcijaIn struct {
    StrKlijent         string `json:"StrKlijent"`
    Servis             int    `json:"Servis"`
    IdVrstaTransakcije int    `json:"IdVrstaTranskacije"` // Правильное написание!
    TipSerijalizacije  int    `json:"TipSerijalizacije"`
    IdTransakcija      string `json:"IdTransakcija"`
    StrIn              string `json:"StrIn,omitempty"`
}
```

### 1.2 Обновить структуру Klijent
**Файл:** `/backend/internal/proj/postexpress/models/client.go`

```go
type Klijent struct {
    Username          string  `json:"Username"`
    Password          string  `json:"Password"`
    Jezik             string  `json:"Jezik"`
    IdTipUredjaja     string  `json:"IdTipUredjaja"` // Изменить на string!
    VerzijaOS         string  `json:"VerzijaOS"`
    NazivUredjaja     string  `json:"NazivUredjaja"`
    ModelUredjaja     string  `json:"ModelUredjaja"`
    VerzijaAplikacije string  `json:"VerzijaAplikacije"`
    IPAdresa          string  `json:"IPAdresa"`
    Geolokacija       *string `json:"Geolokacija"`
}
```

### 1.3 Исправить параметры в client.go
**Файл:** `/backend/internal/proj/postexpress/service/client.go`

```go
// В методе Transaction
transReq := &models.TransakcijaIn{
    StrKlijent:         string(clientJSON),
    Servis:             101, // Изменить с 3 на 101
    IdVrstaTransakcije: req.TransactionType,
    TipSerijalizacije:  2,   // 2 для JSON
    IdTransakcija:      models.GenerateGUID(),
    StrIn:              string(dataJSON),
}

// В структуре ClientData
clientData := &models.ClientData{
    Username:          c.config.Username,
    Password:          c.config.Password,
    Jezik:             c.config.Language,
    IdTipUredjaja:     "2",  // String, не int!
    // ...
}
```

### 1.4 Обновить конфигурацию
**Файл:** `/backend/internal/config/config.go`

```go
// Добавить поле PartnerID
type PostExpressConfig struct {
    BaseURL       string
    Username      string
    Password      string
    PartnerID     int    // Добавить: 10109
    Brand         string // Добавить: SVETU
    Store         string // Добавить: SVETU
    TestMode      bool
    SenderName    string
    SenderAddress string
}
```

---

## 📦 ФАЗА 2: ДОБАВЛЕНИЕ ОБЯЗАТЕЛЬНЫХ ПОЛЕЙ (День 1-2)

### 2.1 Обновить создание манифеста
**Файл:** `/backend/internal/proj/postexpress/service/manifest.go`

#### Добавить проверку типа доставки:
```go
func (s *Service) CreateManifest(ctx context.Context, orders []models.Order) error {
    for _, order := range orders {
        for _, shipment := range order.Shipments {
            // Если курьерская доставка
            if shipment.NacinPrijema == "K" {
                // Добавить PNA услугу
                if !strings.Contains(shipment.PosebneUsluge, "PNA") {
                    shipment.PosebneUsluge = "PNA," + shipment.PosebneUsluge
                }
                
                // Установить MestoPreuzimanja
                if shipment.MestoPreuzimanja == nil {
                    shipment.MestoPreuzimanja = &models.Korisnik{
                        // Копировать данные отправителя
                        ...shipment.Posiljalac,
                    }
                }
            }
            
            // Для физических лиц
            if shipment.Primalac.Vrsta == "F" {
                if shipment.Primalac.Naziv == "" {
                    shipment.Primalac.Naziv = fmt.Sprintf("%s %s", 
                        shipment.Primalac.Prezime, 
                        shipment.Primalac.Ime)
                }
            }
            
            // Для ценных посылок
            if shipment.Vrednost > 0 {
                if !strings.Contains(shipment.PosebneUsluge, "VD") {
                    shipment.PosebneUsluge = "VD," + shipment.PosebneUsluge
                }
            }
        }
    }
}
```

### 2.2 Добавить валидацию
**Файл:** `/backend/internal/proj/postexpress/service/validation.go`

```go
func ValidateShipment(shipment *models.Posiljka) error {
    // Проверка обязательных полей
    if shipment.NacinPrijema == "K" {
        if !strings.Contains(shipment.PosebneUsluge, "PNA") {
            return errors.New("PNA услуга обязательна для курьерской доставки")
        }
        if shipment.MestoPreuzimanja == nil {
            return errors.New("MestoPreuzimanja обязательно для курьерской доставки")
        }
    }
    
    if shipment.Primalac.Vrsta == "F" && shipment.Primalac.Naziv == "" {
        return errors.New("Naziv обязателен для физических лиц")
    }
    
    if shipment.Vrednost > 0 && !strings.Contains(shipment.PosebneUsluge, "VD") {
        return errors.New("VD услуга обязательна для ценных посылок")
    }
    
    return nil
}
```

---

## 🧪 ФАЗА 3: ТЕСТИРОВАНИЕ (День 2)

### 3.1 Создать интеграционные тесты
**Файл:** `/backend/internal/proj/postexpress/service/client_test.go`

```go
func TestCreateManifest(t *testing.T) {
    // Тест создания манифеста с курьерской доставкой
    // Тест создания манифеста для физического лица
    // Тест создания ценной посылки
}

func TestTracking(t *testing.T) {
    // Тест отслеживания существующей посылки
    // Тест отслеживания несуществующей посылки
}
```

### 3.2 Создать E2E тест
**Файл:** `/backend/scripts/test_post_express_e2e.go`

Полный цикл:
1. Создание манифеста
2. Получение номера посылки
3. Отслеживание статуса
4. Проверка всех полей

---

## 🚀 ФАЗА 4: РАЗВЕРТЫВАНИЕ (День 3)

### 4.1 Обновить переменные окружения
```env
# Production
POST_EXPRESS_WSP_USERNAME=b2b@svetu.rs
POST_EXPRESS_WSP_PASSWORD=Sv5et@U!
POST_EXPRESS_WSP_ENDPOINT=http://212.62.32.201/WspWebApi/transakcija
POST_EXPRESS_WSP_PARTNER_ID=10109
POST_EXPRESS_WSP_BRAND=SVETU
POST_EXPRESS_WSP_STORE=SVETU
POST_EXPRESS_TEST_MODE=false
```

### 4.2 Миграция данных
- Обновить существующие заказы с новыми полями
- Добавить недостающие услуги

### 4.3 Мониторинг
- Настроить логирование всех запросов к API
- Добавить метрики успешности
- Настроить алерты на ошибки

---

## 📈 МЕТРИКИ УСПЕХА

| Метрика | Цель | Как измерить |
|---------|------|--------------|
| Успешность создания манифестов | > 95% | Количество успешных / общее количество |
| Время ответа API | < 2 сек | Среднее время ответа |
| Корректность отслеживания | 100% | Все статусы обновляются |
| Ошибки валидации | < 5% | Количество ошибок / общее количество |

---

## ⚠️ РИСКИ И МИТИГАЦИЯ

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| API недоступен | Низкая | Высокое | Retry механизм, очередь |
| Изменение формата API | Низкая | Высокое | Версионирование, тесты |
| Ошибки валидации | Средняя | Среднее | Детальная валидация на frontend |
| Превышение лимитов | Низкая | Среднее | Rate limiting, батчинг |

---

## 📝 ЧЕКЛИСТ ГОТОВНОСТИ

### Backend:
- [ ] Исправлена структура TransakcijaIn
- [ ] Обновлена структура Klijent (IdTipUredjaja как string)
- [ ] Изменены параметры (Servis=101, TipSerijalizacije=2)
- [ ] Добавлены обязательные поля для манифеста
- [ ] Реализована валидация
- [ ] Написаны unit тесты
- [ ] Проведены интеграционные тесты

### Конфигурация:
- [ ] Обновлен .env с новыми учетными данными
- [ ] Добавлены новые параметры (PartnerID, Brand, Store)
- [ ] Настроен logging

### Документация:
- [ ] Обновлена техническая документация
- [ ] Создана инструкция для операторов
- [ ] Документированы все API методы

### Production:
- [ ] Развернуто на staging
- [ ] Проведено нагрузочное тестирование
- [ ] Настроен мониторинг
- [ ] Подготовлен rollback план

---

## 👥 ОТВЕТСТВЕННЫЕ

| Задача | Ответственный | Срок |
|--------|---------------|------|
| Backend доработки | Backend разработчик | День 1 |
| Тестирование | QA инженер | День 2 |
| Развертывание | DevOps | День 3 |
| Документация | Техписатель | День 2-3 |

---

## 📅 TIMELINE

```
День 1 (10.01.2025):
├── 09:00 - Исправление структур данных
├── 11:00 - Обновление client.go
├── 14:00 - Добавление обязательных полей
└── 16:00 - Unit тесты

День 2 (11.01.2025):
├── 09:00 - Интеграционные тесты
├── 11:00 - E2E тестирование
├── 14:00 - Исправление найденных багов
└── 16:00 - Документация

День 3 (12.01.2025):
├── 09:00 - Развертывание на staging
├── 11:00 - Тестирование на staging
├── 14:00 - Развертывание на production
└── 16:00 - Мониторинг и поддержка
```

---

**После выполнения всех пунктов плана интеграция Post Express будет полностью готова к production использованию.**