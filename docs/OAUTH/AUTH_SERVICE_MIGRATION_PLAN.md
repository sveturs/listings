# 🔄 План миграции на Auth Service микросервис

## 📋 Резюме
Документ описывает пошаговый план миграции с монолитной аутентификации на новый Auth Service микросервис.

## 🎯 Текущее состояние (Обновлено: 06.09.2025)

### Монолит (hostel-booking-system)
- **Аутентификация**: встроена в backend на порту 3000
- **Эндпоинты**: `/api/v1/auth/*` ✅ ПРОКСИРУЮТСЯ к Auth Service
- **Google OAuth**: `/auth/google`, `/auth/google/callback` ⚠️ Требует настройки
- **JWT**: собственная реализация в `pkg/jwt` (еще используется)
- **Middleware**: `AuthRequiredJWT` в `internal/middleware` + ✅ новый `AuthProxyMiddleware`
- **База данных**: общая PostgreSQL на порту 5432

### Auth Service микросервис
- **HTTP API**: порт 28080 ✅ РАБОТАЕТ
- **gRPC**: порт 50051 ✅ РАБОТАЕТ
- **Health check**: http://localhost:28080/health ✅ РАБОТАЕТ
- **База данных**: отдельная PostgreSQL на порту 25432 ✅ РАБОТАЕТ
- **Redis**: порт 26379 для кеша сессий ✅ РАБОТАЕТ
- **Статус**: ✅ ЗАПУЩЕН И ИНТЕГРИРОВАН

## 🔄 План миграции

### Фаза 1: Подготовка инфраструктуры ✅ ВЫПОЛНЕНО

#### 1.1 Запуск Auth Service ✅
```bash
cd /data/auth_svetu
docker-compose up -d
```

#### 1.2 Проверка здоровья сервиса ✅
```bash
curl http://localhost:28080/health  # Работает!
curl http://localhost:28080/api/v1/version  # Работает!
```

#### 1.3 Настройка переменных окружения ✅
- ✅ Скопирован `.env.example` в `.env`
- ✅ Настроены Google OAuth credentials
- ✅ Сгенерированы JWT ключи RS256

### Фаза 2: Интеграция Backend ✅ ВЫПОЛНЕНО

#### 2.1 Создание Auth Client в монолите ✅
```go
// backend/internal/service/authclient/client.go - СОЗДАН!
type Client struct {
    httpClient *http.Client
    baseURL    string
}
```

#### 2.2 Модификация Middleware ✅
- ✅ Создан новый `AuthProxyMiddleware` в `backend/internal/middleware/auth_proxy.go`
- ✅ Подключен в `server.go` для проксирования запросов
- ⏳ gRPC валидация пока не реализована
- ⏳ Кеширование в Redis пока не настроено

#### 2.3 Проксирование Auth эндпоинтов ✅
```go
// Настроено проксирование через USE_AUTH_SERVICE=true
/api/v1/auth/login     -> http://localhost:28080/api/v1/auth/login ✅
/api/v1/auth/register  -> http://localhost:28080/api/v1/auth/register ✅
/api/v1/auth/oauth/google -> http://localhost:28080/api/v1/auth/oauth/google ⚠️ 501
/api/v1/auth/oauth/google/callback -> http://localhost:28080/api/v1/auth/oauth/google/callback ⚠️
```

### Фаза 3: Обновление Frontend ✅ ВЫПОЛНЕНО

#### 3.1 Обновление API клиента ✅
```typescript
// frontend/svetu/src/constants/api-endpoints.ts - ОБНОВЛЕН!
auth: {
  login: '/api/v1/auth/login',  ✅
  register: '/api/v1/auth/register',  ✅
  logout: '/api/v1/auth/logout',  ✅
  refresh: '/api/v1/auth/refresh',  ✅
  google: '/api/v1/auth/oauth/google',  ✅
}
```

#### 3.2 Обновление OAuth flows ✅
- ✅ Google OAuth redirect обновлен: `/api/v1/auth/oauth/google`
- ⚠️ Callback handling требует настройки на стороне Auth Service

#### 3.3 Обновление хранилища токенов ✅
- ✅ Access token сохраняется в sessionStorage
- ✅ TokenManager уже использует правильный endpoint для refresh
- ✅ Автоматическое обновление токенов настроено

### Фаза 4: Настройка Nginx (День 6)

#### 4.1 Проксирование Auth Service
```nginx
# /etc/nginx/sites-available/svetu
location /api/v1/auth/ {
    proxy_pass http://localhost:28080/api/v1/auth/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

#### 4.2 Rate limiting
```nginx
limit_req_zone $binary_remote_addr zone=auth:10m rate=5r/s;

location /api/v1/auth/login {
    limit_req zone=auth burst=5 nodelay;
    proxy_pass http://localhost:28080/api/v1/auth/login;
}
```

### Фаза 5: Миграция данных (День 7)

#### 5.1 Экспорт пользователей из монолита
```sql
-- Экспорт пользователей
COPY (
    SELECT id, email, password_hash, name, created_at, updated_at
    FROM users
) TO '/tmp/users_export.csv' CSV HEADER;
```

#### 5.2 Импорт в Auth Service БД
```sql
-- Импорт в auth_db
COPY users(id, email, password_hash, name, created_at, updated_at)
FROM '/tmp/users_export.csv' CSV HEADER;
```

#### 5.3 Синхронизация OAuth связей
- Мигрировать Google OAuth user_id маппинги
- Обновить refresh tokens

### Фаза 6: Тестирование (День 8-9)

#### 6.1 Функциональные тесты
- [ ] Регистрация нового пользователя
- [ ] Вход по email/password
- [ ] Google OAuth flow
- [ ] Обновление токенов
- [ ] Выход из системы
- [ ] Валидация токенов в защищенных эндпоинтах

#### 6.2 Нагрузочное тестирование
```bash
# Тест валидации токенов (10k RPS)
ab -n 100000 -c 100 -H "Authorization: Bearer TOKEN" \
   http://localhost:28080/api/v1/auth/validate
```

#### 6.3 Тестирование отказоустойчивости
- Остановка Auth Service -> fallback на кеш
- Перезапуск PostgreSQL
- Перезапуск Redis

### Фаза 7: Переключение (День 10)

#### 7.1 Канареечное развертывание
- 10% трафика -> Auth Service
- Мониторинг метрик и ошибок
- Постепенное увеличение до 100%

#### 7.2 Полное переключение
- Обновление DNS/Load Balancer
- Переключение всего трафика на Auth Service
- Мониторинг в течение 24 часов

#### 7.3 Откат (при необходимости)
```bash
# Быстрый откат на монолит
nginx -s reload  # с старой конфигурацией
```

## 📊 Метрики успеха

- ✅ 99.99% доступность Auth Service
- ✅ < 50ms латентность валидации токенов
- ✅ 10,000+ RPS для валидации
- ✅ 0% потерь пользовательских сессий
- ✅ Успешная миграция всех пользователей

## ⚠️ Риски и митигация

| Риск | Вероятность | Воздействие | Митигация |
|------|-------------|-------------|-----------|
| Потеря данных пользователей | Низкая | Критическое | Бэкапы перед миграцией |
| Недоступность Auth Service | Средняя | Высокое | Fallback на кеш, мониторинг |
| Проблемы с OAuth | Средняя | Среднее | Тестирование в staging |
| Высокая латентность | Низкая | Среднее | Кеширование, оптимизация |

## 🔄 Rollback план

1. **Немедленный откат Nginx конфигурации**
```bash
cp /etc/nginx/sites-available/svetu.backup /etc/nginx/sites-available/svetu
nginx -s reload
```

2. **Переключение backend на старые handlers**
```bash
# В backend .env
USE_AUTH_SERVICE=false
```

3. **Восстановление данных** (если были изменения)
```bash
pg_restore -d svetubd /backup/pre_migration.dump
```

## 📅 Timeline

| Фаза | Дни | Статус |
|------|-----|--------|
| Подготовка инфраструктуры | 1 | ⏳ Ready |
| Интеграция Backend | 2-3 | ⏳ Ready |
| Обновление Frontend | 4-5 | ⏳ Ready |
| Настройка Nginx | 6 | ⏳ Ready |
| Миграция данных | 7 | ⏳ Ready |
| Тестирование | 8-9 | ⏳ Ready |
| Переключение | 10 | ⏳ Ready |

**Общее время**: 10 дней

## 🚀 Команда для начала миграции

```bash
# Шаг 1: Запустить Auth Service
cd /data/auth_svetu && docker-compose up -d

# Шаг 2: Проверить здоровье
curl http://localhost:28081/health

# Шаг 3: Начать интеграцию
cd /data/hostel-booking-system
git checkout -b feature/auth-service-integration
```

## 📝 Чеклист готовности

- [ ] Auth Service развернут и работает
- [ ] JWT ключи сгенерированы
- [ ] Google OAuth настроен
- [ ] Бэкапы БД созданы
- [ ] Мониторинг настроен
- [ ] План отката подготовлен
- [ ] Команда проинформирована

---

**Документ подготовлен**: 06.09.2025  
**Версия**: 1.0.0  
**Статус**: Готов к выполнению