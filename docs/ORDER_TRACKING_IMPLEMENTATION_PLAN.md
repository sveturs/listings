# 📦 ПЛАН ВНЕДРЕНИЯ СИСТЕМЫ ОТСЛЕЖИВАНИЯ ЗАКАЗОВ
## Дата: 2025-09-16

---

## 🎯 ЦЕЛЬ ПРОЕКТА

Создать универсальную систему отслеживания заказов, которая:
- Интегрируется с множественными службами доставки (Post Express, BEX Express)
- Предоставляет real-time обновления статусов
- Поддерживает уведомления через email, SMS, push, Telegram
- Обеспечивает единый интерфейс для покупателей и продавцов

---

## 📊 ТЕКУЩЕЕ СОСТОЯНИЕ

### Существующие таблицы заказов:
1. **marketplace_orders** - основные заказы маркетплейса
   - Базовые поля для доставки есть
   - Нет связи с системами доставки

2. **storefront_orders** - заказы витрин
   - Отдельная система заказов
   - Требует унификации

3. **post_express_shipments** - отправления Post Express
   - Готова, но не связана с заказами

### Готовая инфраструктура:
- ✅ Post Express API (ждет активации)
- ✅ BEX Express модуль
- ✅ Система уведомлений (email)
- ⚠️ Telegram bot (частично)
- ❌ SMS провайдер
- ❌ Push уведомления

---

## 🏗️ АРХИТЕКТУРА РЕШЕНИЯ

### 1. База данных - новые таблицы

```sql
-- Универсальная таблица отслеживания
CREATE TABLE order_tracking (
    id SERIAL PRIMARY KEY,
    order_id INT NOT NULL,
    order_type VARCHAR(50) NOT NULL, -- 'marketplace', 'storefront'
    provider VARCHAR(50) NOT NULL, -- 'post_express', 'bex_express', 'manual'
    provider_tracking_id VARCHAR(255),
    status VARCHAR(50) NOT NULL,
    status_details JSONB,
    location VARCHAR(255),
    estimated_delivery TIMESTAMP,
    actual_delivery TIMESTAMP,
    last_check_at TIMESTAMP,
    next_check_at TIMESTAMP,
    check_attempts INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- События отслеживания
CREATE TABLE tracking_events (
    id SERIAL PRIMARY KEY,
    tracking_id INT REFERENCES order_tracking(id),
    event_type VARCHAR(50),
    event_description TEXT,
    event_location VARCHAR(255),
    event_data JSONB,
    provider_event_id VARCHAR(255),
    occurred_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Подписки на уведомления
CREATE TABLE tracking_subscriptions (
    id SERIAL PRIMARY KEY,
    tracking_id INT REFERENCES order_tracking(id),
    user_id INT REFERENCES users(id),
    channel VARCHAR(50), -- 'email', 'sms', 'push', 'telegram'
    channel_address VARCHAR(255), -- email, phone, device_id, chat_id
    events VARCHAR[], -- ['shipped', 'in_transit', 'delivered', 'failed']
    active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Настройки провайдеров доставки
CREATE TABLE delivery_providers (
    id SERIAL PRIMARY KEY,
    code VARCHAR(50) UNIQUE,
    name VARCHAR(100),
    api_endpoint VARCHAR(255),
    credentials JSONB, -- зашифровано
    features JSONB, -- {tracking: true, labels: true, pickup: false}
    active BOOLEAN DEFAULT TRUE,
    priority INT DEFAULT 100,
    created_at TIMESTAMP DEFAULT NOW()
);
```

### 2. Backend архитектура

```
backend/internal/proj/tracking/
├── models/
│   ├── tracking.go      # Модели отслеживания
│   ├── event.go         # События
│   └── subscription.go  # Подписки
├── service/
│   ├── tracker.go       # Основной сервис
│   ├── providers/       # Адаптеры провайдеров
│   │   ├── interface.go
│   │   ├── postexpress.go
│   │   ├── bexexpress.go
│   │   └── manual.go
│   ├── notifier.go     # Сервис уведомлений
│   └── scheduler.go     # Планировщик проверок
├── handler/
│   └── handler.go       # HTTP endpoints
└── storage/
    └── postgres/
        └── repository.go
```

### 3. API Endpoints

```yaml
# Создание отслеживания
POST /api/v1/tracking
{
  "order_id": 123,
  "order_type": "marketplace",
  "provider": "post_express",
  "provider_tracking_id": "PE123456"
}

# Получение статуса
GET /api/v1/tracking/{id}
Response: {
  "status": "in_transit",
  "location": "Novi Sad",
  "estimated_delivery": "2025-09-18",
  "events": [...]
}

# Подписка на уведомления
POST /api/v1/tracking/{id}/subscribe
{
  "channels": ["email", "telegram"],
  "events": ["delivered", "failed"]
}

# Webhook для провайдеров
POST /api/v1/tracking/webhook/{provider}
```

### 4. Frontend компоненты

```typescript
// Компонент отслеживания
components/tracking/
├── TrackingWidget.tsx      // Виджет статуса
├── TrackingTimeline.tsx    // Timeline событий
├── TrackingMap.tsx         // Карта местоположения
├── TrackingSubscribe.tsx   // Управление подписками
└── TrackingProvider.tsx    // Context provider

// Страницы
pages/
├── orders/[id]/tracking    // Страница отслеживания
└── tracking/[code]         // Публичное отслеживание
```

---

## 📅 ПЛАН РЕАЛИЗАЦИИ

### ФАЗА 1: Базовая инфраструктура (Неделя 1-2)

#### День 1-2: База данных
- [ ] Создать миграции для новых таблиц
- [ ] Индексы и constraints
- [ ] Связать с существующими таблицами заказов

#### День 3-4: Backend сервис
- [ ] Базовые модели и интерфейсы
- [ ] Repository слой
- [ ] Основной tracking service

#### День 5-7: Провайдеры
- [ ] Адаптер Post Express
- [ ] Адаптер BEX Express
- [ ] Manual tracking (для самовывоза)

### ФАЗА 2: Автоматизация (Неделя 3)

#### День 8-9: Scheduler
- [ ] Cron jobs для проверки статусов
- [ ] Retry логика
- [ ] Rate limiting

#### День 10-11: Webhooks
- [ ] Endpoints для провайдеров
- [ ] Валидация и безопасность
- [ ] Обработка событий

#### День 12-14: Интеграция с заказами
- [ ] Auto-create tracking при создании заказа
- [ ] Синхронизация статусов
- [ ] Миграция старых заказов

### ФАЗА 3: Уведомления (Неделя 4)

#### День 15-16: Email уведомления
- [ ] Шаблоны писем
- [ ] Локализация (ru/sr/en)
- [ ] Тестирование

#### День 17-18: Telegram
- [ ] Bot команды для отслеживания
- [ ] Push уведомления
- [ ] Подписки

#### День 19-21: SMS (опционально)
- [ ] Интеграция с SMS провайдером
- [ ] Шаблоны сообщений
- [ ] Управление стоимостью

### ФАЗА 4: Frontend (Неделя 5-6)

#### День 22-24: Компоненты
- [ ] TrackingWidget
- [ ] TrackingTimeline
- [ ] TrackingMap

#### День 25-27: Страницы
- [ ] Страница отслеживания заказа
- [ ] Публичная страница по коду
- [ ] Интеграция в личный кабинет

#### День 28-30: UX улучшения
- [ ] Real-time обновления (WebSocket)
- [ ] Progressive enhancement
- [ ] Mobile optimization

### ФАЗА 5: Тестирование и оптимизация (Неделя 7)

#### День 31-33: Тестирование
- [ ] Unit тесты
- [ ] Integration тесты
- [ ] E2E тесты

#### День 34-35: Производительность
- [ ] Оптимизация запросов
- [ ] Кэширование
- [ ] Мониторинг

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Polling стратегия:
```go
// Частота проверок в зависимости от статуса
var checkIntervals = map[string]time.Duration{
    "created":     30 * time.Minute,
    "shipped":     1 * time.Hour,
    "in_transit":  2 * time.Hour,
    "out_for_delivery": 30 * time.Minute,
    "delivered":   0, // Не проверяем
    "failed":      0, // Не проверяем
}

// Экспоненциальный backoff при ошибках
backoff := time.Duration(math.Pow(2, float64(attempts))) * time.Minute
```

### Кэширование:
```go
// Redis кэш для частых запросов
cache := redis.NewClient(&redis.Options{
    Addr: "localhost:6379",
})

// TTL в зависимости от статуса
ttl := map[string]time.Duration{
    "delivered": 24 * time.Hour,
    "failed":    24 * time.Hour,
    "default":   5 * time.Minute,
}
```

### WebSocket для real-time:
```javascript
// Frontend WebSocket подключение
const ws = new WebSocket('wss://api.svetu.rs/tracking/ws');

ws.on('message', (data) => {
  const event = JSON.parse(data);
  updateTrackingStatus(event);
});
```

---

## 📊 МЕТРИКИ УСПЕХА

### KPIs:
1. **Точность отслеживания**: >95% правильных статусов
2. **Скорость обновления**: <5 минут от события до UI
3. **Доставляемость уведомлений**: >98%
4. **Пользовательская активность**: >60% проверяют статус
5. **Поддержка провайдеров**: минимум 2 активных

### Мониторинг:
- Grafana dashboard для метрик
- Sentry для ошибок
- Prometheus для системных метрик

---

## 🚀 MVP ФУНКЦИОНАЛ

### Минимальный набор для запуска:
1. ✅ Отслеживание через один провайдер (Post Express)
2. ✅ Email уведомления о ключевых событиях
3. ✅ Базовый UI для просмотра статуса
4. ✅ API для интеграции

### Можно отложить:
1. ⏸️ SMS уведомления
2. ⏸️ Push notifications
3. ⏸️ Карта отслеживания
4. ⏸️ Множественные провайдеры одновременно

---

## 💰 ОЦЕНКА РЕСУРСОВ

### Разработка:
- **Backend**: 80 часов
- **Frontend**: 60 часов
- **DevOps**: 20 часов
- **Тестирование**: 40 часов
- **ИТОГО**: 200 часов (~5 недель для 1 разработчика)

### Инфраструктура:
- Redis для кэша: $20/месяц
- SMS провайдер: $50/месяц (опционально)
- Дополнительная нагрузка на БД: минимальная

---

## ⚠️ РИСКИ И МИТИГАЦИЯ

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Post Express API недоступен | Высокая | Высокое | Fallback на manual tracking |
| Задержки в обновлениях | Средняя | Среднее | Увеличить частоту polling |
| Перегрузка уведомлениями | Низкая | Среднее | Rate limiting, настройки |
| Неточные данные от провайдера | Средняя | Высокое | Валидация, ручная корректировка |

---

## ✅ КРИТЕРИИ ГОТОВНОСТИ

### Definition of Done:
- [ ] Все тесты проходят
- [ ] Документация API готова
- [ ] Метрики настроены
- [ ] Security review пройден
- [ ] Load testing выполнен
- [ ] Rollback план готов

### Acceptance Criteria:
- [ ] Пользователь может отследить заказ
- [ ] Получает уведомления о изменениях
- [ ] Продавец видит все отправления
- [ ] Админ может управлять провайдерами

---

## 📝 СЛЕДУЮЩИЕ ШАГИ

1. **Немедленно**: Решить вопрос с Post Express API
2. **Неделя 1**: Начать с базовой инфраструктуры
3. **Неделя 2**: Реализовать MVP
4. **Неделя 3**: Тестирование с реальными данными
5. **Неделя 4**: Подготовка к production

---

**План подготовлен**: 2025-09-16
**Ответственный**: Development Team
**Статус**: ГОТОВ К РЕАЛИЗАЦИИ