# Техническое задание на разработку админ-панели управления переводами

## 📋 Резюме проекта

### Цель проекта
Создание централизованной системы управления переводами для платформы Sve Tu, которая объединит управление переводами из всех источников (Frontend JSON файлы, Backend БД, OpenSearch индексы) в единой админ-панели.

### Ключевые проблемы, которые решает админка
1. **165+ неполных переводов** (placeholder'ы [RU], [EN], [SR]) во frontend файлах
2. **Отсутствие единого источника правды** - переводы разбросаны по разным системам
3. **Нет валидации консистентности** между языками
4. **Сложность управления** - требуется знание кода для редактирования переводов
5. **Отсутствие версионирования** и отката изменений
6. **Нет контроля качества** машинных переводов

## 🏗️ Архитектура решения

### Компоненты системы

```
┌─────────────────────────────────────────────────────────────────┐
│                    Translation Admin Panel                       │
│                         (Next.js App)                           │
├─────────────────────────────────────────────────────────────────┤
│                     Translation API Gateway                      │
│                         (Go Backend)                            │
├──────────────────┬──────────────────┬──────────────────────────┤
│   Frontend JSON  │   PostgreSQL DB   │   OpenSearch Index      │
│     Storage      │   Translations     │    Sync Service         │
└──────────────────┴──────────────────┴──────────────────────────┘
```

### Основные модули

1. **Translation Manager** - центральный модуль управления
2. **File System Adapter** - работа с JSON файлами frontend
3. **Database Adapter** - работа с таблицей translations
4. **Search Index Adapter** - синхронизация с OpenSearch
5. **Version Control** - версионирование и откат изменений
6. **Quality Control** - проверка качества переводов
7. **Audit Logger** - логирование всех изменений

## 📝 Функциональные требования

### 1. Управление переводами Frontend (JSON файлы)

#### 1.1 Просмотр и редактирование
- [ ] Древовидное отображение структуры модулей и ключей
- [ ] Inline редактирование переводов для всех языков одновременно
- [ ] Поиск по ключам и значениям переводов
- [ ] Фильтрация по модулям (common, auth, marketplace и др.)
- [ ] Отображение статуса перевода (полный/неполный/placeholder)

#### 1.2 Валидация и контроль качества
- [ ] Автоматическое выявление отсутствующих ключей между языками
- [ ] Подсветка placeholder'ов ([RU], [EN], [SR])
- [ ] Проверка на дублирование ключей
- [ ] Валидация переменных в переводах ({variable})
- [ ] Проверка длины переводов (для UI элементов)

#### 1.3 Массовые операции
- [ ] Импорт/экспорт переводов (CSV, JSON, XLSX)
- [ ] Batch перевод через AI провайдеров
- [ ] Копирование переводов между модулями
- [ ] Поиск и замена по паттерну

### 2. Управление переводами Backend (БД)

#### 2.1 CRUD операции
- [ ] Просмотр всех переводов с пагинацией
- [ ] Фильтрация по entity_type, language, is_verified
- [ ] Редактирование translated_text
- [ ] Управление флагами is_machine_translated и is_verified
- [ ] Просмотр и редактирование metadata

#### 2.2 Связи с сущностями
- [ ] Отображение связанных объектов (категории, объявления, атрибуты)
- [ ] Переход к редактированию связанной сущности
- [ ] Массовое обновление переводов для типа сущности

#### 2.3 Статистика и аналитика
- [ ] Дашборд с общей статистикой переводов
- [ ] Графики по языкам, типам сущностей, статусам
- [ ] Отчет о непереведенных сущностях
- [ ] История изменений переводов

### 3. Интеграция с AI провайдерами

#### 3.1 Настройка провайдеров
- [ ] Управление API ключами (OpenAI, Google Translate)
- [ ] Настройка приоритетов провайдеров
- [ ] Лимиты использования и алерты
- [ ] Статистика расходов по провайдерам

#### 3.2 Процесс перевода
- [ ] Выбор провайдера для конкретного перевода
- [ ] Предпросмотр перевода перед сохранением
- [ ] Настройка контекста для улучшения качества
- [ ] Batch перевод с прогресс-баром
- [ ] Откат неудачных переводов

### 4. Синхронизация и консистентность

#### 4.1 Синхронизация между системами
- [ ] Автоматическая синхронизация Frontend JSON → БД
- [ ] Синхронизация БД → OpenSearch при изменениях
- [ ] Конфликты и их разрешение
- [ ] Лог синхронизации с деталями

#### 4.2 Версионирование
- [ ] История изменений каждого перевода
- [ ] Возможность отката к предыдущей версии
- [ ] Сравнение версий (diff view)
- [ ] Комментарии к изменениям

### 5. Управление доступом и роли

#### 5.1 Роли пользователей
- **Super Admin** - полный доступ ко всем функциям
- **Translation Manager** - управление переводами, но не настройками
- **Translator** - только редактирование переводов
- **Reviewer** - проверка и подтверждение переводов
- **Viewer** - только просмотр

#### 5.2 Права доступа
- [ ] Разграничение по языкам (переводчик только для ru)
- [ ] Разграничение по модулям (только marketplace)
- [ ] Approval workflow для критичных переводов
- [ ] Аудит лог всех действий пользователей

### 6. Поиск и навигация

#### 6.1 Глобальный поиск
- [ ] Поиск по всем источникам переводов
- [ ] Fuzzy search для опечаток
- [ ] Поиск по регулярным выражениям
- [ ] Сохраненные поисковые запросы

#### 6.2 Навигация
- [ ] Breadcrumbs для текущего положения
- [ ] Быстрые переходы между модулями
- [ ] Закладки на часто используемые разделы
- [ ] История последних изменений

### 7. Мониторинг и алерты

#### 7.1 Мониторинг качества
- [ ] Автоматическое выявление проблем
- [ ] Алерты о новых placeholder'ах
- [ ] Уведомления о расхождениях между языками
- [ ] Отчеты о прогрессе перевода

#### 7.2 Производительность
- [ ] Мониторинг времени загрузки переводов
- [ ] Алерты о проблемах с синхронизацией
- [ ] Статистика использования кэша
- [ ] Метрики API провайдеров

## 🛠️ Технические требования

### Backend (Go)

#### API Endpoints

```go
// Frontend переводы
GET    /api/v1/admin/translations/frontend/modules           // Список модулей
GET    /api/v1/admin/translations/frontend/module/:name      // Переводы модуля
PUT    /api/v1/admin/translations/frontend/module/:name      // Обновить модуль
POST   /api/v1/admin/translations/frontend/validate          // Валидация
POST   /api/v1/admin/translations/frontend/sync              // Синхронизация с БД

// БД переводы
GET    /api/v1/admin/translations/database                   // Список с фильтрами
GET    /api/v1/admin/translations/database/:id               // Конкретный перевод
PUT    /api/v1/admin/translations/database/:id               // Обновить перевод
DELETE /api/v1/admin/translations/database/:id               // Удалить перевод
POST   /api/v1/admin/translations/database/batch             // Batch операции

// AI переводы
POST   /api/v1/admin/translations/ai/translate               // Перевести текст
POST   /api/v1/admin/translations/ai/batch                   // Batch перевод
GET    /api/v1/admin/translations/ai/providers               // Список провайдеров
PUT    /api/v1/admin/translations/ai/providers/:id           // Настройки провайдера

// Синхронизация
POST   /api/v1/admin/translations/sync/frontend-to-db        // Frontend → БД
POST   /api/v1/admin/translations/sync/db-to-opensearch      // БД → OpenSearch
GET    /api/v1/admin/translations/sync/status                // Статус синхронизации
GET    /api/v1/admin/translations/sync/conflicts             // Конфликты

// Версионирование
GET    /api/v1/admin/translations/versions/:entity/:id       // История версий
POST   /api/v1/admin/translations/versions/rollback          // Откат версии
GET    /api/v1/admin/translations/versions/diff              // Сравнение версий

// Статистика
GET    /api/v1/admin/translations/stats/overview             // Общая статистика
GET    /api/v1/admin/translations/stats/coverage             // Покрытие переводов
GET    /api/v1/admin/translations/stats/quality              // Качество переводов
GET    /api/v1/admin/translations/stats/usage                // Использование
```

#### Структуры данных

```go
// Модель для Frontend перевода
type FrontendTranslation struct {
    Module      string                 `json:"module"`
    Key         string                 `json:"key"`
    Translations map[string]string     `json:"translations"` // lang -> text
    Status      TranslationStatus      `json:"status"`
    Metadata    TranslationMetadata    `json:"metadata"`
}

// Расширенная модель БД перевода
type DatabaseTranslation struct {
    ID                  int                    `json:"id"`
    EntityType         string                 `json:"entity_type"`
    EntityID           int                    `json:"entity_id"`
    Language           string                 `json:"language"`
    FieldName          string                 `json:"field_name"`
    TranslatedText     string                 `json:"translated_text"`
    IsMachineTranslated bool                  `json:"is_machine_translated"`
    IsVerified         bool                   `json:"is_verified"`
    Metadata           map[string]interface{} `json:"metadata"`
    CreatedAt          time.Time              `json:"created_at"`
    UpdatedAt          time.Time              `json:"updated_at"`
    Version            int                    `json:"version"`
}

// Статус перевода
type TranslationStatus string

const (
    StatusComplete    TranslationStatus = "complete"
    StatusIncomplete  TranslationStatus = "incomplete"
    StatusPlaceholder TranslationStatus = "placeholder"
    StatusMissing     TranslationStatus = "missing"
    StatusOutdated    TranslationStatus = "outdated"
)

// Метаданные перевода
type TranslationMetadata struct {
    Provider        string    `json:"provider"`
    TranslatedBy    int       `json:"translated_by"`
    TranslatedAt    time.Time `json:"translated_at"`
    VerifiedBy      int       `json:"verified_by"`
    VerifiedAt      time.Time `json:"verified_at"`
    CharacterCount  int       `json:"character_count"`
    WordCount       int       `json:"word_count"`
    QualityScore    float64   `json:"quality_score"`
}
```

### Frontend (Next.js + TypeScript)

#### Основные компоненты

```typescript
// Компоненты админки
components/
├── translations/
│   ├── TranslationDashboard.tsx      // Главный дашборд
│   ├── ModuleExplorer.tsx            // Навигация по модулям
│   ├── TranslationEditor.tsx         // Редактор переводов
│   ├── TranslationGrid.tsx           // Таблица переводов
│   ├── TranslationDiff.tsx           // Сравнение версий
│   ├── AITranslator.tsx              // AI перевод
│   ├── SyncManager.tsx               // Управление синхронизацией
│   ├── QualityChecker.tsx            // Проверка качества
│   └── StatisticsPanel.tsx           // Статистика
```

#### State Management (Redux Toolkit)

```typescript
// Store structure
interface TranslationState {
    frontend: {
        modules: ModuleData[];
        currentModule: string;
        translations: FrontendTranslation[];
        filters: FilterState;
        validation: ValidationResult[];
    };
    database: {
        translations: DatabaseTranslation[];
        pagination: PaginationState;
        filters: FilterState;
    };
    sync: {
        status: SyncStatus;
        conflicts: ConflictData[];
        lastSync: Date;
    };
    ai: {
        providers: AIProvider[];
        activeProvider: string;
        translationQueue: TranslationTask[];
    };
}
```

### База данных

#### Новые таблицы

```sql
-- История версий переводов
CREATE TABLE translation_versions (
    id SERIAL PRIMARY KEY,
    translation_id INTEGER REFERENCES translations(id),
    version_number INTEGER NOT NULL,
    entity_type VARCHAR(50) NOT NULL,
    entity_id INTEGER NOT NULL,
    language VARCHAR(10) NOT NULL,
    field_name VARCHAR(50) NOT NULL,
    translated_text TEXT NOT NULL,
    changed_by INTEGER REFERENCES users(id),
    changed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    change_comment TEXT,
    metadata JSONB DEFAULT '{}'
);

-- Конфликты синхронизации
CREATE TABLE translation_sync_conflicts (
    id SERIAL PRIMARY KEY,
    source_type VARCHAR(50) NOT NULL, -- 'frontend', 'database', 'opensearch'
    target_type VARCHAR(50) NOT NULL,
    entity_identifier TEXT NOT NULL,
    source_value TEXT,
    target_value TEXT,
    conflict_type VARCHAR(50), -- 'missing', 'different', 'outdated'
    resolved BOOLEAN DEFAULT FALSE,
    resolved_by INTEGER REFERENCES users(id),
    resolved_at TIMESTAMP,
    resolution_type VARCHAR(50), -- 'use_source', 'use_target', 'manual'
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Аудит лог действий
CREATE TABLE translation_audit_log (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    action VARCHAR(100) NOT NULL,
    entity_type VARCHAR(50),
    entity_id INTEGER,
    old_value TEXT,
    new_value TEXT,
    ip_address INET,
    user_agent TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Настройки AI провайдеров
CREATE TABLE translation_providers (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    provider_type VARCHAR(50) NOT NULL, -- 'openai', 'google', 'deepl'
    api_key TEXT ENCRYPTED,
    settings JSONB DEFAULT '{}',
    usage_limit INTEGER,
    usage_current INTEGER DEFAULT 0,
    is_active BOOLEAN DEFAULT TRUE,
    priority INTEGER DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Индексы для производительности
CREATE INDEX idx_translation_versions_lookup ON translation_versions(translation_id, version_number);
CREATE INDEX idx_sync_conflicts_unresolved ON translation_sync_conflicts(resolved) WHERE resolved = FALSE;
CREATE INDEX idx_audit_log_user_date ON translation_audit_log(user_id, created_at);
CREATE INDEX idx_providers_active ON translation_providers(is_active, priority);
```

## 🎨 UI/UX требования

### Дизайн и интерфейс

1. **Использовать DaisyUI компоненты** для консистентности с основным приложением
2. **Dark mode поддержка** - переключение темы
3. **Responsive дизайн** - работа на планшетах и десктопах
4. **Keyboard shortcuts** для быстрой навигации:
   - `Ctrl+S` - сохранить изменения
   - `Ctrl+F` - поиск
   - `Ctrl+Z` - отменить
   - `Tab` - переход между языками

### Макеты основных экранов

#### Главный дашборд
```
┌─────────────────────────────────────────────────────────┐
│  📊 Translation Dashboard                     [User ▼]  │
├─────────────────────────────────────────────────────────┤
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │  Total   │ │ Complete │ │ Missing  │ │ Outdated │  │
│  │  5,234   │ │  4,521   │ │   543    │ │   170    │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
│                                                          │
│  [Coverage Chart]          [Language Distribution]       │
│                                                          │
│  Recent Changes:                                         │
│  • User updated marketplace.title (ru) - 2 min ago     │
│  • AI translated 50 items - 15 min ago                 │
│  • Sync completed frontend → DB - 1 hour ago           │
└─────────────────────────────────────────────────────────┘
```

#### Редактор переводов
```
┌─────────────────────────────────────────────────────────┐
│  🔤 Translation Editor - Module: marketplace            │
├─────────────────────────────────────────────────────────┤
│  Search: [_______________] Filter: [All ▼] [Sync] [AI]  │
├─────────────────────────────────────────────────────────┤
│  Key              │ SR 🇷🇸 │ EN 🇬🇧 │ RU 🇷🇺 │ Status │
├───────────────────┼────────┼────────┼────────┼─────────┤
│ marketplace.title │ Naslov │ Title  │ [RU]   │ ⚠️      │
│ └─ [Edit inline]  │        │        │        │         │
├───────────────────┼────────┼────────┼────────┼─────────┤
│ marketplace.desc  │ Opis   │ Desc.. │ Описа..│ ✅      │
│ └─ [Edit inline]  │        │        │        │         │
└─────────────────────────────────────────────────────────┘
```

## 🔐 Безопасность

### Требования безопасности

1. **Шифрование API ключей** в БД (AES-256)
2. **Rate limiting** для AI переводов
3. **RBAC** (Role-Based Access Control)
4. **Аудит лог** всех изменений
5. **Backup** перед массовыми операциями
6. **Валидация входных данных** против XSS/injection
7. **CSRF защита** для всех мутирующих операций
8. **2FA** для admin аккаунтов

## 📊 Метрики успеха

### KPI проекта

1. **Сокращение placeholder'ов** с 165 до 0 за 2 недели
2. **Время на добавление нового перевода** < 30 секунд
3. **Автоматизация переводов** > 80% через AI
4. **Консистентность между языками** > 99%
5. **Uptime синхронизации** > 99.9%
6. **Скорость загрузки страницы** < 2 секунд

## 🚀 План реализации

### Фаза 1: MVP (2 недели)
- [ ] Backend API для Frontend переводов
- [ ] Базовый UI для просмотра и редактирования
- [ ] Синхронизация Frontend → БД
- [ ] Простая аутентификация

### Фаза 2: AI интеграция (1 неделя)
- [ ] Интеграция OpenAI
- [ ] Batch переводы
- [ ] Контроль качества

### Фаза 3: Расширенные функции (2 недели)
- [ ] Версионирование
- [ ] Конфликты и их разрешение
- [ ] Статистика и аналитика
- [ ] RBAC и аудит

### Фаза 4: Оптимизация (1 неделя)
- [ ] Производительность
- [ ] Кэширование
- [ ] Мониторинг
- [ ] Документация

## 📚 Зависимости

### Backend
- `github.com/go-playground/validator` - валидация
- `github.com/google/uuid` - генерация ID
- `github.com/golang-migrate/migrate` - миграции
- Existing: Fiber, GORM, OpenSearch client

### Frontend
- `react-hot-toast` - уведомления
- `react-diff-viewer` - сравнение версий
- `react-json-tree` - отображение JSON структур
- `ag-grid-react` - продвинутые таблицы
- Existing: Next.js, Redux Toolkit, DaisyUI

## 🔄 Интеграции

### Внешние сервисы
1. **OpenAI API** - GPT-4 для переводов
2. **Google Translate API** - альтернативный провайдер
3. **DeepL API** (опционально) - высококачественные переводы
4. **GitHub API** - версионирование JSON файлов
5. **Slack/Telegram** - уведомления о проблемах

## 📝 Приемочные критерии

### Минимальные требования для запуска
1. ✅ Все placeholder'ы заменены на реальные переводы
2. ✅ Работает синхронизация между всеми системами
3. ✅ Есть базовая аутентификация и авторизация
4. ✅ Логируются все изменения
5. ✅ Есть возможность отката изменений
6. ✅ Покрытие тестами > 80%
7. ✅ Документация для администраторов

## 🎯 Заключение

Данная админ-панель станет центральным хабом управления всеми переводами платформы Sve Tu, обеспечивая:

- **Единый источник правды** для всех переводов
- **Автоматизацию** рутинных задач перевода
- **Контроль качества** и консистентности
- **Прозрачность** через версионирование и аудит
- **Масштабируемость** для новых языков и модулей

Реализация займет примерно 6 недель с поэтапным внедрением функциональности, что позволит начать получать пользу от системы уже через 2 недели после старта разработки.