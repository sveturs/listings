# 🔍 Pre-Check Guidelines

Руководство о том, когда и как запускать проверки качества кода перед коммитами и PR.

---

## 🎯 Когда ОБЯЗАТЕЛЬНО запускать pre-check

### ✅ ВСЕГДА запускай pre-check ПЕРЕД:

1. **Созданием коммита** - если изменял исходный код
2. **Созданием Pull Request** - перед отправкой на ревью
3. **Завершением задачи с изменением кода** - перед отметкой "Done"
4. **Push в удалённый репозиторий** - особенно в main/master

---

## ❌ Когда НЕ нужно запускать pre-check

### Не требуется для:

1. **Read-only задач** - исследование кодабазы, чтение файлов, поиск
2. **Создания документации** - только .md файлы без кода
3. **Тестирования без изменений** - проверка существующей функциональности
4. **Изменения конфигурации** - .env файлы, docker-compose.yml (если нет скриптов)

---

## 🔧 Backend Pre-Check Команды

### Полная проверка:
```bash
cd /data/hostel-booking-system/backend

# 1. Автоформатирование
make format

# 2. Линтинг (проверка качества кода)
make lint

# 3. Регенерация swagger типов (ТОЛЬКО если менял swagger аннотации)
# make generate-types
```

### Быстрая проверка (одной командой):
```bash
cd /data/hostel-booking-system/backend && make format && make lint
```

### Когда запускать `make generate-types`:
- ✅ Изменил swagger комментарии в handlers
- ✅ Добавил новые API endpoints с swagger аннотациями
- ✅ Изменил структуры models, используемые в swagger
- ❌ НЕ нужно если меняешь только бизнес-логику без API

---

## 🎨 Frontend Pre-Check Команды

### Полная проверка:
```bash
cd /data/hostel-booking-system/frontend/svetu

# 1. Автоформатирование
yarn format

# 2. Линтинг
yarn lint

# 3. Проверка сборки (занимает время!)
yarn build
```

### Быстрая проверка (без build):
```bash
cd /data/hostel-booking-system/frontend/svetu && yarn format && yarn lint
```

### Когда пропускать `yarn build`:
- ⚡ Минимальные изменения (1-2 строки)
- ⚡ Изменения только стилей/текстов
- ⚡ Нужна быстрая проверка перед экспериментами
- ⚠️ Но ОБЯЗАТЕЛЬНО запусти build перед финальным коммитом!

---

## ⚙️ Настройка автоматического pre-check

### Pre-commit hooks (рекомендуется):

```bash
# Установить pre-commit (один раз)
# macOS: brew install pre-commit
# Ubuntu: sudo apt install pre-commit
# или: pip install pre-commit

# Активировать в проекте
cd /data/hostel-booking-system
pre-commit install
```

После установки pre-commit hooks будут автоматически запускаться при каждом `git commit`.

---

## 📊 Пример workflow

### Scenario 1: Исправление бага в backend

```bash
# 1. Фиксишь код
vim backend/internal/services/some_service.go

# 2. Pre-check
cd backend && make format && make lint

# 3. Коммит (если всё ОК)
git add -A
git commit -m "fix: исправить ошибку в some_service"
```

### Scenario 2: Новая фича в frontend

```bash
# 1. Разработка
vim frontend/svetu/src/components/NewFeature.tsx

# 2. Быстрая проверка в процессе
cd frontend/svetu && yarn format && yarn lint

# 3. Финальная проверка с build
yarn build

# 4. Коммит
git add -A
git commit -m "feat: добавить новую фичу"
```

### Scenario 3: Fullstack изменения

```bash
# 1. Backend pre-check
cd backend && make format && make lint

# 2. Frontend pre-check (с build!)
cd ../frontend/svetu && yarn format && yarn lint && yarn build

# 3. Если всё успешно - коммит
git add -A
git commit -m "feat: интеграция новой фичи backend + frontend"
```

---

## 🚨 Что делать при ошибках

### Backend lint ошибки:
```bash
# Просмотр ошибок
make lint

# Автоматическое исправление (где возможно)
make format

# Ручное исправление оставшихся ошибок
# vim файл_с_ошибкой.go
```

### Frontend lint ошибки:
```bash
# Автоисправление
yarn lint:fix

# Если не помогло - ручное исправление
yarn lint  # посмотреть что осталось
```

### Build ошибки:
- Читай сообщение об ошибке внимательно
- Проверь import'ы и типы
- Убедись что все зависимости установлены (`yarn install`)

---

## ⚡ Quick Reference

| Ситуация | Backend | Frontend |
|----------|---------|----------|
| Минимальные изменения | `make format && make lint` | `yarn format && yarn lint` |
| Новая фича | `make format && make lint` | `yarn format && yarn lint && yarn build` |
| Перед PR | `make format && make lint` | `yarn format && yarn lint && yarn build` |
| Изменили swagger | `make format && make lint && make generate-types` | - |
| Read-only задача | ❌ не нужно | ❌ не нужно |
| Только .md файлы | ❌ не нужно | ❌ не нужно |

---

## 📚 Дополнительные материалы

- [Backend правила](.ai/backend.md)
- [Frontend правила](.ai/frontend.md)
- [Git правила](.ai/git.md)