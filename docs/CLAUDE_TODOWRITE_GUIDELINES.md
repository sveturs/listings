# 📋 TodoWrite Tool - Руководство по использованию

Правила использования TodoWrite tool для отслеживания прогресса задач.

---

## 🎯 Зачем нужен TodoWrite

TodoWrite tool помогает:
- 📊 Отслеживать прогресс многоша

говых задач
- 🎯 Не забывать про подзадачи
- 👀 Давать пользователю видимость прогресса
- 📝 Структурировать сложные задачи

---

## ✅ Когда ОБЯЗАТЕЛЬНО использовать TodoWrite

### 1. **Multi-step задачи (3+ шага)**
```
Пользователь: "Реализуй систему уведомлений"

→ TodoWrite с задачами:
  1. Создать таблицу notifications в БД
  2. Реализовать backend API для уведомлений
  3. Добавить WebSocket для real-time
  4. Создать UI компонент уведомлений
  5. Интегрировать с существующими фичами
```

### 2. **Сложная разработка новых фич**
```
Пользователь: "Добавь поддержку вариантов товаров (размер, цвет)"

→ TodoWrite с задачами:
  1. Спроектировать схему БД для вариантов
  2. Создать миграции
  3. Реализовать backend CRUD для вариантов
  4. Обновить API endpoints товаров
  5. Создать UI для управления вариантами
  6. Обновить корзину и заказы
  7. Написать тесты
```

### 3. **Рефакторинг в нескольких модулях**
```
Пользователь: "Унифицируй систему загрузки изображений"

→ TodoWrite с задачами:
  1. Изучить текущие реализации (listings, storefronts, chats)
  2. Создать единый ImageService
  3. Обновить marketplace handlers
  4. Обновить storefronts handlers
  5. Обновить chat handlers
  6. Удалить дублирующий код
  7. Тестирование всех типов загрузки
```

### 4. **Исправление множественных багов**
```
Пользователь: "Исправь все ошибки TypeScript в проекте"

→ TodoWrite с задачами:
  1. Запустить tsc и получить список ошибок
  2. Исправить ошибки в components/ (45 ошибок)
  3. Исправить ошибки в services/ (23 ошибки)
  4. Исправить ошибки в utils/ (12 ошибок)
  5. Проверить сборку без ошибок
```

### 5. **По явному запросу пользователя**
```
Пользователь: "Создай todo list для реализации корзины"

→ TodoWrite обязательно, даже если задача простая
```

---

## ❌ Когда НЕ нужен TodoWrite

### 1. **Простые одношаговые задачи**
```
✗ Пользователь: "Почини эту ошибку в файле user_service.go"
→ НЕ нужен TodoWrite, просто исправь

✗ Пользователь: "Добавь валидацию email в форму"
→ НЕ нужен TodoWrite, straightforward fix
```

### 2. **Read-only задачи (исследование)**
```
✗ Пользователь: "Найди где используется функция getUserById"
→ НЕ нужен TodoWrite, просто найди и покажи

✗ Пользователь: "Изучи как работает авторизация в проекте"
→ НЕ нужен TodoWrite, просто исследуй и объясни
```

### 3. **Тестирование без изменений**
```
✗ Пользователь: "Проверь работает ли загрузка изображений"
→ НЕ нужен TodoWrite, просто протестируй

✗ Пользователь: "Проверь что API endpoint /users возвращает"
→ НЕ нужен TodoWrite, запусти curl и покажи
```

### 4. **Создание документации**
```
✗ Пользователь: "Создай документацию для API endpoint /products"
→ НЕ нужен TodoWrite (если только не большая документация на весь API)

✗ Пользователь: "Объясни как работает эта функция"
→ НЕ нужен TodoWrite, просто объясни
```

### 5. **Мелкие правки (1-2 строки)**
```
✗ Пользователь: "Измени текст кнопки на 'Отправить'"
→ НЕ нужен TodoWrite

✗ Пользователь: "Убери console.log из этого файла"
→ НЕ нужен TodoWrite
```

---

## 📊 Примеры с объяснениями

### ✅ Пример 1: НУЖЕН TodoWrite

**Запрос:** "Реализуй фильтрацию товаров по категориям, цене и атрибутам"

**Почему нужен:** Multi-step задача, затрагивает backend + frontend + БД

**TodoWrite:**
```
1. ✓ Изучить текущую схему фильтров в БД
2. → Добавить поддержку атрибутов в backend API
3. ⏳ Создать компонент FilterPanel в frontend
4. ⏳ Интегрировать фильтры с поисковым запросом
5. ⏳ Добавить URL параметры для фильтров
6. ⏳ Тестирование через UI
```

### ❌ Пример 2: НЕ НУЖЕН TodoWrite

**Запрос:** "Исправь ошибку 404 при загрузке изображения"

**Почему не нужен:** Одношаговая задача, локальная проблема

**Действия:**
1. Проверяю URL изображения
2. Нашёл проблему в path
3. Исправил и проверил
4. Готово!

### ✅ Пример 3: НУЖЕН TodoWrite

**Запрос:** "Добавь поддержку мультиязычности (русский, английский, сербский)"

**Почему нужен:** Сложная задача, много файлов, несколько этапов

**TodoWrite:**
```
1. ✓ Настроить next-intl конфигурацию
2. → Создать структуру messages/{ru,en,sr}/
3. ⏳ Перевести все компоненты на i18n
4. ⏳ Добавить переключатель языка в UI
5. ⏳ Обновить API для поддержки ?lang=
6. ⏳ Тестирование переключения языков
```

---

## 🔄 Lifecycle TodoWrite

### Правильное использование:

```markdown
1. **Создание списка** - в начале сложной задачи
2. **Обновление статусов** - после каждого выполненного шага
3. **Добавление новых задач** - если обнаружены дополнительные шаги
4. **Завершение** - когда все задачи completed
```

### Статусы задач:

- `pending` - ещё не начата
- `in_progress` - работаем сейчас (ТОЛЬКО ОДНА задача!)
- `completed` - выполнена

### Важные правила:

1. ✅ **Только ОДНА задача** `in_progress` одновременно
2. ✅ **Отмечай completed** сразу после завершения
3. ✅ **Обновляй список** при обнаружении новых подзадач
4. ❌ **НЕ батчи** - не отмечай несколько задач сразу

---

## 🎯 Шаблоны для типичных ситуаций

### Новая фича:
```
1. Изучить требования и существующий код
2. Спроектировать решение (схема БД, API, UI)
3. Создать миграции БД (если нужно)
4. Реализовать backend logic
5. Реализовать frontend UI
6. Интеграционное тестирование
7. Документация (если требуется)
```

### Рефакторинг:
```
1. Изучить текущую реализацию
2. Определить проблемы и улучшения
3. Создать новую структуру/интерфейс
4. Обновить модуль 1
5. Обновить модуль 2
6. Обновить модуль N
7. Удалить старый код
8. Тестирование всех модулей
```

### Баг фикс (множественный):
```
1. Воспроизвести ошибку
2. Найти root cause
3. Исправить в месте A
4. Исправить в месте B (если связано)
5. Добавить тест для предотвращения
6. Проверить что ошибка исправлена
```

---

## 💡 Tips & Tricks

### Хороший TodoWrite:
- ✅ Конкретные задачи ("Создать таблицу users" вместо "Сделать БД")
- ✅ Измеримый прогресс (можно отметить как done)
- ✅ Логический порядок (зависимости учтены)
- ✅ Актуальный (обновляется по ходу работы)

### Плохой TodoWrite:
- ❌ Слишком общие задачи ("Реализовать всё")
- ❌ Слишком детальные ("Добавить import в строку 5")
- ❌ Не обновляется (outdated)
- ❌ Много задач in_progress одновременно

---

## 📚 Quick Reference

| Ситуация | TodoWrite? | Почему |
|----------|-----------|--------|
| "Реализуй новую фичу X" | ✅ ДА | Multi-step, сложная |
| "Исправь баг в файле Y" | ❌ НЕТ | One-step, простая |
| "Рефактор модуля Z" | ✅ ДА | Много файлов, этапов |
| "Найди где используется..." | ❌ НЕТ | Read-only |
| "Добавь 5 новых эндпоинтов" | ✅ ДА | Множественная задача |
| "Измени текст кнопки" | ❌ НЕТ | Trivial, 1 строка |
| "Создай todo list для..." | ✅ ДА | Явный запрос |
| "Протестируй функцию X" | ❌ НЕТ | Тестирование без изменений |

---

## 🎓 Заключение

**Главное правило:**

Если сомневаешься - лучше **создай** TodoWrite. Хуже не будет, а польза для структурирования задачи будет!

Система напоминает об использовании TodoWrite - это не просто так. Слушай напоминания и используй tool для сложных задач.