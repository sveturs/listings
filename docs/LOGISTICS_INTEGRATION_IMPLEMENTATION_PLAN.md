# 📦 ПЛАН РЕАЛИЗАЦИИ УНИВЕРСАЛЬНОЙ СИСТЕМЫ ЛОГИСТИКИ

## 📋 Обзор
План полной реализации системы логистики с поддержкой множественных провайдеров (6 компаний) и системой отслеживания отправлений. Все задачи могут быть выполнены независимо от получения API доступов.

---

## 🎯 Цели
1. Создать универсальную систему доставки с поддержкой 6+ провайдеров
2. Реализовать полный цикл обработки заказов
3. Внедрить систему отслеживания отправлений
4. Подготовить инфраструктуру для быстрого подключения новых провайдеров

---

## 📐 ФАЗА 1: АРХИТЕКТУРА (3-4 дня)

### 1.1 Создание абстрактного слоя доставки

#### Задачи:
- [ ] Создать интерфейс `DeliveryProvider` для всех провайдеров
- [ ] Реализовать базовый абстрактный класс `BaseDeliveryService`
- [ ] Создать фабрику провайдеров `DeliveryProviderFactory`
- [ ] Реализовать систему конфигурации провайдеров
- [ ] Создать калькулятор стоимости на основе атрибутов товара

#### Структура:
```
backend/internal/proj/delivery/
├── core/
│   ├── interfaces.go         # Интерфейсы для всех провайдеров
│   ├── models.go             # Универсальные модели данных
│   ├── errors.go             # Стандартные ошибки
│   └── attributes.go         # Модели атрибутов доставки
├── calculator/
│   ├── service.go            # Сервис расчета стоимости
│   ├── rules.go              # Правила ценообразования
│   ├── zones.go              # Определение зон доставки
│   └── optimizer.go          # Оптимизатор упаковки
├── service/
│   ├── delivery_service.go   # Главный сервис доставки
│   ├── provider_factory.go   # Фабрика провайдеров
│   └── provider_manager.go   # Управление провайдерами
├── providers/
│   ├── postexpress/          # Post Express провайдер
│   ├── bexexpress/           # BEX Express провайдер
│   ├── dexpress/             # D Express провайдер
│   ├── aks/                  # AKS провайдер
│   ├── cityexpress/          # City Express провайдер
│   └── dhl/                  # DHL провайдер
└── tracking/
    ├── tracker.go            # Универсальный трекер
    └── aggregator.go         # Агрегатор статусов
```

### 1.2 Проектирование БД

#### Миграции для создания таблиц:
- [ ] `delivery_providers` - регистрация провайдеров
- [ ] `delivery_shipments` - универсальная таблица отправлений
- [ ] `delivery_tracking_events` - события отслеживания
- [ ] `delivery_pricing_rules` - правила расчета стоимости
- [ ] `delivery_zones` - зоны доставки
- [ ] `delivery_category_defaults` - дефолтные атрибуты доставки по категориям
- [ ] `delivery_provider_settings` - настройки каждого провайдера

#### SQL миграция:
```sql
-- 000018_universal_delivery_system.up.sql

-- Атрибуты доставки для товаров
ALTER TABLE marketplace_listings ADD COLUMN IF NOT EXISTS delivery_attributes JSONB DEFAULT '{}';
ALTER TABLE storefront_products ADD COLUMN IF NOT EXISTS delivery_attributes JSONB DEFAULT '{}';

-- Структура delivery_attributes:
-- {
--   "weight_kg": 0.5,
--   "dimensions": {
--     "length_cm": 30,
--     "width_cm": 20,
--     "height_cm": 10
--   },
--   "volume_m3": 0.006,
--   "is_fragile": false,
--   "requires_special_handling": false,
--   "stackable": true,
--   "max_stack_weight_kg": 50,
--   "packaging_type": "box", -- box, envelope, pallet, custom
--   "hazmat_class": null
-- }

-- Провайдеры доставки
CREATE TABLE delivery_providers (
    id SERIAL PRIMARY KEY,
    code VARCHAR(50) UNIQUE NOT NULL,
    name VARCHAR(255) NOT NULL,
    logo_url VARCHAR(500),
    is_active BOOLEAN DEFAULT false,
    supports_cod BOOLEAN DEFAULT false,
    supports_insurance BOOLEAN DEFAULT false,
    supports_tracking BOOLEAN DEFAULT true,
    api_config JSONB,
    capabilities JSONB,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Универсальные отправления
CREATE TABLE delivery_shipments (
    id SERIAL PRIMARY KEY,
    provider_id INTEGER REFERENCES delivery_providers(id),
    order_id INTEGER REFERENCES marketplace_orders(id),
    external_id VARCHAR(255),
    tracking_number VARCHAR(255) UNIQUE,
    status VARCHAR(50) NOT NULL DEFAULT 'pending',

    -- Адреса
    sender_info JSONB NOT NULL,
    recipient_info JSONB NOT NULL,

    -- Параметры посылки (с учетом атрибутов товара)
    package_info JSONB NOT NULL, -- включает weight_kg, dimensions, is_fragile и др.

    -- Финансы
    delivery_cost DECIMAL(10,2),
    insurance_cost DECIMAL(10,2),
    cod_amount DECIMAL(10,2),

    -- Детализация стоимости
    cost_breakdown JSONB, -- base_price, weight_surcharge, fragile_surcharge, etc.

    -- Даты
    pickup_date DATE,
    estimated_delivery DATE,
    actual_delivery_date TIMESTAMPTZ,

    -- Метаданные
    provider_response JSONB,
    labels JSONB,

    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- События отслеживания
CREATE TABLE delivery_tracking_events (
    id SERIAL PRIMARY KEY,
    shipment_id INTEGER REFERENCES delivery_shipments(id),
    provider_id INTEGER REFERENCES delivery_providers(id),
    event_time TIMESTAMPTZ NOT NULL,
    status VARCHAR(100) NOT NULL,
    location VARCHAR(500),
    description TEXT,
    raw_data JSONB,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Дефолтные атрибуты доставки по категориям
CREATE TABLE delivery_category_defaults (
    id SERIAL PRIMARY KEY,
    category_id INTEGER REFERENCES marketplace_categories(id),
    default_weight_kg DECIMAL(10,3),
    default_length_cm DECIMAL(10,2),
    default_width_cm DECIMAL(10,2),
    default_height_cm DECIMAL(10,2),
    default_packaging_type VARCHAR(50),
    is_typically_fragile BOOLEAN DEFAULT false,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Правила расчета стоимости доставки
CREATE TABLE delivery_pricing_rules (
    id SERIAL PRIMARY KEY,
    provider_id INTEGER REFERENCES delivery_providers(id),
    rule_type VARCHAR(50) NOT NULL, -- 'weight_based', 'volume_based', 'zone_based', 'combined'

    -- Весовые правила
    weight_ranges JSONB, -- [{"from": 0, "to": 1, "price_per_kg": 5}, ...]

    -- Объемные правила
    volume_ranges JSONB, -- [{"from": 0, "to": 0.01, "price_per_m3": 100}, ...]

    -- Зональные правила
    zone_multipliers JSONB, -- {"local": 1.0, "regional": 1.5, "national": 2.0, "international": 3.5}

    -- Дополнительные сборы
    fragile_surcharge DECIMAL(10,2) DEFAULT 0,
    oversized_surcharge DECIMAL(10,2) DEFAULT 0, -- если любая сторона > 100cm
    special_handling_surcharge DECIMAL(10,2) DEFAULT 0,

    -- Минимальная и максимальная стоимость
    min_price DECIMAL(10,2),
    max_price DECIMAL(10,2),

    -- Формула расчета (для сложных случаев)
    custom_formula TEXT, -- PostgreSQL функция или выражение

    priority INTEGER DEFAULT 0,
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Зоны доставки
CREATE TABLE delivery_zones (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    type VARCHAR(50) NOT NULL, -- 'local', 'regional', 'national', 'international'

    -- Географические границы
    countries TEXT[], -- массив кодов стран
    regions TEXT[], -- массив регионов
    cities TEXT[], -- массив городов
    postal_codes TEXT[], -- массив почтовых индексов

    -- Полигон для точного определения (GIS)
    boundary GEOMETRY(POLYGON, 4326),

    -- Расстояние от центра (для радиусных зон)
    center_point GEOMETRY(POINT, 4326),
    radius_km DECIMAL(10,2),

    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Связь с заказами
ALTER TABLE marketplace_orders
ADD COLUMN delivery_shipment_id INTEGER REFERENCES delivery_shipments(id);

-- Индексы для производительности
CREATE INDEX idx_delivery_shipments_tracking ON delivery_shipments(tracking_number);
CREATE INDEX idx_delivery_shipments_status ON delivery_shipments(status);
CREATE INDEX idx_delivery_tracking_events_shipment ON delivery_tracking_events(shipment_id);
CREATE INDEX idx_delivery_category_defaults_category ON delivery_category_defaults(category_id);
CREATE INDEX idx_delivery_pricing_rules_provider ON delivery_pricing_rules(provider_id);
CREATE INDEX idx_delivery_zones_boundary ON delivery_zones USING GIST(boundary);
CREATE INDEX idx_delivery_zones_center ON delivery_zones USING GIST(center_point);
```

---

## 🔧 ФАЗА 2: BACKEND РЕАЛИЗАЦИЯ (5-7 дней)

### 2.1 Универсальный сервис доставки

- [ ] Реализовать `DeliveryService` с методами:
  - `CalculateRate()` - расчет стоимости на основе атрибутов товара
  - `CalculateBulkRate()` - расчет для нескольких товаров с оптимизацией упаковки
  - `CreateShipment()` - создание отправления
  - `TrackShipment()` - отслеживание
  - `CancelShipment()` - отмена
  - `GetLabel()` - получение этикетки
  - `HandleWebhook()` - обработка вебхуков
  - `GetDeliveryAttributes()` - получение атрибутов товара
  - `UpdateDeliveryAttributes()` - обновление атрибутов товара

### 2.2 Калькулятор стоимости доставки

```go
// backend/internal/proj/delivery/calculator/service.go

type DeliveryCalculator struct {
    db              *sql.DB
    providerManager *ProviderManager
    zoneService     *ZoneService
    pricingRules    *PricingRulesService
}

type CalculationRequest struct {
    // Откуда и куда
    FromLocation    Location        `json:"from_location"`
    ToLocation      Location        `json:"to_location"`

    // Товары с атрибутами
    Items           []ItemWithAttrs `json:"items"`

    // Опции
    ProviderId      *int           `json:"provider_id,omitempty"`
    InsuranceValue  float64        `json:"insurance_value,omitempty"`
}

type ItemWithAttrs struct {
    ProductId       int            `json:"product_id"`
    ProductType     string         `json:"product_type"` // "listing" или "storefront_product"
    Quantity        int            `json:"quantity"`

    // Атрибуты доставки (загружаются из БД если не переданы)
    Attributes      *DeliveryAttrs `json:"attributes,omitempty"`
}

type DeliveryAttrs struct {
    WeightKg        float64        `json:"weight_kg"`
    Dimensions      Dimensions     `json:"dimensions"`
    IsFragile       bool          `json:"is_fragile"`
    PackagingType   string        `json:"packaging_type"`
    SpecialHandling bool          `json:"special_handling"`
    Stackable       bool          `json:"stackable"`
}

type CalculationResponse struct {
    Providers       []ProviderQuote `json:"providers"`
    Cheapest        *ProviderQuote  `json:"cheapest"`
    Fastest         *ProviderQuote  `json:"fastest"`
    Recommended     *ProviderQuote  `json:"recommended"`
}
```

### 2.3 Провайдер-адаптеры

Для каждого из 6 провайдеров создать:
- [ ] Mock-реализацию интерфейса `DeliveryProvider`
- [ ] Конфигурацию подключения
- [ ] Маппинг статусов на универсальные
- [ ] Обработку специфичных особенностей
- [ ] Адаптер расчета с учетом атрибутов товара
- [ ] Преобразование габаритов и веса в формат провайдера

### 2.4 Система отслеживания

- [ ] Создать `TrackingService` с функциями:
  - Агрегация статусов от всех провайдеров
  - Нормализация событий отслеживания
  - Push-уведомления о изменениях статуса
  - Прогнозирование задержек (ML-ready)

### 2.5 Интеграция с заказами

- [ ] Автоматическое создание отправления после оплаты
- [ ] Webhook эндпоинты для каждого провайдера
- [ ] Обновление статусов заказов
- [ ] Генерация трек-номеров
- [ ] Автоматический расчет стоимости доставки в корзине
- [ ] Сохранение атрибутов доставки в заказе

### 2.6 API эндпоинты

```go
// Расчет стоимости доставки
POST   /api/v1/delivery/calculate         // Расчет с произвольными параметрами
POST   /api/v1/delivery/calculate-product // Расчет для конкретного товара
POST   /api/v1/delivery/calculate-cart    // Расчет для корзины

// Атрибуты доставки товаров
GET    /api/v1/products/:id/delivery-attributes    // Получение атрибутов
PUT    /api/v1/products/:id/delivery-attributes    // Обновление атрибутов
GET    /api/v1/categories/:id/delivery-defaults    // Дефолтные атрибуты категории

// Управление отправлениями
POST   /api/v1/delivery/create         // Создание отправления
GET    /api/v1/delivery/track/:number  // Отслеживание
GET    /api/v1/delivery/shipment/:id   // Детали отправления
POST   /api/v1/delivery/cancel/:id     // Отмена
GET    /api/v1/delivery/label/:id      // Получение этикетки
POST   /api/v1/delivery/webhook/:provider // Webhook для провайдера

// Административные роуты
GET    /api/v1/admin/delivery/providers     // Список провайдеров
PUT    /api/v1/admin/delivery/provider/:id  // Обновление настроек
GET    /api/v1/admin/delivery/dashboard     // Dashboard
GET    /api/v1/admin/delivery/problems      // Проблемные доставки
POST   /api/v1/admin/delivery/pricing-rules // Управление правилами расчета
PUT    /api/v1/admin/delivery/category-defaults/:id // Обновление дефолтов категории
```

---

## 🎨 ФАЗА 3: FRONTEND РЕАЛИЗАЦИЯ (4-5 дней)

### 3.1 Компоненты атрибутов доставки

- [ ] `DeliveryAttributesForm` - форма ввода габаритов и веса для C2C
- [ ] `DeliveryAttributesDisplay` - отображение атрибутов на странице товара
- [ ] `CategoryDefaultsHelper` - подсказки на основе категории
- [ ] `BulkAttributesEditor` - массовое редактирование для витрин

### 3.2 Компоненты расчета и выбора доставки

- [ ] `UniversalDeliverySelector` - выбор провайдера и метода
- [ ] `DeliveryCalculator` - калькулятор стоимости с учетом атрибутов
- [ ] `DeliveryAddressForm` - форма адреса доставки
- [ ] `DeliveryMethodCard` - карточка метода с детализацией цены
- [ ] `CartDeliveryPreview` - предварительный расчет в корзине

### 3.3 Отслеживание отправлений

- [ ] `TrackingPage` - страница отслеживания
- [ ] `TrackingTimeline` - таймлайн событий
- [ ] `TrackingMap` - карта с маршрутом
- [ ] `TrackingNotifications` - уведомления о статусе

### 3.4 Личный кабинет

- [ ] Раздел "Мои отправления" для покупателей
- [ ] Раздел "Отправки" для продавцов
- [ ] История доставок
- [ ] Управление адресами
- [ ] Управление атрибутами товаров для продавцов

### 3.5 Админ панель

- [ ] Dashboard с метриками по всем провайдерам
- [ ] Управление провайдерами
- [ ] Мониторинг проблемных доставок
- [ ] Bulk операции (массовая печать этикеток)
- [ ] Редактор правил расчета стоимости
- [ ] Управление дефолтными атрибутами категорий

---

## 💰 ФАЗА 4: ФИНАНСОВАЯ ИНТЕГРАЦИЯ (3-4 дня)

### 4.1 Split платежи для наложенного платежа

- [ ] Создать `PaymentSplitService`
- [ ] Реализовать escrow механизм
- [ ] Автоматизация выплат продавцам
- [ ] Reconciliation с провайдерами

### 4.2 Таблицы для финансов

```sql
-- Escrow счета
CREATE TABLE delivery_escrow_accounts (
    id SERIAL PRIMARY KEY,
    shipment_id INTEGER REFERENCES delivery_shipments(id),
    total_amount DECIMAL(10,2),
    seller_amount DECIMAL(10,2),
    platform_fee DECIMAL(10,2),
    delivery_fee DECIMAL(10,2),
    status VARCHAR(50),
    settled_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- История транзакций
CREATE TABLE delivery_transactions (
    id SERIAL PRIMARY KEY,
    escrow_account_id INTEGER REFERENCES delivery_escrow_accounts(id),
    type VARCHAR(50), -- 'collection', 'payout', 'refund'
    amount DECIMAL(10,2),
    recipient_type VARCHAR(50), -- 'seller', 'platform', 'provider'
    recipient_id INTEGER,
    status VARCHAR(50),
    processed_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

## 🔔 ФАЗА 5: СИСТЕМА УВЕДОМЛЕНИЙ (2-3 дня)

### 5.1 Email уведомления

- [ ] Шаблоны писем для каждого статуса
- [ ] Интеграция с email сервисом
- [ ] Трек-ссылки в письмах

### 5.2 SMS уведомления

- [ ] Интеграция с SMS провайдером
- [ ] Короткие сообщения о ключевых событиях

### 5.3 Push уведомления

- [ ] WebSocket для real-time обновлений
- [ ] Browser push notifications
- [ ] Mobile push (подготовка)

### 5.4 Webhook система для продавцов

- [ ] Регистрация webhook URL продавцами
- [ ] Отправка событий о статусах
- [ ] Retry механизм

---

## 🧪 ФАЗА 6: ТЕСТИРОВАНИЕ (3-4 дня)

### 6.1 Unit тесты

- [ ] Тесты для всех сервисов
- [ ] Тесты для провайдер-адаптеров
- [ ] Тесты для финансовых расчетов

### 6.2 Интеграционные тесты

- [ ] E2E тест полного цикла заказа
- [ ] Тесты webhook обработки
- [ ] Тесты отслеживания

### 6.3 Mock провайдеры

- [ ] Создать mock-провайдер для тестирования
- [ ] Симуляция различных статусов
- [ ] Генерация тестовых событий

---

## 📊 ФАЗА 7: МИГРАЦИЯ ДАННЫХ И НАСТРОЙКА ДЕФОЛТОВ (2-3 дня)

### 7.1 Заполнение дефолтных атрибутов категорий

```sql
-- Заполнение дефолтных атрибутов для популярных категорий
INSERT INTO delivery_category_defaults (category_id, default_weight_kg, default_length_cm, default_width_cm, default_height_cm, default_packaging_type, is_typically_fragile) VALUES
-- Электроника
(1, 0.5, 20, 15, 5, 'box', true),
-- Одежда
(2, 0.3, 30, 25, 5, 'envelope', false),
-- Мебель
(3, 15.0, 120, 60, 80, 'custom', false),
-- Книги
(4, 0.4, 20, 15, 3, 'envelope', false),
-- Продукты питания
(5, 1.0, 25, 20, 15, 'box', false),
-- Косметика
(6, 0.2, 15, 10, 8, 'box', true);
```

### 7.2 Миграция существующих товаров

- [ ] Скрипт заполнения атрибутов для marketplace_listings
- [ ] Скрипт заполнения атрибутов для storefront_products
- [ ] Валидация и исправление некорректных данных
- [ ] Создание отчета о миграции

### 7.3 Настройка правил расчета

- [ ] Создание базовых правил для каждого провайдера
- [ ] Настройка зон доставки
- [ ] Импорт тарифных сеток
- [ ] Тестирование расчетов

---

## 🚀 ФАЗА 8: ПОДГОТОВКА К PRODUCTION (2-3 дня)

### 8.1 Мониторинг и логирование

- [ ] Настроить метрики Prometheus
- [ ] Dashboard в Grafana
- [ ] Алерты на критические события
- [ ] Централизованное логирование

### 8.2 Документация

- [ ] API документация (Swagger)
- [ ] Гайд по интеграции для продавцов
- [ ] Инструкции для службы поддержки
- [ ] Runbook для DevOps
- [ ] Документация по атрибутам доставки

### 8.3 Оптимизация

- [ ] Кеширование тарифов и расчетов
- [ ] Batch обработка webhook
- [ ] Оптимизация запросов к БД
- [ ] CDN для статических ресурсов
- [ ] Кеширование расчетов на 15 минут

---

## 🔄 ФАЗА 9: АВТОМАТИЗАЦИЯ (2-3 дня)

### 9.1 Cron задачи

- [ ] Автоматическое обновление статусов
- [ ] Генерация отчетов
- [ ] Очистка старых данных
- [ ] Проверка "зависших" отправлений
- [ ] Обновление объемного веса для товаров

### 9.2 Бизнес-правила

- [ ] Автовыбор оптимального провайдера на основе атрибутов
- [ ] Правила маршрутизации по зонам
- [ ] Эскалация проблемных доставок
- [ ] SLA мониторинг
- [ ] Оптимизация упаковки нескольких товаров

---

## 📊 МЕТРИКИ УСПЕХА

### Технические метрики:
- Время расчета стоимости < 500ms для 5 товаров
- Время создания отправления < 2 сек
- Обновление трекинга < 5 мин
- Uptime системы > 99.9%
- Поддержка 1000+ отправлений/день
- Заполненность атрибутов доставки > 90%

### Бизнес метрики:
- Конверсия корзина → заказ +15-20%
- Снижение запросов о стоимости доставки -50%
- Точность расчета стоимости (отклонение < 10%)
- Время доставки -20%
- NPS по доставке > 4.5
- Увеличение среднего чека за счет прозрачности доставки +10%

---

## 🗓️ TIMELINE

| Фаза | Длительность | Зависимости | Приоритет |
|------|--------------|-------------|-----------|
| Архитектура + Атрибуты | 3-4 дня | - | Критический |
| Backend + Калькулятор | 5-7 дней | Архитектура | Критический |
| Frontend + Формы атрибутов | 4-5 дней | Backend API | Высокий |
| Финансы | 3-4 дня | Backend | Высокий |
| Уведомления | 2-3 дня | Backend | Средний |
| Тестирование | 3-4 дня | Все фазы | Высокий |
| Миграция данных | 2-3 дня | Backend | Критический |
| Production | 2-3 дня | Тестирование | Критический |
| Автоматизация | 2-3 дня | Production | Средний |

**Общее время**: 26-36 дней (параллельная разработка возможна)

### 🚀 Quick Start (MVP за 5-7 дней):
1. **День 1-2**: Добавление полей атрибутов в БД
2. **День 2-3**: Базовый калькулятор с фиксированными тарифами
3. **День 3-4**: Форма ввода атрибутов для C2C
4. **День 4-5**: Интеграция калькулятора в корзину
5. **День 5-7**: Тестирование и отладка

---

## 👥 КОМАНДА

### Необходимые ресурсы:
- 2 Backend разработчика
- 1 Frontend разработчик
- 1 DevOps инженер
- 1 QA инженер
- 1 Product Manager

---

## ⚠️ РИСКИ И МИТИГАЦИЯ

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Задержка с API доступами | Высокая | Средняя | Mock провайдеры готовы |
| Сложность интеграции 6 провайдеров | Средняя | Высокая | Унифицированная архитектура |
| Проблемы с split платежами | Средняя | Высокая | Ручная обработка как fallback |
| Нагрузка на систему | Низкая | Средняя | Горизонтальное масштабирование |

---

## ✅ DEFINITION OF DONE

### Система считается готовой когда:
1. ✅ Все товары имеют атрибуты доставки (вес, габариты)
2. ✅ Автоматический расчет стоимости работает для всех категорий
3. ✅ Поддержка 6 провайдеров через единый интерфейс
4. ✅ Автоматическое создание отправлений
5. ✅ Реальное время отслеживания
6. ✅ Split платежи работают
7. ✅ Уведомления отправляются
8. ✅ Админ панель функциональна
9. ✅ Покрытие тестами > 80%
10. ✅ Документация готова
11. ✅ Мониторинг настроен
12. ✅ Система готова к подключению реальных API
13. ✅ Точность расчета стоимости > 90%
14. ✅ Оптимизация упаковки работает

---

## 🎯 QUICK WINS

### Что можно сделать быстро для демонстрации прогресса:
1. **День 1-2**: Добавление атрибутов доставки в БД
2. **День 3-4**: Калькулятор стоимости с фиксированными правилами
3. **День 5-6**: Форма атрибутов для C2C объявлений
4. **День 7-8**: Интеграция расчета в корзину
5. **День 9-10**: Dashboard с метриками расчетов

---

## 📝 ЗАМЕТКИ

- Атрибуты доставки хранятся в JSONB для гибкости
- Дефолтные значения загружаются из категорий
- Все провайдеры будут подключаться через конфигурацию без изменения кода
- Mock режим позволит тестировать систему без реальных API
- Архитектура позволит добавлять новых провайдеров за 1-2 дня
- Кеширование расчетов снизит нагрузку на систему
- Оптимизация упаковки может сэкономить до 30% на доставке
- Система готова к международной доставке (заложена в архитектуру)

### ⚠️ Важные моменты реализации:
- **Приватность**: не показывать точный адрес продавца при расчете
- **Валидация**: проверять реалистичность введенных габаритов
- **Fallback**: использовать дефолтные значения категории если атрибуты не заполнены
- **UX**: показывать экономию при покупке нескольких товаров
- **Обучение**: добавить подсказки и примеры для продавцов

---

*Документ обновлен: 2025-01-20*
*Версия: 2.0 - добавлена полная интеграция атрибутов доставки*