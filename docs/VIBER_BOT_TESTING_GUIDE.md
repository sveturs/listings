# 📱 Viber Bot - Руководство по тестированию

## 🎯 Что ты можешь увидеть в Viber прямо сейчас

### Шаг 1: Найди своего бота в Infobip портале

1. **Зайди на портал Infobip:** https://portal.infobip.com
2. **Перейди в:** Channels → Viber → Your Bots
3. **Найди информацию:**
   - ✅ **Sender ID**: `a99fe5d1-6201-4708-b9ff-acdb92b61bc5` (уже знаем)
   - ❓ **Bot Name**: Имя бота (как искать в Viber)
   - ❓ **Bot Link**: Прямая ссылка на бота (viber://pa?chatURI=...)
   - ❓ **QR Code**: QR код для подписки

### Шаг 2: Настрой Webhook URL в Infobip

**КРИТИЧЕСКИ ВАЖНО**: Без webhook бот НЕ сможет отвечать на сообщения!

1. **В Infobip портале:** Перейди в настройки своего бота
2. **Найди раздел:** Webhook URL / Callback URL
3. **Укажи URL:**
   - **Development**: `https://devapi.svetu.rs/api/viber/infobip-webhook`
   - **Production**: `https://api.svetu.rs/api/viber/infobip-webhook` (когда будет)
4. **Сохрани** настройки
5. **Проверь**: Infobip должен показать статус "Active" или "Connected"

**Как это работает:**
```
User (Viber) → Infobip → Webhook URL (твой backend) → Обработка → Ответ
```

### Шаг 3: Отправь себе тестовое сообщение

**Самый простой способ - через скрипт:**

```bash
cd /data/hostel-booking-system/backend/scripts

# Замени номер на свой (формат: 381XXXXXXXXX)
go run send_viber_test.go 381604485063
```

**Что должно произойти:**
1. ✅ Скрипт отправит запрос к твоему backend
2. ✅ Backend отправит сообщение через Infobip API
3. ✅ Infobip доставит сообщение в твой Viber
4. ✅ Ты увидишь сообщение от SveTu бота

**Альтернативный способ - через curl:**

```bash
curl -X POST http://localhost:3000/api/viber/send \
  -H "Content-Type: application/json" \
  -d '{
    "viber_id": "381604485063",
    "text": "Привет! Тест бота SveTu 🚀"
  }'
```

### Шаг 4: Подпишись на бота в Viber

**Способ 1 - Через имя бота:**
1. Открой Viber на телефоне
2. Нажми поиск 🔍
3. Введи имя бота (см. в Infobip портале)
4. Нажми "Subscribe" / "Подписаться"

**Способ 2 - Через ссылку:**
1. В Infobip портале найди "Bot Link"
2. Открой эту ссылку на телефоне
3. Viber откроется автоматически
4. Нажми "Subscribe"

**Способ 3 - Через QR код:**
1. В Infobip портале найди QR код
2. Отсканируй камерой телефона
3. Откроется Viber с ботом
4. Нажми "Subscribe"

### Шаг 5: Протестируй интерактивные функции

**1. Отправь простое сообщение боту:**
```
Привет!
```

Бот должен ответить (если webhook настроен и message handler реализован).

**2. Отправь себе Rich Media карточку с трекингом:**
```bash
cd /data/hostel-booking-system/backend

# Убедись что в БД есть delivery с id=1
# Отправь карточку трекинга
curl -X POST http://localhost:3000/api/viber/send-tracking \
  -H "Content-Type: application/json" \
  -d '{
    "viber_id": "381604485063",
    "delivery_id": 1
  }'
```

**Что ты увидишь:**
- 🗺️ Статическая карта с маршрутом доставки
- 📦 Информация о заказе
- 🚚 Статус и ETA
- 🔘 Кнопка "Открыть живую карту"
- 🔄 Кнопка "Обновить"

**3. Нажми на кнопку "Открыть живую карту":**

Viber откроет встроенный браузер с интерактивной картой:
- 🌍 Полноэкранная карта Mapbox
- 📍 Реальное местоположение курьера (обновляется через WebSocket)
- 🎯 Точка доставки
- ⏱️ Динамическое ETA
- 📱 Оптимизировано для Viber (без header/footer)

URL будет таким:
```
https://dev.svetu.rs/en/track/TRACKING-TOKEN?viber=true&embedded=true
```

---

## 🔍 Что проверить если не работает

### ❌ Сообщения не приходят

**Проверь:**
1. **Backend работает:**
   ```bash
   curl http://localhost:3000/
   # Должно вернуть: Svetu API 0.2.1
   ```

2. **Конфигурация Infobip в .env.dev:**
   ```bash
   grep INFOBIP /data/hostel-booking-system/backend/.env.dev
   ```
   Должно быть:
   ```
   INFOBIP_API_KEY=5563e63c1400300a-8dc2a9ffa207e63b-b6bdc0569de2dd76
   INFOBIP_BASE_URL=api.infobip.com
   INFOBIP_SENDER_ID=a99fe5d1-6201-4708-b9ff-acdb92b61bc5
   ```

3. **Логи backend:**
   ```bash
   tail -100 /tmp/backend.log | grep -i viber
   ```

4. **Статус в Infobip портале:**
   - API key активен
   - Bot approved (не в статусе pending)
   - Webhook URL настроен и active

### ❌ Бот не отвечает на сообщения

**Причина**: Webhook URL не настроен или неправильный

**Решение:**
1. Зайди в Infobip портал
2. Настрой webhook: `https://devapi.svetu.rs/api/viber/infobip-webhook`
3. Проверь что URL доступен извне:
   ```bash
   curl https://devapi.svetu.rs/api/viber/infobip-webhook
   # Должен вернуть 200 или 405 (метод не разрешён, но URL работает)
   ```

### ❌ Tracking карточка не отправляется

**Проверь:**
1. **Delivery существует в БД:**
   ```bash
   psql "postgres://postgres:mX3g1XGhMRUZEX3l@localhost:5432/svetubd?sslmode=disable" \
     -c "SELECT id, order_id, tracking_token FROM deliveries LIMIT 5;"
   ```

2. **Используй правильный delivery_id:**
   ```bash
   # Замени delivery_id на реальный из БД
   curl -X POST http://localhost:3000/api/viber/send-tracking \
     -H "Content-Type: application/json" \
     -d '{"viber_id": "381604485063", "delivery_id": 1}'
   ```

### ❌ Карта не открывается

**Проверь:**
1. **Frontend работает:**
   ```bash
   curl https://dev.svetu.rs/
   ```

2. **Tracking страница существует:**
   ```bash
   curl -I https://dev.svetu.rs/en/track/TEST-TOKEN
   # Должен вернуть 200
   ```

3. **WebSocket работает** (для real-time обновлений)

---

## 📊 Мониторинг и статистика

### Получить статистику сессий:
```bash
curl -X GET http://localhost:3000/api/viber/stats
```

**Покажет:**
- Количество активных сессий (24h window)
- Общее количество пользователей
- Количество сообщений (входящих/исходящих)
- Бесплатные vs платные сообщения

### Проверить БД напрямую:
```bash
psql "postgres://postgres:mX3g1XGhMRUZEX3l@localhost:5432/svetubd?sslmode=disable"
```

**Полезные SQL запросы:**
```sql
-- Все пользователи бота
SELECT * FROM viber_users ORDER BY created_at DESC LIMIT 10;

-- Активные сессии (бесплатное окно 24ч)
SELECT * FROM viber_sessions WHERE active = true;

-- Последние сообщения
SELECT * FROM viber_messages ORDER BY created_at DESC LIMIT 20;

-- Статистика по пользователю
SELECT
  u.viber_id,
  u.name,
  COUNT(DISTINCT s.id) as sessions_count,
  COUNT(m.id) as messages_count
FROM viber_users u
LEFT JOIN viber_sessions s ON s.user_id = u.id
LEFT JOIN viber_messages m ON m.user_id = u.id
GROUP BY u.id;
```

---

## 🚀 Killer Feature: Real-Time Tracking

### Как это выглядит для пользователя:

1. **Пользователь получает уведомление:**
   - 📦 "Ваш заказ #12345 в пути!"
   - 🗺️ Статическая карта с маршрутом
   - 🔘 Кнопка "Открыть живую карту"

2. **Нажимает на кнопку:**
   - Viber открывает встроенный браузер
   - Загружается полноэкранная карта
   - БЕЗ выхода из Viber!

3. **Видит в реальном времени:**
   - 📍 Курьер движется по карте (обновления каждые 5-10 сек)
   - 🎯 Точка доставки
   - ⏱️ ETA обновляется динамически
   - 📏 Расстояние до точки

4. **Интерактивность:**
   - Можно двигать карту
   - Zoom in/out
   - Нажать на маркер курьера
   - Обновить данные (pull-to-refresh)

### Техническая реализация:

```
User в Viber
  ↓
Нажимает "Открыть карту"
  ↓
Viber Embedded Browser
  ↓
https://dev.svetu.rs/en/track/TOKEN?viber=true&embedded=true
  ↓
Next.js страница трекинга
  ↓
WebSocket connection
  ↓
Backend отправляет GPS каждые 5-10 сек
  ↓
Карта обновляется плавно (smooth animation)
```

---

## 🎯 Что дальше?

### Immediate Next Steps:

1. ✅ **Получи информацию о боте:**
   - Зайди в Infobip портал
   - Найди Bot Name
   - Найди Bot Link / QR Code

2. ✅ **Настрой webhook:**
   - URL: `https://devapi.svetu.rs/api/viber/infobip-webhook`
   - Сохрани в Infobip портале

3. ✅ **Отправь тестовое сообщение:**
   ```bash
   cd /data/hostel-booking-system/backend/scripts
   go run send_viber_test.go 381XXXXXXXXX  # твой номер
   ```

4. ✅ **Подпишись на бота в Viber:**
   - Через поиск или ссылку/QR код

5. ✅ **Протестируй Rich Media:**
   ```bash
   curl -X POST http://localhost:3000/api/viber/send-tracking \
     -H "Content-Type: application/json" \
     -d '{"viber_id": "381XXXXXXXXX", "delivery_id": 1}'
   ```

### Optional Enhancements:

- [ ] Добавить больше команд боту (поиск товаров, история заказов)
- [ ] Реализовать conversation state machine
- [ ] Добавить A/B тестирование сообщений
- [ ] Создать админ панель для управления ботом
- [ ] Добавить аналитику (открываемость, клики, конверсии)

---

## 📞 Помощь

**Если что-то не работает:**

1. **Проверь логи backend:**
   ```bash
   tail -f /tmp/backend.log | grep -i viber
   ```

2. **Проверь конфигурацию:**
   ```bash
   grep INFOBIP /data/hostel-booking-system/backend/.env.dev
   ```

3. **Проверь статус в Infobip портале:**
   - https://portal.infobip.com
   - Channels → Viber → Your Bots

4. **Проверь webhook:**
   ```bash
   curl https://devapi.svetu.rs/api/viber/infobip-webhook
   ```

---

**Дата создания:** 2025-10-09
**Версия:** 1.0.0
**Статус:** Production Ready ✅
