# 📍 Адреса всегда на сербском языке (кириллица)

## Решение о локализации адресов

Так как платформа работает в Сербии, было принято решение всегда отображать адреса на сербском языке (кириллица), независимо от выбранного языка интерфейса.

## Обоснование

1. **Местная специфика**: Пользователи в Сербии привыкли видеть адреса на местном языке
2. **Навигация**: Для поиска адреса в реальном мире удобнее использовать местное написание
3. **Единообразие**: Избегаем путаницы с разными написаниями одного и того же адреса

## Техническая реализация

### Frontend изменения

**Файл:** `/frontend/svetu/src/utils/addressUtils.ts`

В функции `getFullLocalizedAddress` теперь всегда используется сербская локаль:

```typescript
export function getFullLocalizedAddress(
  item: {
    location?: string;
    city?: string;
    country?: string;
    translations?: any;
  },
  locale: string
): string {
  // Для адресов в Сербии всегда используем сербскую версию
  const addressLocale = 'sr';

  // ... логика получения адреса из translations[addressLocale]
}
```

## Результат

Теперь на всех страницах товаров адреса отображаются единообразно на сербском языке:
- **Русский интерфейс**: "Београд, Београд, Србија"
- **Английский интерфейс**: "Београд, Београд, Србија"
- **Сербский интерфейс**: "Београд, Београд, Србија"

Вместо смешанных вариантов типа:
- "Belgrade, Belgrade, Serbia" (английский)
- "Белград, Белград, Сербия" (русский)
- Или неправильных переводов на сербский

## Альтернативное решение

Если в будущем потребуется локализация адресов:

1. Убрать строку `const addressLocale = 'sr';`
2. Использовать переданный параметр `locale`
3. Добавить переводы для всех полей адреса (city, country) во все языки в БД

## Исправление данных

Была применена миграция `000034_fix_serbian_address_translations.up.sql` для исправления неправильных переводов:
- "Белград" → "Београд"
- "Сербия" → "Србија"
- "Новый Сад" → "Нови Сад"
- И другие города Сербии

## Связанные файлы

- `/frontend/svetu/src/utils/addressUtils.ts` - утилиты работы с адресами
- `/frontend/svetu/src/app/[locale]/marketplace/[id]/page.tsx` - страница товара
- `/backend/migrations/000034_fix_serbian_address_translations.up.sql` - миграция для исправления переводов
- База данных: таблица `translations` с переводами адресов