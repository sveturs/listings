# 📱 Задание: Восстановление категорий в мобильном меню

## Контекст проблемы

### Что произошло
В процессе оптимизации производительности главной страницы мы обнаружили дублирование запросов к API:
- Запрос `/api/v1/marketplace/popular-categories` выполнялся **дважды** при каждой загрузке страницы
- Один запрос из `HomePageClient.tsx` (для отображения популярных категорий на главной)
- Второй запрос из `Header.tsx` (для мобильного бургер-меню)

### Что мы сделали
Временно закомментировали загрузку категорий в `Header.tsx` (строки 141-198), чтобы устранить дублирование запросов.

### Возникшая проблема
Теперь в мобильном меню (бургер) раздел "Категории" пуст, что ухудшает UX для мобильных пользователей.

## Требования к решению

### Необходимо реализовать
Восстановить отображение популярных категорий в мобильном меню БЕЗ дублирования запросов к API.

### Ограничения
- ❌ НЕ делать дублирующие запросы к API
- ❌ НЕ загружать категории дважды для одной и той же локали
- ✅ Категории должны быть доступны и на главной странице, и в мобильном меню
- ✅ При смене языка категории должны обновляться

## Рекомендуемые варианты решения

### Вариант 1: Глобальный контекст (Рекомендуется)
```typescript
// 1. Создать контекст для популярных категорий
// src/contexts/CategoriesContext.tsx
export const CategoriesContext = createContext<{
  popularCategories: Category[];
  loadPopularCategories: (locale: string) => Promise<void>;
  isLoading: boolean;
}>(...);

// 2. Обернуть приложение в провайдер (в layout.tsx)
<CategoriesProvider>
  <Header />
  {children}
</CategoriesProvider>

// 3. Использовать в компонентах
const { popularCategories } = useCategories();
```

**Преимущества:**
- Единый источник данных
- Автоматическое кеширование
- Легко управлять обновлением при смене языка

### Вариант 2: Загрузка на уровне layout
```typescript
// В app/[locale]/layout.tsx загружать категории и передавать через props
const popularCategories = await loadPopularCategories(locale);
<Header popularCategories={popularCategories} />
```

**Преимущества:**
- SSR-friendly
- Простая реализация

**Недостатки:**
- Нужно прокидывать props через несколько уровней

### Вариант 3: Redux Store
```typescript
// Добавить в существующий Redux store
// src/store/slices/categoriesSlice.ts
const categoriesSlice = createSlice({
  name: 'categories',
  initialState: {
    popular: [],
    isLoading: false,
  },
  // ...
});
```

**Преимущества:**
- Интеграция с существующей системой управления состоянием
- Доступность во всем приложении

### Вариант 4: Условная загрузка (Временное решение)
```typescript
// В Header.tsx загружать только если не на главной странице
const isHomePage = pathname === '/' || pathname === `/${locale}`;
useEffect(() => {
  if (!isHomePage) {
    loadCategories();
  }
}, [isHomePage, locale]);
```

**Недостатки:**
- На главной странице мобильное меню все равно будет без категорий

## Детали реализации

### Где находится код
- **Закомментированный код:** `/data/hostel-booking-system/frontend/svetu/src/components/Header.tsx` (строки 141-198)
- **Основная загрузка категорий:** `/data/hostel-booking-system/frontend/svetu/src/app/[locale]/HomePageClient.tsx` (строка 431)
- **Использование в мобильном меню:** `/data/hostel-booking-system/frontend/svetu/src/components/Header.tsx` (строки 386-402)

### Что нужно учесть
1. Категории должны обновляться при смене языка
2. Нужно добавить иконки и цвета для категорий (как в закомментированном коде)
3. Кеширование для избежания лишних запросов
4. Обработка состояния загрузки

## Критерии приемки
- [ ] Популярные категории отображаются в мобильном меню
- [ ] При загрузке страницы выполняется только ОДИН запрос к `/api/v1/marketplace/popular-categories`
- [ ] При смене языка категории обновляются корректно
- [ ] Нет регрессии производительности
- [ ] Работает и на главной странице, и на других страницах сайта

## Приоритет
🟡 **Средний** - Функционал работает, но UX мобильных пользователей ухудшен

## Оценка времени
2-3 часа на реализацию с контекстом или Redux