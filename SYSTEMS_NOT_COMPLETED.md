# ❌ Что НЕ доделано в системах SveTu

## 🚧 Система отзывов и рейтингов

### ❌ **Критические проблемы (система НЕ работает):**
1. **НЕ подключена к API** - роуты reviews не зарегистрированы в router.go
2. **Сервис не инициализирован** - review service не запускается в main server
3. **Нет интеграции с основным приложением** - код есть, но не подключен

### ❌ **Функционал не доделан:**
- **Подтверждение покупок продавцами** - backend готов, но нет UI
- **Система споров (disputes)** - таблицы в БД есть, API есть, но нет frontend
- **Модерация отзывов** - нет админ панели для управления
- **Ответы продавцов на отзывы** - API готов, но нет интерфейса
- **Уведомления о новых отзывах** - не интегрировано с notification service
- **Защита от спама** - нет проверки на дубликаты и анти-спам
- **Аналитика и экспорт** - нет дашборда с метриками

**Готовность: 80% кода написано, но 0% работает в production**

---

## 🚧 AI переводы

### ❌ **Критические проблемы:**
1. **120+ файлов с ошибками** использования переводов
2. **62 файла** используют `useTranslations()` без указания модуля
3. **Множество отсутствующих ключей** в JSON файлах переводов
4. **Неправильная архитектура модулей** - перекрестные ссылки

### ❌ **Функционал не доделан:**
- **Автоперевод контента** - при создании объявлений/товаров
- **Пакетная обработка** - массовый перевод существующего контента
- **Версионирование в UI** - нет истории изменений переводов
- **Метрики провайдеров** - какой провайдер сколько стоит/используется
- **Валидация в CI/CD** - нет автоматических тестов переводов
- **Синхронизация** - нет автоматической синхронизации между языками

**Готовность: 90% backend готов, но 40% frontend содержит ошибки**

---

## 🔧 Что нужно сделать в первую очередь:

### Для отзывов (1-2 дня работы):

#### 1. Подключить роуты в файле `backend/internal/router/router.go`:
```go
// Добавить импорт
import (
    reviewHandler "backend/internal/proj/reviews/handler"
)

// В функции SetupRoutes добавить:
reviewRoutes := api.Group("/reviews")
reviewHandler.RegisterRoutes(reviewRoutes)
```

#### 2. Инициализировать сервис в `backend/cmd/api/main.go`:
```go
// Добавить импорт
import (
    reviewService "backend/internal/proj/reviews/service"
    reviewStorage "backend/internal/proj/reviews/storage/postgres"
)

// В функции main после инициализации БД:
reviewStore := reviewStorage.NewStorage(db)
reviewSvc := reviewService.NewService(reviewStore, cache)
reviewHandler := reviewHandler.NewHandler(reviewSvc)
```

#### 3. Добавить в frontend страницы для:
- `/reviews` - список отзывов
- `/profile/reviews` - мои отзывы
- `/storefront/[id]/reviews` - отзывы о магазине

### Для переводов (3-4 дня работы):

#### 1. Массовое исправление файлов:
```bash
# Найти все файлы с неправильным использованием
grep -r "useTranslations()" frontend/svetu/src --include="*.tsx" --include="*.ts"

# Исправить на правильный формат:
# useTranslations() → useTranslations('moduleName')
```

#### 2. Добавить отсутствующие ключи переводов:
```javascript
// Проверить какие ключи отсутствуют
// В каждом компоненте с ошибкой MISSING_MESSAGE
// Добавить ключи в соответствующие JSON файлы:
// - frontend/svetu/src/messages/ru/{module}.json
// - frontend/svetu/src/messages/en/{module}.json
// - frontend/svetu/src/messages/sr/{module}.json
```

#### 3. Исправить архитектуру модулей:
```javascript
// Неправильно:
const t = useTranslations('map.cluster');

// Правильно:
const t = useTranslations('map');
// Использование: t('cluster.someKey')
```

#### 4. Создать скрипт валидации:
```javascript
// scripts/validate-translations.js
// Проверка что все ключи есть во всех языках
// Проверка что все используемые ключи существуют
// Запускать в CI/CD pipeline
```

---

## 📊 Детальная статистика проблем

### Система отзывов:
| Компонент | Статус | Что нужно сделать |
|-----------|--------|-------------------|
| Database | ✅ 100% | Готово |
| Backend API | ✅ 95% | Подключить роуты |
| Frontend Components | ✅ 70% | Добавить страницы |
| Integration | ❌ 0% | Интегрировать с main app |
| Admin Panel | ❌ 0% | Создать интерфейс модерации |
| Notifications | ❌ 0% | Подключить уведомления |

### AI переводы:
| Компонент | Статус | Что нужно сделать |
|-----------|--------|-------------------|
| Translation Providers | ✅ 100% | Готово |
| Backend Service | ✅ 90% | Добавить метрики |
| Database | ✅ 100% | Готово |
| Frontend Integration | ⚠️ 60% | Исправить 120+ файлов |
| Admin Panel | ✅ 80% | Добавить версионирование |
| Auto-translation | ❌ 0% | Реализовать автоперевод |

---

## 🎯 План действий

### Неделя 1 (Приоритет: КРИТИЧЕСКИЙ)
1. **День 1-2:** Подключить систему отзывов к API
2. **День 3-4:** Исправить критические ошибки переводов (TOP 20 файлов)
3. **День 5:** Тестирование и багфиксы

### Неделя 2 (Приоритет: ВЫСОКИЙ)
1. **День 1-2:** Создать UI для отзывов (страницы и формы)
2. **День 3-4:** Исправить оставшиеся ошибки переводов
3. **День 5:** Добавить админ панель для модерации отзывов

### Неделя 3 (Приоритет: СРЕДНИЙ)
1. **День 1-2:** Реализовать систему споров и подтверждений
2. **День 3:** Добавить уведомления для отзывов
3. **День 4-5:** Автоперевод контента при создании

### Неделя 4 (Приоритет: НИЗКИЙ)
1. Аналитика и метрики для отзывов
2. Пакетная обработка переводов
3. Интеграционные тесты
4. Документация

---

## ⚠️ Риски если НЕ исправить

### Система отзывов:
- **Невозможность получить обратную связь** от пользователей
- **Отсутствие социального доказательства** для товаров
- **Снижение доверия** к платформе
- **Потеря конкурентного преимущества**

### AI переводы:
- **Ошибки в production** из-за отсутствующих переводов
- **Плохой UX** для не-русскоязычных пользователей
- **Невозможность масштабирования** на другие страны
- **Высокие затраты** на ручные переводы

---

## ✅ Результат после исправления

После выполнения всех задач получим:

1. **Полностью рабочую систему отзывов** с модерацией, спорами и аналитикой
2. **Безошибочную систему переводов** с автопереводом контента
3. **Повышение конверсии** на 20-30% за счет отзывов
4. **Возможность экспансии** на Черногорию и Боснию
5. **Снижение затрат** на переводы на 80%

**Общее время на исправление: 3-4 недели для 1 разработчика**