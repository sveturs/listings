# 🤖 Руководство по использованию Claude помощников

## 📋 Базовый шаблон команды

```bash
screen -dmS <task-name> bash -c 'claude -p --dangerously-skip-permissions "<ЗАДАЧА>" > /tmp/<task-name>.log 2>&1'
```

## 🎯 Оптимальная структура промпта

### 1. Четкая постановка задачи
```
"В директории <ПОЛНЫЙ_ПУТЬ> выполни следующие действия:
1) <конкретное_действие_1>
2) <конкретное_действие_2>
3) <конкретное_действие_3>
```

### 2. Контекст и ограничения
```
Используй:
- Файлы: <список_файлов>
- Инструменты: <список_инструментов>
- Переменные окружения: <если_нужны>

НЕ делай:
- НЕ запускай другие экземпляры claude
- НЕ изменяй файлы вне указанной директории
- НЕ выполняй деструктивные операции без подтверждения
```

### 3. Ожидаемый результат
```
Результат сохрани в: <путь_к_файлу>
Формат вывода: <text/json/sql/etc>
```

## 📚 Примеры эффективных промптов

### Анализ кода
```bash
screen -dmS code-analysis bash -c 'claude -p --dangerously-skip-permissions "Проанализируй код в /data/project/backend:
1) Найди все методы работы с БД в internal/storage
2) Проверь наличие SQL инъекций и проблем безопасности
3) Создай отчет с рекомендациями по улучшению

Результат сохрани в /tmp/security-report.md в формате markdown.
НЕ запускай другие экземпляры claude." > /tmp/code-analysis.log 2>&1'
```

### Создание/модификация кода
```bash
screen -dmS create-tests bash -c 'claude -p --dangerously-skip-permissions "В директории /data/project/backend создай unit тесты:
1) Прочитай файл internal/service/user.go
2) Создай файл internal/service/user_test.go с тестами для всех публичных методов
3) Используй testify/assert для проверок и gomock для моков

Покрытие должно быть не менее 80%.
НЕ запускай другие экземпляры claude." > /tmp/create-tests.log 2>&1'
```

### Работа с БД
```bash
screen -dmS db-migration bash -c 'claude -p --dangerously-skip-permissions "Создай SQL миграцию в /data/project/migrations:
1) Подключись к БД postgres://user:pass@localhost/db
2) Проанализируй структуру таблиц users и orders
3) Создай миграцию 001_add_user_orders_index.sql для оптимизации JOIN запросов

Миграция должна включать:
- UP: создание индексов
- DOWN: удаление индексов
НЕ запускай другие экземпляры claude." > /tmp/db-migration.log 2>&1'
```

### Анализ и исправление данных
```bash
screen -dmS fix-translations bash -c 'claude -p --dangerously-skip-permissions "В БД postgres://user:pass@localhost/db исправь переводы:
1) Найди все записи в таблице translations где language='ru' но текст на сербском
2) Создай SQL скрипт для исправления этих переводов
3) Сохрани скрипт в /tmp/fix-translations.sql

Используй правильные переводы:
- Elektronika -> Электроника
- Nekretnine -> Недвижимость
НЕ запускай другие экземпляры claude." > /tmp/fix-translations.log 2>&1'
```

## 🔍 Мониторинг выполнения

### Проверка статуса
```bash
# Список активных помощников
screen -ls | grep -v "dead"

# Просмотр лога в реальном времени
tail -f /tmp/<task-name>.log

# Проверка завершения
grep -E "(Completed|Finished|Done|Error)" /tmp/<task-name>.log
```

### Массовый мониторинг
```bash
# Скрипт для проверки всех логов
for log in /tmp/*.log; do
    echo "=== $(basename $log) ==="
    tail -5 "$log"
    echo
done
```

## ⚡ Лучшие практики

### ✅ ДЕЛАЙ:
1. **Конкретизируй задачи** - чем точнее описание, тем лучше результат
2. **Указывай полные пути** - избегай относительных путей
3. **Разбивай на шаги** - нумерованный список действий работает лучше
4. **Добавляй примеры** - если нужен специфичный формат вывода
5. **Логируй результаты** - всегда направляй вывод в файл

### ❌ НЕ ДЕЛАЙ:
1. **Не давай расплывчатых задач** - "сделай лучше" не работает
2. **Не забывай про ограничения** - всегда добавляй "НЕ запускай другие экземпляры"
3. **Не запускай критичные операции** - удаление данных, остановка сервисов
4. **Не полагайся на контекст** - помощник не знает о текущей сессии

## 🚀 Продвинутые техники

### Параллельное выполнение
```bash
# Запуск нескольких помощников для разных частей проекта
for module in auth storage api; do
    screen -dmS test-$module bash -c "claude -p --dangerously-skip-permissions 'Создай тесты для модуля $module в /data/project/$module. НЕ запускай другие экземпляры claude.' > /tmp/test-$module.log 2>&1"
done
```

### Цепочка задач
```bash
# Помощник 1: Анализ
screen -dmS analyze bash -c 'claude -p --dangerously-skip-permissions "Проанализируй API endpoints в /data/project и сохрани список в /tmp/endpoints.txt" > /tmp/analyze.log 2>&1'

# Ждем завершения
while ! grep -q "Completed" /tmp/analyze.log 2>/dev/null; do sleep 5; done

# Помощник 2: Создание тестов на основе анализа
screen -dmS create-api-tests bash -c 'claude -p --dangerously-skip-permissions "Используя список из /tmp/endpoints.txt создай Postman коллекцию в /tmp/api-tests.json" > /tmp/create-api-tests.log 2>&1'
```

### Условное выполнение
```bash
screen -dmS conditional-fix bash -c 'claude -p --dangerously-skip-permissions "
Проверь файл /data/project/config.yaml:
- ЕСЛИ найдешь deprecated параметры, создай /tmp/config-migration.md с инструкциями
- ЕСЛИ структура некорректна, создай /tmp/config-fixed.yaml
- ИНАЧЕ создай /tmp/config-ok.txt с текстом \"Configuration is valid\"
НЕ запускай другие экземпляры claude." > /tmp/conditional-fix.log 2>&1'
```

## 📊 Метрики эффективности

Помощники наиболее эффективны для:
- 🟢 **Анализ кода** (90% успешность)
- 🟢 **Создание документации** (85% успешность)
- 🟢 **Написание тестов** (80% успешность)
- 🟡 **Рефакторинг кода** (70% успешность)
- 🟡 **Работа с БД** (65% успешность)
- 🔴 **Реалтайм отладка** (30% успешность)

## 🔧 Troubleshooting

### Помощник завис
```bash
# Найти PID
ps aux | grep "claude -p" | grep <task-name>

# Мягкая остановка
kill -TERM <PID>

# Жесткая остановка (если не помогло)
kill -9 <PID>

# Очистка screen сессии
screen -S <task-name> -X quit
```

### Помощник не запускается
```bash
# Проверить права на директорию логов
ls -la /tmp/

# Проверить синтаксис команды
bash -n -c 'твоя_команда'

# Запустить без screen для отладки
claude -p --dangerously-skip-permissions "твой_промпт"
```

### Результат не тот, что ожидался
1. Проверь конкретность задачи
2. Добавь больше примеров
3. Разбей на более мелкие подзадачи
4. Укажи формат вывода явно

## 💡 Итоговые рекомендации

1. **Используй помощников для фоновых задач** - пока они работают, ты можешь заниматься другим
2. **Не жди мгновенных результатов** - дай помощнику 2-5 минут на выполнение
3. **Проверяй логи регулярно** - не все задачи завершаются успешно
4. **Комбинируй с основной работой** - помощники дополняют, а не заменяют твою работу
5. **Экспериментируй с промптами** - чем больше практики, тем лучше результаты