# Техническое письмо: Проблема с отсутствием Refresh Token в OAuth Flow

**Дата:** 16 сентября 2025
**От:** Команда разработки Sve Tu Platform
**Кому:** Команда Auth Service
**Приоритет:** ВЫСОКИЙ

## 🔴 Проблема

Пользователи жалуются на принудительный логаут **ровно через 15 минут** после авторизации через Google OAuth. Анализ логов и кода выявил критическую проблему в OAuth callback flow.

## 📊 Анализ логов

### Фактические данные из логов (2025-09-15):

**JWT токен (созданный в 18:47:16):**
```
Декодированный JWT:
- iat: 1757962036 (2025-09-15 18:47:16 UTC)
- exp: 1757962936 (2025-09-15 19:02:16 UTC)
- TTL: 900 секунд = 15 минут (✅ соответствует конфигу)
```

**Реальное поведение системы (23:35:12 → 23:40:26):**
```
23:35:12 - POST /api/v1/auth/logout → 401 (токен уже недействителен!)
23:35:12 - User 7 is now offline
23:35:13 - Множественные успешные запросы (200 статус)
23:40:20 - User 7 is now online (переподключение)
23:40:26 - User 7 is now offline

Интервал разлогинивания: ~5 минут!
```

**Парадокс:** Токены создаются на 15 минут, но пользователи разлогиниваются через 5 минут.

## 🔍 Корневая причина

### Проблема 1: Отсутствие refresh_token в OAuth callback

**Файл:** `internal/transport/http/handlers/auth.go`
```go
// ❌ ПРОБЛЕМА: Возвращается только access_token
redirectURL = stateInfo.redirectURI + separator + "token=" + authResponse.AccessToken
```

### Проблема 2: Токены становятся недействительными раньше времени

**Наблюдения из логов:**
- JWT создается на 15 минут (exp = iat + 900 сек)
- Логаут происходит через ~5 минут
- Запрос `/api/v1/auth/logout` возвращает 401 (токен уже недействителен)

**Возможные причины:**
1. **Рассинхронизация времени** между Auth Service и Main Backend
2. **Токен отзывается/инвалидируется** Auth Service'ом раньше времени
3. **Rate limiting** блокирует токен после определенного количества запросов
4. **Проблемы с публичным ключом** для валидации RS256 токенов

### Комбинированная проблема:
1. ❌ OAuth callback возвращает **только access_token**
2. ❌ Frontend не может обновить токен (нет refresh_token)
3. ❌ Access токен становится недействительным **раньше срока истечения**
4. ❌ Пользователь принудительно разлогинивается через ~5 минут

## 💡 Решение

### Приоритет 1: Добавить refresh_token в OAuth callback

**Вариант A: Через URL fragment (Рекомендуемый)**
```go
// Безопасный способ - токены не попадают в server logs
redirectURL = stateInfo.redirectURI + "#" +
    "access_token=" + authResponse.AccessToken +
    "&refresh_token=" + authResponse.RefreshToken
```

**Вариант B: Через query параметры**
```go
// Простой способ - но токены могут попасть в логи
separator := "?"
if strings.Contains(stateInfo.redirectURI, "?") {
    separator = "&"
}
redirectURL = stateInfo.redirectURI + separator +
    "token=" + authResponse.AccessToken +
    "&refresh_token=" + authResponse.RefreshToken
```

### Приоритет 2: Диагностировать преждевременную инвалидацию токенов

**Немедленные проверки:**
1. **Синхронизация времени:**
   ```bash
   # На Auth Service сервере
   timedatectl status
   # Проверить что NTP синхронизировано
   ```

2. **Проверка публичного ключа на Main Backend:**
   ```bash
   # Убедиться что публичный ключ актуальный
   ls -la /path/to/public.pem
   # Проверить содержимое ключа
   ```

3. **Логи Auth Service во время инвалидации:**
   ```bash
   # Мониторить логи в реальном времени
   docker logs auth_service -f | grep -E "revoke|invalid|expire"
   ```

4. **Проверка JWT валидации в Main Backend:**
   - Добавить детальное логирование в `jwt.ValidateAuthServiceToken()`
   - Логировать причину отклонения токена

## 🔧 Конфигурация (текущая)

```env
JWT_ACCESS_TOKEN_DURATION=15m     # ✅ Корректно
JWT_REFRESH_TOKEN_DURATION=720h   # ✅ Корректно (30 дней)
RATE_LIMIT_TOKEN_REFRESH=30/1m    # ✅ Достаточно
```

## 📋 Frontend изменения (со стороны Platform)

Мы готовы внести соответствующие изменения в frontend для обработки refresh_token:

```typescript
// В OAuthProcessor.tsx
const searchParams = useSearchParams();
const token = searchParams.get('token');
const refreshToken = searchParams.get('refresh_token');

if (token) {
  tokenManager.setAccessToken(token);
  if (refreshToken) {
    tokenManager.setRefreshToken(refreshToken); // ← Новая строка
  }
}
```

## 🧪 Тестирование

### До исправления:
```bash
# Проверить текущий OAuth flow
curl -v "https://authpreprod.svetu.rs/api/v1/auth/oauth/google"
# Результат: redirect содержит только token=...
```

### После исправления:
```bash
# Должен содержать оба токена
# Вариант 1: example.com/callback#access_token=...&refresh_token=...
# Вариант 2: example.com/callback?token=...&refresh_token=...
```

## ⏰ Timeline

**Запрашиваемые сроки:**
- Фиксация Auth Service: 2-3 рабочих дня
- Тестирование на preprod: 1 день
- Деплой на production: согласно вашему релизному циклу

**Критичность:** Проблема влияет на **100% пользователей** каждые **5 минут** (не 15!)

**Бизнес-влияние:**
- Пользователи не могут завершить покупки
- Постоянные прерывания в работе с платформой
- Критические жалобы на UX

## 📞 Контакты

Для уточнений по техническим деталям:
- Команда разработки Platform
- Готовы к совместному созвону для обсуждения деталей

---

**P.S.** Мы проанализировали весь auth flow и подтверждаем, что проблема именно в отсутствии передачи refresh_token в OAuth callback. Все остальные компоненты (JWT валидация, token refresh logic) работают корректно.
