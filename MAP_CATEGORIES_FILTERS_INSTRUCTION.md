# Инструкция по работе с категориями и фильтрами на карте

## Дата создания: 21.07.2025

## 📍 Обзор функциональности

Карта маркетплейса поддерживает фильтрацию объявлений по категориям и динамическим атрибутам. Пользователи могут выбирать одну или несколько категорий, применять фильтры по атрибутам категории и видеть только релевантные маркеры на карте.

## 🗂️ Архитектура компонентов

### Frontend компоненты:

1. **`/app/[locale]/map/page.tsx`** - основная страница карты
   - Управляет состоянием фильтров
   - Синхронизирует фильтры с URL параметрами
   - Отправляет запросы к GIS API

2. **`/components/common/CategoryTreeSelector.tsx`** - универсальный селектор категорий
   - Поддерживает множественный выбор для карты
   - Отображает иерархическое дерево категорий
   - Встроенный поиск по категориям

3. **`/components/marketplace/SmartFilters.tsx`** - динамические фильтры атрибутов
   - Загружает атрибуты выбранных категорий
   - Отображает соответствующие UI элементы для каждого типа атрибута
   - Поддерживает text, number, select, multiselect, boolean типы

4. **`/components/GIS/Mobile/MobileFiltersDrawer.tsx`** - мобильная версия фильтров
   - Drawer компонент для мобильных устройств
   - Те же фильтры в адаптивном интерфейсе

### Backend эндпоинты:

1. **`GET /api/v1/gis/search/radius`** - радиусный поиск с фильтрами
   - Параметры:
     - `latitude`, `longitude`, `radius` - координаты и радиус поиска
     - `categories` - массив ID категорий (через запятую)
     - `attributeFilters` - JSON объект с фильтрами атрибутов
     - `min_price`, `max_price` - ценовой диапазон

2. **`GET /api/v1/marketplace/categories/{id}/attributes`** - атрибуты категории
   - Возвращает список атрибутов с флагом `is_filterable`
   - Включает опции для select/multiselect атрибутов

## 🔧 Реализация фильтрации

### 1. Интерфейс фильтров на карте:

```typescript
interface MapFilters {
  categories: number[];          // ID выбранных категорий
  priceFrom: number;            // Минимальная цена
  priceTo: number;              // Максимальная цена
  radius: number;               // Радиус поиска в метрах
  attributes?: Record<string, any>; // Динамические атрибуты
}
```

### 2. Выбор категорий:

```tsx
<CategoryTreeSelector
  value={filters.categories}
  onChange={(value) => setFilters({ 
    ...filters, 
    categories: value as number[] 
  })}
  multiple={true}
  placeholder={t('map.selectCategories')}
  className="w-full"
/>
```

### 3. Динамические фильтры атрибутов:

```tsx
<SmartFilters
  categoryIds={filters.categories}
  values={filters.attributes || {}}
  onChange={(newAttributes) => 
    setFilters({ ...filters, attributes: newAttributes })
  }
  locale={locale}
/>
```

### 4. Отправка запроса с фильтрами:

```typescript
const searchParams = new URLSearchParams({
  latitude: center.lat.toString(),
  longitude: center.lng.toString(),
  radius: filters.radius.toString(),
  limit: '100'
});

// Добавляем категории
if (filters.categories.length > 0) {
  searchParams.append('categories', filters.categories.join(','));
}

// Добавляем атрибуты
if (filters.attributes) {
  searchParams.append('attributeFilters', JSON.stringify(filters.attributes));
}

const response = await fetch(`/api/v1/gis/search/radius?${searchParams}`);
```

## 🔍 Backend обработка фильтров

### 1. Парсинг категорий в handler:

```go
// RadiusSearch handler
if categories := c.Query("categories"); categories != "" {
    categoryStrings := strings.Split(categories, ",")
    categoryIDs := make([]int, 0, len(categoryStrings))
    for _, catStr := range categoryStrings {
        if id, err := strconv.Atoi(strings.TrimSpace(catStr)); err == nil {
            categoryIDs = append(categoryIDs, id)
        }
    }
    req.Filters.CategoryIDs = categoryIDs
}
```

### 2. SQL фильтрация в репозитории:

```go
// В searchUnifiedGeo или searchLegacy
if len(params.CategoryIDs) > 0 {
    placeholders := make([]string, len(params.CategoryIDs))
    for i, catID := range params.CategoryIDs {
        placeholders[i] = fmt.Sprintf("$%d", argIndex)
        args = append(args, catID)
        argIndex++
    }
    query += fmt.Sprintf(` AND (
        (ug.source_type = 'marketplace_listing' AND ml.category_id IN (%s)) OR
        (ug.source_type = 'storefront_product' AND sp.category_id IN (%s))
    )`, strings.Join(placeholders, ","), strings.Join(placeholders, ","))
}
```

## 🐛 Решенные проблемы

### 1. Фильтрация не работала:

**Проблема**: Backend игнорировал фильтры категорий и возвращал все объявления.

**Решение**:
- Исправлен SQL запрос в `searchLegacy` - использовались неправильные алиасы таблиц
- Добавлена фильтрация по категориям в `searchUnifiedGeo`
- Обновлен парсинг категорий в handler для поддержки числовых ID

### 2. Материализованное представление пустое:

**Проблема**: `map_items_cache` не содержало данных.

**Решение**:
```sql
REFRESH MATERIALIZED VIEW map_items_cache;
```

### 3. Категории передавались как строки:

**Проблема**: Frontend отправлял категории как "1101", backend ожидал числа.

**Решение**: Добавлен парсинг строковых ID в числовые в handler.

## 📋 Чеклист для проверки

- [x] Категории отображаются в CategoryTreeSelector
- [x] Множественный выбор категорий работает
- [x] Выбранные категории сохраняются в URL
- [x] Backend фильтрует объявления по категориям
- [x] Динамические атрибуты загружаются для выбранных категорий
- [x] Фильтры атрибутов применяются к результатам
- [x] Мобильная версия фильтров работает корректно
- [x] Производительность приемлемая при большом количестве маркеров

## 🚀 Примеры использования

### 1. Фильтрация по одной категории:
```
/map?categories=1101&lat=44.817600&lng=20.464900&zoom=10.00
```
Показывает только смартфоны (категория 1101).

### 2. Фильтрация по нескольким категориям:
```
/map?categories=1001,1007,1006&lat=44.817600&lng=20.464900&zoom=10.00
```
Показывает электронику, индустрию и спорт.

### 3. С атрибутами:
```
/map?categories=1101&attributes={"condition":"new","brand":"Apple"}
```
Показывает только новые iPhone.

## 🔧 Отладка

### 1. Проверка запросов:
```bash
# Тест фильтрации по категории
curl "http://localhost:3000/api/v1/gis/search/radius?latitude=44.764970&longitude=20.462411&radius=10000&categories=1101"

# Тест множественных категорий
curl "http://localhost:3000/api/v1/gis/search/radius?latitude=44.764970&longitude=20.462411&radius=10000&categories=1001,1007"
```

### 2. Проверка материализованного представления:
```sql
-- Проверить данные в кеше карты
SELECT COUNT(*) FROM map_items_cache;
SELECT DISTINCT category_id, category_name FROM map_items_cache WHERE status = 'active';

-- Обновить кеш если нужно
REFRESH MATERIALIZED VIEW map_items_cache;
```

### 3. Логирование:
Backend логирует:
- Парсинг категорий: `🔍 BACKEND: Parsed categories as IDs`
- SQL фильтры: `🔍 BACKEND PostGIS: Added category filter to unified query`

## 📝 Важные замечания

1. **Производительность**: При большом количестве категорий используется оператор IN в SQL, что может быть медленным. Рекомендуется ограничить количество одновременно выбранных категорий.

2. **Кеширование**: map_items_cache обновляется периодически. Новые объявления могут не сразу появиться на карте.

3. **Атрибуты**: Не все атрибуты категории доступны для фильтрации. Только те, у которых `is_filterable = true`.

4. **URL синхронизация**: Состояние фильтров сохраняется в URL для возможности поделиться ссылкой.

## 🔮 Будущие улучшения

1. **Кластеризация маркеров** - группировка близких маркеров при уменьшении масштаба
2. **Фасетный поиск** - показ количества объявлений для каждого значения фильтра
3. **Сохраненные фильтры** - возможность сохранить набор фильтров для быстрого доступа
4. **Геозоны** - рисование произвольных областей для поиска
5. **Real-time обновления** - WebSocket для новых объявлений в выбранной области