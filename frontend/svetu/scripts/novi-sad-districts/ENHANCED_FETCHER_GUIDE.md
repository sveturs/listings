# 🏘️ Улучшенный инструмент получения границ районов

## Обзор

`enhanced_district_fetcher.js` - это продвинутый скрипт для получения точных границ административных районов городов Сербии. Он решает проблемы существующих скриптов и использует многоуровневую систему источников данных.

## 🎯 Преимущества перед старыми скриптами

### ❌ Проблемы старых скриптов:

- `auto_add_district.js`: неточные данные, простое обрезание координат
- `add_district_overpass.js`: сложности с multipolygon, ограниченный поиск

### ✅ Решения нового скрипта:

- **Многоуровневая fallback система** (Mapbox → Overpass → Nominatim)
- **Интеллектуальная валидация** данных
- **Кэширование** для ускорения повторных запросов
- **Упрощение полигонов** для оптимизации размера
- **Retry логика** для надежности сетевых запросов
- **Поддержка множественных городов**

## 🔧 Установка и настройка

### Зависимости

Скрипт использует только встроенные модули Node.js, дополнительная установка не требуется.

### Переменные окружения (опциональные)

```bash
export MAPBOX_TOKEN="pk.eyJ1..."  # Для лучшего качества данных
```

## 📖 Использование

### Базовый синтаксис

```bash
node enhanced_district_fetcher.js "Название района" "Город" [опции]
```

### Опции командной строки

- `--mapbox-token TOKEN` - Токен Mapbox API (альтернатива переменной окружения)
- `--no-cache` - Отключить кэширование запросов
- `--no-validate` - Пропустить валидацию полученных данных
- `--dry-run` - Только получить данные, не добавлять в файл
- `--city "Город" --fetch-all` - Получить все основные районы города

### Примеры использования

#### 1. Добавление одного района

```bash
# Базовое использование
node enhanced_district_fetcher.js "Veternik" "Novi Sad"

# С Mapbox токеном для лучшего качества
node enhanced_district_fetcher.js "Лиман" "Нови Сад" --mapbox-token pk.eyJ1...

# Тест без добавления в файл
node enhanced_district_fetcher.js "Futog" "Novi Sad" --dry-run
```

#### 2. Массовое добавление районов

```bash
# Получить все основные районы Нови-Сада
node enhanced_district_fetcher.js --city "Novi Sad" --fetch-all

# С Mapbox токеном для максимального качества
node enhanced_district_fetcher.js --city "Novi Sad" --fetch-all --mapbox-token pk.eyJ1...

# Тест массового добавления
node enhanced_district_fetcher.js --city "Beograd" --fetch-all --dry-run
```

#### 3. Отладка и диагностика

```bash
# Отключить кэш для получения свежих данных
node enhanced_district_fetcher.js "Petrovaradin" "Novi Sad" --no-cache

# Пропустить валидацию для проблемных районов
node enhanced_district_fetcher.js "Сремска Каменица" "Нови Сад" --no-validate
```

## 🛠️ Алгоритм работы

### 1. Источники данных (по приоритету):

1. **Mapbox Geocoding API** 🥇

   - Самые точные коммерческие данные
   - Поддержка полигонов `polygon=true`
   - Требует токен

2. **Overpass API** 🥈

   - Расширенный поиск по OSM данным
   - Умная сборка multipolygon
   - Поиск по множественным критериям

3. **Nominatim API** 🥉
   - Fallback для базового покрытия
   - Публичные OSM данные

### 2. Валидация данных:

- ✅ Минимум/максимум координат (4-10000)
- ✅ Размер площади (0.0001-10.0 градусов)
- ✅ Замкнутость полигона
- ✅ Геометрическая корректность

### 3. Оптимизация:

- 🗜️ Упрощение полигонов >1000 точек
- 💾 Кэширование на 7 дней
- 🔄 Retry механизм для сбойных запросов

## 📁 Структура файлов

```
scripts/novi-sad-districts/
├── enhanced_district_fetcher.js    # Основной скрипт
├── ENHANCED_FETCHER_GUIDE.md       # Эта документация
├── auto_add_district.js            # Старый скрипт (Nominatim)
├── add_district_overpass.js        # Старый скрипт (Overpass)
├── safe_remove_districts.js        # Удаление районов
└── README.md                       # Общая документация

/tmp/districts_cache/               # Кэш запросов (создается автоматически)
├── 5f4dcc3b5aa765d61d8327deb882cf99.json
└── ...
```

## 🌍 Поддерживаемые города

Скрипт оптимизирован для крупных городов Сербии:

### ✅ Протестированы:

- **Нови-Сад** - полная поддержка всех районов
- **Белград** - основные районы

### 🚧 Планируется:

- **Ниш** - требует тестирование
- **Крагуевац** - требует тестирование
- **Суботица** - требует тестирование

### Добавление нового города:

```bash
# Тест всех районов города
node enhanced_district_fetcher.js --city "НовыйГород" --fetch-all --dry-run

# Добавление по одному району
node enhanced_district_fetcher.js "Район1" "НовыйГород"
node enhanced_district_fetcher.js "Район2" "НовыйГород"
```

## 🔍 Диагностика проблем

### Частые ошибки и решения:

#### "Район не найден"

```bash
# Попробуйте разные варианты названия
node enhanced_district_fetcher.js "Liman" "Novi Sad"
node enhanced_district_fetcher.js "Лиман" "Нови Сад"

# Используйте --no-cache для свежих данных
node enhanced_district_fetcher.js "Liman" "Novi Sad" --no-cache
```

#### "Валидация не пройдена"

```bash
# Пропустить валидацию и добавить принудительно
node enhanced_district_fetcher.js "Район" "Город" --no-validate

# Или проверить в dry-run режиме
node enhanced_district_fetcher.js "Район" "Город" --dry-run
```

#### Проблемы с сетью

```bash
# Скрипт автоматически повторяет запросы 3 раза
# Если проблемы продолжаются, проверьте интернет соединение
curl -s "https://nominatim.openstreetmap.org/search?q=test" > /dev/null && echo "OK"
```

### Логи и отладка:

Скрипт выводит детальные логи процесса:

```
🔍 Поиск района "Veternik" в Novi Sad
============================================================

🎯 Пробуем: Mapbox Geocoding
📦 Загружено из кэша: a1b2c3d4...
✅ Найдено через Mapbox Geocoding:
   Название: Veternik (Veternik)
   Источник: mapbox
   Координат: 245
   ✅ Валидация пройдена (площадь: 0.002834)

📝 Добавление района в файл...
📁 Создан бэкап: page.tsx.backup.1692547200
✅ Район "Veternik" добавлен!
   ID: veternik
   Цвет: #10B981
   Координат: 245
   Источник: mapbox
```

## 🎯 Лучшие практики

### 1. Для production использования:

```bash
# Получите Mapbox токен для лучшего качества данных
# https://account.mapbox.com/access-tokens/
export MAPBOX_TOKEN="pk.eyJ1..."

# Используйте с валидацией (по умолчанию)
node enhanced_district_fetcher.js "Район" "Город"
```

### 2. Для разработки и тестирования:

```bash
# Тестируйте сначала в dry-run режиме
node enhanced_district_fetcher.js "Район" "Город" --dry-run

# Отключите кэш для получения свежих данных
node enhanced_district_fetcher.js "Район" "Город" --no-cache
```

### 3. Для массового добавления:

```bash
# Добавляйте районы пошагово для контроля качества
node enhanced_district_fetcher.js "Район1" "Город"
# Проверить результат на карте: http://localhost:3001/ru/examples/novi-sad-districts
node enhanced_district_fetcher.js "Район2" "Город"
# И так далее...
```

### 4. Резервное копирование:

Скрипт автоматически создает бэкапы перед изменениями:

```bash
# Восстановление из бэкапа при необходимости
ls *.backup* | sort -r | head -1  # Найти последний бэкап
cp page.tsx.backup.1692547200 page.tsx  # Восстановить
```

## 🚀 Планы развития

- [ ] **HDX Integration** - подключение к официальным данным ООН
- [ ] **GeoSrbija.rs API** - интеграция с национальной платформой
- [ ] **Batch Processing** - массовая обработка из CSV/JSON файлов
- [ ] **Quality Scoring** - автоматическая оценка качества границ
- [ ] **Web Interface** - графический интерфейс для управления районами
- [ ] **Multi-language Support** - поддержка названий на разных языках

## 📞 Поддержка

При возникновении проблем:

1. **Проверьте логи** - скрипт выводит детальную информацию
2. **Используйте --dry-run** для диагностики без изменений
3. **Очистите кэш** с --no-cache для свежих данных
4. **Проверьте сеть** - убедитесь в доступности API

---

**Автор**: SvetU Development Team  
**Версия**: 1.0.0  
**Лицензия**: MIT
