# 🔍 Отчёт аудита системы переводов

**Дата:** 04.08.2025  
**Статус:** ⚠️ Требуется срочный рефакторинг

## 📊 Сводка проблем

### Критические проблемы (🔴)

- **120+ файлов** с неправильным использованием переводов
- **62 файла** используют `useTranslations()` без указания модуля
- **16 файлов** используют неправильный вложенный синтаксис
- **Множество отсутствующих ключей** в JSON файлах

### Основные типы ошибок

#### 1. ❌ Использование без модуля

```typescript
// Неправильно
const t = useTranslations();
t('marketplace.title'); // Ошибка!

// Правильно
const t = useTranslations('marketplace');
t('title');
```

#### 2. ❌ Вложенные namespace

```typescript
// Неправильно
const t = useTranslations('map.cluster');
t('listings');

// Правильно
const t = useTranslations('map');
t('cluster.listings');
```

#### 3. ❌ Перекрёстные ссылки на модули

```typescript
// Неправильно
const t = useTranslations('marketplace');
t('common.save'); // common не загружен!

// Правильно
const tMarketplace = useTranslations('marketplace');
const tCommon = useTranslations('common');
tCommon('save');
```

## 📁 Проблемные файлы (TOP 20)

### Критические (требуют немедленного исправления):

1. `/app/[locale]/page.tsx` - неправильные ссылки на map.redirecting
2. `/app/[locale]/create-listing-ai/page.tsx` - отсутствующие ключи cars
3. `/components/marketplace/ListingDetails.tsx` - useTranslations без модуля
4. `/components/ui/Header.tsx` - useTranslations без модуля
5. `/components/ui/Footer.tsx` - useTranslations без модуля

### С архитектурными проблемами:

6. `/app/[locale]/profile/*` - все страницы профиля используют auth вместо profile
7. `/components/GIS/Map/*` - множество файлов с map.cluster синтаксисом
8. `/components/cars/*` - динамические компоненты без загрузки модуля
9. `/components/storefront/*` - перекрёстные ссылки на модули
10. `/components/marketplace/filters/*` - неправильное использование namespace

## 🔧 План исправления

### Фаза 1: Автоматические исправления (1-2 дня)

1. Запустить скрипт для исправления вложенных namespace
2. Обновить все `useTranslations()` без модуля
3. Добавить отсутствующие ключи в JSON файлы

### Фаза 2: Рефакторинг архитектуры (3-4 дня)

1. Создать модуль `profile` и перенести переводы из `auth`
2. Оптимизировать загрузку модулей в layout
3. Исправить динамические компоненты

### Фаза 3: Валидация и тестирование (1-2 дня)

1. Создать автоматические тесты для переводов
2. Добавить ESLint правила для переводов
3. Документировать best practices

## 🛠️ Скрипты для исправления

### 1. Исправление useTranslations без модуля

```bash
node scripts/fix-empty-translations.js
```

### 2. Исправление вложенных namespace

```bash
node scripts/fix-translation-paths.js
```

### 3. Добавление отсутствующих ключей

```bash
node scripts/add-missing-keys.js
```

## 📈 Метрики после исправления

- ✅ 0 ошибок `MISSING_MESSAGE` в консоли
- ✅ 100% покрытие переводами
- ✅ Оптимизированная загрузка модулей
- ✅ Соответствие архитектуре модульной системы

## ⚡ Рекомендации

1. **Срочно** исправить критические файлы (TOP 5)
2. **Добавить** pre-commit hooks для проверки переводов
3. **Создать** GitHub Actions для валидации переводов в PR
4. **Обучить** команду правильному использованию системы
5. **Документировать** все паттерны в CLAUDE.md

## 🔗 Связанные документы

- `/docs/MODULAR_TRANSLATION_GUIDE.md`
- `/docs/TRANSLATION_MIGRATION_STATUS.md`
- `/CLAUDE.md` (раздел "Исправление ошибок переводов")
