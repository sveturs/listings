# Аудит системы атрибутов AI-объявлений

## 🚨 Критические проблемы

### 1. **Несоответствие AI промпта и маппинга атрибутов**

**Проблема**: AI промпт требует атрибуты, которые не замаплены для категории автомобили:

**AI промпт атрибуты:**
```
brand, model, year, color, fuel_type, transmission, mileage, engine_size
```

**Замапленные атрибуты для automotive (ID 1003):**
```sql
✅ condition    - MAPPED (ID: 2002)
✅ brand        - MAPPED (ID: 2003) 
✅ color        - MAPPED (ID: 2004)
✅ fuel_type    - MAPPED (ID: 2204)
✅ transmission - MAPPED (ID: 2205)
❌ car_model    - NOT MAPPED (ID: 2201) - но есть в БД!
❌ year         - NOT MAPPED (ID: 2202) - но есть в БД!
❌ mileage      - NOT MAPPED (ID: 2203) - но есть в БД!
❌ engine_size  - NOT MAPPED (ID: 3001) - но есть в БД!
```

**Результат**: AI генерирует атрибуты `model`, `year`, `mileage`, `engine_size`, но они **игнорируются** при создании объявления!

### 2. **Ошибка в логике обработки select атрибутов**

**Место**: `/frontend/svetu/src/app/[locale]/create-listing-ai/page.tsx:1504-1506`

**Проблемный код:**
```typescript
if (
  categoryAttr.options &&
  Array.isArray(categoryAttr.options)  // ❌ НЕВЕРНО!
) {
```

**Причина**: Backend возвращает `options` как объект `{values: [...]}`, а не как массив.

**Фактическая структура из API:**
```json
{
  "options": {
    "values": ["petrol", "diesel", "electric", "hybrid", "lpg", "cng"]
  }
}
```

**Результат**: Все select атрибуты (brand, color, fuel_type, transmission) **не проходят валидацию** и игнорируются!

### 3. **AI промпт содержит неточности**

**model vs car_model**: 
- AI промпт: `model` 
- БД атрибут: `car_model`
- Результат: не мапится

### 4. **Отсутствует валидация обязательных атрибутов**

База данных показывает:
```sql
fuel_type    - is_required: TRUE  ❌ Может быть проигнорирован
transmission - is_required: TRUE  ❌ Может быть проигнорирован
car_model    - is_required: TRUE  ❌ НЕ ЗАМАПЛЕН!
year         - is_required: TRUE  ❌ НЕ ЗАМАПЛЕН!
```

## 📊 Статистика атрибутов по категориям

| Категория | Всего атрибутов | Проблема |
|-----------|----------------|----------|
| Electronics (1001) | 8 | Не проверено |
| Fashion (1002) | 3 | Не проверено |
| **Automotive (1003)** | **6** | **4 не замаплены** |
| Real Estate (1004) | 3 | Не проверено |
| Остальные | 3 каждая | Не проверено |

## 🔧 Необходимые исправления

### 1. Исправить маппинг автомобильных атрибутов
```sql
-- Добавить недостающие атрибуты в маппинг
INSERT INTO category_attribute_mapping (category_id, attribute_id) VALUES
(1003, 2201), -- car_model
(1003, 2202), -- year  
(1003, 2203), -- mileage
(1003, 3001); -- engine_size
```

### 2. Исправить логику обработки select атрибутов
```typescript
// Было:
if (categoryAttr.options && Array.isArray(categoryAttr.options))

// Должно быть:
if (categoryAttr.options && categoryAttr.options.values && Array.isArray(categoryAttr.options.values))
```

### 3. Синхронизировать AI промпт с БД
```diff
- attributes: Для авто ТОЛЬКО: brand, model, year, color...
+ attributes: Для авто ТОЛЬКО: brand, car_model, year, color...
```

### 4. Добавить валидацию обязательных атрибутов
- Проверка наличия всех is_required=true атрибутов
- Блокировка создания объявления без обязательных атрибутов

## 🎯 План исправлений

1. **[Критично]** Исправить маппинг automotive атрибутов
2. **[Критично]** Исправить логику select атрибутов  
3. **[Важно]** Синхронизировать AI промпт
4. **[Важно]** Добавить валидацию
5. **[Рекомендуется]** Провести аудит остальных категорий

## 📋 Влияние на пользователей

**Текущее состояние**: 
- ❌ Объявления создаются БЕЗ ключевых атрибутов (модель, год, пробег)
- ❌ AI генерирует данные, которые теряются
- ❌ Поиск и фильтрация не работают корректно
- ❌ Пользователи НЕ получают ожидаемого качества

**После исправления**:
- ✅ Все атрибуты сохраняются корректно
- ✅ Полнота данных объявлений
- ✅ Корректная работа поиска
- ✅ Повышение качества платформы